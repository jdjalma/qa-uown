# Application Lifecycle — Pitfalls #92–#137

> Slice of the pitfall catalog. Numbers are **global and stable** (cross-refs `pitfall #N` resolve across slices). Index + how to append: [../pitfalls.md](../pitfalls.md). Rules/sequence: [../../SKILL.md](../../SKILL.md).

| # | Symptom | Cause | Fix |
|---|---------|-------|-----|
| 92 | Click on a Website (customer portal) sidebar item gives `TimeoutError` / pointer event intercepted right after a payment or save | A residual full-screen overlay (post-payment or post-save spinner/loading) is still mounted and intercepts pointer events over the sidebar link. The click resolves the locator but never connects. <!-- discovered in unified-flow.spec.ts, commit 4f30c0d dismiss overlay before sidebar click, 2026-06-02 --> | `WebsiteBasePage.goToSidebarLink` now calls `await this.waitForSpinner()` BEFORE attempting the click. Any sidebar navigation in the Website after a mutating action (pay/save) must wait for the overlay to disappear first. Do NOT mask with `force: true`. Cross-link: [[page-object-pattern]] WebsiteBasePage. |
| 93 | Query on `uown_sticky` filtering/reading column `retry_attempt` returns `column "retry_attempt" does not exist` | The column that counts retry attempts in the `uown_sticky` table (the recovery session) is called **`number_of_attempts`** (integer, default `0`, schema doc col 9). Column `retry_attempt` does NOT exist in `uown_sticky`. The name `retry_attempt` is confused with the sibling TABLE `uown_sticky_retry_attempt` (per-attempt record, columns `attempt_number` + `retry_attempt_time`) - these are different things: the aggregated session (`uown_sticky.number_of_attempts`) vs the per-attempt rows (`uown_sticky_retry_attempt`). Old briefings/skills incorrectly referenced `retry_attempt`. <!-- discovered in exploratory-testing-qa1-master S7, qa1, 2026-06-10 --> | For aggregated attempt count on the session: `SELECT number_of_attempts FROM uown_sticky WHERE pk=$1`. For per-attempt detail: `SELECT attempt_number, retry_attempt_time FROM uown_sticky_retry_attempt WHERE ...`. Authoritative schema: [`docs/database-schema.md`](../../../../docs/database-schema.md) sections `uown_sticky` (col 9) and `uown_sticky_retry_attempt`. NEVER use `retry_attempt` as a column name in `uown_sticky`. |
| 94 | Query on `uown_scheduled_task` filtering by column `name` returns 0 rows or `column "name" does not exist`; `last_trigger_time` read as proof of Quartz execution gives a false-negative | The actual columns in `uown_scheduled_task` are: task name = **`scheduled_task_name`** (NOT `name`); cron = **`cron_trigger`** (NOT `cron`/`cron_expression`). Furthermore, **`last_trigger_time` reflects ONLY MANUAL triggers** (via `triggerScheduledTask`), NOT automatic Quartz scheduler executions - using `last_trigger_time` as an oracle of "sweep ran on schedule" produces a silent false-negative. <!-- discovered in exploratory-testing-qa1-master S2, qa1, 2026-06-10 --> | Filter by `WHERE scheduled_task_name = '{X}'`. To confirm a REAL Quartz execution: the correct oracle is `qrtz_triggers.prev_fire_time` (last scheduled execution fired) + presence of a row in `uown_sweep_logs` (sweep recorded result). Do NOT infer execution from `last_trigger_time`. Cross-link: pitfall #95, [[volatile-knowledge-registry]] category 2. |
| 95 | Query on `uown_sweep_logs` using columns `task_name` / `processed` / `created_timestamp` returns `column does not exist` | The actual columns in `uown_sweep_logs` are: **`sweep_name`** (NOT `task_name`), **`number_of_records_processed`** (NOT `processed`), **`error`**, **`source`**, **`start_time`** / **`end_time`** (NOT `created_timestamp`). Old scripts assumed abbreviated names. <!-- discovered in exploratory-testing-qa1-master S2, qa1, 2026-06-10 --> | Use the real names: `SELECT sweep_name, number_of_records_processed, error, source, start_time, end_time FROM uown_sweep_logs WHERE sweep_name=$1`. Remember that `number_of_records_processed` is written AFTER processing (pitfall #87) - an immediate read returns `0`. Cross-link: pitfall #87, #94, [[payment-flows]] Email Sweep validation section, [[volatile-knowledge-registry]] category 2. |
| 96 | Spec/query for e-sign asserting `status='SENT'` in `uown_esign_document` never matches | The value `SENT` does NOT exist in the status enum of `uown_esign_document` (actual column = **`status`**, varchar 255, col 37 - NOT `document_status`). The actual enum values are **`STORED` / `SENT_TO_CUSTOMER` / `COMPLETED` / `ERROR` / `CANCELLED`**. The "sent to customer" state is `SENT_TO_CUSTOMER` (NOT `SENT`); the successful terminal state is `COMPLETED` (NOT `SIGNED`). Confusing this with the `uown_email_queue` enum (which DOES use `SENT`/`STORED`) leads to an assertion that never passes. <!-- discovered in exploratory-testing-qa1-master S7, qa1, 2026-06-10 --> | In e-sign document assertions use `status='SENT_TO_CUSTOMER'` for sending and `status='COMPLETED'` for completion. Authoritative schema: [`docs/database-schema.md`](../../../../docs/database-schema.md) section `uown_esign_document` (col 37 `status`). Cross-link: [[gowsign-knowledge]] status enum section. |
| 97 | `POST /ConfigurationManagement/createOrUpdateConfig` + `forceReloadConfig` does NOT change the behavior of keys served by the `system.config` in `application.yaml` (e.g. `required.fields.for.<ClientType>` from the validator, `is.preauthorization.required.for.servicing`); the app keeps using the code default. **Symptom:** config written to the DB (`uown_configuration_management`, row present, endpoint 200) + reload 200, but runtime ignores it (e.g. `sendApplication` without `mainNextPayDate` for DANIELS_JEWELERS still returns 400 `mainNextPayDate is required`). | There are **TWO disconnected config families** in svc. (a) **DB-backed** `common.service.ConfigurationManagementService` (`configRepo` in `uown_configuration_management`) - this is what `createOrUpdateConfig` writes to; used by e.g. `TrustpilotService`, `WebhookService`. (b) **YAML/Hazelcast** `svc.config.configuration.ConfigurationManagement` → `ConfigurationUtility.getString` reads from `IMap configurationMap` which is re-hydrated on EVERY read ONLY from `SystemConfigurationProperties` (`system.config.*` from YAML) via `getConfigurationUtility` → `configurationMap.setAll(...)` (`ConfigurationAnnotationProcessor.java:40-43`). NOTHING loads the DB table into the IMap; `forceReloadConfig` (`evictAll`+reinject) is re-overlaid by YAML on the next read. The application validator uses family (b) (`LosRequestMessageConstraintValidatorConfig.java:3,20-25`) → DB key never reaches it → falls back to code default (`...,mainNextPayDate`). <!-- discovered in exploratory-testing-qa1-master S9, qa1, 2026-06-10 --> | **Determine the family by the `import` at the call site.** `import com.uownleasing.common.service.ConfigurationManagementService` → DB-backed → `createOrUpdateConfig` WORKS. `import com.uownleasing.svc.config.configuration.ConfigurationManagement` → YAML/Hazelcast → only via `application.yaml` (repo `configuration`, e.g. commit `62e2fc20`) + **pod deploy/restart** (infra access, outside QA scope). Do NOT rely on `createOrUpdateConfig`+`forceReloadConfig` for family (b) keys; always validate with a fresh repro after applying. Cross-link: memory `reference_config_management_endpoint.md` (corrected 2026-06-10; preauth toggle marked for re-verification for the same reason). |
| 98 | `SearchPage.searchAndSelectFirst` (Origination navbar quick search) does NOT navigate when invoked from the **/funding** page (Funding Queue, filters panel open): the search fills the input, the BFF `simpleSearch` fires and returns the lead with HTTP 200, BUT the autocomplete dropdown **never renders the anchor** `nav a[href*="/customers/"]` -> the waiter (5s) falls back to Enter, does not find the table link, and **returns SILENTLY**. `navigateToOriginationCustomer` sees the URL still at `/funding`, retries 3x and throws `failed after 3 UI search attempts`. **Diagnosis:** error `[Navigation] ... still on .../funding, retrying...` 3x after a funding step, with the search input visible and the value filled in the screenshot but NO dropdown. NOT timing, NOT invalid session, NOT FUNDED lead unsearchable (the `simpleSearch` returns the FUNDED lead by `Lead #` normally, status 200). | The `#search-input` in the navbar is a React-controlled autocomplete that only **renders the results dropdown when the input is focused/active**. `fill`/`clear`/`pressSequentially` update the value and trigger the debounced `input` event (BFF runs, returns 200), but on pages that manage their own focus (Funding Queue, whose filters panel captures focus) the component computes the results and does NOT mount the dropdown anchor without explicit focus. On /overview and /customers the page flow itself leaves the input in a state that renders the dropdown, masking the bug. <!-- DOM-first confirmed live in sandbox 2026-06-12: without `click` the anchor is absent 3/3 runs (BFF fired on all runs, status 200); with `click` before fill the anchor renders 3/3 and navigates; overview without regression. CLAUDE.md #15 --> | In `searchAndSelectFirst`, call `await this.quickSearchInput.click()` (focus) BEFORE `clear`+`fill`. The `click` makes the dropdown reliably rendered on ANY page (validated: funding 3/3 OK, overview/customers without regression). Do NOT mask with timeout increase, retry, `force:true`, or re-login (the session is valid — the pre-navigation re-login in `tests/ci/unified-flow.spec.ts:305` was a workaround for another symptom, NOT the root cause; the page-object fix makes it unnecessary). Do NOT rely only on the Enter fallback (it is silent when there is no table link). Fix in `src/pages/search.page.ts` `searchAndSelectFirst`. Cross-link: [[dom-investigation]], [[selector-hardening]]. |
| 99 | Website (customer portal) sidebar navigation steps **pass silently** even when the SPA is broken/logged out — only a later step that waits for a real field (`changeEmailToGeneric` → `wsPrimaryEmailField.waitFor visible`) fails, masking the root cause (auth loss) several steps earlier. **Symptom:** `goToSidebarLink('documents')` and `goToSidebarLink('contact us')` log OK, then `update contact info` → `TimeoutError: locator.waitFor #primaryEmail...` + infinite spinner screenshot (`status: Loading...`). The log `[Website] Sidebar fallback: navigating directly to .../update-contact` appears, indicating the click path never found the sidebar items. | Two chained masking failures in `WebsiteBasePage`: (a) `goToSidebarLink` falls back to `navigateBySidebarUrlFallback` → `page.goto(slug)` which does a **full reload and loses the SPA auth** (the SPA itself, if the session already died, redirects to `/login` and stays in an infinite spinner); (b) `waitForSpinner` (`common.helpers.ts:5-25`) **swallows** the `TimeoutError` from `waitFor({state:'hidden'})` inside `try/catch`, so the method RETURNS even with a spinner that never disappears → the step "passes". Result: the 3 "invisible" items (`Update Contact Info`, `Account Settings`, `Account Summary`) did NOT disappear due to label/class drift — they do not exist because the page was already the LOGIN screen. <!-- DOM-first live sandbox 2026-06-12, account 17249: label "Account Settings", item "Update Contact Info", prefix `side-bar_sideBarContainer__item__*` and selector `#primaryEmail` ALL correct/confirmed; real cause = force-logout on /documents (see observation OBS-WS-DOCS-LOGOUT). CLAUDE.md #15 --> **[RECONFIRMED 2026-06-23 sandbox, account 17294, MCP Playwright live — `[CONFIRMED]` active]:** `GET /uown/svc/getFilesForAccount?accountPk=17294` → **HTTP 403**, body `{"unauthorized":true}`, valid JWT in header (userToken confirmed in localStorage before the click). Frontend reacts: 2x `GET /logout` (304) + redirect to `/` (login form). localStorage.accountStore post-redirect: `accountPk=null`, `userToken=null` — token completely cleared. Network sequence: req#46 `getFilesForAccount`→403 → req#49/51 `/logout` → req#50/52 `/` → login screen. Bug active in sandbox. Step remains commented in `tests/e2e/unified-flow.spec.ts:656`. <!-- MCP investigation 2026-06-23, rule #16 source-tag: [network-capture:MCP-sandbox-2026-06-23] --> | Do NOT rely on "sidebar step passed" as proof of navigation in the Website — the `page.goto` fallback + swallowed `waitForSpinner` makes the pass silent. A navigation step is ONLY reliable if followed by an assert on an element of the destination page (e.g. `wsPrimaryEmailField` itself). When a sidebar step "passes" but the next one times out on an infinite spinner: suspect auth loss (full reload via goto) BEFORE touching sidebar label/class. The `page.goto` fallback that loses auth should NOT be the normal path — prefer keeping SPA navigation (click) and, if the session died due to an upstream product bug, the test should FAIL visibly at that point, not several steps later. Cross-link: pitfall #92, [[dom-investigation]], [[page-object-pattern]] WebsiteBasePage. |
| 100 | **`sendApplication` returns `BLACKLIST_DENIED` even with an approvable SSN/email when the static fixture address has accumulated a blacklist entry in sandbox.** The LOS `blacklistCheck` matches by **`streetAddress1` + `zipCode`** (not by SSN/email/name). Reusing the static fixture CA address (`654 Sunset Blvd` / `90028`) between runs eventually hits a blacklist entry created by a previous run (blacklist pk:2165 in sandbox 2026-06-12) → every new application at that address is denied, regardless of a unique email. Unique email (pitfall #1) does NOT protect: the match is by address, not by identity. | `blacklistCheck` matches `streetAddress1 + zipCode`; a static fixture address is "poisoned" as soon as any run creates a blacklist entry for it. <!-- discovered in modify-lease.spec.ts cycle 1, sandbox 2026-06-12, blacklist pk:2165 --> | Use `buildTestData({ ..., uniqueAddress: true })` to vary `streetAddress1` per run (e.g. suffix `Unit 1-<hash>`), keeping city/state/zip valid for the risk tier. This prevents a poisoned static address from blocking fresh applications. Default for EVERY test that creates an application in a state with a static address fixture (CA TireAgent etc.). Do NOT use direct UPDATE/DELETE on the blacklist table (Exception 3). Cross-link: pitfall #1 (unique email — complementary, not a substitute). |
| 101 | **Action button in the customer summary (Origination) renders OFF-SCREEN to the right and `click({force:true})` does NOT trigger the React handler → modal never opens → misleading timeout on a field INSIDE the modal.** **Symptom:** `TimeoutError: locator.waitFor #numberOfItems to be visible` (or `locator.fill` on the same field) several steps after clicking "Modify Lease"; screenshot shows the customer page WITHOUT the modal open and the action buttons panel still visible. Field `#numberOfItems` is always visible WHEN the modal opens — the problem is the modal never mounted. | The action buttons (`Modify Lease`, `Send to Signed`, ...) live in an `overflow-auto` horizontal container (`customer-summary_customerSummary__accountSummary`). Buttons beyond the visible width render off-screen to the right (e.g. "Modify Lease" at x≈1625 in a 1440 viewport) reporting `visible:true` (offsetParent present). `locator.click` fails with `<ancestor> intercepts pointer events`; `click({force:true})` clicks the central coordinate of the element which is OUTSIDE the scroll viewport → the React onClick never fires → no modal opens. Additionally, the old `modifyLease` masked the failure: `Promise.race([...]).catch(()=>{})` swallowed the "modal did not open" and continued to `deleteAllInvoiceItems`, making the failure surface at `waitFor(#numberOfItems)` in the spec — wrong line. Subsidiary: the "Modify Lease" confirmation ALWAYS opens a CANCEL/**Continue** dialog (exact label `Continue`, NOT the `CONTINUE` from the caution text) before the real modal; and the delete icon is an `aria-hidden` `<svg data-icon="trash-can">` inside a `<div>` without a role, subject to interception by sibling inputs. <!-- DOM-first MCP live sandbox 2026-06-12, leads 97502/97509, viewport 1440×900 --> | `clickActionButton` must JS-dispatch the click (`page.evaluate`: find the button by text → `scrollIntoView({block:'center'})` → `el.click()`), NEVER `click({force:true})` on a button inside a container with horizontal overflow. `modifyLease`: make the "Continue" step mandatory (`waitFor visible` + click, without `.catch`) and wait for `#numberOfItems` visible WITHOUT `.catch` (failure here = modal did not open, real cause exposed at the right point). `deleteAllInvoiceItems`: JS-dispatch click on the svg trash wrapper (count-based loop, without delete confirmation). FORBIDDEN: increasing timeout, `force:true`, `waitForTimeout`, masking try/catch. Cross-link: [[dom-investigation]], [[selector-hardening]], `rules/page-objects.md` (DOM-first). |
| 102 | **`useNeuroIdCheck=true` (flag under test) is SILENTLY reset to `false` by the merchant preflight → NeuroID never fires → all `count >= 1` guards fail with a false-negative.** **Symptom:** signing-retry test with NeuroID "passes" trivially or fails on `expect(count1).toBeGreaterThanOrEqual(1)` even though the flow is correct; `uown_neuro_id_verification` stays empty for the `lead_pk`. | `useNeuroIdCheck` is in the `mustBeFalse` set of the merchant preflight contract (`src/data/merchant-config-contract.ts`). `createPreQualifiedApplication` calls `ensureMerchantReady` by default → auto-heal via `createOrUpdateMerchant` RESETS `use_neuro_id_check` to `false`, disabling NeuroID before the flow runs. Unique email / SSN has no effect — the gate is the merchant flag. <!-- qa2, 2026-06-15 --> | `skipMerchantPreflight: true` in EVERY `createPreQualifiedApplication` call when `useNeuroIdCheck=true` is the flag under test (preflight must not mutate the flag being tested). Compensate with a read-only inline pre-assert: `SELECT use_neuro_id_check FROM uown_merchant WHERE ref_merchant_code='OW90337-0001'` + `test.skip` if `false` (environment did not provision the flag → honest skip, not false-negative). Analogous to Pitfall #10 (config drift) and [[fraud-vendors-knowledge]] Pitfall #6/#9 (any fraud flag in `mustBeFalse` is auto-healed). |
| 104 | Config Columns panel on the `/merchant` page (Origination) is not found: selector `configColumnsPanel` (`[role='dialog']:has-text('Configure the view')`) returns **0 elements** | The **Overview** page uses a modal/dialog for Config Columns; the **Merchants** page (`/merchant`) uses a **Bootstrap `div.dropdown.show`** — completely different DOM patterns. The generic Overview selector does not match in Merchants. <!-- MCP Playwright qa1, 2026-06-15 --> | Use `SELECTORS.configColumnsPanelMerchants` (`div.dropdown.show:has-text('Configure the view')`) specific to `/merchant`. `MerchantListPage.openConfigColumns/closeConfigColumns` already use it. Cross-link: [[page-object-pattern]] MerchantListPage; `docs/knowledge-base/merchants-config-columns-export.md`. |
| 105 | `label:has-text('UW Pipeline') input[type='checkbox']` does not find the column checkbox in Config Columns on `/merchant` | The checkboxes do NOT have a `<label>` wrapping the input — they are **native `input[type='checkbox']`** whose `id` and `name` equal the column label (`value="true"` when checked, `value=""` when unchecked). <!-- qa1, 2026-06-15 --> | Locate directly by attribute: `input[type='checkbox'][name="UW Pipeline"]`. `MerchantListPage.columnCheckbox(name)` already does this. Cross-link: [[page-object-pattern]] MerchantListPage. |
| 106 | Test code waits for an Apply/Save button after toggling a column in Config Columns on `/merchant` — the wait never resolves | Column selection is **immediate** (BR-01): the table updates on the checkbox click, **without a confirmation step** (no Apply/Save button). <!-- qa1, 2026-06-15 --> | After `setColumn(name, checked)`, the UI already reflects the new state — there is no Apply button to click. Do not add a wait for Apply/Save. Cross-link: [[page-object-pattern]] MerchantListPage. |
| 107 | Active filter on `/merchant` (Origination): cannot select "All" via dropdown, and changing the option does not re-filter the table | The Active filter is a **react-select `#isActive` with only 2 options (`Active`/`Inactive`)** — there is NO "All" option (to show all merchants, **clear the selection via X**). The page default on load is `Active` (`isActive: true`). The change is **NOT automatically applied** — requires clicking the **Search** button (BR-06, `button[name='searchButton']`). <!-- qa1, 2026-06-15 --> | `MerchantListPage.setActiveFilter('Active'|'Inactive')` selects the option and clicks Search. For "all", clear the react-select selection and click Search. Do not assume auto-apply on change. Cross-link: [[page-object-pattern]] MerchantListPage; [[volatile-knowledge-registry]] category 4 (selectors on pages with recent refactor). |
| 103 | **`uown_sv_outbound_api_log` does NOT correlate NeuroID calls by `lead_pk` for pre-funding leads** — `account_pk`/`source_uuid`/`return_uuid` are all NULL in those rows, making "NeuroID was/was not called" assertions by lead impossible. **Symptom:** attempt to count NeuroID calls via `uown_sv_outbound_api_log WHERE lead_pk=...` returns 0 rows (column `lead_pk` nonexistent/no match) even though NeuroID ran. | The table has the rows (`url ILIKE '%neuro-id.com%'`) but without any correlation key for the `lead_pk` in a pre-funding state (account does not yet exist). Discarded by the discovery probe. <!-- discovered via discovery probe src/scripts/probe-neuroid.ts, qa2, 2026-06-15 --> | Use `uown_neuro_id_verification WHERE lead_pk = $1` (helper `countNeuroIdCalls`, [[helpers-catalog]]) as the truth source for NeuroID call counts in signing tests (Option B). Cross-link: [[fraud-vendors-knowledge]] § NeuroID + Pitfall #10. |
| 108 | **`/merchantSetting` (Origination): `selectMerchantRowByText('OL90202-0001')` gives `TimeoutError ... .rdt_TableRow:has-text("OL90202-0001") not visible` — the merchant row simply does NOT exist in the DOM.** **Symptom:** timeout on a row selector right after `loadMerchants()`; the page snapshot shows the table with ~20 rows (Synchrony, Vedo, Bridge...) but WITHOUT the target merchant. Increasing timeout would never resolve it (the row never appears). | The Merchant Setting table renders only the **first page (~20 rows)** by default. The old `loadMerchants()` clicked Filters → "Search" button with the search box EMPTY, which reapplies the empty filter and returns the same default page — NEVER loads all merchants. terraceFinance (`OL90202-0001`) is not among the ~20 → the row genuinely does not exist until filtered. The correct search box is the `textbox "Search"` with `placeholder "Search table"` INSIDE the Filters panel. <!-- DOM-first via trace error-context.md a11y snapshot, qa2, 2026-06-16 (MCP unavailable — fallback chain [[dom-investigation]]) --> | Type the code in the "Search table" box and apply BEFORE selecting the row. Selectors: `msMerchantSearchTableInput` (`input[placeholder='Search table']`) + `msMerchantFilterSearchButton` (`button:has-text('Search')`). `MerchantSettingPage.filterMerchantTable(code)` does fill+Search; `selectMerchantRowByText` calls it automatically if the row is not visible. FORBIDDEN to increase timeout / `force:true`. Cross-link: [[selector-hardening]], [[dom-investigation]], [[page-object-pattern]] MerchantSettingPage. |
| 109 | **SSN ending in 9 (`generateTestSSN(false)`) is APPROVED (UW_APPROVED) in qa2 for terraceFinance, when the documented convention says UW_DENIED.** **Symptom:** negative denial test fails with `expected a denial status, got "UW_APPROVED"`; the ending-in-9 lead records an approval snapshot normally. | The "ending-in-9 → UW_DENIED" rule is a **test-server** gate controlled by a boolean config in `SendApplicationService` — it does NOT depend on the credit engine. **`[CONFIRMED]`** (code): `svc/.../application/SendApplicationService.java:361-365` only denies when `!isProduction()` **AND** `getBoolean("com.uownleasing.svc.service.SendApplicationService.deny.ssn.ending.with.9", true)` **AND** `mainSSN.endsWith("9")`. In qa2 the ending-in-9 lead is **approved** (activity log shows BlackBox/ABB deciding, 0 vendor calls) → the gate did not fire → the config is **effectively `false`** in qa2 (the key is ABSENT from `uown_configuration_management`, so the default `true` should apply; the `false` override comes from deploy-level properties above the table — `[HYPOTHESIS]` on the exact layer). <!-- qa2, 2026-06-16; reproduce via probe-uw-denial-engine.ts --> | Do NOT assume ending-in-9 denies outside qa1/sandbox. Config: `com.uownleasing.svc.service.SendApplicationService.deny.ssn.ending.with.9` (set via `POST /ConfigurationManagement/createOrUpdateConfig` + `GET /ConfigurationManagement/forceReloadConfig`). **TESTED 2026-06-16: setting the flag=`true` (API 200, DB confirmed, cache reloaded) was NOT enough in qa2 — a fresh ending-in-9 lead still returned `UW_APPROVED` (lead 16583).** Denial also requires `!isProduction()` AND the flag (line 361); with the flag `true` still approving, qa2 is effectively treated as **production** for this gate (or the deployed build is before commit `d6c943f411`). → Config alone **does NOT enable** denial in qa2; escalate to dev/DevOps (confirm `isProduction()`/svc build on qa2). For denial tests: use **sandbox/qa1** (mock honored). Config was reverted (row deleted). Pre-UW deny (Blacklist → BLACKLIST_DENIED, no-business-in-state) is NOT "denied by underwriting". If config cannot be changed → run the negative scenario in sandbox/qa1, OR documented `test.skip` + question to PO. Cross-link: [[ssn-test-modalities]], [[fraud-vendors-knowledge]], `docs/knowledge-base/underwriting-and-funding-test-data-paths.md`. |
| 110 | **EPO 5% / EPO 10% triple-checkbox in `/merchantSetting` (GDS Data): `#epo5-false`/`#epo10-false` resolve in the DOM but stay `not visible` → `locator.check` timeout (`merchant-setting.page.ts:setEpoTriple`).** **Symptom:** `Element is not visible — waiting for locator('#epo5-false')`; `waitFor('attached')` passes but `check({force:true})` fails/clicks an invisible input. The EPO immutability test never gets to run. | Each EPO field is a **collapse dropdown**, NOT an inline set of three checkboxes: `div#toggler` (visible row) contains `input#epoN-main` (enable) + `svg.fa-caret-down.cursor-pointer` (toggle); the panel `div.checkbox-dropdown_...card__....collapse` (`display:none`) contains `input#epoN` (True) and `input#epoN-false` (False). Checking `-main` alone auto-checks value=true but does **NOT open the panel** → the True/False inputs remain inside an invisible `.collapse`. `force:true` was masking this (clicking an input with `offsetParent===null`). <!-- DOM-first live qa2 via npx tsx inspect script (MCP not provisioned), 2026-06-16 --> | Enable `#epoN-main` (check, without force), **click the caret** `#toggler:has(#epoN-main) svg.fa-caret-down` to expand the `.collapse`, wait for `state:'visible'` on the True/False target and `check()` **WITHOUT force**. Idempotent: only clicks the caret if the target is not yet visible. Selectors: `msEpo5CaretToggle`/`msEpo10CaretToggle`. `MerchantSettingPage.setEpoTriple` already does this. NOTE: `#toggler` id is duplicated (invalid HTML) — scope via `:has(#epoN-main)` to get 1 caret. FORBIDDEN: `force:true`/timeout bump. Cross-link: [[selector-hardening]], [[dom-investigation]], [[page-object-pattern]] MerchantSettingPage. |
| 112 | **Merchant Settings Snapshot: where the snapshot comes from and which table/column/audit-trail is involved — getting this wrong makes the test assert a nonexistent lead_notes entry OR re-read the live merchant.** **Symptom:** snapshot test looks for a note in `uown_los_lead_notes` (not found → false-negative) OR expects the account snapshot to reflect the live merchant state at funding time (it does not — it is a copy from the lead). | **Confirmed mechanics:** (a) **Lead snapshot** is written in the `ApplicationApprovedEvent` (end of `ApplicationProcessor`), created **only for APPROVED applications** (denied/abandoned do not generate a snapshot). (b) **Account snapshot** is written in `LosToSvcImportService.importFromLosToSvc` and **COPIES the lead snapshot** (NEVER re-reads the live merchant), gated on the existence of the lead snapshot — if the lead snapshot is missing, the account is still created and the snapshot is **silently skipped** (no error). Tables: `uown_los_lead_merchant_settings_snapshot` / `uown_sv_account_merchant_settings_snapshot`; columns: `epo5, epo10, uw_pipeline, fraud_threshold` (+ `lead_pk`/`account_pk`/`merchant_pk`/`row_created_timestamp`). **Audit trail = application INFO/WARN logs, NOT `uown_los_lead_notes`** → do NOT assert lead_notes for snapshot creation. Scope is **audit/reporting/analytics only — no functional runtime consumer** (snapshot does not alter decision/funding). <!-- `[user-provided:dev-test-plan]` --> | To validate snapshot: (1) approved lead → `SELECT ... FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk=$1`; (2) post-funding → `SELECT ... FROM uown_sv_account_merchant_settings_snapshot WHERE account_pk=$1` and assert **equality with the lead snapshot** (not with the live merchant). For the denial path: assert **absence** of a snapshot row. Do NOT use `uown_los_lead_notes` as an oracle for snapshot creation. Cross-link: [[ssn-test-modalities]] (denial recipe for denied branch), [[merchant-preflight]]. |
| 113 | **qa2 DB tunnel transient blocker: test fails fast on the FIRST DB read (`assertMerchantContract`) without being a contract drift or a feature finding.** **Symptom:** suite breaks right at the first `db.*`/preflight with a connection error (`127.0.0.1:5445`) or `password authentication failed for user "svc_user"` — intermittent, in the middle of a run that was previously OK. | The qa2 DB via `kubectl port-forward` at `127.0.0.1:5445` can **drop mid-run** and the `svc_user` auth can **transiently fail**. Since `assertMerchantContract` (merchant preflight) is the first DB access, the failure surfaces there and looks like a contract/feature issue. **NOT a merchant config drift and NOT a feature finding** — it is infra. <!-- `[test-execution:qa2,2026-06-17]` --> | Recognize as **infra/transient** (similar to pitfall #18, `[env-blocker]`): re-probe the tunnel/DB (reopen port-forward, validate `SELECT 1`) and **re-run**. Do NOT classify as a bug, do NOT open a contract pitfall, do NOT touch the merchant. Cross-link: pitfall #18 (Cloud SQL proxy / `svc_user` auth). **[env-blocker]** |
| 111 | **Last sub-test of `test.describe.serial` fails with `"context" and "page" fixtures are not supported in "afterAll"` (same for `testEnv`) AND the teardown never executes → merchant config drift leaks into the next run.** **Symptom:** Playwright marks the last serial sub-test as ❌ even though the body passes (DB oracle confirms the feature OK); config restore never runs. | `test.afterAll` only accepts **worker-scoped** fixtures. In this repo's fixture: `db`/`email` are worker (OK in afterAll); `page`, `context`, `request`, `api`, `testEnv` are **test-scoped** (FORBIDDEN in afterAll). Destructuring `{ page, testEnv }` in afterAll breaks with this error and aborts the entire teardown. <!-- qa2, 2026-06-16 --> | In `afterAll` use ONLY `{ browser, db }`. Create your own context: `const ctx = await browser.newContext(); const page = await ctx.newPage(); try { ... } finally { await ctx.close(); }` (pattern already used in `tests/e2e/gowsign/*`). Derive env the way the `db` fixture does: `new ConfigEnvironment(process.env.ENV ?? '<default>')` (there is no `testEnv`/`envName` in afterAll). Restore via UI = the feature's edit path (NOT raw DB, Rule #9). Cross-link: [[page-object-pattern]] §Anti-patterns, [[common-operations]]. |
| 114 | **Reusing a `redirectUrl` (signing/contract link) captured from a previous `sendInvoice` opens `"Invalid link. Please contact merchant"` instead of the GowSign modal.** **Symptom:** `TimeoutError: locator.waitFor` on `.alternative-contract-vendor_iframeContainer__yAn5c` (modal never opens); error-context snapshot shows `alert: "Invalid link. Please contact merchant"`. Affects tests that invoke `sendInvoice` N times (one per frequency/plan) and store each `redirectUrl` to open later — the FIRST (oldest) link is already dead. | **A lead has ONE live contract link.** Each `sendInvoice` MINTS a NEW shortCode and SUPERSEDES the previous one — the old `redirectUrl` becomes invalid. There is exactly ONE `uown_esign_document` per lead (1 GOWSIGN doc, same UUID), created when the link is opened (CC submit → CONTRACT_CREATED), NOT one per frequency. DOM-proven qa2 2026-06-18, lead 16668: 1st WEEKLY `hqrSW2US` → "Invalid link"; re-issued WEEKLY `mWjbG7eN` → live CC page. NOT a timing flake nor a wrong selector (the most recent link opened the same modal successfully). | **Re-issue `sendInvoice` for the target frequency IMMEDIATELY before opening the render** and use the `redirectUrl` returned at that instant (live link at open time). The oracle VALUE math (costPrice/n/nextPayment/salesTax/contract from `paymentDetailsList`) does NOT need a live link — capture once; only the RENDER drive needs a fresh link. FORBIDDEN to mask with timeout bump / retry / selector swap (Rule #15 — the selector is correct). Cross-link: [[gowsign-knowledge]] §host iframe + §enum status, [[ui-first-principle]]. <!-- qa2, 2026-06-18 --> |
| 115 | **Overview has TWO filter forms with identical date inputs; a positional selector (`nth()`) hits the WRONG form.** **Symptom:** the test fills in a date but the table/CSV does not change (affected the metric cards) or vice versa — the lever appears to do nothing, without an error. | Overview renders 2 independent forms, both with MM/DD/YYYY inputs: **(a) top-bar KPI form** — ids `#from`/`#to`, toggle class `overview_filterButton__`, drives the metric cards; **(b) TABLE panel** — ids `#fromDate`/`#toDate`, toggle class `index-module_filterButton__`, drives the table + the CSV export. An `nth()` positional selector hits the KPI form. DOM-proven qa2 2026-06-18. | Target the **table panel inputs by id** (`#fromDate`/`#toDate`), never by position; expand via the panel's own toggle (`expandTableFilters`, see #117). FORBIDDEN: `nth()` when there are duplicate forms. Cross-link: [[selector-hardening]] §multiple forms, [[page-object-pattern]] OverviewPage. <!-- #1321, qa2, 2026-06-18 --> |
| 116 | **Overview: a "future-only" date window in the table panel does NOT empty the table** (empty-set lever is unreliable). **Symptom:** the test sets `#fromDate`/`#toDate` to a future range expecting 0 rows (to exercise empty-table / Email-disabled), but the table remains populated. | `#fromDate` (table panel) **resets to today** on re-render (Formik initial value) — the future date does not "stick", so it does not filter out existing leads. DOM-proven qa2 2026-06-18. | Use the **free-text "Search table"** (`SELECTORS.overviewTableSearch`, method `OverviewPage.searchTable(value)`) with a value guaranteed not to exist → deterministic empty set. Cross-link: [[page-object-pattern]] OverviewPage.searchTable. <!-- #1321, qa2, 2026-06-18 --> |
| 117 | **Overview: the table-filter panel re-collapses right after the toggle click (1 click is not enough).** **Symptom:** `expandTableFilters` clicks the toggle, the panel opens and closes itself milliseconds later; the `#fromDate`/`#toDate` inputs become `not visible` intermittently. | `verifyDashboardLoaded` resolves on `Promise.race` as soon as the Filters button appears — **BEFORE** the table finishes loading in QA2. The panel is a **width-collapse animation**; a re-render triggered by the table's delayed loading **re-collapses** the panel right after the toggle. DOM-proven qa2 2026-06-18. | `expandTableFilters` uses a **retry loop** (re-clicks the toggle until the target input becomes `state:'visible'` in a stable manner), NOT a single click. Do NOT mask with `waitForTimeout` (Rule #15) — the retry is idempotent and observes the panel's real state. Cross-link: [[page-object-pattern]] OverviewPage.expandTableFilters, [[dom-investigation]]. <!-- #1321, qa2, 2026-06-18 --> |
| 118 | **Clicking "Download CSV" opens the EMAIL modal (clicks the wrong button).** **Symptom:** the direct download test goes through the email path — opens `"Which email should we send this CSV file to?"` instead of capturing a `Download`; no file arrives. | Email CSV and Download CSV are rendered by the SAME component (`FilteredCsvDownload`) and **share the class `filtered-csv-download_csvButton`**; **Email CSV is the FIRST in the DOM**. A bare class selector + `.first()` resolves to Email CSV. DOM-proven qa2 2026-06-18 via MCP. | Disambiguate the Download button by its unique text: `SELECTORS.csvDownloadButton = "button[class*='filtered-csv-download_csvButton']:has-text('Download CSV')"`. NEVER select Download by class alone. Cross-link: [[selector-hardening]] §shared class, [[page-object-pattern]] FilteredCsvDownloadControls. <!-- #1321, qa2, 2026-06-18 --> |
| 123 | **Overview TABLE-panel `#fromDate`/`#toDate` ignore `fill()` — Formik does not update, and the 48 MiB guard has three distinct traps for automation.** **Symptom (a):** the test fills the table panel date range via `fill()`, the submitted query uses the PREVIOUS value (range does not change) → wrong row count. **Symptom (b):** with a large dataset (79k rows) `submitFilters()` resolves too early and button assertions run against the still-loading table. **Symptom (c):** the Download-CSV-disabled tooltip does not appear as text inside the `<span id="overview-csv-download-...">`. | **(a)** The `#fromDate`/`#toDate` inputs are `type="search"` controlled by Formik. `fill()` writes to the DOM but **does NOT trigger Formik's `onChange`** — the internal state stays with the previous value (same root as pitfalls #85 for the DatePicker). **(b)** `submitFilters()` uses `tableRow.first().waitFor()`, but the spinner `<tr>` counts as "row visible" before data arrives on large datasets. **(c)** The tooltip is a **Bootstrap portal** rendered OUTSIDE the wrapper span: `<div role="tooltip" class="tooltip show bs-tooltip-auto">`. DOM-proven sandbox 2026-06-18 (#1321). | **(a)** `setFormikDate(selector, value)` (private in `overview.page.ts`): accesses `element.__reactProps.onChange` via `page.evaluate` and fires the React fiber handler directly; uses an internal `setTimeout(150ms)` BETWEEN fromDate and toDate (the fromDate re-render recreates the toDate node, invalidating the reference). Do NOT use `fill()` on these inputs. **(b)** `page.waitForFunction` waiting for real pagination (`parseInt(match[3]) > 1000`) with timeout 120s BEFORE checking button state (in spec CT-08). **(c)** new selector `csvDownloadTooltipPortal: "div[role='tooltip'].tooltip.show, div.tooltip.show"` and `getDownloadDisabledTooltip` searches the portal, not the inner text of the span. Cross-link: [[selector-hardening]] §portal tooltip / Formik type=search, [[page-object-pattern]] OverviewPage.setFormikDate / FilteredCsvDownloadControls.getDownloadDisabledTooltip. <!-- #1321, sandbox, 2026-06-18, CT-08 PASS 79605 rows → 55.4MB > 48MB → Download disabled --> |
| 124 | **GDS-snapshot fields `npm_segment`/`tam_score` (`uown_los_uwdata`/`uown_sv_uwdata`) read as NULL → false-bug.** **Symptom:** the test creates an application, expects `tam_score`/`npm_segment` to be populated, and sees NULL, classifying it as a defect — but the env/merchant never routed the path that writes these fields. | The fields are written ONLY on a **GDS decision with `eligible_terms=16`** (`GdsResponseParser.setCurrentUWConfigData`, MR svc !1469). `tam_score` is additionally **TireAgent-only**. In **qa2**: TireAgent is decided by GDS but **capped at 13m** (never 16), and the Second Look (`100000053`) **DENIES/short-circuits** → `tam_score` unreachable (`count=0` over 6046 los + 2037 sv). `npm_segment` DOES populate in qa2 via Kornerstone/UOWN/PayTomorrow 16m GDS. DB-proven qa2 2026-06-19. | **ALWAYS** read `decided_by_agent` + `eligible_terms` BEFORE asserting the fields (`getUwScoresByLeadPk`): if `decided_by_agent!='GDS'` OR `eligible_terms` without `16` → `test.skip(reason)`, NEVER a red assert (Rule #10). `tam_score` requires an env where the TireAgent Second Look approves 16m (stg; dev2 to confirm — PENDING Marcos). Cross-link: [[ssn-test-modalities]] C.2, [[fraud-vendors-knowledge]], `docs/knowledge-base/npm-segment-tam-score-snapshot-routing.md`. <!-- qa2, 2026-06-19 --> |
| 126 | **Protection Plan / Buddy notes go to `uown_los_activity_log`, NOT to `uown_los_lead_notes`.** **Symptom:** test asserts opt-in/opt-out/enrollment note via `uown_los_lead_notes` and finds 0 rows (false-negative), even with the correct `uown_los_protection_plan` row. | `uown_los_lead_notes` carries UW progression (`[SubmitApplication]`/`[UnderwritingService]`); PP/Buddy events go to `uown_los_activity_log`. Canonical opt-IN texts: "Customer signed and opted in for protection plan. Initiating next steps... connector token and enroll with Buddy." → "Successfully initiated protection plan with Buddy. Status: COMPLETED". `[db-observation:uown_los_activity_log, qa2, leads 16801-16809, 2026-06-21]`. | Query `uown_los_activity_log WHERE lead_pk=$1` for any PP/Buddy note. Keep `uown_los_lead_notes` only for UW progression + "missing data" guard. Cross-link: [[activity-log-validation]], [[volatile-knowledge-registry]]. <!-- qa2, 2026-06-21 --> |
| 127 | **Buddy enrollment only FINALIZES at e-signature; opt-in on Terms creates only a PENDING row.** **Symptom:** test opts in/out, checks the DB and sees `status=PENDING`/`policy_id=null`/no note → fails; increasing the poll does not help if the test does not sign. | "Proceed to signature" persists `uown_los_protection_plan` opt_in PENDING (`policy_id=null`); the Buddy async callback sets `status=COMPLETED` + mints `policy_id` ONLY after the contract is signed (~45s after the row is created). Widget flow: Sign Contract → check `#isInfoConfirmed`+`#isEverythingAgreed` → "See Protection Benefits" → opt-in/out radios → "Proceed to signature". `[db-observation:uown_los_protection_plan, qa2, 2026-06-21]`. | Drive `ContractPage.completeESign()` after the opt-in/out and do a generous poll (~240s) for `status=COMPLETED`+`policy_id` (helper `waitForProtectionPlanCompleted`). Cross-link: [[application-lifecycle]] (signing), [[gowsign-knowledge]]. <!-- qa2, 2026-06-21 --> |
| 128 | **Cross-coverage (already-covered customer) shows a DIFFERENT Buddy widget, WITHOUT radios.** **Symptom:** `acceptAndProceedWithProtectionPlan` throws "no frame exposed >= 2 radios after 5 attempts" for a lead whose email already has an active enrollment. | When the customer's email is already enrolled (a previous lease opted in), the widget displays "Our records indicate that you're already enrolled!" + only the PROCEED TO SIGNATURE button (0 radios). DOM-proven qa2 2026-06-21 (CT-05, screenshot). | Use `TermsOfAgreementPage.acceptProtectionPlanAlreadyEnrolled()` (`terms-of-agreement.page.ts`), which detects the "already enrolled" panel and clicks Proceed directly. Do NOT use the radios flow for an already-covered email. Cross-link: [[page-object-pattern]] TermsOfAgreementPage, [[ui-first-principle]]. <!-- qa2, 2026-06-21 --> |
| 129 | **DB/API-only specs in `docs/taskTestingUown/` have no browserless Playwright project.** **Symptom:** a task spec without a UI flow does not run under ANY of the `task-testing-*` projects (both require a portal tag @origination/@servicing + browser auth dependency); running under the default project tries to log in to the portal. | The two `task-testing-*` projects do `grep /@servicing|@origination/` + `dependencies:['auth-*']` (browser login + storageState). An API/DB-only spec (e.g. a sweep) has no portal tag → 0 selected. | Run via a temporary browserless config `playwright.browserless.config.ts` (loads `.env`, no browser, no auth dep, `workers:1`, serial). **Do NOT delete `playwright.browserless.config.ts`** until a permanent project exists. **ADR-candidate:** add a permanent browserless `task-testing-api` project. Cross-link: [[e2e-examples]]. <!-- sandbox, 2026-06-21 --> |
| 130 | **Sandbox DB tunnel: port 5445 is reused across envs (qa1/qa2/dev3/sandbox).** **Symptom:** queries silently hit a non-sandbox cluster (auth fails as `password authentication failed for svc_user`, OR returns data from another env). | The manual port-forward is re-pointed depending on the day's env; `.env UOWN_DB_URL_SBX` says 5446 (stale) but the live tunnel is usually 5445. Connecting via raw URL with the sandbox password only failed because 5445 was pointing to the wrong cluster — NOT due to a special character in the password. | Before asserting: verify the tunnel points to sandbox via `to_regclass('uown_sticky')` + a canonical row (sticky pk36/account 5084). Connect via pg **config-object (raw password)** OR connection string **URL-encoded**. Cross-link: [[sticky-refund-tests-sandbox-only]], `docs/knowledge-base/sticky-recover-cancel-sweep.md`. <!-- sandbox, 2026-06-21 --> |
| 131 | **pg returns `bigint`/`pk` columns as JS strings.** **Symptom:** numeric matchers (`toBeGreaterThan(0)`) fail with "received value must be a number" receiving `"39"`. | The `pg` driver serializes `bigint` as a string to avoid losing precision. | Coerce with `Number()` before a numeric matcher (`expect(Number(row.pk)).toBeGreaterThan(0)`). Cross-link: [[db-polling-pattern]]. <!-- sandbox, 2026-06-21 --> |
| 132 | **[FutureFpdCheckStep] A customer (SSN) with an already SIGNED lead + a future first-payment-date is DENIED on ANY new application.** **Symptom:** new lead falls into DENIED early in the pipeline; activity log shows `"Signed lead with future fpd already exists"`. Affects signing tests that re-run with the SAME SSN. | The origination pipeline rejects a new app when the same SSN already has a SIGNED lead whose first-payment-date has not yet arrived (idempotency/deduplication guard pre-UW). Observed: lead 16862 DENIED because a previous run left 16861 SIGNED. <!-- qa2, 2026-06-22 --> | **A test that SIGNS must use a FRESH SSN per run** (`generateTestSSN`/`resolveSsn`), never a sticky fixed SSN if the run will reach SIGNED. Reusing the sticky `082390916` is safe only as long as no run drives it to SIGNED. Cross-link: [[ssn-test-modalities]] §C, [[test-data-hierarchy]]. |
| 133 | **CANCELLED_DUP_SSN: creating a new app with an SSN that already has an ACTIVE lease CANCELS the predecessor lease.** **Symptom:** a lead/lease created in a previous CT appears CANCELLED unexpectedly; activity log: `"Lead is cancelled due to new app... CANCELLED_DUP_SSN"`. Breaks suites that assume the previous lead is intact. | The pipeline cancels the predecessor lease of the same SSN when originating a new one (dedup by active SSN). <!-- qa2, 2026-06-22 --> | Use **distinct SSNs between parallel leads**, OR interleave create→use (create and consume one lead before creating the next with a different SSN). Do not keep two active leads from the same SSN expecting both to survive. Cross-link: pitfall #132, [[ssn-test-modalities]]. |
| 134 | **Address-based blacklist: `uown_los_black_list` matches by `street_address1 + zip_code` — a "proven-good" pinned address may have been manually blacklisted.** **Symptom:** pre-UW denial on the blacklist step for an address that previously worked; no code error. | The blacklist match is by `street_address1 + zip_code`. The long-pinned OH address `1875 N Sandusky Ave / Bucyrus 44820` was **manually blacklisted** (pk 2191/2196, 2026-06-19) → every app at that address denies at the blacklist step. <!-- qa2, 2026-06-22 --> | **Never hard-pin a single street for repeated app creation.** Use `randomAddress(state)` (`src/data/realistic/`) which generates a unique street per call (blacklist-immune). Check `SELECT * FROM uown_los_black_list WHERE street_address1 ILIKE $1 AND zip_code=$2` before assuming an address is clean. Cross-link: [[helpers-catalog]] realistic factory, [[test-data-hierarchy]]. |
| 135 | **pdf-parse breaks thousands separators: a rendered total `"1,693.41"` comes out as `"169 3.41"` (comma became an internal space).** **Symptom:** money parser captures `169` or `3.41` instead of `1693.41`; monetary value assertion fails by an order of magnitude on a PDF that is visually correct. | `pdf-parse` when flattening the PDF drops the thousands comma and injects an internal space. A regex `[0-9.,]+` stops at the space and captures only one piece. <!-- qa2, 2026-06-22 --> | Money parser MUST **remove spaces AND commas** (`.replace(/[\s,]/g, '')`) BEFORE reading the number, not rely on a `[0-9.,]+`. Cross-link: `contract-pdf.helper.ts`, [[gowsign-knowledge]] §pitfall float. |
| 136 | **`MERCHANT_PREFLIGHT_SKIP=true` globally in `.env` (left by a parallel session) causes a fresh-application test to SKIP the preflight; with the merchant's 13m programs derived to 0 by another session, `sendInvoice` returns an empty `paymentDetailsList` → `redirectUrl required` (`""`).** **Symptom:** lead reaches `UW_APPROVED`, `[SendInvoiceService]` creates SchedSummary, but the test explodes at `expect(contractUrl).toBeTruthy()` BEFORE any UI. Logs: `[merchant-preflight] OW90218-0001 bypassed via MERCHANT_PREFLIGHT_SKIP=true` + `[LeadProgramService] No eligible programs, returning 13 month`. | The env var `MERCHANT_PREFLIGHT_SKIP` overrides the `skipMerchantPreflight` option of `ensureMerchantReady` even when the test does NOT ask for a skip. TireAgent (`OW90218-0001`) was left with `programs-13m: 0` (drift from parallel sessions) → no eligible program → no payment plan → no `redirectUrl`. <!-- re-run, qa2, 2026-06-22 --> | A test that CREATES a fresh application must run with preflight ENABLED (`MERCHANT_PREFLIGHT_SKIP=false AUTO_HEAL_MERCHANT=true`) — the auto-heal re-enables the programs (queued pks 138+360, active 138) and the `useNeuroIdCheck` flag. Do NOT confuse an empty `redirectUrl` with a product bug: it is drifted merchant config. **Secondary (same re-run):** the raw Visa card `4111...` is rejected by the qa2 pre-auth gateway ("Invalid card.") — use `TEST_CARDS.MASTERCARD_APPROVED` (application-lifecycle CC rule). Cross-link: rule #12 (merchant preflight), [[merchant-preflight]], `.claude/rules/testing.md` §Application Lifecycle (CC). |
| 137 | **stg: Origination UI test explodes on a portal-agent selector (e.g. `input[placeholder='Search table']` in `MerchantSettingPage.filterMerchantTable`) while `error-context.md`/screenshot shows the "Merchant Login" screen (title `Uown \| Merchant`). NOT a selector/timing bug.** **Symptom:** `TimeoutError` on an agent input that exists in qa2 with the same code; the page is the merchant login screen. | `POST origination-stg.uownleasing.com/login` returns **HTTP 423 Locked** for the `manager`/`admin` account (both map to username `manager`) → no agent session is created → the SPA renders "Merchant Login" → `/merchantSetting` (and other agent routes) redirect to login, and the expected inputs never exist. **Account-scoped, not host/form:** `supervisor` logs in with **200** on the same host (portal is healthy; only the `manager` account is locked), but without Merchant-Setting permission. The lock lives in the agent auth identity service, **NOT** in the svc DB at `UOWN_DB_URL_STG` (cannot be read/reset by the framework). <!-- F-001 RU05.26.1.53.0_merchantSettingsSnapshotTracking, stg, 2026-06-22, [CONFIRMED] live login XHR 423 vs qa2 200 --> | **Discriminator:** capture the login XHR status — **423 = account locked (env issue), do NOT increase timeout/retry/`force`/`waitForTimeout`**. Cross-check with `supervisor` (→ 200 on the same host confirms portal is healthy, only the account is locked). **Unblock:** unlock `manager` in the agent auth service; verify the current `STG_MANAGER_PASSWORD` BEFORE re-running (a stale password re-locks the account via repeated failed logins from the `auth-origination` setup). Guard rail (suggestion for qa-implementer, NOT implemented): `auth-origination` setup should assert login **200** and fail-fast with "stg agent account locked / wrong password" instead of saving a `merchant.sid` storageState that is not authenticated and makes every downstream test fail with an opaque timeout. Cross-link: [[dom-investigation]] (423 masks selector timeout), [[volatile-knowledge-registry]] §6 (env provisioning / agent-account lock drift-prone), `src/pages/origination/merchant-setting.page.ts:105`. |
