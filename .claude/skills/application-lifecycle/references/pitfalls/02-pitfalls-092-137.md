# Application Lifecycle — Pitfalls #92–#137

> Fatia do catalogo de pitfalls. Numeros sao **globais e estaveis** (cross-refs `pitfall #N` resolvem entre fatias). Indice + como anexar: [../pitfalls.md](../pitfalls.md). Regras/sequencia: [../../SKILL.md](../../SKILL.md).

| # | Sintoma | Causa | Fix |
|---|---------|-------|-----|
| 92 | Click em item da sidebar do Website (customer portal) da `TimeoutError` / pointer event interceptado logo apos um pagamento ou save | Um overlay full-screen residual (spinner/loading pos-pagamento, pos-save) ainda esta montado e intercepta os pointer events sobre o link da sidebar. O click resolve o locator mas nunca conecta. <!-- descoberto em unified-flow.spec.ts, commit 4f30c0d dismiss overlay before sidebar click, 2026-06-02 --> | `WebsiteBasePage.goToSidebarLink` agora chama `await this.waitForSpinner` ANTES de tentar o click. Qualquer navegacao de sidebar no Website apos acao mutante (pay/save) deve aguardar o overlay sumir primeiro. NAO mascarar com `force: true`. Cross-link: [[page-object-pattern]] WebsiteBasePage. |
| 93 | Query em `uown_sticky` filtrando/lendo coluna `retry_attempt` retorna `column "retry_attempt" does not exist` | A coluna que conta as tentativas de retry na tabela `uown_sticky` (a sessao de recovery) chama-se **`number_of_attempts`** (integer, default `0`, schema doc col 9). NAO existe coluna `retry_attempt` em `uown_sticky`. O nome `retry_attempt` confunde com a TABELA irma `uown_sticky_retry_attempt` (registro per-tentativa, colunas `attempt_number` + `retry_attempt_time`) - sao coisas diferentes: a sessao agregada (`uown_sticky.number_of_attempts`) vs as linhas por tentativa (`uown_sticky_retry_attempt`). Briefing/skills antigos referenciavam `retry_attempt` por engano. <!-- descoberto em exploratory-testing-qa1-master S7, qa1, 2026-06-10 --> | Para contagem agregada de tentativas na sessao: `SELECT number_of_attempts FROM uown_sticky WHERE pk=$1`. Para detalhe por tentativa: `SELECT attempt_number, retry_attempt_time FROM uown_sticky_retry_attempt WHERE ...`. Schema autoritativo: [`docs/database-schema.md`](../../../../docs/database-schema.md) secoes `uown_sticky` (col 9) e `uown_sticky_retry_attempt`. NUNCA usar `retry_attempt` como nome de coluna em `uown_sticky`. |
| 94 | Query em `uown_scheduled_task` filtrando coluna `name` retorna 0 rows ou `column "name" does not exist`; `last_trigger_time` lido como prova de execucao Quartz da falso-negativo | As colunas reais de `uown_scheduled_task` sao: nome do task = **`scheduled_task_name`** (NAO `name`); cron = **`cron_trigger`** (NAO `cron`/`cron_expression`). Alem disso **`last_trigger_time` reflete APENAS triggers MANUAIS** (via `triggerScheduledTask`), NAO execucoes automaticas do scheduler Quartz - usar `last_trigger_time` como oracle de "o sweep rodou no horario" da falso-negativo silencioso. <!-- descoberto em exploratory-testing-qa1-master S2, qa1, 2026-06-10 --> | Filtrar por `WHERE scheduled_task_name = '{X}'`. Para confirmar execucao Quartz REAL: oracle correto e `qrtz_triggers.prev_fire_time` (ultima execucao agendada disparada) + presenca de row em `uown_sweep_logs` (sweep gravou resultado). NAO inferir execucao por `last_trigger_time`. Cross-link: pitfall #95, [[volatile-knowledge-registry]] categoria 2. |
| 95 | Query em `uown_sweep_logs` usando colunas `task_name` / `processed` / `created_timestamp` retorna `column does not exist` | As colunas reais de `uown_sweep_logs` sao: **`sweep_name`** (NAO `task_name`), **`number_of_records_processed`** (NAO `processed`), **`error`**, **`source`**, **`start_time`** / **`end_time`** (NAO `created_timestamp`). Scripts antigos assumiam nomes abreviados. <!-- descoberto em exploratory-testing-qa1-master S2, qa1, 2026-06-10 --> | Usar os nomes reais: `SELECT sweep_name, number_of_records_processed, error, source, start_time, end_time FROM uown_sweep_logs WHERE sweep_name=$1`. Lembrar que `number_of_records_processed` e escrito DEPOIS do processamento (pitfall #87) - leitura imediata retorna `0`. Cross-link: pitfall #87, #94, [[payment-flows]] secao Email Sweep validation, [[volatile-knowledge-registry]] categoria 2. |
| 96 | Spec/query de e-sign assertando `status='SENT'` em `uown_esign_document` nunca casa | O valor `SENT` NAO existe no enum de status de `uown_esign_document` (coluna real = **`status`**, varchar 255, col 37 - NAO `document_status`). Os valores reais do enum sao **`STORED` / `SENT_TO_CUSTOMER` / `COMPLETED` / `ERROR` / `CANCELLED`**. O estado "enviado ao cliente" e `SENT_TO_CUSTOMER` (NAO `SENT`); o estado terminal de sucesso e `COMPLETED` (NAO `SIGNED`). Confundir com o enum de `uown_email_queue` (que SIM usa `SENT`/`STORED`) leva a assertion que nunca passa. <!-- descoberto em exploratory-testing-qa1-master S7, qa1, 2026-06-10 --> | Em assercoes de e-sign document usar `status='SENT_TO_CUSTOMER'` para envio e `status='COMPLETED'` para conclusao. Schema autoritativo: [`docs/database-schema.md`](../../../../docs/database-schema.md) secao `uown_esign_document` (col 37 `status`). Cross-link: [[gowsign-knowledge]] secao status enum. |
| 97 | `POST /ConfigurationManagement/createOrUpdateConfig` + `forceReloadConfig` NAO altera o comportamento de chaves servidas pelo `system.config` do `application.yaml` (ex: `required.fields.for.<ClientType>` do validator, `is.preauthorization.required.for.servicing`); a app continua usando o default do codigo. **Sintoma:** config gravada no DB (`uown_configuration_management`, row presente, endpoint 200) + reload 200, mas o runtime ignora (ex: `sendApplication` sem `mainNextPayDate` para DANIELS_JEWELERS ainda retorna 400 `mainNextPayDate is required`). | Existem **DUAS familias de config desconexas** no svc. (a) **DB-backed** `common.service.ConfigurationManagementService` (`configRepo` em `uown_configuration_management`) - e o que `createOrUpdateConfig` grava; usada por ex. `TrustpilotService`, `WebhookService`. (b) **YAML/Hazelcast** `svc.config.configuration.ConfigurationManagement` → `ConfigurationUtility.getString` le de `IMap configurationMap` que e re-hidratado a CADA leitura SO de `SystemConfigurationProperties` (`system.config.*` do YAML) via `getConfigurationUtility` → `configurationMap.setAll(...)` (`ConfigurationAnnotationProcessor.java:40-43`). NADA carrega a tabela DB para dentro do IMap; `forceReloadConfig` (`evictAll`+reinject) e re-overlayado pelo YAML na proxima leitura. O validator de application usa a familia (b) (`LosRequestMessageConstraintValidatorConfig.java:3,20-25`) → chave do DB nunca chega → cai no default do codigo (`...,mainNextPayDate`). <!-- descoberto em exploratory-testing-qa1-master S9, qa1, 2026-06-10 --> | **Determinar a familia pelo `import` no call-site.** `import com.uownleasing.common.service.ConfigurationManagementService` → DB-backed → `createOrUpdateConfig` FUNCIONA. `import com.uownleasing.svc.config.configuration.ConfigurationManagement` → YAML/Hazelcast → so via `application.yaml` (repo `configuration`, ex. commit `62e2fc20`) + **deploy/restart de pod** (acesso infra, fora do alcance QA). NAO confiar em `createOrUpdateConfig`+`forceReloadConfig` para chaves da familia (b); validar sempre com repro fresco apos aplicar. Cross-link: memoria `reference_config_management_endpoint.md` (corrigida 2026-06-10; toggle preauth marcado para re-verificar pelo mesmo motivo). |
| 98 | `SearchPage.searchAndSelectFirst` (quick search do navbar Origination) NAO navega quando invocado a partir da pagina **/funding** (Funding Queue, filters panel aberto): a busca preenche o input, o BFF `simpleSearch` dispara e retorna o lead com HTTP 200, MAS o dropdown de autocomplete **nunca renderiza o anchor** `nav a[href*="/customers/"]` -> o waiter (5s) cai no fallback Enter, nao acha table link, e retorna SILENCIOSAMENTE. `navigateToOriginationCustomer` ve a URL ainda em `/funding`, repete 3x e lanca `failed after 3 UI search attempts`. **Diagnostico:** erro `[Navigation] ... still on .../funding, retrying...` 3x apos uma etapa de funding, com search input visivel e valor preenchido no screenshot mas SEM dropdown. NAO e timing, NAO e sessao invalida, NAO e lead FUNDED nao-pesquisavel (o `simpleSearch` retorna o lead FUNDED por `Lead #` normalmente, status 200). | O input `#search-input` do navbar e um React-controlled autocomplete que so **renderiza o dropdown de resultados quando o input esta focado/ativo**. `fill`/`clear`/`pressSequentially` atualizam o value e disparam o `input` event debounced (BFF roda, retorna 200), mas em paginas que gerenciam o proprio foco (Funding Queue, cujo filters panel captura foco) o componente computa os resultados e NAO monta o anchor do dropdown sem foco explicito. Na /overview e /customers o proprio fluxo da pagina deixa o input em estado que renderiza o dropdown, mascarando o bug. <!-- DOM-first confirmado live em sandbox 2026-06-12: sem `click` o anchor fica ausente 3/3 runs (BFF firou em todos os runs, status 200); com `click` antes do fill o anchor renderiza 3/3 e navega; overview sem regressao. CLAUDE.md #15 --> | Em `searchAndSelectFirst`, dar `await this.quickSearchInput.click` (focar) ANTES de `clear`+`fill`. O `click` torna o dropdown renderizado de forma confiavel em QUALQUER pagina (validado: funding 3/3 OK, overview/customers sem regressao). NAO mascarar com aumento de timeout, retry, `force:true`, nem com re-login (a sessao e valida — o re-login pre-navegacao no `tests/ci/unified-flow.spec.ts:305` era workaround de outro sintoma, NAO a causa raiz; o page-object fix dispensa-o). NAO confiar so no fallback Enter (e silencioso quando nao ha table link). Fix em `src/pages/search.page.ts` `searchAndSelectFirst`. Cross-link: [[dom-investigation]], [[selector-hardening]]. |
| 99 | Steps de navegacao por sidebar no Website (customer portal) **passam silenciosamente** mesmo quando a SPA esta quebrada/deslogada — so um step posterior que aguarda um campo real (`changeEmailToGeneric` → `wsPrimaryEmailField.waitFor visible`) e que falha, mascarando a causa raiz (perda de auth) varios steps antes. **Sintoma:** `goToSidebarLink('documents')` e `goToSidebarLink('contact us')` logam OK, depois `update contact info` → `TimeoutError: locator.waitFor #primaryEmail...` + screenshot de spinner infinito (`status: Loading...`). O log `[Website] Sidebar fallback: navigating directly to .../update-contact` aparece, indicando que o click path nunca achou os itens de sidebar. | Duas falhas de mascaramento encadeadas no `WebsiteBasePage`: (a) `goToSidebarLink` cai em `navigateBySidebarUrlFallback` → `page.goto(slug)` que faz **full reload e perde a auth da SPA** (a propria SPA, se a sessao ja morreu, redireciona pra `/login` e fica em spinner infinito); (b) `waitForSpinner` (`common.helpers.ts:5-25`) **engole** o `TimeoutError` do `waitFor({state:'hidden'})` dentro de `try/catch`, entao o metodo RETORNA mesmo num spinner que nunca some → o step "passa". Resultado: os 3 itens "invisiveis" (`Update Contact Info`, `Account Settings`, `Account Summary`) NAO sumiram por label/class drift — eles nao existem porque a pagina ja era a tela de LOGIN. <!-- DOM-first live sandbox 2026-06-12, account 17249: label "Account Settings", item "Update Contact Info", prefixo `side-bar_sideBarContainer__item__*` e selector `#primaryEmail` TODOS corretos/confirmados; causa real = força-logout em /documents (ver observacao OBS-WS-DOCS-LOGOUT). CLAUDE.md #15 --> | NAO confiar em "step de sidebar passou" como prova de navegacao no Website — o `page.goto` fallback + `waitForSpinner` engolido tornam o pass silencioso. Um step de navegacao SO e confiavel se seguido de assert de um elemento da pagina-destino (ex: o proprio `wsPrimaryEmailField`). Quando um step de sidebar "passa" mas o seguinte da timeout em spinner infinito: suspeitar de perda de auth (full reload via goto) ANTES de mexer em label/class de sidebar. O `page.goto` fallback que perde auth NAO deve ser o path normal — preferir manter a navegacao SPA (click) e, se a sessao morreu por bug de produto upstream, o teste deve FALHAR visivelmente nesse ponto, nao varios steps depois. Cross-link: pitfall #92, [[dom-investigation]], [[page-object-pattern]] WebsiteBasePage. |
| 100 | **`sendApplication` retorna `BLACKLIST_DENIED` mesmo com SSN/email aprovaveis quando o endereco estatico do fixture acumulou uma entrada de blacklist em sandbox.** O `blacklistCheck` do LOS casa por **`streetAddress1` + `zipCode`** (nao por SSN/email/nome). Reuso do endereco estatico do fixture CA (`654 Sunset Blvd` / `90028`) entre runs eventualmente bate numa entrada de blacklist criada por execucao anterior (blacklist pk:2165 em sandbox 2026-06-12) → toda nova application naquele endereco e negada, independentemente de email unico. Email unico (pitfall #1) NAO protege: o match e por endereco, nao por identidade. | `blacklistCheck` casa `streetAddress1 + zipCode`; endereco estatico de fixture e "envenenado" assim que qualquer run cria uma entrada de blacklist para ele. <!-- descoberto em modify-lease.spec.ts cycle 1, sandbox 2026-06-12, blacklist pk:2165 --> | Usar `buildTestData({ ..., uniqueAddress: true })` para variar `streetAddress1` por run (ex: sufixo `Unit 1-<hash>`), mantendo cidade/estado/zip validos para o risk tier. Isso impede que um endereco estatico envenenado bloqueie fresh applications. Default para TODO teste que cria application em estado com fixture de endereco estatico (CA TireAgent etc.). NAO usar UPDATE/DELETE direto na tabela de blacklist (Exception 3). Cross-link: pitfall #1 (email unico — complementar, nao substituto). |
| 101 | **Botao de acao do customer summary (Origination) renderiza OFF-SCREEN a direita e `click({force:true})` NAO dispara o handler React → modal nunca abre → timeout enganoso em campo DENTRO do modal.** **Sintoma:** `TimeoutError: locator.waitFor #numberOfItems to be visible` (ou `locator.fill` no mesmo campo) varios passos depois de clicar "Modify Lease"; screenshot mostra a customer page SEM modal aberto e o painel de action buttons ainda visivel. O campo `#numberOfItems` em si esta sempre visivel QUANDO o modal abre — o problema e o modal nunca ter montado. | Os action buttons (`Modify Lease`, `Send to Signed`, …) vivem num container `overflow-auto` horizontal (`customer-summary_customerSummary__accountSummary`). Botoes alem da largura visivel renderizam off-screen a direita (ex: "Modify Lease" em x≈1625 num viewport 1440) reportando `visible:true` (offsetParent presente). `locator.click` falha com `<ancestor> intercepts pointer events`; `click({force:true})` clica na coordenada central do elemento que esta FORA do scroll viewport → o onClick React nunca dispara → nenhum modal abre. Alem disso, o `modifyLease` antigo mascarava a falha: `Promise.race([...]).catch(=>{})` engolia o "modal nao abriu" e seguia para `deleteAllInvoiceItems`, fazendo a falha aflorar no `waitFor(#numberOfItems)` da spec — linha errada. Subsidiario: a confirmacao "Modify Lease" SEMPRE abre um dialog CANCEL/**Continue** (label exato `Continue`, NAO o `CONTINUE` do texto de caution) antes do modal real; e o icone de delete e um `<svg data-icon="trash-can">` `aria-hidden` num `<div>` sem role, sujeito a interceptacao por inputs irmaos. <!-- DOM-first MCP live sandbox 2026-06-12, leads 97502/97509, viewport 1440×900 --> | `clickActionButton` deve JS-dispatch o click (`page.evaluate`: achar o button por texto → `scrollIntoView({block:'center'})` → `el.click`), NUNCA `click({force:true})` em botao de container com overflow horizontal. `modifyLease`: tornar o passo "Continue" obrigatorio (`waitFor visible` + click, sem `.catch`) e aguardar `#numberOfItems` visible SEM `.catch` (falha aqui = modal nao abriu, causa real exposta no ponto certo). `deleteAllInvoiceItems`: JS-dispatch click no wrapper do svg trash (count-based loop, sem confirmacao de delete). PROIBIDO: aumentar timeout, `force:true`, `waitForTimeout`, try/catch de mascaramento. Cross-link: [[dom-investigation]], [[selector-hardening]], `rules/page-objects.md` (DOM-first). |
| 102 | **`useNeuroIdCheck=true` (flag sob teste) e SILENCIOSAMENTE resetado para `false` pelo merchant preflight → NeuroID nunca dispara → todos os `count >= 1` guards falham com falso-negativo.** **Sintoma:** teste de signing-retry com NeuroID "passa" trivialmente ou falha em `expect(count1).toBeGreaterThanOrEqual(1)` mesmo o fluxo estando correto; `uown_neuro_id_verification` fica vazio para o `lead_pk`. | `useNeuroIdCheck` esta no conjunto `mustBeFalse` do contrato de merchant preflight (`src/data/merchant-config-contract.ts`). `createPreQualifiedApplication` chama `ensureMerchantReady` por default → auto-heal via `createOrUpdateMerchant` RESETA `use_neuro_id_check` para `false`, desabilitando o NeuroID antes do fluxo rodar. Email unico / SSN nao tem efeito — o gate e a flag do merchant. <!-- qa2, 2026-06-15 --> | `skipMerchantPreflight: true` em TODA chamada `createPreQualifiedApplication` quando `useNeuroIdCheck=true` e a flag sob teste (preflight nao deve mutar a flag em teste). Compensar com pre-assert read-only inline: `SELECT use_neuro_id_check FROM uown_merchant WHERE ref_merchant_code='OW90337-0001'` + `test.skip` se `false` (ambiente nao provisionou a flag → skip honesto, nao falso-negativo). Analogo ao Pitfall #10 (config drift) e ao [[fraud-vendors-knowledge]] Pitfall #6/#9 (qualquer fraud flag em `mustBeFalse` e auto-healed). |
| 104 | Config Columns panel na pagina `/merchant` (Origination) nao e encontrado: o seletor `configColumnsPanel` (`[role='dialog']:has-text('Configure the view')`) retorna **0 elementos** | A pagina **Overview** usa um modal/dialog para Config Columns; a pagina **Merchants** (`/merchant`) usa um **Bootstrap `div.dropdown.show`** — padroes de DOM completamente diferentes. O seletor generico do Overview nao casa no Merchants. <!-- MCP Playwright qa1, 2026-06-15 --> | Usar `SELECTORS.configColumnsPanelMerchants` (`div.dropdown.show:has-text('Configure the view')`) especifico para `/merchant`. `MerchantListPage.openConfigColumns/closeConfigColumns` ja usam. Cross-link: [[page-object-pattern]] MerchantListPage; `docs/knowledge-base/merchants-config-columns-export.md`. |
| 105 | `label:has-text('UW Pipeline') input[type='checkbox']` nao encontra o checkbox de coluna no Config Columns de `/merchant` | Os checkboxes **NAO tem `<label>` envolvendo o input** — sao `input[type='checkbox']` **nativos** cujo `id` e `name` = o label da coluna (`value="true"` quando marcado, `value=""` quando desmarcado). <!-- qa1, 2026-06-15 --> | Localizar diretamente por atributo: `input[type='checkbox'][name="UW Pipeline"]`. `MerchantListPage.columnCheckbox(name)` ja faz isso. Cross-link: [[page-object-pattern]] MerchantListPage. |
| 106 | Codigo de teste espera por botao Apply/Save apos togglar uma coluna no Config Columns de `/merchant` — o wait nunca resolve | A selecao de colunas e **imediata** (BR-01): a tabela atualiza no clique do checkbox, **sem step de confirmacao** (sem botao Apply/Save). <!-- qa1, 2026-06-15 --> | Apos `setColumn(name, checked)`, a UI ja reflete o novo estado — nao ha botao Apply a clicar. Nao adicionar wait por Apply/Save. Cross-link: [[page-object-pattern]] MerchantListPage. |
| 107 | Filtro Active em `/merchant` (Origination): nao ha como selecionar "All" via dropdown, e mudar a opcao nao re-filtra a tabela | O filtro Active e um **react-select `#isActive` com apenas 2 opcoes (`Active`/`Inactive`)** — NAO existe opcao "All" (para exibir todos os merchants, **limpar a selecao via X**). O default da pagina ao carregar e `Active` (`isActive: true`). A mudanca **NAO e aplicada automaticamente** — exige clique no botao **Search** (BR-06, `button[name='searchButton']`). <!-- qa1, 2026-06-15 --> | `MerchantListPage.setActiveFilter('Active'|'Inactive')` seleciona a opcao e clica Search. Para "todos", limpar a selecao do react-select e clicar Search. Nao assumir auto-apply na mudanca. Cross-link: [[page-object-pattern]] MerchantListPage; [[volatile-knowledge-registry]] categoria 4 (selectors em pages com refactor recente). |
| 103 | **`uown_sv_outbound_api_log` NAO correlaciona chamadas NeuroID por `lead_pk` para leads pre-funding** — `account_pk`/`source_uuid`/`return_uuid` sao todos NULL nessas linhas, impossibilitando assertivas de "NeuroID foi/nao foi chamado" por lead. **Sintoma:** tentativa de contar chamadas NeuroID via `uown_sv_outbound_api_log WHERE lead_pk=...` retorna 0 rows (coluna `lead_pk` inexistente/sem match) apesar de o NeuroID ter rodado. | A tabela tem as linhas (`url ILIKE '%neuro-id.com%'`) mas sem nenhuma chave de correlacao com o `lead_pk` em estado pre-funding (account ainda nao existe). Foi descartado pelo discovery probe. <!-- descoberto via discovery probe src/scripts/probe-neuroid.ts, qa2, 2026-06-15 --> | Usar `uown_neuro_id_verification WHERE lead_pk = $1` (helper `countNeuroIdCalls`, [[helpers-catalog]]) como fonte de truth de contagem NeuroID em testes de signing (Opcao B). Cross-link: [[fraud-vendors-knowledge]] § NeuroID + Pitfall #10. |
| 108 | **`/merchantSetting` (Origination): `selectMerchantRowByText('OL90202-0001')` da `TimeoutError ... .rdt_TableRow:has-text("OL90202-0001") not visible` — a row do merchant simplesmente NAO existe no DOM.** **Sintoma:** timeout num row selector logo apos `loadMerchants()`; o page snapshot mostra a tabela com ~20 rows (Synchrony, Vedo, Bridge…) mas SEM o merchant alvo. Aumentar timeout nunca resolveria (a row nunca aparece). | A tabela de Merchant Setting renderiza apenas a **primeira pagina (~20 rows)** por default. `loadMerchants()` antigo clicava Filters → botao "Search" com a caixa de busca VAZIA, o que reaplica o filtro vazio e retorna a mesma pagina default — NUNCA carrega todos os merchants. terraceFinance (`OL90202-0001`) nao esta entre os ~20 → a row genuinamente nao existe ate ser filtrada. A caixa correta e o `textbox "Search"` com `placeholder "Search table"` DENTRO do painel Filters. <!-- DOM-first via trace error-context.md a11y snapshot, qa2, 2026-06-16 (MCP indisponivel — fallback chain [[dom-investigation]]) --> | Digitar o codigo na caixa "Search table" e aplicar ANTES de selecionar a row. Selectors: `msMerchantSearchTableInput` (`input[placeholder='Search table']`) + `msMerchantFilterSearchButton` (`button:has-text('Search')`). `MerchantSettingPage.filterMerchantTable(code)` faz fill+Search; `selectMerchantRowByText` chama-o automaticamente se a row nao estiver visivel. PROIBIDO aumentar timeout / `force:true`. Cross-link: [[selector-hardening]], [[dom-investigation]], [[page-object-pattern]] MerchantSettingPage. |
| 109 | **SSN terminando em 9 (`generateTestSSN(false)`) e APROVADO (UW_APPROVED) em qa2 para terraceFinance, quando a convencao documentada diz UW_DENIED.** **Sintoma:** teste negativo de denial falha com `expected a denial status, got "UW_APPROVED"`; o lead ending-in-9 grava snapshot de aprovacao normalmente. | A regra "ending-in-9 → UW_DENIED" e um gate de **test-server** controlado por uma config boolean em `SendApplicationService` — NAO depende da credit engine. **`[CONFIRMADO]`** (codigo): `svc/.../application/SendApplicationService.java:361-365` so nega quando `!isProduction()` **E** `getBoolean("com.uownleasing.svc.service.SendApplicationService.deny.ssn.ending.with.9", true)` **E** `mainSSN.endsWith("9")`. Em qa2 o lead ending-in-9 e **aprovado** (activity log mostra BlackBox/ABB decidindo, 0 vendor calls) → o gate nao disparou → a config esta **efetivamente `false`** em qa2 (a key esta AUSENTE da tabela `uown_configuration_management`, entao o default `true` deveria valer; logo o override vem de properties de deploy acima da tabela — `[HIPÓTESE]` na camada exata). <!-- qa2, 2026-06-16; reproduzir via probe-uw-denial-engine.ts --> | NAO assumir que ending-in-9 nega fora de qa1/sandbox. Config: `com.uownleasing.svc.service.SendApplicationService.deny.ssn.ending.with.9` (set via `POST /ConfigurationManagement/createOrUpdateConfig` + `GET /ConfigurationManagement/forceReloadConfig`). **TESTADO 2026-06-16: setar a flag=`true` (API 200, DB confirmado, cache reloaded) NAO bastou em qa2 — um lead ending-in-9 fresco ainda voltou `UW_APPROVED` (lead 16583).** A denial exige `!isProduction()` **E** a flag (linha 361); com a flag `true` ainda aprovando, qa2 e efetivamente tratado como **produção** p/ esse gate (ou o build deployado e anterior ao commit `d6c943f411`). → Config sozinha **NAO habilita** denial em qa2; escalar dev/DevOps (confirmar `isProduction()`/build do svc qa2). Para testes de denial: usar **sandbox/qa1** (mock honrado). Config foi revertida (row deletada). Pre-UW deny (Blacklist → BLACKLIST_DENIED, no-business-in-state) NAO e "denied by underwriting". Se a config nao puder ser mudada → rodar o cenario negativo em sandbox/qa1, OU `test.skip` documentado + pergunta a PO. Cross-link: [[ssn-test-modalities]], [[fraud-vendors-knowledge]], `docs/knowledge-base/underwriting-and-funding-test-data-paths.md`. |
| 110 | **EPO 5% / EPO 10% triple-checkbox em `/merchantSetting` (GDS Data): `#epo5-false`/`#epo10-false` resolvem no DOM mas ficam `not visible` → `locator.check` timeout (`merchant-setting.page.ts:setEpoTriple`).** **Sintoma:** `Element is not visible — waiting for locator('#epo5-false')`; `waitFor('attached')` passa mas `check({force:true})` falha/clica num input invisivel. O teste de imutabilidade do EPO nunca chega a rodar. | Cada campo EPO e um **dropdown collapse**, NAO um trio de checkboxes inline: `div#toggler` (linha visivel) contem `input#epoN-main` (enable) + `svg.fa-caret-down.cursor-pointer` (toggle); o painel `div.checkbox-dropdown_…card__… .collapse` (`display:none`) contem `input#epoN` (True) e `input#epoN-false` (False). Checar `-main` apenas auto-marca o value=true mas **NAO abre o painel** → os inputs True/False continuam dentro de um `.collapse` invisivel. `force:true` mascarava (clicava num input com `offsetParent===null`). <!-- DOM-first live qa2 via npx tsx inspect script (MCP nao provisionado), 2026-06-16 --> | Habilitar `#epoN-main` (check, sem force), **clicar o caret** `#toggler:has(#epoN-main) svg.fa-caret-down` para expandir o `.collapse`, esperar `state:'visible'` no True/False alvo e `check()` **SEM force**. Idempotente: so clica o caret se o alvo ainda nao estiver visivel. Selectors: `msEpo5CaretToggle`/`msEpo10CaretToggle`. `MerchantSettingPage.setEpoTriple` ja faz isso. NOTE: `#toggler` id e duplicado (HTML invalido) — escopar via `:has(#epoN-main)` para 1 caret. PROIBIDO `force:true`/timeout bump. Cross-link: [[selector-hardening]], [[dom-investigation]], [[page-object-pattern]] MerchantSettingPage. |
| 112 | **Merchant Settings Snapshot: de onde sai o snapshot e qual a tabela/coluna/audit-trail — assumir errado faz o teste assertar lead_notes inexistente OU re-ler o merchant vivo.** **Sintoma:** teste de snapshot procura nota em `uown_los_lead_notes` (nao acha → falso-negativo) OU espera o account snapshot refletir o estado live do merchant no momento do funding (nao reflete — e copia do lead). | **Mecanica confirmada:** (a) **Lead snapshot** e escrito no `ApplicationApprovedEvent` (fim de `ApplicationProcessor`), criado **apenas para applications APROVADAS** (denied/abandoned nao geram snapshot). (b) **Account snapshot** e escrito em `LosToSvcImportService.importFromLosToSvc` e **COPIA o lead snapshot** (NUNCA re-le o merchant vivo), gated na existencia do lead snapshot — se o lead snapshot esta ausente, a conta ainda e criada e o snapshot e **silenciosamente pulado** (sem erro). Tabelas: `uown_los_lead_merchant_settings_snapshot` / `uown_sv_account_merchant_settings_snapshot`; colunas: `epo5, epo10, uw_pipeline, fraud_threshold` (+ `lead_pk`/`account_pk`/`merchant_pk`/`row_created_timestamp`). **Audit trail = logs INFO/WARN da APLICACAO, NAO `uown_los_lead_notes`** → NAO assertar lead_notes p/ criacao de snapshot. Escopo e **audit/reporting/analytics apenas — sem consumidor funcional em runtime** (snapshot nao altera decisao/funding). <!-- `[user-provided:dev-test-plan]` --> | Para validar snapshot: (1) lead aprovado → `SELECT ... FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk=$1`; (2) pos-funding → `SELECT ... FROM uown_sv_account_merchant_settings_snapshot WHERE account_pk=$1` e assertar **igualdade com o lead snapshot** (nao com o merchant vivo). Para denial path: assertar **ausencia** de row de snapshot. NAO usar `uown_los_lead_notes` como oraculo de criacao de snapshot. Cross-link: [[ssn-test-modalities]] (denial recipe p/ branch denied), [[merchant-preflight]]. |
| 113 | **qa2 DB tunnel transient blocker: teste fail-fast no PRIMEIRO read DB (`assertMerchantContract`) sem ser drift de contrato nem finding de feature.** **Sintoma:** suite estoura logo no primeiro `db.*`/preflight com erro de conexao (`127.0.0.1:5445`) ou `password authentication failed for user "svc_user"` — intermitente, no meio de um run que vinha OK. | O DB qa2 via `kubectl port-forward` em `127.0.0.1:5445` pode **cair no meio do run** e o auth de `svc_user` pode **rejeitar transitoriamente**. Como `assertMerchantContract` (merchant preflight) e o primeiro acesso DB, a falha aparece ali e parece contrato/feature. **NAO e drift de merchant config e NAO e finding de feature** — e infra. <!-- `[test-execution:qa2,2026-06-17]` --> | Reconhecer como **infra/transiente** (≈ pitfall #18, `[env-blocker]`): re-probar o tunnel/DB (reabrir port-forward, validar `SELECT 1`) e **re-rodar**. NAO classificar como bug, NAO abrir pitfall de contrato, NAO mexer no merchant. Cross-link: pitfall #18 (Cloud SQL proxy / `svc_user` auth). **[env-blocker]** |
| 111 | **Ultimo sub-test de `test.describe.serial` falha com `"context" and "page" fixtures are not supported in "afterAll"` (idem `testEnv`) E o teardown nunca executa → drift de merchant vaza para a proxima execucao.** **Sintoma:** Playwright marca o ultimo sub-test serial como ❌ mesmo com o corpo passando (oraculo DB confirma a feature OK); restore de config nunca roda. | `test.afterAll` so aceita fixtures **worker-scoped**. No fixture deste repo: `db`/`email` sao worker (OK em afterAll); `page`, `context`, `request`, `api`, `testEnv` sao **test-scoped** (PROIBIDOS em afterAll). Destruturar `{ page, testEnv }` em afterAll quebra com esse erro e aborta o teardown inteiro. <!-- qa2, 2026-06-16 --> | Em `afterAll` usar SOMENTE `{ browser, db }`. Criar o proprio contexto: `const ctx = await browser.newContext(); const page = await ctx.newPage(); try { … } finally { await ctx.close(); }` (padrao ja usado em `tests/e2e/gowsign/*`). Derivar env como o fixture `db` faz: `new ConfigEnvironment(process.env.ENV ?? '<default>')` (NAO ha `testEnv`/`envName` em afterAll). Restore via UI = caminho de edit da feature (NAO DB raw, Rule #9). Cross-link: [[page-object-pattern]] §Anti-patterns, [[common-operations]]. |
| 114 | **Reusar um `redirectUrl` (signing/contract link) capturado de um `sendInvoice` anterior abre `"Invalid link. Please contact merchant"` em vez do modal GowSign.** **Sintoma:** `TimeoutError: locator.waitFor` em `.alternative-contract-vendor_iframeContainer__yAn5c` (modal nunca abre); error-context snapshot mostra `alert: "Invalid link. Please contact merchant"`. Atinge testes que invocam `sendInvoice` N vezes (uma por frequencia/plano) e guardam cada `redirectUrl` para abrir depois — o PRIMEIRO (mais antigo) link ja morreu. | **Um lead tem UM unico contract link vivo.** Cada `sendInvoice` MINTA um NOVO shortCode e SUPERSEDE o anterior — o `redirectUrl` antigo vira invalido. Existe exatamente UM `uown_esign_document` por lead (1 GOWSIGN doc, mesmo UUID), criado quando o link e aberto (CC submit → CONTRACT_CREATED), NAO um por frequencia. DOM-proven qa2 2026-06-18, lead 16668: 1º WEEKLY `hqrSW2US` → "Invalid link"; re-issued WEEKLY `mWjbG7eN` → pagina CC viva. NAO e flake de timing nem seletor errado (o link mais recente abriu o mesmo modal com sucesso). | **Re-issuar `sendInvoice` para a frequencia-alvo IMEDIATAMENTE antes de abrir o render** e usar o `redirectUrl` retornado naquele instante (link vivo at open time). A oracle VALUE math (costPrice/n/nextPayment/salesTax/contract de `paymentDetailsList`) NAO precisa de link vivo — capturar 1x; so o RENDER drive precisa de link fresco. PROIBIDO mascarar com bump de timeout / retry / troca de seletor (Rule #15 — o seletor esta correto). Cross-link: [[gowsign-knowledge]] §host iframe + §enum status, [[ui-first-principle]]. <!-- qa2, 2026-06-18 --> |
| 115 | **Overview tem DOIS forms de filtro com inputs de data identicos; um seletor posicional (`nth()`) acerta o form ERRADO.** **Sintoma:** o teste preenche uma data mas a tabela/CSV nao muda (afetou os metric cards) ou vice-versa — o lever aparenta nao funcionar, sem erro. | Overview renderiza 2 forms independentes, ambos com inputs MM/DD/YYYY: **(a) top-bar KPI form** — ids `#from`/`#to`, toggle class `overview_filterButton__`, drives os metric cards; **(b) TABLE panel** — ids `#fromDate`/`#toDate`, toggle class `index-module_filterButton__`, drives a tabela + o CSV export. Um `nth()` posicional pega o KPI form. DOM-proven qa2 2026-06-18. | Alvejar os inputs do **table panel por id** (`#fromDate`/`#toDate`), nunca por posicao; expandir via o toggle do proprio painel (`expandTableFilters`, ver #117). PROIBIDO `nth()` quando ha forms duplicados. Cross-link: [[selector-hardening]] §multiplos forms, [[page-object-pattern]] OverviewPage. <!-- #1321, qa2, 2026-06-18 --> |
| 116 | **Overview: uma janela de data "future-only" no table panel NAO esvazia a tabela** (empty-set lever nao confiavel). **Sintoma:** o teste seta `#fromDate`/`#toDate` para um intervalo no futuro esperando 0 rows (para exercitar empty-table / Email-disabled), mas a tabela continua populada. | `#fromDate` (table panel) **reseta para hoje** ao re-renderizar (Formik initial value) — a data futura nao "gruda", logo nao filtra para fora os leads existentes. DOM-proven qa2 2026-06-18. | Usar o **free-text "Search table"** (`SELECTORS.overviewTableSearch`, metodo `OverviewPage.searchTable(value)`) com um valor garantidamente nao-existente → empty set deterministico. Cross-link: [[page-object-pattern]] OverviewPage.searchTable. <!-- #1321, qa2, 2026-06-18 --> |
| 117 | **Overview: o painel de table-filter re-colapsa logo apos o clique de toggle (1 clique nao basta).** **Sintoma:** `expandTableFilters` clica o toggle, o painel abre e fecha sozinho milissegundos depois; os inputs `#fromDate`/`#toDate` ficam `not visible` intermitentemente. | `verifyDashboardLoaded` resolve no `Promise.race` assim que o botao Filters aparece — **ANTES** de a tabela terminar de carregar em QA2. O painel e uma **width-collapse animation**; um re-render disparado pelo carregamento tardio da tabela **re-colapsa** o painel logo apos o toggle. DOM-proven qa2 2026-06-18. | `expandTableFilters` usa um **retry loop** (re-clica o toggle ate o input alvo ficar `state:'visible'` de forma estavel), NAO um unico clique. NAO mascarar com `waitForTimeout` (Rule #15) — o retry e idempotente e observa o estado real do painel. Cross-link: [[page-object-pattern]] OverviewPage.expandTableFilters, [[dom-investigation]]. <!-- #1321, qa2, 2026-06-18 --> |
| 118 | **Clicar "Download CSV" abre o modal de EMAIL (clica o botao errado).** **Sintoma:** o teste de download direto passa pelo caminho de email — abre `"Which email should we send this CSV file to?"` em vez de capturar um `Download`; nenhum arquivo chega. | Email CSV e Download CSV sao renderizados pelo MESMO componente (`FilteredCsvDownload`) e **compartilham a classe `filtered-csv-download_csvButton`**; **Email CSV e o PRIMEIRO no DOM**. Um seletor de classe nua + `.first()` resolve para Email CSV. DOM-proven qa2 2026-06-18 via MCP. | Desambiguar o botao Download por texto unico: `SELECTORS.csvDownloadButton = "button[class*='filtered-csv-download_csvButton']:has-text('Download CSV')"`. NUNCA selecionar o Download por classe sozinha. Cross-link: [[selector-hardening]] §classe compartilhada, [[page-object-pattern]] FilteredCsvDownloadControls. <!-- #1321, qa2, 2026-06-18 --> |
| 123 | **Overview TABLE-panel `#fromDate`/`#toDate` ignoram `fill()` — Formik nao atualiza, e o guard de 48 MiB tem tres traps distintas para automacao.** **Sintoma (a):** o teste preenche o date range do table panel via `fill()`, a query submetida usa o valor ANTERIOR (range nao muda) → contagem de linhas errada. **Sintoma (b):** com dataset grande (79k rows) `submitFilters()` resolve cedo demais e os asserts de botao rodam contra a tabela ainda em loading. **Sintoma (c):** o tooltip do Download-CSV-disabled nao aparece como texto dentro do `<span id="overview-csv-download-...">`. | **(a)** Os inputs `#fromDate`/`#toDate` sao `type="search"` controlados por Formik. `fill()` escreve no DOM mas **NAO dispara o `onChange` do Formik** — o estado interno fica com o valor anterior (mesma raiz dos pitfalls #85 da DatePicker). **(b)** `submitFilters()` usa `tableRow.first().waitFor()`, mas o `<tr>` do spinner conta como "row visible" antes dos dados chegarem em datasets grandes. **(c)** O tooltip e um **portal Bootstrap** renderizado FORA do span wrapper: `<div role="tooltip" class="tooltip show bs-tooltip-auto">`. DOM-proven sandbox 2026-06-18 (#1321). | **(a)** `setFormikDate(selector, value)` (privado em `overview.page.ts`): acessa `element.__reactProps.onChange` via `page.evaluate` e dispara o handler do React fiber diretamente; usar `setTimeout(150ms)` interno ENTRE fromDate e toDate (o re-render do fromDate recria o no toDate, invalidando a referencia). NAO usar `fill()` nesses inputs. **(b)** `page.waitForFunction` aguardando a paginacao real (`parseInt(match[3]) > 1000`) com timeout 120s ANTES de checar estado de botoes (no spec CT-08). **(c)** novo selector `csvDownloadTooltipPortal: "div[role='tooltip'].tooltip.show, div.tooltip.show"` e `getDownloadDisabledTooltip` busca o portal, nao o texto interno do span. Cross-link: [[selector-hardening]] §portal tooltip / Formik type=search, [[page-object-pattern]] OverviewPage.setFormikDate / FilteredCsvDownloadControls.getDownloadDisabledTooltip. <!-- #1321, sandbox, 2026-06-18, CT-08 PASS 79605 rows → 55.4MB > 48MB → Download disabled --> |
| 124 | **Campos GDS-snapshot `npm_segment`/`tam_score` (`uown_los_uwdata`/`uown_sv_uwdata`) lidos como NULL → falso-bug.** **Sintoma:** o teste cria uma application, espera `tam_score`/`npm_segment` populados e ve NULL, classificando como defeito — mas o env/merchant nunca roteou o caminho que grava esses campos. | Os campos sao gravados SO na decisao **GDS com `eligible_terms=16`** (`GdsResponseParser.setCurrentUWConfigData`, MR svc !1469). `tam_score` e adicionalmente **TireAgent-only**. Em **qa2**: TireAgent e decidido por GDS mas **cap 13m** (nunca 16), e a Second Look (`100000053`) **DENEGA/short-circuita** → `tam_score` inalcancavel (`count=0` sobre 6046 los + 2037 sv). `npm_segment` SIM popula em qa2 via Kornerstone/UOWN/PayTomorrow 16m GDS. DB-proven qa2 2026-06-19. | **SEMPRE** ler `decided_by_agent` + `eligible_terms` ANTES de asserir os campos (`getUwScoresByLeadPk`): se `decided_by_agent!='GDS'` OU `eligible_terms` sem `16` → `test.skip(reason)`, NUNCA assert vermelho (Rule #10). `tam_score` exige env onde Second Look TireAgent aprova 16m (stg; dev2 a confirmar — PENDENTE Marcos). Cross-link: [[ssn-test-modalities]] C.2, [[fraud-vendors-knowledge]], `docs/knowledge-base/npm-segment-tam-score-snapshot-routing.md`. <!-- qa2, 2026-06-19 --> |
| 126 | **Notas de Protection Plan / Buddy ficam em `uown_los_activity_log`, NAO em `uown_los_lead_notes`.** **Sintoma:** teste assere a nota de opt-in/opt-out/enrollment via `uown_los_lead_notes` e acha 0 rows (falso-negativo), mesmo com a row de `uown_los_protection_plan` correta. | `uown_los_lead_notes` carrega a progressao UW (`[SubmitApplication]`/`[UnderwritingService]`); os eventos PP/Buddy vao para `uown_los_activity_log`. Textos canonicos opt-IN: "Customer signed and opted in for protection plan. Initiating next steps... connector token and enroll with Buddy." → "Successfully initiated protection plan with Buddy. Status: COMPLETED". `[db-observation:uown_los_activity_log, qa2, leads 16801-16809, 2026-06-21]`. | Consultar `uown_los_activity_log WHERE lead_pk=$1` para qualquer nota de PP/Buddy. Manter `uown_los_lead_notes` so para progressao UW + guard de "missing data". Cross-link: [[activity-log-validation]], [[volatile-knowledge-registry]]. <!-- qa2, 2026-06-21 --> |
| 127 | **Enrollment do Buddy so FINALIZA no e-signature; opt-in na Terms cria apenas uma row PENDING.** **Sintoma:** teste opta-in/out, checa o DB e ve `status=PENDING`/`policy_id=null`/sem nota → falha; aumentar o poll nao adianta se o teste nao assina. | "Proceed to signature" persiste `uown_los_protection_plan` opt_in PENDING (`policy_id=null`); o callback async do Buddy seta `status=COMPLETED` + minta `policy_id` SO depois do contrato assinado (~45s apos a row nascer). Fluxo widget: Sign Contract → marcar `#isInfoConfirmed`+`#isEverythingAgreed` → "See Protection Benefits" → radios opt-in/out → "Proceed to signature". `[db-observation:uown_los_protection_plan, qa2, 2026-06-21]`. | Dirigir `ContractPage.completeESign()` apos o opt-in/out e fazer poll generoso (~240s) por `status=COMPLETED`+`policy_id` (helper `waitForProtectionPlanCompleted`). Cross-link: [[application-lifecycle]] (signing), [[gowsign-knowledge]]. <!-- qa2, 2026-06-21 --> |
| 128 | **Cross-coverage (cliente ja-coberto) mostra um widget Buddy DIFERENTE, SEM radios.** **Sintoma:** `acceptAndProceedWithProtectionPlan` lanca "no frame exposed >= 2 radios after 5 attempts" para um lead cujo email ja tem enrollment ativo. | Quando o email do cliente ja esta enrolled (lease anterior optou-in), o widget exibe "Our records indicate that you're already enrolled!" + apenas o botao PROCEED TO SIGNATURE (0 radios). DOM-proven qa2 2026-06-21 (CT-05, screenshot). | Usar `TermsOfAgreementPage.acceptProtectionPlanAlreadyEnrolled()` (`terms-of-agreement.page.ts`), que detecta o painel "already enrolled" e clica Proceed direto. NAO usar o fluxo de radios para email ja-coberto. Cross-link: [[page-object-pattern]] TermsOfAgreementPage, [[ui-first-principle]]. <!-- qa2, 2026-06-21 --> |
| 129 | **Specs DB/API-only em `docs/taskTestingUown/` nao tem projeto Playwright browserless.** **Sintoma:** uma spec de task sem fluxo de UI nao roda por NENHUM dos `task-testing-*` (ambos exigem tag de portal @origination/@servicing + dependencia de auth com browser); rodar pelo projeto padrao tenta logar no portal. | Os dois `task-testing-*` projects fazem `grep /@servicing|@origination/` + `dependencies:['auth-*']` (browser login + storageState). Uma spec API/DB-only (ex.: sweep) nao tem tag de portal → 0 selecionada. | Rodar via config temporaria browserless `playwright.browserless.config.ts` (carrega `.env`, sem browser, sem auth dep, `workers:1`, serial). **NAO deletar `playwright.browserless.config.ts`** ate existir um projeto permanente. **ADR-candidate:** adicionar `task-testing-api` browserless permanente. Cross-link: [[e2e-examples]]. <!-- sandbox, 2026-06-21 --> |
| 130 | **Tunnel DB sandbox: a porta 5445 e reusada entre envs (qa1/qa2/dev3/sandbox).** **Sintoma:** queries batem num cluster nao-sandbox silenciosamente (auth falha como `password authentication failed for svc_user`, OU retorna dados de outro env). | O port-forward manual e repontado conforme o env do dia; `.env UOWN_DB_URL_SBX` diz 5446 (stale) mas o tunnel vivo costuma ser 5445. Conectar via URL crua com a senha sandbox so falhou porque a 5445 apontava pro cluster errado — NAO por caractere especial na senha. | Antes de asserir: verificar que o tunnel aponta pra sandbox via `to_regclass('uown_sticky')` + uma row canonica (sticky pk36/account 5084). Conectar via pg **config-object (senha crua)** OU connection string **URL-encoded**. Cross-link: [[sticky-refund-tests-sandbox-only]], `docs/knowledge-base/sticky-recover-cancel-sweep.md`. <!-- sandbox, 2026-06-21 --> |
| 131 | **pg retorna colunas `bigint`/`pk` como string JS.** **Sintoma:** matchers numericos (`toBeGreaterThan(0)`) falham com "received value must be a number" recebendo `"39"`. | O driver `pg` serializa `bigint` como string p/ nao perder precisao. | Coagir com `Number()` antes de matcher numerico (`expect(Number(row.pk)).toBeGreaterThan(0)`). Cross-link: [[db-polling-pattern]]. <!-- sandbox, 2026-06-21 --> |
| 132 | **[FutureFpdCheckStep] Um cliente (SSN) com um lead ja SIGNED + first-payment-date no futuro e DENEGADO em QUALQUER nova application.** **Sintoma:** lead novo cai em DENIED logo na pipeline; activity log mostra `"Signed lead with future fpd already exists"`. Atinge testes de signing que re-rodam com o MESMO SSN. | O origination pipeline rejeita uma nova app quando o mesmo SSN ja tem um lead SIGNED cujo first-payment-date ainda nao chegou (guard de idempotencia/duplicidade pre-UW). Observado: lead 16862 DENEGADO porque um run anterior deixou 16861 SIGNED. <!-- qa2, 2026-06-22 --> | **Um teste que ASSINA deve usar SSN FRESCO por run** (`generateTestSSN`/`resolveSsn`), nunca um SSN sticky fixo se o run vai chegar a SIGNED. Reusar o sticky `082390916` so e seguro enquanto nenhum run o leva a SIGNED. Cross-link: [[ssn-test-modalities]] §C, [[test-data-hierarchy]]. |
| 133 | **CANCELLED_DUP_SSN: criar uma nova app com um SSN que ja tem lease ATIVO CANCELA o lease predecessor.** **Sintoma:** um lead/lease criado num CT anterior aparece CANCELLED inesperadamente; activity log: `"Lead is cancelled due to new app... CANCELLED_DUP_SSN"`. Quebra suites que assumem o lead anterior intacto. | O pipeline cancela o lease anterior do mesmo SSN ao originar um novo (dedup por SSN ativo). <!-- qa2, 2026-06-22 --> | Usar **SSNs distintos entre leads paralelos**, OU interleave create→use (criar e consumir um lead antes de criar o proximo com SSN diferente). Nao manter dois leads ativos do mesmo SSN esperando ambos sobreviverem. Cross-link: pitfall #132, [[ssn-test-modalities]]. |
| 134 | **Blacklist por endereco: `uown_los_black_list` casa por `street_address1 + zip_code` — um endereco "proven-good" pinado pode ter sido blacklisted manualmente.** **Sintoma:** denial PRE-UW no blacklist step para um endereco que antes funcionava; nenhum erro de codigo. | O blacklist match e por `street_address1 + zip_code`. O endereco OH ha muito pinado `1875 N Sandusky Ave / Bucyrus 44820` foi **blacklisted manualmente** (pk 2191/2196, 2026-06-19) → toda app nesse endereco denega no blacklist step. <!-- qa2, 2026-06-22 --> | **Nunca hard-pinar um unico street para criacao repetida de apps.** Usar `randomAddress(state)` (`src/data/realistic/`) que gera street unico por call (blacklist-immune). Verificar `SELECT * FROM uown_los_black_list WHERE street_address1 ILIKE $1 AND zip_code=$2` antes de assumir um endereco como bom. Cross-link: [[helpers-catalog]] realistic factory, [[test-data-hierarchy]]. |
| 135 | **pdf-parse quebra milhares: um total renderizado `"1,693.41"` sai como `"169 3.41"` (virgula virou espaco interno).** **Sintoma:** parser de dinheiro captura `169` ou `3.41` em vez de `1693.41`; assertion de valor monetario falha por ordem de grandeza num PDF que visualmente esta correto. | `pdf-parse` ao flatten o PDF dropa a virgula de milhar e injeta um espaco interno. Um regex `[0-9.,]+` para no espaco e captura so um pedaco. <!-- qa2, 2026-06-22 --> | Money parser DEVE **remover espacos E virgulas** (`.replace(/[\s,]/g, '')`) ANTES de ler o numero, nao confiar num `[0-9.,]+`. Cross-link: `contract-pdf.helper.ts`, [[gowsign-knowledge]] §pitfall float. |
| 136 | **`MERCHANT_PREFLIGHT_SKIP=true` global no `.env` (deixado por sessao paralela) faz teste de fresh-application PULAR o preflight; com os programas 13m do merchant derivados a 0 por outra sessao, `sendInvoice` retorna `paymentDetailsList` vazio → `redirectUrl required` (`""`).** **Sintoma:** lead chega a `UW_APPROVED`, `[SendInvoiceService]` cria SchedSummary, mas o teste estoura em `expect(contractUrl).toBeTruthy()` ANTES de qualquer UI. Logs: `[merchant-preflight] OW90218-0001 bypassed via MERCHANT_PREFLIGHT_SKIP=true` + `[LeadProgramService] No eligible programs, returning 13 month`. | A env var `MERCHANT_PREFLIGHT_SKIP` sobrepoe o option `skipMerchantPreflight` do `ensureMerchantReady` mesmo quando o teste NAO pede skip. TireAgent (`OW90218-0001`) ficou com `programs-13m: 0` (drift de sessoes paralelas) → sem programa elegivel → sem payment plan → sem `redirectUrl`. <!-- re-run, qa2, 2026-06-22 --> | Teste que CRIA fresh application deve rodar com preflight HABILITADO (`MERCHANT_PREFLIGHT_SKIP=false AUTO_HEAL_MERCHANT=true`) — o auto-heal religa os programas (queued pks 138+360, active 138) e o flag `useNeuroIdCheck`. NAO confundir `redirectUrl` vazio com bug de produto: e merchant config drifted. **Secundario (mesmo re-run):** o cartao Visa cru `4111…` e rejeitado pelo gateway pre-auth qa2 ("Invalid card.") — usar `TEST_CARDS.MASTERCARD_APPROVED` (regra application-lifecycle CC). Cross-link: rule #12 (merchant preflight), [[merchant-preflight]], `.claude/rules/testing.md` §Application Lifecycle (CC). |
| 137 | **stg: teste de UI Origination estoura em selector do portal-agente (ex. `input[placeholder='Search table']` em `MerchantSettingPage.filterMerchantTable`) enquanto `error-context.md`/screenshot mostra a tela "Merchant Login" (title `Uown \| Merchant`). NAO e bug de selector/timing.** **Sintoma:** `TimeoutError` num input de agente que existe em qa2 com o mesmo codigo; a pagina esta na tela de login de merchant. | `POST origination-stg.uownleasing.com/login` retorna **HTTP 423 Locked** para a conta `manager`/`admin` (ambas mapeiam username `manager`) → nenhuma sessao de agente e criada → o SPA renderiza "Merchant Login" → `/merchantSetting` (e outras rotas de agente) redirecionam pro login, e os inputs esperados nunca existem. **Account-scoped, nao host/form:** `supervisor` loga **200** no mesmo host (portal saudavel; so a conta `manager` locked), porem sem permissao Merchant-Setting. O lock vive no agent auth identity service, **NAO** no svc DB de `UOWN_DB_URL_STG` (nao da pra ler/resetar pelo framework). <!-- F-001 RU05.26.1.53.0_merchantSettingsSnapshotTracking, stg, 2026-06-22, [CONFIRMADO] live login XHR 423 vs qa2 200 --> | **Discriminador:** capturar o status do XHR de login — **423 = conta locked (env issue), NAO aumentar timeout/retry/`force`/`waitForTimeout`**. Cross-check com `supervisor` (→ 200 no mesmo host confirma portal ok, so a conta locked). **Unblock:** desbloquear `manager` no agent auth service; verificar `STG_MANAGER_PASSWORD` atual ANTES de re-rodar (senha stale re-locka a conta via logins falhos repetidos do setup `auth-origination`). Guardrail (sugestao p/ qa-implementer, NAO implementado): `auth-origination` setup deveria assertar login **200** e fail-fast com "stg agent account locked / wrong password" em vez de salvar um storageState `merchant.sid` nao-autenticado que falha todo teste downstream como timeout opaco. Cross-link: [[dom-investigation]] (423 mascara selector timeout), [[volatile-knowledge-registry]] §6 (env provisioning / agent-account lock drift-prone), `src/pages/origination/merchant-setting.page.ts:105`. |
