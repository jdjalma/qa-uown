# Application Lifecycle — Pitfalls #1–#91

> Slice of the pitfall catalog. Numbers are **global and stable** (cross-refs `pitfall #N` resolve across slices). Index + how to append: [../pitfalls.md](../pitfalls.md). Rules/sequence: [../../SKILL.md](../../SKILL.md).

| # | Symptom | Cause | Fix |
|---|---------|-------|-----|
| 1 | `Expected APPROVED but got: DENIED` without a clear reason | Email reused between runs - DataMismatchStep (ADDRESS_MISMATCH) | `buildTestData` WITHOUT `emailOverride` - unique email per run. Change the primary email AFTER the drive via `createOrUpdateEmail` (steps 17-18) |
| 2 | `submitApplication` HTTP 500 "Merchant program is required to determine fee" | No `getMissingFields` before | Step 6 mandatory |
| 3 | `UnexpectedRollbackException` in submitApplication (HTTP 200 with error in body) | `TEST_CARDS.VISA_APPROVED` (BIN 5146) causes rollback in qa1/qa2 | Use `TEST_CARDS.MASTERCARD_APPROVED` (BIN 5500) |
| 4 | `Invalid merchantId. Received XYZ` HTTP 400 in sendApplication | Merchant not provisioned in the LOS endpoint (Servicing only) | Check with DevOps if merchant has `ref_merchant_code` registered in LOS. If not, request provisioning OR use another already registered merchant |
| 5 | Kornerstone merchant rejects HTTP 400 in sendApplication | Missing `mainBankRoutingNumber` + `mainBankAccountNumber` | Kornerstone routing (per business rule #439) requires banking data in the body. Use the `sendApplication(body)` overload + `buildSendApplicationBody`: `body.mainBankRoutingNumber = TEST_BANK.DEFAULT_ROUTING; body.mainBankAccountNumber = TEST_BANK.DEFAULT_ACCOUNT;` |
| 6 | `settleApplication` HTTP 500 | Lead is not in SIGNED | Call `changeLeadStatus(leadPk, 'SIGNED')` first (step 8) |
| 7 | `makeCreditCardPayments` hangs 5 min and times out | Gateway issue in qa2 OR malformed body OR account not in ACTIVE | Verify account_status=ACTIVE before. If it persists, use `arrangementType: 'NORMAL'` (not SETTLEMENT) - synchronous with fewer side-effects |
| 8 | `sendInvoice` HTTP 500 `UnexpectedRollbackException` for `eligible_terms='16'` | Incomplete qa1 config for non-MONTHLY frequencies + 16 months | Use `selectedPaymentFrequency='MONTHLY'` (bypass fallback) + DB-patch: `eligible_terms='16'` + `merchant_program_pk=207` |
| 9 | `uown_sv_payment` with status=PAID missing - email sweep fails silently | Account forced to SETTLED_IN_FULL via direct UPDATE (without going through a real SETTLEMENT payment) | ALWAYS pay via `makeCreditCardPayments(SETTLEMENT)` - the listener populates uown_sv_payment. A direct status UPDATE does NOT populate the payments table, and the template SQL (`settled-in-full.sql`) has an INNER JOIN requiring that payment |
| 10 | `sendApplication` HTTP 400 / 500 random OR flow fails at an unlikely step (submit, sign, fund) after weeks of working | Merchant config drift in qa2 - someone unchecked `isCcRequired`, `chargeProcessingFee`, `epo10` etc., or 13m/16m programs were removed | `createPreQualifiedApplication` calls `ensureMerchantReady` before `sendApplication` (auto). Contract in `src/data/merchant-config-contract.ts`. Auto-heal via `createOrUpdateMerchant` by default; disable with `AUTO_HEAL_MERCHANT=false` in `.env` when you don't want to mutate shared state. Insufficient 13m/16m programs = fail-fast (requires manual correction) |
| 11 | `makeCreditCardPayments` HTTP 500 `DataIntegrityViolationException: constraint [fk_uown_cc_transaction_arrangement]` on every new account (UOWN + Kornerstone) | **Confirmed bug in svc** in qa2 - `/uown/svc/makeCreditCardPayments` (plural, creating a CC payment arrangement) rolls back the entire `@Transactional` of `PaymentArrangementService.runTransactions`, even with a pre-tokenized card + `useCardOnFile=true`. Reproducible via UI at `svc-website-qa2.uownleasing.com/customer-information/{accountPk}`. DB forensics shows 0 arrangements + orphan cc_transaction of $1.01 from tokenization. Unknown root cause - likely a Hibernate flush ordering issue or DB trigger; the FK is just the visible trigger of the rollback | **No automation workaround** - test must fail honestly until svc is fixed. Workarounds investigated and discarded: (a) pre-tokenization via `createOrUpdateCreditCard` + `useCardOnFile:true` does NOT help (same result); (b) singular endpoint `/makeCreditCardPayment` works but does not create an arrangement nor trigger the `SETTLED_IN_FULL` listener; (c) direct DB UPDATE conflicts with pitfall #9 (sweep depends on `uown_sv_payment(PAID)` created by the real listener). Report bug in svc project; re-run when fix arrives. |
| 12 | `uown_merchant_activity_log` query fails or returns 0 rows - program change log not found | Table is snake_case `uown_merchant_activity_log` - NOT `"MerchantActivityLog"` (PascalCase as the JPA entity annotation suggests). Reference columns: `merchant_pk` and `program_pk` (not `merchantPk`/`programPk`). `log_type` for program changes = `PROGRAM_DATA_CHANGE`. <!-- discovered on 2026-04-22 --> | Use snake_case in SQL: `SELECT * FROM uown_merchant_activity_log WHERE program_pk = $1 AND log_type = 'PROGRAM_DATA_CHANGE'` |
| 13 | `ProgramActivationDeactivationSweep` not found via `getScheduledTaskByName` | The correct scheduled task name is `ProgramActivationDeactivationSweep` - NOT `merchantProgramActivation...` nor `programActivationDeactivation...` without the full prefix. <!-- discovered on 2026-04-22 --> | Use `api.scheduledTask.getScheduledTaskByName('ProgramActivationDeactivationSweep')` |
| 14 | `createOrUpdateProgram` creates a program but the merchant does not see it in applications | `createOrUpdateProgram` creates/updates the record in `uown_merchant_program` but does NOT create the merchant-program association. Requires a separate call to `addProgramsToMerchant` to insert into `uown_merchant_to_program`. <!-- discovered on 2026-04-22 --> | Always chain: `createOrUpdateProgram(body)` -> `addProgramsToMerchant(merchantPk, [programPk])` |
| 15 | `is_active` on the program appears correct but the application uses an "inactive" program or vice versa | The `is_active` flag in `uown_merchant_program` is ALWAYS overwritten by the backend via `ProgramActivationUtils.isActiveOnDate(activationDate, deactivationDate, today)`. The source of truth is the `activation_date` and `deactivation_date` columns - not the boolean field. Directly updating `is_active` without adjusting dates can create an inconsistent state. <!-- discovered on 2026-04-22 --> | To activate/deactivate a program via tests: set `activation_date`/`deactivation_date` via `updateMerchantProgramDates(pk, {...}, authorizedBy)` - never update `is_active` directly |
| 16 | SQL query on `activation_date`/`deactivation_date` returns a JS Date object instead of a string, causing comparisons to fail | `date` columns from PostgreSQL are returned as `Date` objects in the `pg` driver. Direct string comparisons fail. <!-- discovered on 2026-04-22 --> | Use `::text` cast in queries: `SELECT activation_date::text, deactivation_date::text FROM uown_merchant_program WHERE pk = $1` |
| 17 | UI returns `activation_date`/`deactivation_date` in ISO format (`YYYY-MM-DD`) in some contexts and `MM/DD/YYYY` in others | `ProgramDetailsPage` date inputs alternate between formats depending on browser locale and HTML field type (`date` vs `text`). <!-- discovered on 2026-04-22 --> | Use `normalizeDateInputValue(raw)` from `program-details.page.ts` to normalize before comparing |
| 18 | `getEmailTemplateName` / `getMerchantChargeProcessingFee` / any `db.*` fails with `password authentication failed for user "svc_user"` | Sandbox PostgreSQL at `127.0.0.1:5445` requires the Cloud SQL proxy to be active. Without the tunnel, every DB call fails. On a developer machine outside the CI context, the proxy must be tunneled manually. <!-- discovered in qa-flow, 2026-04-23 --> | (a) Wrap DB calls in try/catch and emit `[ENV-GAP]` annotation if the proxy is inactive - primary content assertions (email body) continue; OR (b) pre-check DB connectivity in global setup with graceful skip. Specs that forget this manifest as cascading silent skips in serial blocks. **[env-blocker]** |
| 19 | Template identity assertion fails or gives a false positive - correct body marker in the repo but result diverges from the real email | `uown_template.template_content` in sandbox may diverge from the version committed in the repo. A unique marker (e.g. slogan `"you've got this"`) may not be in the deployed DB content. <!-- discovered in qa-flow, 2026-04-23 --> | Template assertions must cover: (1) body does NOT contain banner text that should be suppressed, (2) placeholder `merchantLocationName` resolves to the expected name (confirms correct backend routing), (3) footer confirms UOWN vs Kornerstone. Verify `uown_template.template_content` directly before writing slogan/specific-copy assertions. |
| 20 | `sorErrorDescription="SSN should have 9 digits. Received XXXXXXXXXX"` in `sendApplication` - SSN with 10 digits | `generateTestSSN(false)` was generating 10-digit SSNs. Cause: base was `100000000 + randomInt(89999999)` (9 digits) + suffix `"9"` = 10 digits total. <!-- discovered in CT-10 denied SSN regression, 2026-05-06 --> | Fix applied in `src/config/constants.ts:88-93`: base changed to `10000000 + randomInt(89999999)` (8 digits) + `"9"` = 9 digits. Always verify when testing a "denied" SSN path that the generated SSN has exactly 9 digits. |
| 21 | `getMissingFields` fails with HTTP 500 `"Invalid link. Please contact merchant"` after `setupApplicationViaApi(..., { extractContractUrl: true })` | `setupApplicationViaApi` extracts `redirectUrl` from `sendApplication.paymentDetailsList` (line ~402 of `src/helpers/api-setup.helpers.ts`). If `sendInvoice` runs next (the default - only skipped via `skipInvoice: true`), the backend regenerates the payment plan and invalidates the previous URL. The `shortCode` from that URL becomes stale. <!-- discovered in stg run, 2026-05-07 --> | When `sendApplication` already includes order data (common in tests) AND the `redirectUrl` returned by the helper is needed, pass `skipInvoice: true` together with `extractContractUrl: true`. The internal `submitPaymentInfoViaApi: true` flow does not suffer from this because it extracts the `redirectUrl` from the invoice response (line ~444) - not from `sendApplication`. |
| 22 | `POST /uown/los/authorizeCreditCard` returns `{"message": "Invalid card. Please try again", "status": 500}` (2 keys) instead of the slim DTO `{status, error, errorCode, preAuthStatus}` (4 keys) | When the body is invalid (e.g. `ccNumber="4111"` incomplete, `ccExp` expired, `cvc` wrong length), the request is rejected BEFORE `ApplicationService.authorizeCreditCard` by the Spring global exception handler. The `status` in the envelope is the HTTP status code as a number (not the enum `CCTransactionStatus`). <!-- discovered in stg run, 2026-05-07 --> | Tests that assert the shape of `/authorizeCreditCard` MUST handle TWO formats: (a) slim 4-key DTO when the controller was reached (approved, denied, last-name mismatch, BIN mismatch) - assert with strict shape; (b) Spring 2-key envelope when validation rejected the input before the controller - assert presence of `message`. Dispatch by `Object.keys(body).length === 4`. |
| 23 | `sendApplication` returns HTTP 400 `"Invalid merchantId. Received KS3015"` OR `getMerchantsByRefCode` returns `"Merchant not found for refCode: \"fifthavefurnitureny\""` | Kornerstone merchant KS3015 / FifthAveFurnitureNY **is not provisioned in qa1** - only in qa2. The env var `MERCHANT_FIFTH_AVE_FURNITURE_NY_QA2_PASSWORD` in the catalog explicitly indicates it is qa2-only. <!-- discovered in qa-flow smoke run qa1, 2026-05-13 --> | For Kornerstone coverage in qa1: (a) check via `getMerchantsByRefCode('fifthavefurnitureny')` BEFORE running (returns empty if not provisioned); (b) use another Kornerstone merchant available in qa1; OR (c) change `ENV=qa2` for the entire suite. Do not try to force-create the merchant - requires DevOps provisioning. |
| 24 | `OriginationCustomerPage.modifyLease` throws `TimeoutError: locator.waitFor: Timeout 5000ms exceeded` - invoice form never appears | The "Modify Lease" button on the customer page is gated by the lead status. Called on a lead in `CC_AUTH_PASSED` (just after Submit on the Complete page), the flow does not display the invoice form because the lead is not yet in `SIGNED`. <!-- discovered in qa-flow, 2026-05-13 --> | Always call `await driveLeadToSigned(api, merchant, ctx)` BETWEEN the customer Submit AND the `modifyLease` call. Reference: `tests/e2e/origination/modify-lease.spec.ts:197` already does this correctly. |
| 25 | `MissingDataFormPage.fillBankAccount` throws `strict mode violation: getByText('CHECKING', { exact: true }) resolved to 2 elements` | The locator `getByText(bank.accountType, { exact: true })` (file `src/pages/origination/missing-data-form.page.ts:51`) matches both the selected value displayed in the react-select and the option in the open menu, resulting in 2 elements. <!-- discovered in qa-flow, 2026-05-13 --> | Scoped selector for the option: `getByRole('option', { name: bank.accountType })` or `[id^="react-select"][id$="option-N"]`. **GAP not fixed** - documented for follow-up. |
| 26 | CSS-module hash selector (e.g. `.missing-data-panel_missingDataPanel__feeAmount__cn7Wg`) returns `null` after an FE rebuild | CSS-module hashes are generated by webpack at build time - they change with every rebuild (e.g. qa1 R1.52.0 vs previous). Any selector of the form `.[componentName]__[element]__[hash]` is fragile by design. <!-- discovered in qa-flow qa1, 2026-05-13 --> | Always prefer semantic locators: `getByText(/\$.*Authorization/)`, `getByRole(...)`, `getByLabel(...)`, `getByTestId(...)`. CSS-module hashes are forbidden in new selectors. `MissingDataFormPage.getProcessingFeeAmount` and `isProcessingFeeDisplayed` have already been fixed with `getByText(/\$\s?[\d,]+\.?\d*\s+Authorization/i)`. |
| 27 | Turning on email queue / sweeps in a non-prod environment triggers mass sending of old emails (~19k in Dev3, 2026-05-14) | Email queue processes all records in `pending` status upon activation - sweeps do not distinguish between legitimate new emails and accumulated backlog from previous runs. `EmailQueueSweep` (and potentially other sweeps with persistent queues) runs the entire backlog. <!-- discovered in the Dev3 spring upgrade incident, 2026-05-14 --> | **Before enabling email queue/sweeps in any non-prod env** (Dev1/2/3, qa1/qa2, stg): run `UPDATE uown_sv_email SET status='ERROR' WHERE status='pending'` (or equivalent status that neutralizes the queue) **with explicit user authorization** - it is an UPDATE, violates CLAUDE.md Exception 3 without authorization. Alternative without mutation: `SELECT count(*) FROM uown_sv_email WHERE status='pending'` before enabling to assess blast radius and decide whether to proceed. Applies to any sweep with a persistent queue (email queue is the most impactful case). **[env-blocker]** |
| 28 | `sendInvoice` with `orderType:'1'` (modify invoice / LEASE_MOD path) returns HTTP 400 `sorErrorDescription:"Cost is greater than the approved amount"` + `authApprovalStatus:"DECLINED"` + `transactionStatus:"A0"` | `orderTotal` sent exceeds the UW `approvedAmount`. **`approvedAmount` (UW decision, e.g. $2170) != order total (invoice items, e.g. $800).** Helper `createPreQualifiedApplication` returns `result.approvedAmount` which is the UW-approved amount, NOT the items total. Using `approvedAmount + delta` as the new `orderTotal` exceeds the ceiling and the backend rejects it. <!-- discovered in LEASEMOD GowSign CT-03, 2026-05-18 --> | Always ensure `orderTotal < approvedAmount`. Do NOT use `approvedAmount` as the baseline for orderTotal - use the order total **of the items** (default ~$800 when `createPreQualifiedApplication` sends Ottoman $500 + Recliner $300). |
| 29 | `sendInvoice` with `orderType:'1'` returns 200 OK but **does not create a LEASE_MOD** - log shows `"Invoice decrease. Set lead to SIGNED"` + `"LeaseType : LEASE"` + `"EsignMode : EMBEDDED"`; the `ADDED : Contract[..., contractType = LEASE_MOD ...]` note is NOT generated | `orderTotal` sent is **less than** the existing order total. Backend classifies it as an "Invoice decrease" and modifies the LEASE in-place (does not issue a new LEASE_MOD contract). LEASE_MOD is only created when `newOrderTotal > currentOrderTotal`. <!-- discovered in LEASEMOD GowSign CT-03, 2026-05-18 --> | To force a LEASE_MOD via API: `newTotal > currentInvoiceTotal`. Default applicant order = $800 (~Ottoman $500 + Recliner $300). Pattern: `const newTotal = 1100;` (with `initialAmount=1000 + modIncrement=100` or similar) - > $800 and < approvedAmount. Validate via log `"Invoice increase"` in the assertion. |
| 30 | `addActivityLog` / `addLogNote` returns HTTP 400 `InvalidFormatException: Cannot deserialize value of type 'LogType' from String "GENERAL"` | `LogType` is a strict enum. Valid values: `IMPORT, REVIEW, DATA_CHANGE, DUE_DATE_MOVES, CORRESPONDENCE, STATUS_CHANGE, CREDIT_CARD, BANK_ACCOUNT, ACH, PAYMENT, REWIND_REPLAY, DEFAULT, UNDERWRITING, INTERNAL, INFORMATION, MERCHANT_DATA_CHANGE, FRAUD, ERROR, PROGRAM_DATA_CHANGE, STATE_CONFIG_CHANGE, PAYWALLET, UWENGINE, ALLOCATION, SKIT_CALL_LOG, CUSTOMER_ASSISTANCE, ESCALATION, OUTREACH_MERCHANT, OUTREACH_PLATFORM, OUTREACH_ISR, OTHER, PEO_SUPPORT, POP_SHIPMENT, EMAIL, INBOUND_CALL, MERCHANT_REVIEW, SOCIAL_MEDIA, NEW_MERCHANT, FREQUENCY_CHANGE`. `"GENERAL"` does NOT exist. <!-- discovered in fix CT-03b/07a/07b/10a/10b, 2026-05-20 --> | For test activity log / note use `logType: 'INFORMATION'` (generic, low side-effect). For domain-specific logs (DATA_CHANGE, PAYMENT, etc.) consult the enum first. |
| 31 | `LosExternalMerchantController.searchApplicationStatus` (POST /uown/los/merchant/applications/search) returns 401 when tested with a Bearer token from `LosPartnerAuthClient` OR "Authorization header missing" without a bearer | **Source-verified:** there is NO Spring Security nor `@PreAuthorize` on the controller. Auth is via body fields `userName/setupPassword/merchantNumber` (inline credentials), validated at runtime by `LosRequestMessageConstraintValidator.validateApplicationStatusRequest`. Bearer token is ignored. <!-- discovered in fix CT-04/CT-10c, 2026-05-20 --> | To call `searchApplicationStatus` via `LosPartnerApplicationClient`: use `buildApplicationStatusBody(merchant, leadUuid)` (already populates userName/setupPassword/merchantNumber/accountNumber). Do NOT call `losPartnerAuth.authorize` before - it is not necessary. Header `X-API-Version: 2` is required (controller class-level mapping). |
| 32 | `AspectInboundApiLog` in `uown_sv_inbound_api_log.header` stores header names in **lowercase** (`x-run-id=...`), even though the client sends `X-Run-Id`. Postgres queries `header LIKE '%X-Run-Id%'` return 0 rows even when the row exists | Map serialized via `Map.toString` after `request.getHeaderNames` - Spring/servlet container normalizes names to lowercase when populating the `HashMap`. `LIKE` in Postgres is case-sensitive. <!-- discovered in fix CT-01/02/03a/11/12, 2026-05-20 --> | In test queries on `*_inbound_api_log.header`, always use `ILIKE` (case-insensitive). Helpers `fetchSvLogsByHeader` / `fetchLosLogsByHeader` in WI-525 tests corrected. |
| 33 | `AspectInboundApiLog.setApiLogInfo` extracts `source_uuid` via `StringUtils.substringBetween(body.toLowerCase, "uuid\":", ",")`. If `uuid` is not at the root level followed by a comma, `source_uuid` stays empty | String-based extraction (not JSON parsing) is fragile: depends on ORDER and SYNTACTIC POSITION. Node/TS JSON.stringify generates `{"uuid":"x"}` without a trailing comma - `substringBetween` returns `null`. <!-- discovered in fix CT-05/CT-08/CT-09, 2026-05-20 --> | In any POST body whose log row needs to be correlated by `source_uuid`: declare `uuid` as the **first key** of the object literal, string value without special characters, and ensure there is at least one key after it to force the comma. Pattern: `const body = { uuid: correlationUuid, ...restOfBody }`. |
| 34 | Filters `WHERE row_created_timestamp >= runStart.toISOString` in `uown_sv_inbound_api_log` return 0 rows in qa1 even when the rows exist | JVM TZ misconfigured in qa1 - `row_created_timestamp` is written ~1h **before** real UTC. The window calculated at test runtime (correct UTC) does not cover the effectively persisted timestamp. <!-- discovered in run 2, 2026-05-20 --> | Correlate log rows by **unique marker** (`source_uuid`, `header ILIKE '%<runId>%'`, or custom `_marker` field in the body), NEVER by `row_created_timestamp` window. **[env-blocker]** |
| 35 | Error-path scenarios on endpoints covered by the AOP `AspectInboundApiLog` produce either 1 or 2 rows in `uown_*_inbound_api_log` for the same `x-request-id` | Pointcut L10 (`CustomExceptionHandler.*(..)`) fires for **some** `@ExceptionHandler` routes and not others. `handleInvalidDateFormat` replaces the controller in the AOP chain - **1 row**. `logExceptions` (triggered by `InvalidFieldsException` / `SvcException`) runs as an **additional advice** - **2 rows**. <!-- discovered in run 2, 2026-05-20 --> | Count assertions MUST specify the handler path. Pattern: `SELECT api, COUNT(*) FROM uown_los_inbound_api_log WHERE source_uuid = $1 GROUP BY api`. Never assert "exactly 1 row" universally for error paths. |
| 36 | Open-redirect test uses `framenavigated` + regex on substring and gives a false-positive | Regex on substring matches the entire URL, including query-params controlled by the attacker in the input URL. <!-- discovered in test-bug, 2026-05-20 --> | Parse via `new URL(navigatedUrl).host === 'evil.com'` instead of regex on substring. A whitelist of allowed hosts is more robust. |
| 37 | `await page.goto(url)` throws `net::ERR_HTTP_RESPONSE_CODE_FAILURE` when server returns 4xx/5xx | Default Chromium behavior: `goto` throws on HTTP error responses. <!-- discovered in test-bug, 2026-05-20 --> | Wrap in try/catch when the test deliberately sends a malformed/boundary payload. Validate status via `request.get({ maxRedirects: 0 })` if the goal is only to check the HTTP status. |
| 38 | `StickyRecoverSweep` fires without error but does NOT create a `uown_sticky` session for an account morphed by the fast-path helper | The sweep requires the account to be delinquent: `JOIN uown_sv_sched_summary s` + `WHERE s.delinquency_as_of_date <= CURRENT_DATE`. The helper morphed the cct but did not touch `delinquency_as_of_date`. <!-- discovered in CT-01 2026-05-20 --> | `createStickyEligibleFromExistingAccount` now also UPDATEs `uown_sv_sched_summary.delinquency_as_of_date := CURRENT_DATE - INTERVAL '7 days'` on the morphed account when the current DAOD is NULL or in the future. |
| 39 | `POST /uown/los/submitApplication` returns HTTP 500 with body `"UnexpectedRollbackException: Transaction silently rolled back because it has been marked as rollback-only"`. Reproducible on all merchants. | Silent regression introduced in recent svc commits. Some internal step of `submitApplication` marks the JPA transaction as `rollback-only`. <!-- discovered in pipeline CT-01..CT-09, 2026-05-21 --> | Mark dependent specs with `test.fixme(true, '@blocked-by-svc-regression-submitApplication-2026-05-21')`. **[env-blocker]** **MITIGATED in qa1 on 2026-05-24:** `sendApplication` returns 200 in qa1; 89 leads FUNDED in the last 14 days confirm resolution. |
| 40 | `CreditCardHistoryPage.getStickyRecoveryStatus(cctPk)` returns `-` even with `uown_sticky` row populated | Race condition in FE Servicing MobX: `getCCTransactions` + `getStickyRecoveries` in `Promise.all`. `CreditCardHistoryTable` is NOT wrapped in `observer`. <!-- discovered in CT-11 2026-05-21 --> | Navigate to `/customer-information/{accountPk}` FIRST (hydrates `customerStore.accountPk`), then click the "CC Transactions" tab - instead of direct URL to `/credit-card-history/`. |
| 41 | `uown_sv_sql_config.sql_name` is case-sensitive UPPERCASE - `WHERE sql_name='getSettlementAmount'` returns 0 rows | The BE service uppercases the value on lookup before executing the query. <!-- discovered in investigation 2026-05-22 --> | Always use UPPERCASE when querying `uown_sv_sql_config`. |
| 42 | `Days Past Due` (panel) != `Days Delinquent` (breakdown) | Panel uses `uown_sv_sched_summary.days_past_due` (periodic sweep). Breakdown calculates `CURRENT_DATE - delinquency_as_of_date` at runtime. <!-- discovered on 2026-05-22 --> | Do NOT use `days_past_due` from the panel to validate the settlement discount. After `ageAccount(db, pk, N)`, the breakdown reflects `N` but the panel shows the previous value. |
| 43 | Settlement/EPO Breakdown modal opens EMPTY when value is $0.00 | Confirmed FE bug (BUG-3). Accounts with rating B or CANCELLED. <!-- discovered on 2026-05-22 --> | Ensure account is in a delinquency range with value > $0.00 BEFORE calling `open`. |
| 44 | TCA in panel vs TCA in breakdown diverge when there are active fees (late fee, NSF) | Panel sums TCA + active fees. Breakdown uses raw TCA from `uown_sv_sched_summary`. <!-- discovered on 2026-05-22 --> | Extract raw TCA via DB, do NOT read from the UI panel. |
| 45 | Artificial aging via UPDATE requires a mandatory try/finally restore | Omitting the finally leaves the seed account corrupted. <!-- 2026-05-22 --> | Use `withAgedAccount(db, pk, days, async () => { ... })` or a `try/finally` block. Helper `account-aging.helpers.ts` already encapsulates this. |
| 46 | Backend calls `getSettlementAmount` 2x in `ServicingInformationService` | Heavy SQL executed 2x per page request. <!-- code review MR 2026-05-22 --> | Code smell identified, NOT a test blocker. |
| 47 | Any selector that depends on a CSS-Module class, accessible name by sibling text, or `input[type="checkbox"]` for the combobox on the AMS Active Merchants page fails at runtime | The AMS Active filter widget combines 3 fragility surfaces: (a) `role="checkbox"` without `aria-label`; (b) volatile CSS-Module hash; (c) react-select option portal mount timing. <!-- discovered in 6 passes, 2026-05-22 --> | **APPROVED strategies:** (1) Keyboard contract: open combobox - ArrowDown - Enter; (2) React-select option ID: `[id^="react-select-"][id$="-option-0"]`. |
| 48 | `VerifyCustomerInformationModal` backdrop intercepts clicks at `/customer-information/{pk}` | Modal auto-mounts on EVERY navigation via React `useEffect`. Dismiss via Confirm sets `utilityStore.isVerified=true`. <!-- discovered in pipeline, 2026-05-22 --> | Use `dismissCustomerInfoConfirmation(page, timeoutMs?)` from `src/helpers/servicing-dialogs.helpers.ts`. |
| 49 | Servicing modals use a custom wrapper, NOT Bootstrap `.modal-body`/`.modal-header` | Settlement Breakdown modal uses `<div class="overflow-auto p-3">`. Close is `svg.fa-xmark-large`, not `.btn-close`. <!-- discovered on 2026-05-22 --> | DOM-first via MCP (rule 15) BEFORE declaring any Servicing modal selector. |
| 50 | Selector validated live via MCP does not guarantee a runtime pass | Canonical case: `[class*="customOptionStyles"]` returned 2 in MCP, 0 in runtime 65 min later. <!-- discovered in S3, 2026-05-22 --> | MCP-live validation is NECESSARY but NOT SUFFICIENT. Run the full test at least 2x before declaring the selector stable. |
| 51 | `/{shortCode}/complete` renders "Choose Payment Program" screen BEFORE the CC form | Calling `fillCreditCardInfo` directly after `page.goto` results in `TimeoutError` on `#ccFirstName`. <!-- discovered in CT-05 2026-05-22 --> | Call `await contract.choosePlanByName(planName)` BEFORE `fillCreditCardInfo`. |
| 52 | `moveDueDatesByDays` returns HTTP 400 for a WEEKLY lease when `moveNumberOfDays > 3` | Rule in `MoveDueDatesService.java:117-126`: `WEEKLY - maxOffset=3`; others - 7. <!-- discovered on 2026-05-22 --> | Read `customerPaymentFrequency` from the account. If WEEKLY, use offset <=3. Safe universal strategy: offset=3. |
| 53 | `driveLeadToFunding` / `createPreQualifiedApplication` creates WEEKLY leads by default | Default in 3 places in `application.body.ts`. No assertion fails visibly. <!-- discovered on 2026-05-22 --> | For a different frequency: pass override in the body. Verify with `SELECT customer_payment_frequency FROM uown_sv_sched_summary WHERE account_pk = $1`. |
| 54 | Endpoints `/uown/los/*` require a BFF session cookie (`merchant.sid` set after login) | An isolated API key is NOT enough - `MerchantCodeAspect` AOP returns 401. Pagination params are `@RequestParam` (query string), NOT body. Response shape is a WRAPPER `{ searchResults, count, moreResults }`, NOT a flat array. <!-- discovered on 2026-05-24 --> | **3 workarounds**: (a) host=svc bypass; (b) browser fetch from page context; (c) UI flow (default rule #14). |
| 55 | `uown_los_lead_personal_info` does NOT exist in qa1 | Canonical phone table is `uown_los_phone`. <!-- discovered on 2026-05-24 --> | Correct query: `SELECT (area_code \|\| phone_number) AS phone FROM uown_los_phone WHERE lead_pk=$1 AND phone_type='MOBILE' LIMIT 1`. |
| 56 | SVC search only returns leads with `account_pk IS NOT NULL` | Servicing query JOINs `uown_sv_account`, leads SIGNED but never funded do NOT appear. <!-- discovered on 2026-05-24 --> | Happy lead fixture MUST filter `WHERE lead.account_pk IS NOT NULL`. |
| 57 | Hard-coded PK fixture is fragile in qa1 - drift by reseed | Lead Karen (pk=11319) had `account_pk=NULL`. <!-- discovered on 2026-05-24 --> | Resolve PKs dynamically in `beforeAll` via query. |
| 58 | MR fixing a field in specialized SQLs must audit ALL sibling SQLs | MR !1370 fixed 9/10 SQLs - `getLosSearch_FreeText.sql` was left with the wrong alias. <!-- discovered on 2026-05-24 --> | Audit aliases for ALL variants via script. Assertion on ALL searchTypes. |
| 59 | Expression index with `UPPER(col)` is only used if WHERE also applies `UPPER` to the column | `WHERE invoice_number = UPPER(:searchString)` with UPPER only on the parameter - Seq Scan. <!-- discovered on 2026-05-24 --> | Fix: `WHERE UPPER(invoice_number) = UPPER(:searchString)`. |
| 60 | `POST /uown/los/simpleSearch/{term}` accepts a missing body with 200 OK | Spring `@RequestBody(required=false)`. <!-- discovered on 2026-05-24 --> | Document as expected anomaly. |
| 61 | `task-testing` project storageState/baseURL mismatch | Specs covering BOTH Origination AND Servicing fail. <!-- discovered on 2026-05-24 --> | Project split into `task-testing-origination` and `task-testing-servicing`. Each spec MUST declare `@origination` or `@servicing`. |
| 62 | `termnull` smell in `uown_los_lead_notes` indicates a lead with `main_next_pay_date = NULL` | Settle note generates `termnull` - signal of the NPD=null bug. <!-- discovered on 2026-05-24 --> | Diagnosis: `SELECT COUNT(*) FROM uown_los_lead_notes WHERE notes ILIKE '%termnull%'`. Zero rows = fix applied. |
| 63 | `sendApplication` returns HTTP 400 `"mainNextPayDate is required"` when the field is omitted | Contract drift post: API now mandatorily validates `mainNextPayDate`. <!-- discovered on 2026-05-24 --> | NPD=null scenario is only reproducible via the natural path. It is NOT possible to force it via the sendApplication body post. |
| 64 | `OriginationCustomerPage.settleLeaseViaDocuments` throws "Unable to locate a lease document" | Filter uses regex `/^UOWN_/` which does not match KS3015 documents. <!-- discovered on 2026-05-24 --> | **[OBSERVATION]** - workaround: use `settleLeaseViaButton`. Follow-up: inspect DOM with MCP. |
| 65 | **[SUPERSEDED by #66]** Activity-log polling of 60s insufficient in qa1 under load | **SUPERSEDED:** real root cause is TZ drift, not backend timeout. | Use the fix from #66. |
| 66 | Silent DB timestamp drift - `WHERE col >= $1::timestamp` with ISO UTC from JS excludes recently written rows | `timestamp WITHOUT time zone` columns written in DB-server local time (NY EDT/EST). JS sends UTC, cast strips Z. <!-- discovered on 2026-05-24 --> | Fix: `(col AT TIME ZONE current_setting('TimeZone')) >= $1::timestamptz - INTERVAL 'Nh'`. Alternative: rely on `WHERE lead_pk = $1 ORDER BY pk DESC LIMIT N`. |
| 67 | `button:has-text('Sign')` collides with `"Change to Signed"` - strict mode violation | Locator matches multiple elements simultaneously. <!-- discovered on 2026-05-24 --> | Fix: `getByRole('button', { name: /^E[-\s]?Sign$/i })`. |
| 68 | **[SUPERSEDED by #69]** Origination SPA `ensureAuthenticated` race | **SUPERSEDED:** see pitfall #69 for canonical v8 fix. | |
| 69 | Origination SPA full-suite auth-retry: storageState from `auth.setup` contains a JWT with TTL ~15 min in qa1; tests after CT-10 load an EXPIRED JWT | Stale stores from storageState trivially satisfy the `*Store$` guard. SPA route-guard bounces to `/` async. <!-- discovered on 2026-05-24 --> | **Canonical fix v8 (`customer.page.ts:248-340`):** (A) Pre-emptive JWT exp check; (B) Caller-supplied intendedPath; (C) page.reload instead of goto(base); (D) Real hydration guard via poll until JWT `exp > now + 60s`. Forbidden anti-patterns: guard by `*Store$`, `goto(base)` during SPA nav, trusting SPA auto-return, lazy `isLoginPage` detection. |
| 70 | Consumer apply flow entry path is `/getApplication/{code}`, NOT `apply-{env}/{code}/start` directly | Navigating to `apply-{env}.{host}/{merchantCode}/start` directly returns "application link has expired". The redirector generates a short-lived token. <!-- discovered on 2026-05-24 --> | Always enter via `origination-{env}.uownleasing.com/getApplication/{code}`. |
| 71 | **[RETRACTED 2026-05-24]** ~~Two distinct wizards served on the same path~~ | There is ONE wizard (Next.js SPA). The old page object had wrong casing and removed fields. <!-- corrected 2026-05-24 --> | Accessibility snapshot is not enough for IDs - use `browser_evaluate` with `document.querySelectorAll('input')`. |
| 72 | Backend validates `nextPayDate` <= 10 days in the future; returns structured HTTP 400 | Wizard completes without a UI error, final submit shows "Something Went Wrong!". <!-- discovered on 2026-05-24 --> | Use `nextPayDate = calculateDate(7)` (aligned with WEEKLY). |
| 73 | Kornerstone consumer wizard page 2 requires bank routing + account + CC BIN (required) | UOWN treats them as optional. Without them in KS, the Next button stays silently disabled. <!-- discovered on 2026-05-24 --> | Always pass values for all 3 fields via `ApplicationWizardPage.fillEmploymentInfo`. |
| 74 | Origination SPA: `page.goto(originationBase)` with a valid storageState still redirects to the login screen | The SPA router serves `/` as the login landing page; the route guard does not automatically navigate to the dashboard after hydrating auth from storageState. `storageState` loaded into the browser context is NOT enough to land on the dashboard. <!-- discovered on 2026-05-27 --> | Navigate to `${originationBase}/overview` (or a specific authenticated route) instead of bare `originationBase`. Root `/` is ALWAYS the login screen from the React Router's perspective. |
| 75 | Auth tests in Origination: POST without `merchant.sid` returns HTTP 404, NOT 401 | Routes `POST /uown/get*` are registered INSIDE the Express session middleware scope. A request without the `merchant.sid` cookie is rejected by the middleware BEFORE the route handler, resulting in "route not found" (404) instead of "unauthorized" (401). <!-- discovered on 2026-05-27 --> | To test auth rejection: send `merchant.sid` cookie WITH an invalid/expired value (gets 401). To verify behavior without any cookie: expect 404. Do NOT confuse 404 with "route does not exist". Pattern: `merchant.sid` present + `userToken` absent = 401. |
| 76 | SPA localStorage caching: deleting the session cookie does NOT clear `overviewStore`; dashboard displays stale data | Origination Overview React SPA reads `overviewStore` from localStorage on init and renders metric cards without new API calls when it detects cached data. `clearCookies` + `page.reload` keeps the cards visible with old data, even if the backend blocks the APIs (404 without cookie). <!-- discovered in stg, 2026-05-27 --> | Expected behavior (UX/client-side concern, NOT a security failure — backend blocks correctly). Auth tests that validate "data does not appear after logout" MUST: (a) explicitly clear localStorage via `page.evaluate(() => localStorage.clear())` before reload, OR (b) open a new browser context without storage. Asserting absence of data with the cookie deleted but localStorage not cleared is an incorrect assertion. |
| 77 | Payment/Reverse refund in Servicing is NOT at `/payment-transaction` | The `/payment-transaction` screen (History - Transaction) shows a financial summary but does NOT have a per-row reverse icon. The Reverse/Refund flow lives in `/payment-history/{accountPk}` (History - Payments). The reverse icon is `svg[data-icon="arrow-rotate-left"]` (NOT `.fa-undo` as in old code). The refund activity log goes to `uown_sv_activity_log` (Servicing action), NOT `uown_los_lead_notes` (LOS). <!-- DOM-first validated in dev3 account 94, svc-website-dev3, 2026-06-01 --> | Navigate to `/payment-history/{accountPk}` and use `PaymentHistoryPage.openReverseForPaymentByAmount`. Validate log in `uown_sv_activity_log` (same table as Move Due Date - payment-flows pitfall). Selectors in `common.selectors.ts:219-233`. |
| 78 | `reverseReason` in the refund modal is a React Select (`<div>`), NOT a native `<select>` - `selectOption` fails | The `#reverseReason` control in the reverse modal is a React Select rendered as a `<div>`. `page.selectOption` only works on native `<select>` and gives a no-op/silent error. Dropdown options (exact text in DOM): "Reverse", "Fully Refund", "**Partially** Refund" (NOT "Partial Refund" - the enum `ReverseReason.PARTIAL_REFUND` has the wrong text value). The `#paymentAmount` field is only visible when "Partially Refund" is selected. <!-- DOM-first validated in dev3, 2026-06-01 --> | Select via click on the control (`reverseReasonControl`) + click on the menu option (`reverseReasonOption`). Confirm `tagName` via `browser_evaluate` before using `selectOption` in any reverse/refund test. Use `PaymentHistoryPage.setReverseReason`. Note the text "Partially Refund" (not "Partial Refund"). |
| 79 | ACH Make Payment assert (Servicing `#makePayment` modal) times out at 60s despite success toast | **ACH Make Payment via Servicing is ASYNCHRONOUS.** Immediate synchronous effect: `uown_sv_achpayment` with `status='PENDING'`, `amount=X`, `ach_process_type='REQUEST'` + activity log `ADDED : ACHPayment[...status=PENDING...amount=X...]` in `uown_sv_activity_log`. The `uown_sv_payment` row, the log `ADDED : Payment[paymentType=ACH...]`, and `status='PICKED_TO_SEND'` exist only AFTER THE SWEEP (cron `CreateScheduledACHPaymentsSweep`, daily at 19:00). Waiting for `uown_sv_payment` for ACH within a test timeout = waiting for post-sweep state that never arrives. <!-- discovered in make-payment-servicing.spec.ts, dev3, 2026-06-01 --> | Assert `uown_sv_achpayment WHERE status='PENDING'` + `ADDED : ACHPayment` log. NEVER assert `uown_sv_payment` for ACH within a reasonable timeout. Cross-link: [[payment-flows]] Make Payment section. |
| 80 | Overpayment test in the Make Payment modal fails when asserting rejection | **The Servicing `#makePayment` modal does NOT validate an upper limit - overpayment is INTENTIONALLY accepted.** Amount > remaining balance (or > EPO payoff) results in: CC SALE APPROVED for the submitted amount, `uown_sv_payment` row created, log `ADDED : Payment[...]`, and account transitions to `PAID_OUT_EARLY_EPO` if amount >= EPO amount. Confirmed by the user (2026-06-01) as expected behavior - excess refund is a separate back-office process. <!-- discovered in make-payment-servicing.spec.ts, dev3, 2026-06-01 --> | Do NOT assert overpayment rejection. Overpayment tests must assert positive behavior (payment created, CC APPROVED, status transition when applicable). Cross-link: [[payment-flows]] Make Payment section. |
| 82 | Code/JSDoc assumes Arrangement Type in the Make Payment modal is backend-derived from the amount (no UI field) | **FALSE.** The Servicing Make Payment -> Payment Arrangement modal has an EXPLICIT React Select for Arrangement Type: `label[for="paymentArrangementType"]` with options `NORMAL` / `SETTLEMENT`. The old JSDoc for `makeCcPaymentArrangement` (2026-03-17) stated "UI does NOT expose an explicit arrangementType field; backend derives it from amount" — that comment was WRONG/outdated. Also confirmed: Frequency dropdown has `Weekly`/`BiWeekly`/`Monthly`/`SemiMonthly` (use exact regex); `totalPaymentAmount` auto-distributes across installments; ACH arrangement = `NOT_STARTED` + installments `PENDING` (asynchronous), CC arrangement = `SUCCESS` (synchronous). <!-- DOM-first validated in dev3, payment-arrangement-servicing pipeline, 2026-06-01 --> | Use `SELECTORS.arrangementTypeDropdown` (label-scoped) + click on the control + click on the option (React Select, `selectOption` does not work). Pass `arrangementType: 'NORMAL' \| 'SETTLEMENT'` in `makeCcPaymentArrangement` / `makeAchPaymentArrangement`. Cross-link: [[payment-flows]] Payment Arrangement via Servicing UI section. |
| 81 | Helper/custom function calls `submitApplication` without `getMissingFields` before -> `submitApplication` fails silently with "Merchant program is required", lead never reaches CONTRACT_CREATED, and `settleApplication` returns HTTP 500. **Diagnostic symptom:** `settleApplication` returns 500 **AND** the lead is in `CC_AUTH_PASSED` (never reached CONTRACT_CREATED) -> root cause is almost always `submitApplication` without `getMissingFields`. | Without `getMissingFields`, `merchantProgramPk` is not resolved and the backend rejects the submit. "Silent" variant of pitfall #2 (which covers the direct HTTP 500 case): here the submit fails without being asserted, masking the root cause that only resurfaces in the downstream `settleApplication`. `createAndDriveToFunding` in `tests/api/lease-cancellation-api.spec.ts` was calling `submitApplication` directly, without extracting shortCode/planId nor asserting `submitResp.ok`. <!-- discovered in cycle 1 of lease-cancellation-api validation, dev3, 2026-06-01 --> | Any helper or custom function calling `submitApplication` MUST first: (1) extract `shortCode` + `planId` from `paymentDetailsList[0].redirectUrl` in the `sendInvoice` response; (2) call `getMissingFields(shortCode, { planId })` to resolve `merchantProgramPk`; (3) **explicitly assert `submitResp.ok`** (never swallow silently). Prefer `createPreQualifiedApplication` (already chains the canonical sequence). Cross-link: pitfall #2, [[common-operations]] submitApplication section. |
| 83 | ACH arrangement stuck in `payment_arrangement_status='IN_PROGRESS'` after `SendACHPaymentsSweep` in env without a real processor (dev3) - `recalculateAchArrangementStatus` never reaches `SUCCESS` | In env without real Profituity (dev3), the ACH payment stops at `status='PICKED_TO_SEND'` after the sweep and never advances to `SETTLED` (no processor callback). `recalculateAchArrangementStatus` returns `IN_PROGRESS` because `PICKED_TO_SEND ∈ PENDING_STATUSES` - the decision matrix (business-rule §54 step 2) classifies it as "pending transactions exist". Finalization logs (`Arrangement finalized as SUCCESS`/`FAILED`) are NOT emitted by any synthetic path - only by the Java `PaymentArrangementACHListener` on a REAL processor callback. <!-- discovered in payment-arrangement-servicing pipeline (S4/S5), dev3, 2026-06-01 --> | To synthesize the terminal state in an env without a processor: **UPDATE ACH payments to `status='SETTLED'` (Exception 3 - requires explicit user authorization) BEFORE calling the recalc**. After the UPDATE, `recalculateAchArrangementStatus` advances to `SUCCESS`. Assertion: DB state (status + is_active) is HARD; CREATION log ("ACH Arrangement created", organic pre-sweep) is HARD; FINALIZATION log in the synthetic path mark as `@blocked-by-missing-log` (do NOT remove - documents debt vs rule #13) + question to dev for an env with a processor. Cross-validation: S6 (CC SETTLEMENT, REAL synchronous path) **does** generate the finalization log - proves the log comes from backend execution, not from the recalc helper. Cross-link: payment-flows pitfall #16, business-rule §54. **[env-blocker]** |
| 84 | ACH arrangement listener does not update the arrangement in intermediate states - arrangement stays `NOT_STARTED` while payments transition `PENDING → PICKED_TO_SEND → SENT` | `PaymentArrangementACHListener` only triggers an arrangement update on **terminal** payment states (`SETTLED` / `RETURNED` / `ACK_ERROR`). Intermediate states (`PENDING`, `PICKED_TO_SEND`, `SENT`) do NOT advance the arrangement - it stays `NOT_STARTED` until the first terminal state. Confirmed in dev3: arrangements 63/64/65 with payments in `SENT` and arrangement still `NOT_STARTED`. <!-- discovered in payment-arrangement-servicing pipeline, dev3, 2026-06-01 --> | Do NOT assert arrangement status transition based on an intermediate payment state. The arrangement only leaves `NOT_STARTED` when at least one payment reaches a terminal state. In envs without a processor, synthesize a terminal state via authorized UPDATE (pitfall #83) to unblock. Cross-link: payment-flows sweep chain section, business-rule §54. |
| 85 | Date pickers `#startDate` / `#endDate` of the Make Payment Arrangement modal (Servicing) IGNORE `pressSequentially` / `fill` / `type` - React does not see the value change, endDate always stays = startDate = today → always 1 installment (root of bugs F-001 and F-007/S7) | The fields are date pickers rendered as `<input type="search">` controlled by a React component (same DatePicker as `application-wizard.page.ts`). `fill`/`type`/`pressSequentially` write to the DOM but do NOT trigger the `onChange` that React observes - the component's internal state stays with the previous value. <!-- discovered in payment-arrangement-servicing pipeline (F-007/S7 resolution), dev3, 2026-06-01 --> | Set via native `HTMLInputElement.prototype.value` setter + dispatch of `input` and `change` events (pattern already used in `application-wizard.page.ts` for the same DatePicker). Correct selectors: `#startDate` / `#endDate`. With the fix, `today → today+28` Weekly correctly generates 5 installments (confirms that F-007/S7 was not a product bug - it was the date picker ignoring the text). Cross-link: [[payment-flows]] Payment Arrangement via Servicing UI section. |
| 86 | CC multi-installment arrangement does not reach `SUCCESS` - arrangement stays in `IN_PROGRESS` even after `simulateCcSweepForArrangement` | With a date range generating N installments, only the installment with `posting_date = today` processes synchronously (APPROVED). Future installments (`posting_date > today`) stay `PENDING` → arrangement goes to `IN_PROGRESS`, not `SUCCESS`. `simulateCcSweepForArrangement` is date-gated (`posting_date <= CURRENT_DATE`) - does not touch future ones. **This is the CORRECT product behavior** (future installments process when the posting date arrives), NOT a bug. <!-- discovered in payment-arrangement-servicing pipeline, dev3, 2026-06-01 --> | In envs without a real processor (dev3) the future posting date never triggers the real sweep. To synthesize the terminal state in the test: use `db.approveAllPendingCcSalesForArrangement(arrangementPk)` (no date gate, approves ALL PENDING SALEs) - authorized stand-in for an env without a processor (Exception 3, same pattern as S4/S5), then `recalculateArrangementStatus`. DB-confirmed dev3: arrangement pk100 with 5 SALEs (pk3328 APPROVED + pk3329-3332 PENDING). Cross-link: [[payment-flows]] Payment Arrangement via Servicing UI section; [[helpers-catalog]]. |
| 87 | `uown_sweep_logs.number_of_records_processed` read immediately after trigger returns `0` even when the sweep successfully processes accounts | The Java sweep creates the row in `uown_sweep_logs` with `number_of_records_processed=0` BEFORE processing and only updates the field to the real count AFTER processing. Immediate reads (`< 5s` after trigger via `waitForRecord` + immediate read) capture the pre-processing value. <!-- discovered in email-sweeps-servicing.spec.ts, dev3, 2026-06-02 --> | Do NOT assert `processed >= 1` via immediate read from `uown_sweep_logs`. Use the destination table with a monotonic PK as primary evidence: `uown_email_queue` (new row with the expected `template_name` + `row_created_timestamp` from today). `uown_sweep_logs` is only reliable after the sweep has actually finished. Cross-link: [[payment-flows]] Email Sweep validation section. |
| 88 | Eligibility query for `settledInFullAccountEmailSweep` finds accounts the sweep does NOT process (e.g. account 103 settled in April outside the window) | The sweep has a CASE-WHEN on `settled_in_full_date_time` based on the day of the week (DOW): DOW 1/2 (Mon/Tue) → `DATE(settled_in_full_date_time) = CURRENT_DATE - 4`; DOW 3 (Wed) → `IN (CURRENT_DATE-4, -3, -2)`; DOW 4/5 (Thu/Fri) → `= CURRENT_DATE - 2`; weekend (`DOW NOT BETWEEN 1 AND 5`) → sweep does not run. An eligibility query that omits the CASE-WHEN overestimates the eligible set. <!-- discovered in email-sweeps-servicing.spec.ts S1, dev3, 2026-06-02 --> | Use the EXACT sweep query (including the DOW CASE-WHEN) to select the test account. On 2026-06-02 (Tue) only account 117 (settled May 29 = today-4) was eligible; account 103 (settled April 19) was NOT. Cross-link: [[payment-flows]] Email Sweep selection conditions section. |
| 89 | `FirstPaymentReminderSweep` skips the account despite `uown_sv_sched_summary.first_payment_due_date` being within the window `<= CURRENT_DATE + 3` | The sweep requires BOTH sides to be aligned: `schedSummary.first_payment_due_date <= CURRENT_DATE + 3` AND `receivable.due_date = schedSummary.first_payment_due_date` (with `receivable_type IN ('REGULAR_PAYMENT')`, `allocation_status='UNPAID'`, `status='ACTIVE'`). Updating only `sched_summary` leaves the JOIN `receivable.due_date = first_payment_due_date` at 0 rows. Fresh accounts have `first_payment_due_date = today+7` (outside the window). <!-- discovered in email-sweeps-servicing.spec.ts S3, dev3, 2026-06-02 --> | Update `uown_sv_sched_summary.first_payment_due_date` AND `uown_sv_receivable.due_date` on the first REGULAR_PAYMENT UNPAID ACTIVE to the SAME value `<= today+3` (UPDATE - Exception 3, requires authorization). Cross-link: [[payment-flows]] Email Sweep selection conditions section. |
| 90 | Consecutive manual triggers of `settledInFullAccountEmailSweep` return `processed=0` for an already eligible account | Same-day dedup done in Java logic (NOT in SQL): the sweep skips accounts that already have an email with status `STORED`/`SENT`/`PICKED_TO_STORE` on the current day. After the first successful trigger, re-triggers on the same day return 0. <!-- discovered in email-sweeps-servicing.spec.ts S1, dev3, 2026-06-02 --> | In the test, assert the PRESENCE of a row in `uown_email_queue` with `row_created_timestamp` from today (NOT `>= triggerTs`) and tolerate `processed=0`. Do not re-trigger expecting an incremental count. Cross-link: [[payment-flows]] Email Sweep selection conditions section. |
| 91 | Website (customer portal) OTP is flaky / picks up the wrong code: `getVerificationCode` returns the OTP from a previous run (or another test), causing "invalid code" rejection in the portal | Gmail IMAP `to:` search IGNORES the `+alias` suffix of plus-addressing and returns emails from ALL aliases under the same root inbox (`fintechgroup777@gmail.com`). Without a watermark, the helper picks the most recent email matching the OTP regex, which may be from another run. `envelope.date` (sender's `Date:` header) may also drift and is not reliable as a floor. <!-- discovered in unified-flow.spec.ts, commit 4f30c0d harden OTP freshness, 2026-06-02 --> | Snapshot `const uid = await email.snapshotInboxUid()` **BEFORE** triggering the OTP, then `email.getVerificationCode(recipient, timeout, { sinceUid: uid })`. UID is a monotonic server-assigned value (strict watermark). The helper also double-checks `envelope.to` for an exact alias match. Cross-link: [[helpers-catalog]] email.helpers.ts section. |
