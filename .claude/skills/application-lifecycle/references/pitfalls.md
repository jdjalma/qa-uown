# Application Lifecycle — Pitfalls Catalog

> Este arquivo e registro detalhado de pitfalls. Para regras, sequencia canonica e checklist, ver [SKILL.md](../SKILL.md).

---

## Pitfalls (causas de falhas bobas)

| # | Sintoma | Causa | Fix |
|---|---------|-------|-----|
| 1 | `Expected APPROVED but got: DENIED` sem motivo claro | Email reusado entre runs - DataMismatchStep (ADDRESS_MISMATCH) | `buildTestData` SEM `emailOverride` - email unico por run. Trocar primary email DEPOIS do drive via `createOrUpdateEmail` (passo 17-18) |
| 2 | `submitApplication` HTTP 500 "Merchant program is required to determine fee" | Sem `getMissingFields` antes | Passo 6 obrigatorio |
| 3 | `UnexpectedRollbackException` no submitApplication (HTTP 200 com erro no body) | `TEST_CARDS.VISA_APPROVED` (BIN 5146) causa rollback em qa1/qa2 | Usar `TEST_CARDS.MASTERCARD_APPROVED` (BIN 5500) |
| 4 | `Invalid merchantId. Received XYZ` HTTP 400 em sendApplication | Merchant nao provisionado no LOS endpoint (so em Servicing) | Verificar com DevOps se merchant tem `ref_merchant_code` cadastrado no LOS. Se nao, pedir provisionamento OU usar outro merchant ja cadastrado |
| 5 | Kornerstone merchant rejeita HTTP 400 no sendApplication | Sem `mainBankRoutingNumber` + `mainBankAccountNumber` | Kornerstone routing (per business rule #439) exige banking data no body. Usar overload `sendApplication(body)` + `buildSendApplicationBody`: `body.mainBankRoutingNumber = TEST_BANK.DEFAULT_ROUTING; body.mainBankAccountNumber = TEST_BANK.DEFAULT_ACCOUNT;` |
| 6 | `settleApplication` HTTP 500 | Lead nao esta em SIGNED | Chamar `changeLeadStatus(leadPk, 'SIGNED')` antes (passo 8) |
| 7 | `makeCreditCardPayments` trava 5 min e timeout | Gateway issue qa2 OU body mal formado OU conta nao em ACTIVE | Verificar account_status=ACTIVE antes. Se persistir, usar `arrangementType: 'NORMAL'` (nao SETTLEMENT) - sincrono mas menos side-effects |
| 8 | `sendInvoice` HTTP 500 `UnexpectedRollbackException` para `eligible_terms='16'` | Config qa1 incompleto para frequencias nao-MONTHLY + 16 meses | Usar `selectedPaymentFrequency='MONTHLY'` (bypass fallback) + DB-patch: `eligible_terms='16'` + `merchant_program_pk=207` |
| 9 | `uown_sv_payment` com status=PAID inexistente - sweep de email falha silenciosamente | Conta forcada para SETTLED_IN_FULL via UPDATE direto (sem passar por SETTLEMENT payment real) | SEMPRE quitar via `makeCreditCardPayments(SETTLEMENT)` - o listener popula uown_sv_payment. UPDATE direto no status NAO popula a tabela de pagamentos, e o template SQL (`settled-in-full.sql`) tem INNER JOIN exigindo esse payment |
| 10 | `sendApplication` HTTP 400 / 500 aleatorio OU fluxo falha em passo improvavel (submit, sign, fund) apos semanas funcionando | Drift na configuracao do merchant em qa2 - alguem desmarcou `isCcRequired`, `chargeProcessingFee`, `epo10` etc, ou programas 13m/16m foram removidos | `createPreQualifiedApplication` chama `ensureMerchantReady` antes do `sendApplication` (auto). Contrato em `src/data/merchant-config-contract.ts`. Auto-heal via `createOrUpdateMerchant` por default; desligar com `AUTO_HEAL_MERCHANT=false` no `.env` quando nao quiser mutar estado compartilhado. Programas 13m/16m insuficientes = fail-fast (precisa correcao manual) |
| 11 | `makeCreditCardPayments` HTTP 500 `DataIntegrityViolationException: constraint [fk_uown_cc_transaction_arrangement]` em toda nova conta (UOWN + Kornerstone) | **Bug confirmado no svc** em qa2 - `/uown/svc/makeCreditCardPayments` (plural, criacao de payment arrangement com CC) roll-backa todo o `@Transactional` de `PaymentArrangementService.runTransactions`, mesmo com card pre-tokenizado + `useCardOnFile=true`. Reproduzivel via UI em `svc-website-qa2.uownleasing.com/customer-information/{accountPk}`. DB forense mostra 0 arrangements + cc_transaction orfa de $1.01 da tokenizacao. Root cause desconhecida - provavel issue de ordering de flush Hibernate ou trigger DB; o FK e so o gatilho visivel do rollback | **Sem workaround automation** - teste deve falhar honestamente ate o svc ser corrigido. Workarounds investigados e descartados: (a) tokenizacao previa via `createOrUpdateCreditCard` + `useCardOnFile:true` NAO evita (mesmo resultado); (b) endpoint singular `/makeCreditCardPayment` funciona mas nao cria arrangement nem dispara listener de `SETTLED_IN_FULL`; (c) UPDATE direto no DB conflita com pitfall #9 (sweep depende de `uown_sv_payment(PAID)` criado pelo listener real). Reportar bug no projeto svc; reexecutar quando fix chegar. |
| 12 | `uown_merchant_activity_log` query falha ou retorna 0 rows - log de mudanca de programa nao encontrado | Tabela e snake_case `uown_merchant_activity_log` - NAO `"MerchantActivityLog"` (PascalCase como a JPA entity annotation sugere). Colunas de referencia: `merchant_pk` e `program_pk` (nao `merchantPk`/`programPk`). `log_type` para mudancas de programa = `PROGRAM_DATA_CHANGE`. <!-- descoberto na task scheduleProgramActivationDeactivationDates, 2026-04-22 --> | Usar snake_case no SQL: `SELECT * FROM uown_merchant_activity_log WHERE program_pk = $1 AND log_type = 'PROGRAM_DATA_CHANGE'` |
| 13 | `ProgramActivationDeactivationSweep` nao encontrado via `getScheduledTaskByName` | Nome correto do scheduled task e `ProgramActivationDeactivationSweep` - NAO `merchantProgramActivation...` nem `programActivationDeactivation...` sem o prefixo completo. <!-- descoberto na task scheduleProgramActivationDeactivationDates, 2026-04-22 --> | Usar `api.scheduledTask.getScheduledTaskByName('ProgramActivationDeactivationSweep')` |
| 14 | `createOrUpdateProgram` cria programa mas merchant nao o enxerga nas aplicacoes | `createOrUpdateProgram` cria/atualiza o registro em `uown_merchant_program` mas NAO faz a associacao merchant-program. Requer chamada separada a `addProgramsToMerchant` para inserir em `uown_merchant_to_program`. <!-- descoberto na task scheduleProgramActivationDeactivationDates, 2026-04-22 --> | Sempre encadear: `createOrUpdateProgram(body)` - `addProgramsToMerchant(merchantPk, [programPk])` |
| 15 | `is_active` no programa parece correto mas aplicacao usa programa "inativo" ou vice-versa | O flag `is_active` em `uown_merchant_program` e SEMPRE sobrescrito pelo backend via `ProgramActivationUtils.isActiveOnDate(activationDate, deactivationDate, today)`. Source of Truth sao as datas `activation_date` e `deactivation_date` - nao o campo booleano. UPDATE direto em `is_active` sem ajustar datas pode criar estado inconsistente. <!-- descoberto na task scheduleProgramActivationDeactivationDates, 2026-04-22 --> | Para ativar/desativar programa via testes: setar `activation_date`/`deactivation_date` via `updateMerchantProgramDates(pk, {...}, authorizedBy)` - nunca atualizar `is_active` diretamente |
| 16 | Query SQL em `activation_date`/`deactivation_date` retorna objeto Date JS em vez de string, causando comparacao falhar | Colunas `date` do PostgreSQL retornam como `Date` objects no driver `pg`. Comparacoes string diretas falham. <!-- descoberto na task scheduleProgramActivationDeactivationDates, 2026-04-22 --> | Usar cast `::text` nas queries: `SELECT activation_date::text, deactivation_date::text FROM uown_merchant_program WHERE pk = $1` |
| 17 | UI retorna data de `activation_date`/`deactivation_date` em formato ISO (`YYYY-MM-DD`) em alguns contextos e `MM/DD/YYYY` em outros | `ProgramDetailsPage` date inputs alternam entre formatos dependendo do browser locale e do tipo do campo HTML (`date` vs `text`). <!-- descoberto na task scheduleProgramActivationDeactivationDates, 2026-04-22 --> | Usar `normalizeDateInputValue(raw)` de `program-details.page.ts` para normalizar antes de comparar |
| 18 | `getEmailTemplateName` / `getMerchantChargeProcessingFee` / qualquer `db.*` falha com `password authentication failed for user "svc_user"` | Sandbox PostgreSQL em `127.0.0.1:5445` requer Cloud SQL proxy ativo. Sem o tunnel, toda chamada DB falha. Em developer machine fora do contexto CI, o proxy precisa ser tunelado manualmente. <!-- descoberto em task #489 qa-flow, 2026-04-23 --> | (a) Envolver DB calls em try/catch e emitir anotacao `[ENV-GAP]` se proxy inativo - assertions primarias de conteudo (email body) continuam; OU (b) pre-verificar conectividade DB em global setup com graceful skip. Specs que esquecem isso se manifestam como skips silenciosos em cascata em blocos serial. **[env-blocker]** |
| 19 | Assertion de template identity falha ou da falso positivo - body marker correto no repo mas resultado diverge do email real | `uown_template.template_content` em sandbox pode divergir da versao commitada no repo. Marker unico (ex: slogan `"you've got this"`) pode nao estar no conteudo DB deployado. <!-- descoberto em task #489 qa-flow, 2026-04-23 --> | Assertions de template devem cobrir: (1) body NAO contem banner text que deve ser suprimido, (2) placeholder `merchantLocationName` resolve para o nome esperado (confirma roteamento backend correto), (3) footer confirma UOWN vs Kornerstone. Verificar `uown_template.template_content` diretamente antes de escrever assertions de slogan/copy especifico. |
| 20 | `sorErrorDescription="SSN should have 9 digits. Received XXXXXXXXXX"` em `sendApplication` - SSN com 10 digitos | `generateTestSSN(false)` gerava SSNs de 10 digitos. Causa: base era `100000000 + randomInt(89999999)` (9 digitos) + sufixo `"9"` = 10 digitos total. <!-- descoberto em task #505 CT-10 denied SSN regression, 2026-05-06 --> | Fix aplicado em `src/config/constants.ts:88-93`: base mudada para `10000000 + randomInt(89999999)` (8 digitos) + `"9"` = 9 digitos. Verificar sempre que testar SSN "denied" path que o SSN gerado tem exatamente 9 digitos. |
| 21 | `getMissingFields` falha com HTTP 500 `"Invalid link. Please contact merchant"` apos `setupApplicationViaApi(..., { extractContractUrl: true })` | `setupApplicationViaApi` extrai `redirectUrl` de `sendApplication.paymentDetailsList` (linha ~402 de `src/helpers/api-setup.helpers.ts`). Se `sendInvoice` rodar em seguida (o default - so pula via `skipInvoice: true`), o backend regenera o plano de pagamento e invalida a URL anterior. O `shortCode` daquela URL torna-se stale. <!-- descoberto em Task #507 stg run, 2026-05-07 --> | Quando `sendApplication` ja inclui dados de pedido (caso comum em testes) E e necessario usar o `redirectUrl` retornado pelo helper, passar `skipInvoice: true` junto com `extractContractUrl: true`. O fluxo interno `submitPaymentInfoViaApi: true` nao sofre desse problema porque extrai o `redirectUrl` da resposta do invoice (linha ~444) - nao do `sendApplication`. |
| 22 | `POST /uown/los/authorizeCreditCard` retorna `{"message": "Invalid card. Please try again", "status": 500}` (2 keys) em vez do slim DTO `{status, error, errorCode, preAuthStatus}` (4 keys) | Quando o body e invalido (ex: `ccNumber="4111"` incompleto, `ccExp` expirado, `cvc` com comprimento errado), a requisicao e rejeitada ANTES de `ApplicationService.authorizeCreditCard()` pelo global exception handler do Spring. O `status` no envelope e o HTTP status code como numero (nao o enum `CCTransactionStatus`). <!-- descoberto em Task #507 stg run, 2026-05-07 --> | Testes que assertam o shape de `/authorizeCreditCard` DEVEM tratar DOIS formatos: (a) slim DTO 4-key quando o controller foi alcancado (aprovado, negado, last-name mismatch, BIN mismatch) - assertar com strict shape; (b) envelope Spring 2-key quando validacao rejeitou o input pre-controller - assertar presenca de `message`. Dispatch por `Object.keys(body).length === 4`. |
| 23 | `sendApplication` retorna HTTP 400 `"Invalid merchantId. Received KS3015"` OU `getMerchantsByRefCode` retorna `"Merchant not found for refCode: \"fifthavefurnitureny\""` | Merchant Kornerstone KS3015 / FifthAveFurnitureNY **nao esta provisionado em qa1** - apenas em qa2. O env var `MERCHANT_FIFTH_AVE_FURNITURE_NY_QA2_PASSWORD` no catalogo indica explicitamente que e qa2-only. <!-- descoberto em Task #1285 qa-flow smoke run qa1, 2026-05-13 --> | Para cobertura Kornerstone em qa1: (a) verificar via `getMerchantsByRefCode('fifthavefurnitureny')` ANTES de rodar (retorna vazio se nao provisionado); (b) usar outro merchant Kornerstone disponivel em qa1; OU (c) mudar `ENV=qa2` para o suite inteiro. Nao tentar force-criar o merchant - requer provisionamento DevOps. |
| 24 | `OriginationCustomerPage.modifyLease` lanca `TimeoutError: locator.waitFor: Timeout 5000ms exceeded` - invoice form nunca aparece | O botao "Modify Lease" na customer page e gate-ado pelo estado do lead. Chamado em lead em `CC_AUTH_PASSED` (logo apos Submit na Complete page), o flow nao exibe o invoice form porque o lead ainda nao esta em `SIGNED`. <!-- descoberto em Task #1285 qa-flow, 2026-05-13 --> | Sempre chamar `await driveLeadToSigned(api, merchant, ctx)` ENTRE o Submit do customer E a chamada a `modifyLease`. Referencia: `tests/e2e/origination/modify-lease.spec.ts:197` ja faz isso corretamente. |
| 25 | `MissingDataFormPage.fillBankAccount` lanca `strict mode violation: getByText('CHECKING', { exact: true }) resolved to 2 elements` | O locator `getByText(bank.accountType, { exact: true })` (arquivo `src/pages/origination/missing-data-form.page.ts:51`) matcheia tanto o valor selecionado no display do react-select quanto a opcao no menu aberto, resultando em 2 elementos. <!-- descoberto em Task #1285 qa-flow, 2026-05-13 --> | Scoped selector para a opcao: `getByRole('option', { name: bank.accountType })` ou `[id^="react-select"][id$="option-N"]`. **GAP nao corrigido em #1285** - documentado para follow-up. |
| 26 | Selector CSS-module hash (ex: `.missing-data-panel_missingDataPanel__feeAmount__cn7Wg`) retorna `null` apos rebuild do FE | CSS-module hashes sao gerados pelo webpack na build - mudam a cada rebuild (ex: qa1 R1.52.0 vs anterior). Qualquer selector do tipo `.[componentName]__[element]__[hash]` e fragil por design. <!-- descoberto em Task #1285 qa-flow qa1, 2026-05-13 --> | Sempre preferir locators semanticos: `getByText(/\$.*Authorization/)`, `getByRole(...)`, `getByLabel(...)`, `getByTestId(...)`. CSS-module hashes sao proibidos em novos seletores. `MissingDataFormPage.getProcessingFeeAmount` e `isProcessingFeeDisplayed` ja foram corrigidos com `getByText(/\$\s?[\d,]+\.?\d*\s+Authorization/i)`. |
| 27 | Ao ligar email queue / sweeps em ambiente nao-prod, vendas em massa de emails antigos sao disparadas (~19k em Dev3, 2026-05-14) | Email queue processa todos os registros em status `pending` na ativacao - sweeps nao distinguem entre emails legitimos novos e backlog acumulado de runs anteriores. `EmailQueueSweep` (e potencialmente outras sweeps com fila persistente) rodam o backlog inteiro. <!-- descoberto no incidente Dev3 spring upgrade, 2026-05-14 --> | **Antes de ligar email queue/sweeps em qualquer env nao-prod** (Dev1/2/3, qa1/qa2, stg): rodar `UPDATE uown_sv_email SET status='ERROR' WHERE status='pending'` (ou status equivalente que neutralize a fila) **com autorizacao explicita do user** - e UPDATE, viola Exception 3 do CLAUDE.md sem autorizacao. Alternativa sem mutacao: `SELECT count(*) FROM uown_sv_email WHERE status='pending'` antes de ligar pra dimensionar o blast radius e decidir se vale a pena ligar agora. Aplica-se a qualquer sweep com fila persistente (email queue e o caso mais bombastico). **[env-blocker]** |
| 28 | `sendInvoice` com `orderType:'1'` (modify invoice / LEASE_MOD path) retorna HTTP 400 `sorErrorDescription:"Cost is greater than the approved amount"` + `authApprovalStatus:"DECLINED"` + `transactionStatus:"A0"` | `orderTotal` enviado excede o `approvedAmount` do UW. **`approvedAmount` (UW decision, ex: $2170) != order total (itens do invoice, ex: $800).** Helper `createPreQualifiedApplication` retorna `result.approvedAmount` que e o UW-approved, NAO o total dos itens. Usar `approvedAmount + delta` como novo `orderTotal` excede o teto e o backend rejeita. <!-- descoberto em Task #521 LEASEMOD GowSign CT-03, 2026-05-18 --> | Sempre garantir `orderTotal < approvedAmount`. NAO usar `approvedAmount` como baseline de orderTotal - usar o order total **dos itens** (default ~$800 quando `createPreQualifiedApplication` envia Ottoman $500 + Recliner $300). |
| 29 | `sendInvoice` com `orderType:'1'` retorna 200 OK mas **nao cria LEASE_MOD** - log mostra `"Invoice decrease. Set lead to SIGNED"` + `"LeaseType : LEASE"` + `"EsignMode : EMBEDDED"`; o `ADDED : Contract[..., contractType = LEASE_MOD ...]` note NAO e gerado | `orderTotal` enviado e **menor** que o order total existente. Backend classifica como "Invoice decrease" e modifica a LEASE in-place (nao emite contract LEASE_MOD novo). LEASE_MOD so e criado quando `newOrderTotal > currentOrderTotal`. <!-- descoberto em Task #521 LEASEMOD GowSign CT-03, 2026-05-18 --> | Para forcar LEASE_MOD via API: `newTotal > currentInvoiceTotal`. Default applicant order = $800 (~Ottoman $500 + Recliner $300). Pattern: `const newTotal = 1100;` (com `initialAmount=1000 + modIncrement=100` ou similar) - > $800 e < approvedAmount. Validar via log `"Invoice increase"` na assertion. |
| 30 | `addActivityLog` / `addLogNote` retorna HTTP 400 `InvalidFormatException: Cannot deserialize value of type 'LogType' from String "GENERAL"` | `LogType` e enum estrito. Valores validos: `IMPORT, REVIEW, DATA_CHANGE, DUE_DATE_MOVES, CORRESPONDENCE, STATUS_CHANGE, CREDIT_CARD, BANK_ACCOUNT, ACH, PAYMENT, REWIND_REPLAY, DEFAULT, UNDERWRITING, INTERNAL, INFORMATION, MERCHANT_DATA_CHANGE, FRAUD, ERROR, PROGRAM_DATA_CHANGE, STATE_CONFIG_CHANGE, PAYWALLET, UWENGINE, ALLOCATION, SKIT_CALL_LOG, CUSTOMER_ASSISTANCE, ESCALATION, OUTREACH_MERCHANT, OUTREACH_PLATFORM, OUTREACH_ISR, OTHER, PEO_SUPPORT, POP_SHIPMENT, EMAIL, INBOUND_CALL, MERCHANT_REVIEW, SOCIAL_MEDIA, NEW_MERCHANT, FREQUENCY_CHANGE`. `"GENERAL"` NAO existe. <!-- descoberto em WI-525 fix CT-03b/07a/07b/10a/10b, 2026-05-20 --> | Para activity log / note de teste use `logType: 'INFORMATION'` (generico, baixo side-effect). Para logs especificos de dominio (DATA_CHANGE, PAYMENT, etc.) consultar o enum antes. |
| 31 | `LosExternalMerchantController.searchApplicationStatus` (POST /uown/los/merchant/applications/search) retorna 401 ao testar com Bearer token de `LosPartnerAuthClient` OU "Authorization header missing" sem bearer | **Source-verified:** NAO ha Spring Security nem `@PreAuthorize` no controller. Auth e via body fields `userName/setupPassword/merchantNumber` (credenciais inline), validadas em runtime por `LosRequestMessageConstraintValidator.validateApplicationStatusRequest`. Bearer token e ignorado. <!-- descoberto em WI-525 fix CT-04/CT-10c, 2026-05-20 --> | Para chamar `searchApplicationStatus` via `LosPartnerApplicationClient`: usar `buildApplicationStatusBody(merchant, leadUuid)` (ja popula userName/setupPassword/merchantNumber/accountNumber). NAO chamar `losPartnerAuth.authorize` antes - nao e necessario. Header `X-API-Version: 2` e necessario (controller class-level mapping). |
| 32 | `AspectInboundApiLog` em `uown_sv_inbound_api_log.header` armazena nomes de header em **minuscula** (`x-run-id=...`), apesar do client enviar `X-Run-Id`. Queries Postgres `header LIKE '%X-Run-Id%'` retornam 0 rows mesmo quando o row existe | Map serializado via `Map.toString()` apos `request.getHeaderNames()` - Spring/servlet container normaliza nomes para lowercase ao popular o `HashMap`. `LIKE` em Postgres e case-sensitive. <!-- descoberto em WI-525 fix CT-01/02/03a/11/12, 2026-05-20 --> | Em queries de teste sobre `*_inbound_api_log.header`, sempre usar `ILIKE` (case-insensitive). Helpers `fetchSvLogsByHeader` / `fetchLosLogsByHeader` em testes WI-525 corrigidos. |
| 33 | `AspectInboundApiLog.setApiLogInfo` extrai `source_uuid` via `StringUtils.substringBetween(body.toLowerCase(), "uuid\":", ",")`. Se `uuid` nao estiver no nivel raiz seguido de virgula, `source_uuid` fica vazio | Extracao string-based (nao JSON parsing) e fragil: depende de ORDEM e POSICAO sintatica. JSON.stringify de Node/TS gera `{"uuid":"x"}` sem virgula final - `substringBetween` retorna `null`. <!-- descoberto em WI-525 fix CT-05/CT-08/CT-09, 2026-05-20 --> | Em qualquer body POST cujo log row precise ser correlacionado por `source_uuid`: declarar `uuid` como **primeira chave** do object literal, valor string sem caracteres especiais, e garantir que existe pelo menos uma chave depois para forcar a virgula. Pattern: `const body = { uuid: correlationUuid, ...restOfBody }`. |
| 34 | Filtros `WHERE row_created_timestamp >= runStart.toISOString()` em `uown_sv_inbound_api_log` retornam 0 rows em qa1 mesmo quando as linhas existem | JVM TZ misconfigurada em qa1 - `row_created_timestamp` e gravado ~1h **atras** do real UTC. Janela calculada em runtime do teste (UTC correto) nao cobre o timestamp efetivamente persistido. <!-- descoberto em WI-525 run 2, 2026-05-20 --> | Correlacionar log rows por **marker unico** (`source_uuid`, `header ILIKE '%<runId>%'`, ou custom `_marker` field no body), NUNCA por janela de `row_created_timestamp`. **[env-blocker]** |
| 35 | Cenarios de error-path em endpoints cobertos pelo AOP `AspectInboundApiLog` produzem ora 1 ora 2 rows em `uown_*_inbound_api_log` para o mesmo `x-request-id` | Pointcut L10 (`CustomExceptionHandler.*(..)`) fira para **algumas** rotas de `@ExceptionHandler` e outras nao. `handleInvalidDateFormat` substitui o controller na cadeia AOP - **1 row**. `logExceptions` (acionado por `InvalidFieldsException` / `SvcException`) roda como **advice adicional** - **2 rows**. <!-- descoberto em WI-525 run 2, 2026-05-20 --> | Assertion de contagem DEVE especificar o caminho do handler. Pattern: `SELECT api, COUNT(*) FROM uown_los_inbound_api_log WHERE source_uuid = $1 GROUP BY api`. Nunca assertar "exatamente 1 row" universalmente para error paths. |
| 36 | Teste de open-redirect usa `framenavigated` + regex em substring e da falso-positivo | Regex em substring casa a URL inteira, inclusive query-params controlados pelo atacante na URL de entrada. <!-- descoberto em Task #1293, test-bug, 2026-05-20 --> | Parsear via `new URL(navigatedUrl).host === 'evil.com'` em vez de regex em substring. Whitelist de hosts permitidos e mais robusto. |
| 37 | `await page.goto(url)` lanca `net::ERR_HTTP_RESPONSE_CODE_FAILURE` quando servidor retorna 4xx/5xx | Comportamento default do Chromium: `goto` lanca em respostas de erro HTTP. <!-- descoberto em Task #1293, test-bug, 2026-05-20 --> | Envelopar em try/catch quando o teste deliberadamente envia payload malformado/limite. Validar status via `request.get({ maxRedirects: 0 })` se o objetivo e so checar HTTP status. |
| 38 | `StickyRecoverSweep` dispara sem erro mas NAO cria sessao `uown_sticky` para um cct morfado pelo fast-path helper | O sweep exige que o account esteja delinquente: `JOIN uown_sv_sched_summary s` + `WHERE s.delinquency_as_of_date <= CURRENT_DATE`. Helper morfava o cct mas nao tocava em `delinquency_as_of_date`. <!-- descoberto em CT-01 svc#485, 2026-05-20 --> | `createStickyEligibleFromExistingAccount` agora tambem UPDATEa `uown_sv_sched_summary.delinquency_as_of_date := CURRENT_DATE - INTERVAL '7 days'` na conta morfada quando o DAOD atual e NULL ou futuro. |
| 39 | `POST /uown/los/submitApplication` retorna HTTP 500 com body `"UnexpectedRollbackException: Transaction silently rolled back because it has been marked as rollback-only"`. Reproduzivel em todos os merchants. | Regressao silenciosa introduzida em commits recentes do svc. Algum step interno do `submitApplication` marca a transacao JPA como `rollback-only`. <!-- descoberto em svc#485 pipeline CT-01..CT-09, 2026-05-21 --> | Marcar specs dependentes com `test.fixme(true, '@blocked-by-svc-regression-submitApplication-2026-05-21')`. **[env-blocker]** **MITIGADO qa1 em 2026-05-24:** `sendApplication` retorna 200 em qa1; 89 leads FUNDED nos ultimos 14 dias confirmam resolucao. |
| 40 | `CreditCardHistoryPage.getStickyRecoveryStatus(cctPk)` retorna `-` mesmo com `uown_sticky` row populado | Race condition MobX no FE Servicing: `getCCTransactions` + `getStickyRecoveries` em `Promise.all`. `CreditCardHistoryTable` NAO e wrapped em `observer()`. <!-- descoberto em CT-11 svc#485, 2026-05-21 --> | Navegar para `/customer-information/{accountPk}` PRIMEIRO (hidrata `customerStore.accountPk`), depois clicar tab "CC Transactions" - em vez de direct URL para `/credit-card-history/`. |
| 41 | `uown_sv_sql_config.sql_name` e case-sensitive UPPERCASE - `WHERE sql_name='getSettlementAmount'` retorna 0 rows | Service do BE faz uppercase no lookup antes de executar a query. <!-- descoberto na investigacao svc#512, 2026-05-22 --> | Sempre usar UPPERCASE ao consultar `uown_sv_sql_config`. |
| 42 | `Days Past Due` (painel) != `Days Delinquent` (breakdown) | Painel usa `uown_sv_sched_summary.days_past_due` (sweep periodico). Breakdown calcula `CURRENT_DATE - delinquency_as_of_date` em runtime. <!-- descoberto em svc#512, 2026-05-22 --> | NAO usar `days_past_due` do painel para validar desconto de settlement. Apos `ageAccount(db, pk, N)`, breakdown reflete `N` mas painel mostra valor anterior. |
| 43 | Modal Settlement/EPO Breakdown abre VAZIO quando valor e $0.00 | Bug FE confirmado (BUG-3 svc#512). Contas com rating B ou CANCELLED. <!-- descoberto em svc#512, 2026-05-22 --> | Garantir account em faixa de delinquencia com valor > $0.00 ANTES de chamar `open()`. |
| 44 | TCA no painel vs TCA no breakdown divergem quando ha fees ativos (late fee, NSF) | Painel soma TCA + fees ativos. Breakdown usa TCA bruto de `uown_sv_sched_summary`. <!-- descoberto em svc#512, 2026-05-22 --> | Extrair TCA bruto via DB, NAO ler do painel UI. |
| 45 | Aging artificial via UPDATE requer try/finally restore obrigatorio | Omitir o finally deixa seed account corrompido. <!-- svc#512, 2026-05-22 --> | Usar `withAgedAccount(db, pk, days, async () => { ... })` ou bloco `try/finally`. Helper `account-aging.helpers.ts` ja encapsula. |
| 46 | Backend chama `getSettlementAmount()` 2x no `ServicingInformationService` | SQL pesado executado 2x por requisicao de pagina. <!-- code review MR svc#512, 2026-05-22 --> | Code smell identificado, NAO e bloqueador de testes. |
| 47 | Qualquer seletor que depende de CSS-Module class, accessible name por sibling text, ou `input[type="checkbox"]` para o combobox AMS Active Merchants page falha em runtime | Widget AMS Active filter combina 3 superficies de fragilidade: (a) `role="checkbox"` sem `aria-label`; (b) CSS-Module hash volatil; (c) react-select option portal mount timing. <!-- descoberto em svc#504, 6 passadas, 2026-05-22 --> | **Estrategias APROVADAS:** (1) Keyboard contract: open combobox - ArrowDown - Enter; (2) React-select option ID: `[id^="react-select-"][id$="-option-0"]`. |
| 48 | `VerifyCustomerInformationModal` backdrop intercepts clicks em `/customer-information/{pk}` | Modal auto-monta em TODA navegacao via React `useEffect`. Dismiss via Confirm define `utilityStore.isVerified=true`. <!-- descoberto em svc#512 pipeline, 2026-05-22 --> | Usar `dismissCustomerInfoConfirmation(page, timeoutMs?)` de `src/helpers/servicing-dialogs.helpers.ts`. |
| 49 | Servicing modals usam wrapper custom, NAO Bootstrap `.modal-body`/`.modal-header` | Settlement Breakdown modal usa `<div class="overflow-auto p-3">`. Close e `svg.fa-xmark-large`, nao `.btn-close`. <!-- descoberto em svc#512, 2026-05-22 --> | DOM-first via MCP (regra 15) ANTES de declarar qualquer selector de modal Servicing. |
| 50 | Seletor validado ao vivo via MCP nao garante runtime pass | Caso canonico: `[class*="customOptionStyles"]` retornou 2 em MCP, 0 em runtime 65min depois. <!-- descoberto em svc#504 S3, 2026-05-22 --> | MCP-live validation e NECESSARIO mas NAO SUFICIENTE. Rodar teste completo pelo menos 2x antes de declarar seletor estavel. |
| 51 | `/{shortCode}/complete` renderiza tela "Choose Payment Program" ANTES do CC form | Chamar `fillCreditCardInfo()` direto apos `page.goto` resulta em `TimeoutError` no `#ccFirstName`. <!-- descoberto em CT-05 Task #1291, 2026-05-22 --> | Chamar `await contract.choosePlanByName(planName)` ANTES de `fillCreditCardInfo`. |
| 52 | `moveDueDatesByDays` retorna HTTP 400 para lease WEEKLY quando `moveNumberOfDays > 3` | Regra em `MoveDueDatesService.java:117-126`: `WEEKLY - maxOffset=3`; demais - 7. <!-- descoberto em svc#536, 2026-05-22 --> | Ler `customerPaymentFrequency` do account. Se WEEKLY, usar offset <=3. Estrategia segura universal: offset=3. |
| 53 | `driveLeadToFunding` / `createPreQualifiedApplication` cria leads WEEKLY por default | Default em 3 lugares de `application.body.ts`. Nenhum assertion falha visivelmente. <!-- descoberto em svc#536, 2026-05-22 --> | Para frequencia diferente: passar override no body. Verificar com `SELECT customer_payment_frequency FROM uown_sv_sched_summary WHERE account_pk = $1`. |
| 54 | Endpoints `/uown/los/*` exigem cookie session do BFF (`merchant.sid` setado apos login) | API key isolada NAO basta - `MerchantCodeAspect` AOP retorna 401. Params de paginacao sao `@RequestParam` (query string), NAO body. Response shape e WRAPPER `{ searchResults, count, moreResults }`, NAO array flat. <!-- descoberto em svc#454, 2026-05-24 --> | **3 workarounds**: (a) host=svc bypass; (b) browser fetch from page context; (c) UI flow (default regra #14). |
| 55 | `uown_los_lead_personal_info` NAO existe em qa1 | Tabela canonica de phone e `uown_los_phone`. <!-- descoberto em svc#454, 2026-05-24 --> | Query correta: `SELECT (area_code \|\| phone_number) AS phone FROM uown_los_phone WHERE lead_pk=$1 AND phone_type='MOBILE' LIMIT 1`. |
| 56 | SVC search so retorna leads com `account_pk IS NOT NULL` | Servicing query JOIN `uown_sv_account`, leads SIGNED mas nunca funded NAO aparecem. <!-- descoberto em svc#454, 2026-05-24 --> | Fixture happy lead DEVE filtrar `WHERE lead.account_pk IS NOT NULL`. |
| 57 | Fixture de PK hard-coded e fragil em qa1 - drift por reseed | Lead Karen (pk=11319) tinha `account_pk=NULL`. <!-- descoberto em svc#454, 2026-05-24 --> | Resolver PKs dinamicamente no `beforeAll` via query. |
| 58 | MR que corrige campo em SQLs especializados deve auditar TODOS os SQLs irmaos | MR !1370 corrigiu 9/10 SQLs - `getLosSearch_FreeText.sql` ficou com alias errado. <!-- descoberto em svc#454, 2026-05-24 --> | Auditar aliases de TODAS as variantes via script. Assertion em TODOS os searchTypes. |
| 59 | Expression index com `UPPER(col)` so e utilizado se WHERE tambem aplicar `UPPER()` na coluna | `WHERE invoice_number = UPPER(:searchString)` com UPPER so no parametro - Seq Scan. <!-- descoberto em svc#454, 2026-05-24 --> | Fix: `WHERE UPPER(invoice_number) = UPPER(:searchString)`. |
| 60 | `POST /uown/los/simpleSearch/{term}` aceita body ausente com 200 OK | Spring `@RequestBody(required=false)`. <!-- descoberto em svc#454, 2026-05-24 --> | Documentar como anomalia esperada. |
| 61 | Projeto `task-testing` storageState/baseURL mismatch | Specs cobrindo TANTO Origination QUANTO Servicing falham. <!-- descoberto em svc#530, 2026-05-24 --> | Projeto splitado em `task-testing-origination` e `task-testing-servicing`. Cada spec DEVE declarar `@origination` ou `@servicing`. |
| 62 | `termnull` smell em `uown_los_lead_notes` indica lead com `main_next_pay_date = NULL` | Nota de Settle gera `termnull` - sinal do bug NPD=null do svc#530. <!-- descoberto em svc#530, 2026-05-24 --> | Diagnostico: `SELECT COUNT(*) FROM uown_los_lead_notes WHERE notes ILIKE '%termnull%'`. Zero rows = fix aplicado. |
| 63 | `sendApplication` retorna HTTP 400 `"mainNextPayDate is required"` quando campo omitido | Contract drift pos svc#530: API valida obrigatoriamente `mainNextPayDate`. <!-- descoberto em svc#530, 2026-05-24 --> | Cenario NPD=null so reproduzivel pelo caminho natural. NAO e possivel forcar via body do sendApplication pos svc#530. |
| 64 | `OriginationCustomerPage.settleLeaseViaDocuments` lanca "Unable to locate a lease document" | Filtro usa regex `/^UOWN_/` que nao casa com documentos KS3015. <!-- descoberto em svc#530, 2026-05-24 --> | **[OBSERVACAO]** - workaround: usar `settleLeaseViaButton`. Follow-up: inspecionar DOM com MCP. |
| 65 | **[SUPERSEDED por #66]** Activity-log polling de 60s insuficiente em qa1 sob carga | **SUPERSEDED:** causa raiz real e TZ drift (#66), nao timeout de backend. | Usar fix do #66. |
| 66 | DB timestamp drift silencioso - `WHERE col >= $1::timestamp` com ISO UTC vindo de JS exclui rows recem-escritas | Colunas `timestamp WITHOUT time zone` gravadas em DB-server local time (NY EDT/EST). JS envia UTC, cast strips Z. <!-- descoberto em svc#530, 2026-05-24 --> | Fix: `(col AT TIME ZONE current_setting('TimeZone')) >= $1::timestamptz - INTERVAL 'Nh'`. Alternativa: confiar em `WHERE lead_pk = $1 ORDER BY pk DESC LIMIT N`. |
| 67 | `button:has-text('Sign')` colide com `"Change to Signed"` - strict mode violation | Locator casa com multiplos elementos simultaneamente. <!-- descoberto em svc#530, 2026-05-24 --> | Fix: `getByRole('button', { name: /^E[-\s]?Sign$/i })`. |
| 68 | **[SUPERSEDED por #69]** Origination SPA `ensureAuthenticated` race | **SUPERSEDED:** ver pitfall #69 para fix v8 canonico. | |
| 69 | Origination SPA full-suite auth-retry: storageState do `auth.setup` contem JWT com TTL ~15 min em qa1; testes apos CT-10 carregam JWT EXPIRADO | Stores stale do storageState satisfazem trivialmente a guarda `*Store$`. SPA route-guard bounceia para `/` async. <!-- descoberto em svc#530, 2026-05-24 --> | **Fix canonico v8 (`customer.page.ts:248-340`):** (A) Pre-emptive JWT exp check; (B) Caller-supplied intendedPath; (C) page.reload() em vez de goto(base); (D) Guarda de hidratacao real via poll ate JWT `exp > now + 60s`. Anti-patterns proibidos: guarda por `*Store$`, `goto(base)` durante SPA nav, confianca em SPA auto-return, deteccao lazy `isLoginPage()`. |
| 70 | Consumer apply flow entry path canonico e `/getApplication/{code}`, NAO `apply-{env}/{code}/start` direto | Navegar para `apply-{env}.{host}/{merchantCode}/start` direto retorna "application link has expired". O redirector gera token short-lived. <!-- descoberto em task #1304, 2026-05-24 --> | Sempre entrar via `origination-{env}.uownleasing.com/getApplication/{code}`. |
| 71 | **[RETRATADO 2026-05-24]** ~~Dois wizards distintos servidos no mesmo path~~ | Existe UM wizard (Next.js SPA). Page object antigo tinha casing errado e campos removidos. <!-- corrigido 2026-05-24 --> | Accessibility snapshot nao basta para IDs - usar `browser_evaluate` com `document.querySelectorAll('input')`. |
| 72 | Backend valida `nextPayDate` <= 10 dias no futuro; retorna HTTP 400 estruturado | Wizard completa sem erro UI, submit final mostra "Something Went Wrong!". <!-- descoberto em task #1304, 2026-05-24 --> | Usar `nextPayDate = calculateDate(7)` (alinhado com WEEKLY). |
| 73 | Kornerstone consumer wizard pagina 2 exige bank routing + account + CC BIN (required) | UOWN trata como opcionais. Sem eles em KS, botao Next fica disabled silenciosamente. <!-- descoberto em task #1304, 2026-05-24 --> | Sempre passar valores para os 3 campos via `ApplicationWizardPage.fillEmploymentInfo`. |
| 74 | Origination SPA: `page.goto(originationBase)` com storageState valido ainda redireciona para tela de login | SPA router serve `/` como landing page de login; a guarda de rota nao navega automaticamente para o dashboard apos hidratar auth do storageState. `storageState` carregado no contexto do browser NAO e suficiente para pousar no dashboard. <!-- descoberto em task #1296, 2026-05-27 --> | Navegar para `${originationBase}/overview` (ou rota autenticada especifica) em vez de `originationBase` puro. Root `/` e SEMPRE tela de login do ponto de vista do React Router. |
| 75 | Auth tests em Origination: POST sem `merchant.sid` retorna HTTP 404, NAO 401 | Rotas `POST /uown/get*` sao registradas DENTRO do scope do session middleware Express. Requisicao sem cookie `merchant.sid` e rejeitada pelo middleware ANTES do route handler, resultando em "route not found" (404) em vez de "unauthorized" (401). <!-- descoberto em task #1296, 2026-05-27 --> | Para testar rejeicao de auth: enviar `merchant.sid` cookie COM valor invalido/expirado (obtem 401). Para verificar comportamento sem cookie nenhum: esperar 404. NAO confundir 404 com "rota inexistente". Padrao: `merchant.sid` presente + `userToken` ausente = 401. |
| 76 | SPA localStorage caching: apagar cookie de sessao NAO limpa `overviewStore`; dashboard exibe dados stale | React SPA Origination Overview le `overviewStore` do localStorage no init e renderiza cards de metricas sem fazer novas chamadas API quando detecta dados em cache. `clearCookies()` + `page.reload()` mantem os cards visiveis com dados antigos, mesmo que o backend bloqueie as APIs (404 sem cookie). <!-- descoberto em task #1296 stg, 2026-05-27 --> | Comportamento esperado (UX/client-side concern, NAO falha de seguranca — backend bloqueia corretamente). Testes de auth que validam "dados nao aparecem apos logout" DEVEM: (a) limpar localStorage explicitamente via `page.evaluate(() => localStorage.clear())` antes do reload, OU (b) abrir novo contexto de browser sem storage. Assertar ausencia de dados com cookie deletado sem limpar localStorage e assertion incorreta. |
| 77 | Refund/Reverse de pagamento no Servicing NAO esta em `/payment-transaction` | A tela `/payment-transaction` (History - Transaction) mostra resumo financeiro mas NAO tem icone de reverse por linha. O fluxo de Reverse/Refund vive em `/payment-history/{accountPk}` (History - Payments). Icone de reverse e `svg[data-icon="arrow-rotate-left"]` (NAO `.fa-undo` como no codigo antigo). Activity log do refund vai para `uown_sv_activity_log` (acao de Servicing), NAO `uown_los_lead_notes` (LOS). <!-- DOM-first validado em dev3 conta 94, svc-website-dev3, 2026-06-01 --> | Navegar para `/payment-history/{accountPk}` e usar `PaymentHistoryPage.openReverseForPaymentByAmount`. Validar log em `uown_sv_activity_log` (mesma tabela do Move Due Date - pitfall de payment-flows). Selectors em `common.selectors.ts:219-233`. |
| 78 | `reverseReason` no modal de refund e React Select (`<div>`), NAO `<select>` nativo - `selectOption()` falha | O controle `#reverseReason` no modal de reverse e um React Select renderizado como `<div>`. `page.selectOption()` so funciona em `<select>` nativo e da no-op/erro silencioso. Opcoes do dropdown (textos exatos no DOM): "Reverse", "Fully Refund", "**Partially** Refund" (NAO "Partial Refund" - o enum `ReverseReason.PARTIAL_REFUND` tem o valor de texto errado). Campo `#paymentAmount` so fica visivel quando "Partially Refund" e selecionado. <!-- DOM-first validado em dev3, 2026-06-01 --> | Selecionar via clique no controle (`reverseReasonControl`) + clique na opcao do menu (`reverseReasonOption`). Confirmar `tagName` via `browser_evaluate` antes de usar `selectOption()` em qualquer teste de reverse/refund. Usar `PaymentHistoryPage.setReverseReason`. Atencao ao texto "Partially Refund" (nao "Partial Refund"). |
| 79 | Assert de ACH Make Payment (modal `#makePayment` Servicing) tima out a 60s apesar de toast de sucesso | **ACH Make Payment via Servicing e ASSINCRONO.** Efeito sincrono imediato: `uown_sv_achpayment` com `status='PENDING'`, `amount=X`, `ach_process_type='REQUEST'` + activity log `ADDED : ACHPayment[...status=PENDING...amount=X...]` em `uown_sv_activity_log`. O `uown_sv_payment` row, o log `ADDED : Payment[paymentType=ACH...]` e o `status='PICKED_TO_SEND'` so existem POS-SWEEP (cron `CreateScheduledACHPaymentsSweep`, 19:00 diario). Esperar `uown_sv_payment` para ACH em timeout de teste = esperar estado pos-sweep que nao chega. <!-- descoberto em make-payment-servicing.spec.ts, dev3, 2026-06-01 --> | Assertir `uown_sv_achpayment WHERE status='PENDING'` + `ADDED : ACHPayment` log. NUNCA assertir `uown_sv_payment` para ACH dentro de timeout razoavel. Cross-link: [[payment-flows]] secao Make Payment. |
| 80 | Teste de overpayment no modal Make Payment falha ao assertir rejeicao | **O modal `#makePayment` do Servicing NAO valida teto de valor - overpayment e aceito INTENCIONALMENTE.** Amount > saldo restante (ou > EPO payoff) resulta em: CC SALE APPROVED no valor submetido, `uown_sv_payment` row criada, log `ADDED : Payment[...]`, e conta transiciona para `PAID_OUT_EARLY_EPO` se amount >= EPO amount. Confirmado pelo usuario (2026-06-01) como comportamento esperado - reembolso do excedente e processo de back-office separado. <!-- descoberto em make-payment-servicing.spec.ts, dev3, 2026-06-01 --> | NAO assertir rejeicao de overpayment. Testes de overpayment devem assertir o comportamento positivo (payment criado, CC APPROVED, transicao de status quando aplicavel). Cross-link: [[payment-flows]] secao Make Payment. |
| 82 | Codigo/JSDoc assume que o Arrangement Type do modal Make Payment e backend-derivado do amount (sem campo UI) | **FALSO.** O modal Make Payment → Payment Arrangement do Servicing tem um React Select EXPLICITO para Arrangement Type: `label[for="paymentArrangementType"]` com opcoes `NORMAL` / `SETTLEMENT`. O JSDoc antigo de `makeCcPaymentArrangement` (2026-03-17) afirmava "UI does NOT expose an explicit arrangementType field; backend derives it from amount" — comentario ERRADO/desatualizado. Tambem confirmado: Frequency dropdown tem `Weekly`/`BiWeekly`/`Monthly`/`SemiMonthly` (usar exact regex); `totalPaymentAmount` auto-distribui pelas parcelas; ACH arrangement = `NOT_STARTED` + parcelas `PENDING` (assincrono), CC arrangement = `SUCCESS` (sincrono). <!-- DOM-first validado em dev3, payment-arrangement-servicing pipeline, 2026-06-01 --> | Usar `SELECTORS.arrangementTypeDropdown` (label-scoped) + clique no controle + clique na opcao (React Select, `selectOption()` nao funciona). Passar `arrangementType: 'NORMAL' \| 'SETTLEMENT'` em `makeCcPaymentArrangement` / `makeAchPaymentArrangement`. Cross-link: [[payment-flows]] secao Payment Arrangement via Servicing UI. |
| 81 | Helper/funcao customizada chama `submitApplication` sem `getMissingFields` antes -> `submitApplication` falha silenciosamente com "Merchant program is required", lead nunca chega a CONTRACT_CREATED, e `settleApplication` retorna HTTP 500. **Sintoma de diagnostico:** `settleApplication` retorna 500 **E** o lead esta em `CC_AUTH_PASSED` (nunca chegou a CONTRACT_CREATED) -> causa raiz e quase sempre `submitApplication` sem `getMissingFields`. | Sem `getMissingFields`, o `merchantProgramPk` nao e resolvido e o backend rejeita o submit. Variante "silenciosa" do pitfall #2 (que cobre o caso HTTP 500 direto): aqui o submit falha sem ser assertido, mascarando a causa raiz que so reaparece no `settleApplication` downstream. `createAndDriveToFunding` em `tests/api/lease-cancellation-api.spec.ts` chamava `submitApplication` direto, sem extrair shortCode/planId nem assertir `submitResp.ok`. <!-- descoberto em ciclo 1 da validacao lease-cancellation-api, dev3, 2026-06-01 --> | Qualquer helper ou funcao customizada que chama `submitApplication` DEVE antes: (1) extrair `shortCode` + `planId` do `paymentDetailsList[0].redirectUrl` da resposta de `sendInvoice`; (2) chamar `getMissingFields(shortCode, { planId })` para resolver `merchantProgramPk`; (3) **assertir `submitResp.ok` explicitamente** (nunca swallow silencioso). Preferir `createPreQualifiedApplication` (ja encadeia a sequencia canonica). Cross-link: pitfall #2, [[common-operations]] secao submitApplication. |
| 83 | ACH arrangement preso em `payment_arrangement_status='IN_PROGRESS'` apos `SendACHPaymentsSweep` em env sem processador real (dev3) - `recalculateAchArrangementStatus` nunca chega a `SUCCESS` | Em env sem Profituity real (dev3), o ACH payment para em `status='PICKED_TO_SEND'` apos o sweep e nunca avanca para `SETTLED` (nao ha callback do processor). `recalculateAchArrangementStatus` retorna `IN_PROGRESS` porque `PICKED_TO_SEND ∈ PENDING_STATUSES` - a matriz de decisao (business-rule §54 passo 2) classifica como "transacoes pendentes existem". Os logs de finalizacao (`Arrangement finalized as SUCCESS`/`FAILED`) NAO sao emitidos por nenhum path sintetico - so pelo `PaymentArrangementACHListener` Java em callback REAL do processor. <!-- descoberto em payment-arrangement-servicing pipeline (S4/S5), dev3, 2026-06-01 --> | Para sintetizar o estado terminal em env sem processor: **UPDATE ACH payments para `status='SETTLED'` (Exception 3 - exige autorizacao explicita do user) ANTES de chamar o recalc**. Apos o UPDATE, `recalculateAchArrangementStatus` avanca para `SUCCESS`. Assertion: estado DB (status + is_active) e HARD; log de CRIACAO ("ACH Arrangement created", organico pre-sweep) e HARD; log de FINALIZACAO no path sintetico marca-se `@blocked-by-missing-log` (NAO remover - documenta divida vs rule #13) + Q ao dev para env com processor. Confirmacao cruzada: S6 (CC SETTLEMENT, path sincrono REAL) **gera** o log de finalizacao - prova que o log vem da execucao backend, nao do recalc helper. Cross-link: payment-flows pitfall #16, business-rule §54. **[env-blocker]** |
| 84 | Listener de ACH arrangement nao atualiza o arranjo em estados intermediarios - arrangement permanece `NOT_STARTED` enquanto payments transitam `PENDING → PICKED_TO_SEND → SENT` | `PaymentArrangementACHListener` so dispara atualizacao do arranjo em estados **terminais** do payment (`SETTLED` / `RETURNED` / `ACK_ERROR`). Estados intermediarios (`PENDING`, `PICKED_TO_SEND`, `SENT`) NAO promovem o arranjo - ele fica `NOT_STARTED` ate o primeiro terminal. Confirmado em dev3: arranjos 63/64/65 com payments em `SENT` e arrangement ainda `NOT_STARTED`. <!-- descoberto em payment-arrangement-servicing pipeline, dev3, 2026-06-01 --> | NAO assertir transicao de arrangement status com base em estado intermediario do payment. O arranjo so sai de `NOT_STARTED` quando ao menos um payment atinge terminal. Em env sem processor, sintetizar terminal via UPDATE autorizado (pitfall #83) para destravar. Cross-link: payment-flows secao sweep chain, business-rule §54. |
| 85 | Date pickers `#startDate` / `#endDate` do modal Make Payment Arrangement (Servicing) IGNORAM `pressSequentially` / `fill` / `type` - o React nao ve a mudanca de valor, endDate fica sempre = startDate = today → 1 parcela sempre (raiz dos bugs F-001 e F-007/S7) | Os campos sao date pickers renderizados como `<input type="search">` controlados por componente React (mesma DatePicker de `application-wizard.page.ts`). `fill`/`type`/`pressSequentially` escrevem no DOM mas NAO disparam o `onChange` que o React observa - o estado interno do componente continua com o valor anterior. <!-- descoberto em payment-arrangement-servicing pipeline (F-007/S7 resolution), dev3, 2026-06-01 --> | Setar via native `HTMLInputElement.prototype.value` setter + dispatch de `input` e `change` events (padrao ja existente em `application-wizard.page.ts` para a mesma DatePicker). Seletores corretos: `#startDate` / `#endDate`. Com o fix, `today → today+28` Weekly gera 5 parcelas corretamente (confirma que F-007/S7 nao era bug de produto - era o date picker ignorando o texto). Cross-link: [[payment-flows]] secao Payment Arrangement via Servicing UI. |
| 86 | CC multi-installment arrangement nao chega a `SUCCESS` - arranjo fica em `IN_PROGRESS` mesmo apos `simulateCcSweepForArrangement` | Com range de datas gerando N installments, somente a parcela de `posting_date = today` processa sincronamente (APPROVED). Parcelas futuras (`posting_date > today`) ficam `PENDING` → arranjo vai para `IN_PROGRESS`, nao `SUCCESS`. `simulateCcSweepForArrangement` e date-gated (`posting_date <= CURRENT_DATE`) - nao toca as futuras. **Este e o comportamento CORRETO do produto** (parcelas futuras processam quando o posting date chega), NAO bug. <!-- descoberto em payment-arrangement-servicing pipeline, dev3, 2026-06-01 --> | Em env sem processor real (dev3) o posting date futuro nunca dispara o sweep real. Para sintetizar o estado terminal no teste: usar `db.approveAllPendingCcSalesForArrangement(arrangementPk)` (sem date gate, aprova TODAS as SALEs PENDING) - stand-in autorizado para ambiente sem processador (Exception 3, mesmo padrao de S4/S5), depois `recalculateArrangementStatus`. DB-confirmado dev3: arrangement pk100 com 5 SALEs (pk3328 APPROVED + pk3329-3332 PENDING). Cross-link: [[payment-flows]] secao Payment Arrangement via Servicing UI; [[helpers-catalog]]. |
| 87 | `uown_sweep_logs.number_of_records_processed` lido logo apos trigger retorna `0` mesmo quando o sweep processa contas com sucesso | O Java sweep cria a row em `uown_sweep_logs` com `number_of_records_processed=0` ANTES de processar e so atualiza o campo para o count real DEPOIS de processar. Leituras imediatas (`< 5s` apos trigger via `waitForRecord` + leitura imediata) pegam o valor pre-processamento. <!-- descoberto em email-sweeps-servicing.spec.ts, dev3, 2026-06-02 --> | NAO assertir `processed >= 1` via leitura imediata de `uown_sweep_logs`. Usar como evidencia primaria a tabela de destino com PK monotonico: `uown_email_queue` (row nova com `template_name` esperado + `row_created_timestamp` de hoje). `uown_sweep_logs` so e confiavel apos o sweep terminar de fato. Cross-link: [[payment-flows]] secao Email Sweep validation. |
| 88 | Query de elegibilidade para `settledInFullAccountEmailSweep` encontra contas que o sweep NAO processa (ex: account 103 settled em April fora da janela) | O sweep tem CASE-WHEN na `settled_in_full_date_time` baseado no dia da semana (DOW): DOW 1/2 (Seg/Ter) → `DATE(settled_in_full_date_time) = CURRENT_DATE - 4`; DOW 3 (Qua) → `IN (CURRENT_DATE-4, -3, -2)`; DOW 4/5 (Qui/Sex) → `= CURRENT_DATE - 2`; fim de semana (`DOW NOT BETWEEN 1 AND 5`) → sweep nao roda. Query de eligibility que omite o CASE-WHEN superestima o conjunto elegivel. <!-- descoberto em email-sweeps-servicing.spec.ts S1, dev3, 2026-06-02 --> | Usar a query EXATA do sweep (incluindo o CASE-WHEN de DOW) para selecionar a conta de teste. Em 2026-06-02 (Ter) so account 117 (settled 29/Mai = hoje-4) era elegivel; account 103 (settled April 19) NAO. Cross-link: [[payment-flows]] secao Email Sweep selection conditions. |
| 89 | `FirstPaymentReminderSweep` pula a conta apesar de `uown_sv_sched_summary.first_payment_due_date` estar dentro da janela `<= CURRENT_DATE + 3` | O sweep exige AMBOS lados alinhados: `schedSummary.first_payment_due_date <= CURRENT_DATE + 3` E `receivable.due_date = schedSummary.first_payment_due_date` (com `receivable_type IN ('REGULAR_PAYMENT')`, `allocation_status='UNPAID'`, `status='ACTIVE'`). Atualizar so o `sched_summary` deixa o JOIN `receivable.due_date = first_payment_due_date` em 0 rows. Contas fresh tem `first_payment_due_date = today+7` (fora da janela). <!-- descoberto em email-sweeps-servicing.spec.ts S3, dev3, 2026-06-02 --> | Atualizar `uown_sv_sched_summary.first_payment_due_date` E a `uown_sv_receivable.due_date` do primeiro REGULAR_PAYMENT UNPAID ACTIVE para o MESMO valor `<= today+3` (UPDATE - Exception 3, exige autorizacao). Cross-link: [[payment-flows]] secao Email Sweep selection conditions. |
| 90 | Triggers manuais consecutivos do `settledInFullAccountEmailSweep` retornam `processed=0` para uma conta ja elegivel | Dedup same-day feito na logica Java (NAO no SQL): o sweep pula contas que ja tem email com status `STORED`/`SENT`/`PICKED_TO_STORE` no dia corrente. Apos o primeiro trigger bem-sucedido, re-triggers do mesmo dia retornam 0. <!-- descoberto em email-sweeps-servicing.spec.ts S1, dev3, 2026-06-02 --> | Em teste, assertir PRESENCA de row em `uown_email_queue` com `row_created_timestamp` de hoje (NAO `>= triggerTs`) e tolerar `processed=0`. Nao re-triggerar esperando count incremental. Cross-link: [[payment-flows]] secao Email Sweep selection conditions. |
| 91 | OTP do Website (customer portal) flaky / pega codigo errado: `getVerificationCode` retorna o OTP de um run anterior (ou de outro teste), causando rejeicao "invalid code" no portal | Gmail IMAP `to:` search IGNORA o sufixo `+alias` do plus-addressing e retorna emails de TODOS os aliases sob o mesmo inbox raiz (`fintechgroup777@gmail.com`). Sem watermark, o helper pega o email mais recente que casa o regex de OTP, que pode ser de outro run. `envelope.date` (header `Date:` do sender) tambem pode driftar e nao e confiavel como floor. <!-- descoberto em unified-flow.spec.ts, commit 4f30c0d harden OTP freshness, 2026-06-02 --> | Snapshot `const uid = await email.snapshotInboxUid()` **ANTES** de disparar o OTP, depois `email.getVerificationCode(recipient, timeout, { sinceUid: uid })`. UID e monotonico server-assigned (watermark estrito). O helper ainda double-checka `envelope.to` por match exato do alias. Cross-link: [[helpers-catalog]] secao email.helpers.ts. |
| 92 | Click em item da sidebar do Website (customer portal) da `TimeoutError` / pointer event interceptado logo apos um pagamento ou save | Um overlay full-screen residual (spinner/loading pos-pagamento, pos-save) ainda esta montado e intercepta os pointer events sobre o link da sidebar. O click resolve o locator mas nunca conecta. <!-- descoberto em unified-flow.spec.ts, commit 4f30c0d dismiss overlay before sidebar click, 2026-06-02 --> | `WebsiteBasePage.goToSidebarLink` agora chama `await this.waitForSpinner()` ANTES de tentar o click. Qualquer navegacao de sidebar no Website apos acao mutante (pay/save) deve aguardar o overlay sumir primeiro. NAO mascarar com `force: true`. Cross-link: [[page-object-pattern]] WebsiteBasePage. |
| 93 | Query em `uown_sticky` filtrando/lendo coluna `retry_attempt` retorna `column "retry_attempt" does not exist` | A coluna que conta as tentativas de retry na tabela `uown_sticky` (a sessao de recovery) chama-se **`number_of_attempts`** (integer, default `0`, schema doc col 9). NAO existe coluna `retry_attempt` em `uown_sticky`. O nome `retry_attempt` confunde com a TABELA irma `uown_sticky_retry_attempt` (registro per-tentativa, colunas `attempt_number` + `retry_attempt_time`) - sao coisas diferentes: a sessao agregada (`uown_sticky.number_of_attempts`) vs as linhas por tentativa (`uown_sticky_retry_attempt`). Briefing/skills antigos referenciavam `retry_attempt` por engano. <!-- descoberto em exploratory-testing-qa1-master S7, qa1, 2026-06-10 --> | Para contagem agregada de tentativas na sessao: `SELECT number_of_attempts FROM uown_sticky WHERE pk=$1`. Para detalhe por tentativa: `SELECT attempt_number, retry_attempt_time FROM uown_sticky_retry_attempt WHERE ...`. Schema autoritativo: [`docs/database-schema.md`](../../../../docs/database-schema.md) secoes `uown_sticky` (col 9) e `uown_sticky_retry_attempt`. NUNCA usar `retry_attempt` como nome de coluna em `uown_sticky`. |
| 94 | Query em `uown_scheduled_task` filtrando coluna `name` retorna 0 rows ou `column "name" does not exist`; `last_trigger_time` lido como prova de execucao Quartz da falso-negativo | As colunas reais de `uown_scheduled_task` sao: nome do task = **`scheduled_task_name`** (NAO `name`); cron = **`cron_trigger`** (NAO `cron`/`cron_expression`). Alem disso **`last_trigger_time` reflete APENAS triggers MANUAIS** (via `triggerScheduledTask`), NAO execucoes automaticas do scheduler Quartz - usar `last_trigger_time` como oracle de "o sweep rodou no horario" da falso-negativo silencioso. <!-- descoberto em exploratory-testing-qa1-master S2, qa1, 2026-06-10 --> | Filtrar por `WHERE scheduled_task_name = '{X}'`. Para confirmar execucao Quartz REAL: oracle correto e `qrtz_triggers.prev_fire_time` (ultima execucao agendada disparada) + presenca de row em `uown_sweep_logs` (sweep gravou resultado). NAO inferir execucao por `last_trigger_time`. Cross-link: pitfall #95, [[volatile-knowledge-registry]] categoria 2. |
| 95 | Query em `uown_sweep_logs` usando colunas `task_name` / `processed` / `created_timestamp` retorna `column does not exist` | As colunas reais de `uown_sweep_logs` sao: **`sweep_name`** (NAO `task_name`), **`number_of_records_processed`** (NAO `processed`), **`error`**, **`source`**, **`start_time`** / **`end_time`** (NAO `created_timestamp`). Scripts antigos assumiam nomes abreviados. <!-- descoberto em exploratory-testing-qa1-master S2, qa1, 2026-06-10 --> | Usar os nomes reais: `SELECT sweep_name, number_of_records_processed, error, source, start_time, end_time FROM uown_sweep_logs WHERE sweep_name=$1`. Lembrar que `number_of_records_processed` e escrito DEPOIS do processamento (pitfall #87) - leitura imediata retorna `0`. Cross-link: pitfall #87, #94, [[payment-flows]] secao Email Sweep validation, [[volatile-knowledge-registry]] categoria 2. |
| 96 | Spec/query de e-sign assertando `status='SENT'` em `uown_esign_document` nunca casa | O valor `SENT` NAO existe no enum de status de `uown_esign_document` (coluna real = **`status`**, varchar 255, col 37 - NAO `document_status`). Os valores reais do enum sao **`STORED` / `SENT_TO_CUSTOMER` / `COMPLETED` / `ERROR` / `CANCELLED`**. O estado "enviado ao cliente" e `SENT_TO_CUSTOMER` (NAO `SENT`); o estado terminal de sucesso e `COMPLETED` (NAO `SIGNED`). Confundir com o enum de `uown_email_queue` (que SIM usa `SENT`/`STORED`) leva a assertion que nunca passa. <!-- descoberto em exploratory-testing-qa1-master S7, qa1, 2026-06-10 --> | Em assercoes de e-sign document usar `status='SENT_TO_CUSTOMER'` para envio e `status='COMPLETED'` para conclusao. Schema autoritativo: [`docs/database-schema.md`](../../../../docs/database-schema.md) secao `uown_esign_document` (col 37 `status`). Cross-link: [[gowsign-knowledge]] secao status enum. |
| 97 | `POST /ConfigurationManagement/createOrUpdateConfig` + `forceReloadConfig` NAO altera o comportamento de chaves servidas pelo `system.config` do `application.yaml` (ex: `required.fields.for.<ClientType>` do validator, `is.preauthorization.required.for.servicing`); a app continua usando o default do codigo. **Sintoma:** config gravada no DB (`uown_configuration_management`, row presente, endpoint 200) + reload 200, mas o runtime ignora (ex: `sendApplication` sem `mainNextPayDate` para DANIELS_JEWELERS ainda retorna 400 `mainNextPayDate is required`). | Existem **DUAS familias de config desconexas** no svc. (a) **DB-backed** `common.service.ConfigurationManagementService` (`configRepo` em `uown_configuration_management`) - e o que `createOrUpdateConfig` grava; usada por ex. `TrustpilotService`, `WebhookService`. (b) **YAML/Hazelcast** `svc.config.configuration.ConfigurationManagement` → `ConfigurationUtility.getString` le de `IMap configurationMap` que e re-hidratado a CADA leitura SO de `SystemConfigurationProperties` (`system.config.*` do YAML) via `getConfigurationUtility()` → `configurationMap.setAll(...)` (`ConfigurationAnnotationProcessor.java:40-43`). NADA carrega a tabela DB para dentro do IMap; `forceReloadConfig` (`evictAll`+reinject) e re-overlayado pelo YAML na proxima leitura. O validator de application usa a familia (b) (`LosRequestMessageConstraintValidatorConfig.java:3,20-25`) → chave do DB nunca chega → cai no default do codigo (`...,mainNextPayDate`). <!-- descoberto em exploratory-testing-qa1-master S9, qa1, 2026-06-10 --> | **Determinar a familia pelo `import` no call-site.** `import com.uownleasing.common.service.ConfigurationManagementService` → DB-backed → `createOrUpdateConfig` FUNCIONA. `import com.uownleasing.svc.config.configuration.ConfigurationManagement` → YAML/Hazelcast → so via `application.yaml` (repo `configuration`, ex. commit `62e2fc20`) + **deploy/restart de pod** (acesso infra, fora do alcance QA). NAO confiar em `createOrUpdateConfig`+`forceReloadConfig` para chaves da familia (b); validar sempre com repro fresco apos aplicar. Cross-link: memoria `reference_config_management_endpoint.md` (corrigida 2026-06-10; toggle preauth marcado para re-verificar pelo mesmo motivo). |

---

## Observacoes cross-cutting - compliance candidates (require separate tickets)

| # | Observacao | Source | Status |
|---|-----------|--------|--------|
| F-OOS-1 | **`POST /uown/sendApplicationToCustomer` retorna HTTP 500 para `refMerchantCode` invalido - deveria ser 4xx.** Polui monitoring/Sentry com 500s que semanticamente sao "not found" ou "bad request". | Task #1293, 2026-05-20 | AGUARDANDO TRIAGE (Yuri). |
| F-OOS-2 | **AMS Merchants page - filtro Active: checkbox sem `aria-label`.** WCAG 1.3.1 e 4.1.2. | svc#504 F-007, 2026-05-22 | `[OBSERVACAO]` - NAO criar GitLab issue sem autorizacao. |
| OBS-01 | **Audit log persists raw POST body (PII).** `AspectInboundApiLog.setApiLogInfo:177` grava body sem redaction. Bodies incluem SSN, DOB, banking. | `AspectInboundApiLog.java:177` | PRE-EXISTING. Ticket separado de compliance. |
| OBS-02 | **Audit log persists Authorization header.** Todos os headers gravados incluindo Bearer tokens. | `AspectInboundApiLog.java:186-193` | PRE-EXISTING. Ticket separado de compliance. |
| SW-OBS-QA1-001 | **`refreshTrustPilotAccessKeySweep` grava o access token Bearer do TrustPilot em plaintext no campo `error` de `uown_sweep_logs`** (~15 dias de historico retidos). Token exposto a qualquer SELECT na tabela. Achado de SEGURANCA do PRODUTO (nao pitfall de framework de teste) - escalar ao dev manualmente. | exploratory-testing-qa1-master SW-OBS-QA1-001, qa1, 2026-06-10 | `[OBSERVACAO]` de seguranca de produto - ESCALAR AO DEV. NAO criar GitLab issue sem autorizacao. |

> **Importante**: observacoes OBS-01/OBS-02 sao herdadas, NAO introduzidas por WI-525. Tratar como tech-debt de seguranca.
