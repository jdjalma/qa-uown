# Relatório de Teste: RU04.26.1.51.0_bankAccountRatingLetterValidation_497

> **Execução reportada:** 2026-04-27, stg/TerraceFinance — run único de `ENV=stg npx playwright test docs/taskTestingUown/RU04.26.1.51.0_bankAccountRatingLetterValidation_497/RU04.26.1.51.0_bankAccountRatingLetterValidation_497.spec.ts --project=task-testing --reporter=list`. Todos os dados (accountPks, durações, resultados) referem-se exclusivamente a esta execução em stg.
>
> O relatório anterior (qa2, 2026-04-21) foi preservado em [`RU04.26.1.51.0_bankAccountRatingLetterValidation_497-report-qa2.md`](./RU04.26.1.51.0_bankAccountRatingLetterValidation_497-report-qa2.md) e os screenshots correspondentes em [`./screenshots-qa2/`](./screenshots-qa2/).

## Informações da Tarefa

| Campo | Valor |
|-------|-------|
| **Título** | UOWN \| SVC \| Account rating is not being updated correctly |
| **URL GitLab** | https://gitlab.com/uown/backend/svc/-/work_items/497 |
| **Milestone** | RU04.26.1.51.0 |
| **Labels** | bug::production, dev::backend, priority::high, workflow::ready-for-qa |
| **Pipeline** | new-flow (re-execução em stg) |

## Descrição

Em produção, as contas **566171** e **528451** tiveram pagamentos ACH criados por um agente interno (`airral.capili`) usando `achProcessType=REQUEST` com `postingDate` futuro. O fluxo `createOrUpdateAchPayment` deveria auto-setar o rating letter para `P` (Payment Arrangement) quando todas as condições fossem atendidas, porém o check `username == "customer portal"` no backend barrou a atualização para usuários internos. Como consequência, o rating letter precisou ser ajustado manualmente em produção, desligando o auto-pay.

**Escopo de QA solicitado:**
1. Adição de bank account com `Set as default payment? = Yes`
2. Adição de bank account com `Set as default payment? = No`
3. Deleção de bank account
4. Validação do rating letter em UI (Servicing Information + Activity Log), API (`getAccountByPk`) e DB (`uown_sv_account.rating`) em todos os cenários
5. Replicação do cenário exato do bug #497 (ACH `REQUEST` + posting futuro por usuário interno) via UI e via API

## Execução do Teste

| Campo | Valor |
|-------|-------|
| **Arquivo de Teste** | `docs/taskTestingUown/RU04.26.1.51.0_bankAccountRatingLetterValidation_497/RU04.26.1.51.0_bankAccountRatingLetterValidation_497.spec.ts` |
| **Ambiente** | stg |
| **Projeto Playwright** | task-testing |
| **Data de Execução** | 2026-04-27 |
| **Duração Total** | 9m36s (9.6m) |
| **Resultado** | 6 passou / 0 falhou / 0 skipped (+1 auth setup) |
| **Vídeo** | Gravado (`VIDEO=on`) |
| **Trace** | Habilitado (`TRACE=on-first-retry`) |
| **DB SVC stg** | `34.121.232.252:5432/svc` (consulta read-only para validação cross-check) |

## Evidências (Dados Criados nesta Execução)

Contas criadas em stg via `provisionFundedAccount` (merchant `TerraceFinance`, state `CA`, orderTotal `$1000`). `accountPk` e `rating`/`autoPayTypes` finais confirmados via query direta em `uown_sv_account` no DB stg após o término da execução:

| CT | leadPk | accountPk | URL Servicing | Rating final (DB) | autoPayTypes final (DB) | Duração |
|----|-------:|----------:|---------------|:------------------:|:-----------------------:|--------:|
| CT-01 | 6559165 | 589069 | `https://svc-website-stg.uownleasing.com/customer-information/589069` | `null` | `CC,ACH` | 1m00s |
| CT-02 | 6559166 | 589070 | `https://svc-website-stg.uownleasing.com/customer-information/589070` | `null` | `CC` | 1m00s |
| CT-03 | 6559167 | 589071 | `https://svc-website-stg.uownleasing.com/customer-information/589071` | `null` | `CC` | 1m00s |
| CT-04 | 6559168 | 589072 | `https://svc-website-stg.uownleasing.com/customer-information/589072` | `null` | `CC,ACH` | 2m00s |
| CT-05 | 6559169 | 589073 | `https://svc-website-stg.uownleasing.com/customer-information/589073` | `P` | `NONE` | 1m00s |
| CT-06 Parte A | 6559170 | 589074 | `https://svc-website-stg.uownleasing.com/customer-information/589074` | `P` | `NONE` | — |
| CT-06 Parte B | 6559171 | 589075 | `https://svc-website-stg.uownleasing.com/customer-information/589075` | `P` | `NONE` | — |
| **CT-06 total** | — | — | — | — | — | **3m12s** |

Dados complementares utilizados:

| Tipo | Valor | Papel no Teste |
|------|-------|----------------|
| Bank Account pré-existente | criada automaticamente por `submitApplication` | Removida pelo `provisionFundedAccount` antes de cada CT (clean slate) |
| Merchant | `TerraceFinance` | Merchant stg usado para todas as originações |
| Usuário Servicing | `automation.gow` (interno, NÃO customer portal) | Autenticado via `loginServicingAsInternalUser` — crítico para validação do fix #497 em CT-06 |
| Routing Number de teste | `121042882` (Wells Fargo público) | Todas as bank accounts criadas nos testes |
| Username CT-06 (API + UI) | `automation.gow` (interno) | Usado em ambas Parte A e Parte B para reproduzir o cenário do bug |

> **Observação cross-check DB (após execução):** os autoPayTypes finais observados no DB stg correspondem exatamente ao comportamento esperado pela business rule §58: ACH é mantido em CT-01 (default=Yes), ausente em CT-02 (default=No), removido em CT-03 (delete da única default), preservado em CT-04 (delete da BA não-default), bloqueado em CT-05 (rating P pré-existente) e suprimido em CT-06 quando o sistema bloqueou o auto-pay ao rebaixar o rating para P pós-arrangement.

## Capturas de Tela

| CT | Arquivo | Descrição |
|----|---------|-----------|
| CT-01 | `docs/taskTestingUown/RU04.26.1.51.0_bankAccountRatingLetterValidation_497/screenshots/RU04.26.1.51.0_bankAccountRatingLetterValidation_497-CT-01-default-yes-after-add.png` | Servicing Customer page após add bank account com `default=Yes` — Rating Letter inalterado, ACH indicator ativo |
| CT-02 | `docs/taskTestingUown/RU04.26.1.51.0_bankAccountRatingLetterValidation_497/screenshots/RU04.26.1.51.0_bankAccountRatingLetterValidation_497-CT-02-default-no-after-add.png` | Servicing Customer page após add bank account com `default=No` — Rating Letter inalterado, ACH indicator ausente |
| CT-03 | `docs/taskTestingUown/RU04.26.1.51.0_bankAccountRatingLetterValidation_497/screenshots/RU04.26.1.51.0_bankAccountRatingLetterValidation_497-CT-03-after-delete-default.png` | Servicing Customer page após deletar a única bank account default — ACH removido do autopay, Rating preservado |
| CT-04 | `docs/taskTestingUown/RU04.26.1.51.0_bankAccountRatingLetterValidation_497/screenshots/RU04.26.1.51.0_bankAccountRatingLetterValidation_497-CT-04-after-delete-non-default.png` | Servicing Customer page após deletar BA2 (não-default) — BA1 permanece ativa com ACH, Rating preservado |
| CT-05 | `docs/taskTestingUown/RU04.26.1.51.0_bankAccountRatingLetterValidation_497/screenshots/RU04.26.1.51.0_bankAccountRatingLetterValidation_497-CT-05-rating-P-after-add.png` | Servicing Customer page com rating `P` pré-existente após add bank com `default=Yes` — ACH NÃO habilitado, Rating permanece `P` |
| CT-06 Parte A | `docs/taskTestingUown/RU04.26.1.51.0_bankAccountRatingLetterValidation_497/screenshots/RU04.26.1.51.0_bankAccountRatingLetterValidation_497-CT-06-partA-after-ui-ach.png` | Servicing Customer page após ACH payment via UI como `automation.gow` — Rating Letter = `P - Payment Arrangement` (fix #497 confirmado) |
| CT-06 Parte B | `docs/taskTestingUown/RU04.26.1.51.0_bankAccountRatingLetterValidation_497/screenshots/RU04.26.1.51.0_bankAccountRatingLetterValidation_497-CT-06-partB-after-api-ach.png` | Servicing Customer page após `createOrUpdateAchPayments` via API — Rating Letter = `P - Payment Arrangement` (fix #497 confirmado) |

> Os 7 PNGs equivalentes da execução em qa2 (2026-04-21) foram preservados em `docs/taskTestingUown/RU04.26.1.51.0_bankAccountRatingLetterValidation_497/screenshots-qa2/`.

## Cenários

### CT-01

**Objetivo:** Validar que adicionar uma bank account com `Set as default payment? = Yes` em uma conta com rating neutro (`null`) preserva o rating letter e habilita ACH no autopay.

**O que é verificado:** O sistema aceita o cadastro da conta bancária como método padrão, adiciona `ACH` ao conjunto `autoPayTypes` da conta (conforme business rule 58) e mantém o campo `rating` em `uown_sv_account` inalterado. A UI (Servicing Information), a API (`getAccountByPk.rating`) e o DB retornam exatamente o mesmo valor de rating que existia antes da ação. O activity log registra o evento `BANK_ACCOUNT` com mensagem `ADDED : BankAccount[..., autoPay = TRUE]` e não há registro de "Rating letter changed".

Exemplos:
| Campo | Valor Observado |
|-------|-----------------|
| Rating antes | `null` |
| Rating depois (UI/API/DB) | `null` |
| Bank account `auto_pay` | `true` |
| ACH em `autoPayTypes` | SIM (DB final: `CC,ACH`) |
| accountPk | 589069 |
| Duração | 1m00s |

**PASSOU**

---

### CT-02

**Objetivo:** Validar que adicionar uma bank account com `Set as default payment? = No` não altera rating letter nem habilita ACH no autopay da conta.

**O que é verificado:** A bank account é persistida com `auto_pay = false` e **não** gera entrada em `autoPayTypes` da conta. O rating letter permanece idêntico ao estado baseline nos três pontos (UI/API/DB). O único registro de auditoria gerado é `ADDED : BankAccount[..., autoPay = FALSE]`, sem evento de "ACH enabled" nem de "Rating letter changed".

Exemplos:
| Campo | Valor Observado |
|-------|-----------------|
| Rating antes/depois | `null` |
| Bank account `auto_pay` | `false` |
| ACH em `autoPayTypes` | NÃO (DB final: `CC` apenas) |
| accountPk | 589070 |
| Duração | 1m00s |

**PASSOU**

---

### CT-03

**Objetivo:** Validar que deletar a única bank account ativa (que é a default) remove `ACH` do autopay e preserva o rating letter.

**O que é verificado:** O sistema marca o registro em `uown_sv_bank_account` com `is_deleted = true` e `auto_pay = false`, e remove `ACH` do conjunto `auto_pay_types` da conta. O rating letter não é alterado. É gerado um activity log `LogType = BANK_ACCOUNT` com mensagem de deleção e o alerta "ACH autopay turned off" é exibido na UI por ter havido autopay ativo antes da ação.

Exemplos:
| Campo | Valor Observado |
|-------|-----------------|
| Rating antes/depois | `null` |
| `is_deleted` após | `true` |
| ACH em `autoPayTypes` | Removido (DB final: `CC` apenas) |
| accountPk | 589071 |
| Duração | 1m00s |

**PASSOU**

---

### CT-04

**Objetivo:** Validar que deletar uma bank account não-default mantém a outra bank account (default) ativa no autopay e preserva o rating letter.

**O que é verificado:** Com duas bank accounts na conta (BA1 com `auto_pay=true` e BA2 com `auto_pay=false`), ao deletar BA2 apenas o registro de BA2 recebe `is_deleted=true`; BA1 permanece ativa, `auto_pay_types` continua contendo `ACH` e o rating letter não muda. Somente um log de deleção é gerado — o de BA2.

Exemplos:
| Campo | Valor Observado |
|-------|-----------------|
| Rating antes/depois | `null` |
| BA1 após deleção | `auto_pay=true`, `is_deleted=false` |
| BA2 após deleção | `auto_pay=false`, `is_deleted=true` |
| ACH em `autoPayTypes` | Mantido (DB final: `CC,ACH`) |
| accountPk | 589072 |
| Duração | 2m00s |

> Observação técnica: BA1 foi criada via API (`createOrUpdateBankAccount`) e BA2 via UI (modal Add Account). Isto porque o endpoint `createOrUpdateBankAccount` comporta-se como UPSERT quando chamado duas vezes com mesmo `(customerPk, accountPk)` — a segunda chamada sobrescreveria a primeira. O uso da UI para a segunda conta cria corretamente um novo registro.

**PASSOU**

---

### CT-05

**Objetivo:** Validar que um rating letter pré-existente `P` (Payment Arrangement) impede o sistema de habilitar ACH no autopay mesmo quando a bank account é cadastrada com `Set as default payment? = Yes`.

**O que é verificado:** A bank account é persistida (linha em `uown_sv_bank_account` com `auto_pay=true`), porém o backend aplica a precedência da business rule 58 — o rating `P` bloqueia a adição de `ACH` em `auto_pay_types`. UI/API/DB concordam: `rating` permanece `P` e `autoPayTypes` não contém `ACH`. O activity log registra o `ADDED : BankAccount` mas nenhum evento "ACH enabled".

Exemplos:
| Campo | Valor Observado |
|-------|-----------------|
| Rating antes/depois | `P` |
| Bank account `auto_pay` | `true` |
| ACH em `autoPayTypes` | NÃO (DB final: `NONE` — bloqueado pelo rating P) |
| accountPk | 589073 |
| Duração | 1m00s |

**PASSOU**

---

### CT-06

**Objetivo:** Replicar o cenário exato do bug #497 em duas camadas (UI + API) — criar ACH payment com `achProcessType=REQUEST` e `postingDate` futuro autenticado como usuário interno, e validar que o rating letter é auto-setado para `P` de forma consistente em UI, API e DB.

**O que é verificado:** O fix do bug #497 está presente em stg. Em ambas as partes (UI e API), após criar o ACH payment com `REQUEST + postingDate futuro` autenticado como `automation.gow` (usuário interno — NÃO customer portal), o sistema auto-seta `uown_sv_account.rating` para `'P'`. A UI exibe `P - Payment Arrangement` no campo Rating Letter, a API `getAccountByPk` retorna `rating = "P"` e o DB persiste `rating = 'P'`. Os três pontos de verificação são consistentes. O check anterior `username == "customer portal"` não está mais barrando a atualização para usuários internos. Adicionalmente, ao rebaixar o rating para `P`, o backend remove o ACH do conjunto `auto_pay_types` da conta (`autoPayTypes = NONE`), comportamento esperado pela business rule §58.

Exemplos:
| Parte | Usuário | `achProcessType` | `postingDate` | accountPk | Rating antes | Rating depois (UI) | Rating depois (API) | Rating depois (DB) | autoPayTypes (DB) |
|-------|---------|------------------|---------------|----------:|--------------|--------------------|---------------------|--------------------|-------------------|
| A (UI) | `automation.gow` | `REQUEST` | hoje + 7d | 589074 | `null` | `P - Payment Arrangement` | `"P"` | `'P'` | `NONE` |
| B (API) | `automation.gow` | `REQUEST` | hoje + 7d | 589075 | `null` | `P - Payment Arrangement` | `"P"` | `'P'` | `NONE` |

> **Duração total do CT-06 (Parte A + Parte B):** 3m12s.

**PASSOU**

---

## Cobertura dos Requisitos

| Requisito | Coberto | Cenário |
|-----------|:-------:|---------|
| Adição de bank account (`default=Yes`) preserva rating letter | SIM | CT-01 |
| Adição de bank account (`default=No`) preserva rating letter e não liga ACH | SIM | CT-02 |
| Deleção de bank account preserva rating letter e atualiza autopay | SIM | CT-03, CT-04 |
| Rating letter validado em UI (Servicing Information) | SIM | CT-01..CT-06 |
| Rating letter validado em API (`getAccountByPk`) | SIM | CT-01..CT-06 |
| Rating letter validado em DB (`uown_sv_account.rating`) | SIM | CT-01..CT-06 |
| Rating letter validado em Activity Log | SIM | CT-01..CT-06 |
| Rating `P/C/M` tem precedência sobre `autoPay=true` (business rule 58) | SIM (P) | CT-05, CT-06 |
| Bug #497 — ACH REQUEST + posting futuro por usuário interno auto-seta rating `P` | SIM | CT-06 Partes A e B |

## Bugs de Aplicação Encontrados

Nenhum bug de aplicação encontrado nesta execução em stg.

O comportamento descrito no ticket #497 (rating letter não atualizado para usuários internos) **não foi reproduzido** em stg — CT-06 Partes A e B demonstram que o rating letter é corretamente setado para `P` quando as condições ACH `REQUEST` + posting date futuro são atendidas, autenticado como usuário interno (`automation.gow`). A UI exibe `P - Payment Arrangement`, a API retorna `rating: "P"` e o DB persiste `rating = 'P'` de forma consistente nos accountPks 589074 (UI) e 589075 (API).

Conclusão: o fix do bug #497 está ativo em stg, confirmando o resultado já observado em qa2. O ticket pode prosseguir para `workflow::qa-approved` em ambos os ambientes.

## Comparativo qa2 ⇄ stg

| Cenário | Resultado qa2 (2026-04-21) | Resultado stg (2026-04-27) | Convergência |
|---------|----------------------------|----------------------------|:------------:|
| CT-01 | PASSOU (accountPk=11376) | PASSOU (accountPk=589069) | SIM |
| CT-02 | PASSOU (accountPk=11377) | PASSOU (accountPk=589070) | SIM |
| CT-03 | PASSOU (accountPk=11378) | PASSOU (accountPk=589071) | SIM |
| CT-04 | PASSOU (accountPk=11379) | PASSOU (accountPk=589072) | SIM |
| CT-05 | PASSOU (accountPk=11380) | PASSOU (accountPk=589073) | SIM |
| CT-06 Parte A | PASSOU (accountPk=11381, rating=P) | PASSOU (accountPk=589074, rating=P) | SIM |
| CT-06 Parte B | PASSOU (accountPk=11382, rating=P) | PASSOU (accountPk=589075, rating=P) | SIM |

> Em ambos os ambientes (qa2 e stg) o fix do bug #497 está presente. A re-execução em stg validou a paridade de comportamento e confirmou a precedência do rating `P` sobre o `autoPay=true` flag em CT-05 e CT-06, conforme business rule §58.

## Resumo da Validação

| Verificação | Resultado |
| ----------- | --------- |
| Todos os cenários da tarefa cobertos | SIM |
| Contratos de API conferem com Postman | SIM |
| Schema do BD confere com migration | SIM |
| Regras de negócio validadas | SIM |
| Bugs de aplicação encontrados | NÃO |
| Total de cenários | 6 |
| Passaram | 6 |
| Falharam | 0 |
| Skipped | 0 |
| Vídeo gravado | SIM |
| Screenshots salvos | SIM (7 arquivos em `docs/taskTestingUown/RU04.26.1.51.0_bankAccountRatingLetterValidation_497/screenshots/`; qa2 preservados em `screenshots-qa2/`) |
| Reflexos QA cobertos | Bloco 11 (Qualquer Mutation — activity log validado em todos CTs), Bloco 19 (Rating Letters — validado em UI/API/DB nos 6 CTs), Bloco 58 (Auto-Pay sync — validado em CT-01..CT-06) |
