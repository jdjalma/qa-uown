# Test Scenarios — Task #497
## `RU04.26.1.51.0_bankAccountRatingLetterValidation_497`

> **GitLab:** [work-items/497](https://gitlab.com/uown/backend/svc/-/work_items/497)
> **Título:** UOWN | SVC | Account rating is not being updated correctly
> **Tipo:** Production bug (priority: high, workflow: ready-for-qa)
> **Milestone:** RU04.26.1.51.0

---

## Contexto do bug

Em produção, as contas **566171** e **528451** foram criadas ACHPayment (`REQUEST` + posting date futuro) por um agente interno (`airral.capili`). O fluxo `createOrUpdateAchPayment` deveria auto-setar o rating letter para `P` quando todas as condições fossem atendidas, mas o check `username == "customer portal"` barrou a atualização para usuários internos. Resultado: o rating letter precisou ser ajustado manualmente, desligando o auto-pay.

**Pergunta aberta do ticket:** a validação de `username` é comportamento esperado ou deve também suportar usuários internos/manuais?

---

## Escopo de testes solicitado pelo usuário

1. **Adição de bank account** com `Set as default payment? = Yes`
2. **Adição de bank account** com `Set as default payment? = No`
3. **Deleção de bank account**

Em cada caso, validar o **Rating Letter** em:
- **UI** → Servicing Information (campo "Rating Letter")
- **Activity Log** → painel de histórico da conta
- **API** → response do endpoint `getAccountById`/`getLeadByAccountPk`
- **DB** → coluna `rating` em `uown_sv_account`

---

## User Stories

### US-497-01: Adição de bank account preserva rating letter

**As** agente interno do Servicing (ex: `automation.gow`),
**I want** adicionar uma conta bancária a uma conta de cliente existente,
**So that** o cliente tenha um método ACH cadastrado para pagamentos, sem que o rating letter seja alterado pela ação em si.

**Acceptance Criteria**
- [ ] Adicionar bank account com `Set as default payment? = Yes` → auto-pay habilita ACH **se rating permitir** (≠ C/P/M); rating letter **não muda** pelo ato de adicionar conta
- [ ] Adicionar bank account com `Set as default payment? = No` → bank account salva com `autoPay=false`; rating letter **não muda**; auto-pay ACH fica desabilitado
- [ ] Qualquer adição gera activity log com `LogType = BANK_ACCOUNT` (mensagem "ADDED : BankAccount[...]")
- [ ] Rating letter exibido no painel Servicing Information, retornado pela API e persistido no DB permanecem idênticos ao estado pré-adição

| ID | Cenário | Prioridade | Tipo |
|----|---------|:----------:|:----:|
| CT-01 | Adicionar bank account (`default=Yes`) → rating letter preservado, ACH autopay ligado | High | E2E + API + DB |
| CT-02 | Adicionar bank account (`default=No`) → rating letter preservado, ACH autopay desligado | High | E2E + API + DB |

---

### US-497-02: Deleção de bank account preserva rating letter e atualiza autopay

**As** agente interno do Servicing,
**I want** deletar uma conta bancária existente,
**So that** o método ACH seja removido quando apropriado, mantendo o rating letter consistente.

**Acceptance Criteria**
- [ ] Deletar bank account → registro marcado `isDeleted=true` e `autoPay=false`
- [ ] Se era a última conta bancária ativa → ACH removido de `autoPayTypes` da conta
- [ ] Rating letter **não muda** pela deleção
- [ ] Activity log com `LogType = BANK_ACCOUNT` (mensagem "Deleted Bank Account...")
- [ ] Alerta "ACH autopay turned off" **apenas** se havia autopay ativo

| ID | Cenário | Prioridade | Tipo |
|----|---------|:----------:|:----:|
| CT-03 | Deletar bank account que era default → ACH desligado, rating letter preservado | High | E2E + API + DB |
| CT-04 | Deletar bank account não-default → outras contas preservadas, rating letter preservado | Medium | E2E + API + DB |

---

### US-497-03: Rating letter pré-existente `P/C/M` bloqueia ACH autopay mesmo com `default=Yes`

**As** sistema de Servicing,
**I want** garantir que rating letter `P/C/M` tem precedência sobre a flag `autoPay`,
**So that** contas em promise-to-pay / bankruptcy / military nunca reativem ACH automaticamente ao adicionar bank account.

**Acceptance Criteria**
- [ ] Conta com rating `P` (ou `C`, `M`) + adicionar bank account com `default=Yes` → ACH **NÃO** é ligado no autopay
- [ ] Rating letter permanece `P`/`C`/`M` (inalterado)
- [ ] Log reflete que autopay NÃO foi habilitado (nenhum "ACH enabled" log gerado)

| ID | Cenário | Prioridade | Tipo |
|----|---------|:----------:|:----:|
| CT-05 | Rating `P` pré-existente + add bank (`default=Yes`) → ACH autopay **NÃO** liga | High | E2E + API + DB |

---

### US-497-04: Bug #497 — ACH payment arrangement por usuário interno e auto-update de rating letter

**As** QA,
**I want** replicar o cenário exato do ticket #497 (ACH `REQUEST` + posting futuro via usuário interno) primeiro pela UI, depois via API,
**So that** valide se o rating letter é auto-setado para `P` independente do username do criador, garantindo regressão zero após o fix.

**Acceptance Criteria**
- [ ] Cenário via UI: criar ACH payment arrangement como usuário interno → validar rating letter em UI, API e DB
- [ ] Cenário via API: chamar `createOrUpdateAchPayment` como usuário interno → validar rating letter em UI, API e DB
- [ ] Ambos os fluxos devem ter comportamento consistente (rating letter igual nos 3 pontos)
- [ ] Se bug persistir: documentar discrepância no relatório de execução

| ID | Cenário | Prioridade | Tipo |
|----|---------|:----------:|:----:|
| CT-06 | Bug #497 replicação — ACH payment via UI + API com rating letter validation em UI/API/DB | High | E2E + API + DB |

---

## Cenários Detalhados (CT-XX)

### CT-01: Adicionar bank account (`default=Yes`) com rating `null`/`S`

**Tipo:** E2E + API + DB
**Portal:** Servicing
**Tag:** `@regression` `@critical`

**Pré-condição:**
- Conta ativa criada via fluxo de aplicação (rating letter `null` — estado padrão pós-origination)
- Sem bank accounts ativas (para garantir `ACH` ausente de `autoPayTypes`)

**Passos:**
1. **[Setup]** Via API (application flow), criar lead → contrato → settled → derivar `accountPk`
2. **[Baseline]** Ler rating letter via:
   - **UI** — `ServicingCustomerPage.getRatingLetter()`
   - **API** — `accountClient.getAccountByPk(accountPk)` → campo `rating`
   - **DB** — `db.getAccountRating(accountPk)`
   - Todos devem retornar `null` ou `-`
3. **[Action]** Na UI Servicing → card "Bank Account" → "Add Account":
   - `Type = CHECKING`
   - `Routing Number = 121042882` (test routing)
   - `Account Number = 12345678`
   - `Set as default payment? = Yes`
   - Submit
4. **[Assert UI]** `ServicingCustomerPage.getRatingLetter()` continua `null`/`-` **[reflex]**
5. **[Assert UI — autopay]** Autopay indicator da conta mostra `ACH` ativo **[reflex]**
6. **[Assert DB]** `db.getAccountRating(accountPk)` = `null` (inalterado) **[reflex]**
7. **[Assert DB — bank]** `db.getBankAccounts(accountPk)` → 1 registro com `auto_pay=true`, `is_deleted=false`
8. **[Assert API]** `accountClient.getAccountByPk(accountPk).rating` = `null`
9. **[Assert Log]** `db.getActivityLogsByAccountPk(accountPk)` contém entry com `LogType=BANK_ACCOUNT` e mensagem "ADDED : BankAccount[..., autoPay = TRUE]" **[reflex — bloco 11 Qualquer Mutation]**
10. **[Assert Log]** NÃO há entry "Rating letter changed" após a ação (apenas logs pre-existentes)

**Resultado esperado:**
- Rating letter UI/API/DB todos consistentes em `null` pós-ação
- Bank account persistido com autopay habilitado
- ACH adicionado a `autoPayTypes` (business rule 58)

---

### CT-02: Adicionar bank account (`default=No`) com rating `null`/`S`

**Tipo:** E2E + API + DB
**Portal:** Servicing
**Tag:** `@regression` `@critical`

**Pré-condição:** mesma de CT-01

**Comportamento confirmado pelo usuário:**
- Adicionar bank account com `default=No` → **NÃO** adiciona ACH ao autopay
- **NÃO** gera log de mudança de autopay/ACH
- Bank account é salva, mas sem efeito colateral em autopay

**Passos:** iguais a CT-01, exceto no passo 3:
3. `Set as default payment? = No`

**Asserts diferentes:**
- Passo 5 (UI autopay): indicator **NÃO** mostra ACH (permanece `NONE`)
- Passo 7 (DB bank account): registro persistido com `auto_pay=false`
- Passo 8 (API `autoPayTypes`): ACH **NÃO** está presente
- Passo 9 (Log): apenas log de criação do bank account (`LogType=BANK_ACCOUNT` "ADDED : BankAccount[..., autoPay = FALSE]") — **NÃO** há log de "ACH enabled" nem mudança de autopayTypes
- Passo 10 (Log): rating letter continua inalterado (nenhum "Rating letter changed")

---

### CT-03: Deletar bank account default → ACH removido, rating preservado

**Tipo:** E2E + API + DB
**Portal:** Servicing
**Tag:** `@regression` `@critical`

**Pré-condição:**
- Conta com 1 bank account ativa (`autoPay=true`)
- Rating letter `null`
- ACH presente em `autoPayTypes`

**Passos:**
1. **[Baseline]** Capturar rating letter (UI/API/DB) e autopay types
2. **[Action]** UI → "View All" no card Bank Account → selecionar conta → botão "Delete"
3. **[Assert UI]** Rating letter inalterado **[reflex]**
4. **[Assert UI]** Autopay indicator = `NONE` (ACH removido)
5. **[Assert DB]** `db.getBankAccounts(accountPk)` → registro com `is_deleted=true`, `auto_pay=false`
6. **[Assert DB]** Rating letter inalterado
7. **[Assert API]** `accountClient.getAccountByPk(accountPk).rating` inalterado
8. **[Assert API]** `autoPayTypes` não contém mais ACH
9. **[Assert Log]** Entry com `LogType=BANK_ACCOUNT`, mensagem "Deleted Bank Account..." **[reflex]**
10. **[Assert Log]** NÃO há "Rating letter changed"

---

### CT-04: Deletar bank account **não**-default → outras contas mantidas

**Tipo:** E2E + API + DB
**Portal:** Servicing
**Tag:** `@regression`

**Pré-condição:**
- Conta com 2 bank accounts: BA1 (`autoPay=true`) e BA2 (`autoPay=false`)

**Passos:**
1. Deletar BA2
2. Validar BA1 permanece `autoPay=true`, `is_deleted=false`
3. Rating letter inalterado (UI/API/DB)
4. ACH continua em `autoPayTypes`
5. Log de deleção presente para BA2 apenas

---

### CT-05: Rating `P` pré-existente bloqueia ACH autopay ao adicionar bank account (`default=Yes`)

**Tipo:** E2E + API + DB
**Portal:** Servicing
**Tag:** `@regression` `@critical` `@businessRule`

**Pré-condição:**
- Conta ativa com rating letter = `P` (setado via payment arrangement ou UI manual como setup)
- Sem bank accounts ativas
- `autoPayTypes` vazio

**Passos:**
1. **[Baseline]** Confirmar rating = `P` em UI/API/DB
2. **[Action]** Adicionar bank account com `default=Yes`
3. **[Assert UI]** Rating letter ainda `P` **[reflex]**
4. **[Assert UI]** Autopay indicator = `NONE` (ACH **não** liga apesar de `default=Yes`)
5. **[Assert DB]** Bank account salva com `auto_pay=true` (flag do usuário respeitada na linha)
6. **[Assert DB]** `autoPayTypes` (em `uown_sv_account.auto_pay_types` ou tabela correlata) **não** contém ACH
7. **[Assert API]** Rating `P` + autoPayTypes sem ACH
8. **[Assert Log]** `LogType=BANK_ACCOUNT` "ADDED" está presente; **NÃO** há "ACH enabled"; Rating inalterado **[reflex]**

**Resultado esperado:**
- Business rule 58 — "Rating letter tem precedência sobre flags explícitas" é comprovada
- Este cenário protege contra regressões do bug do ticket #497 (onde o sistema DEVE respeitar o rating existente)

---

### CT-06: Bug do ticket #497 — ACH payment arrangement por usuário interno **não** auto-seta rating letter

**Tipo:** E2E + API + DB
**Portal:** Servicing
**Tag:** `@regression` `@critical` `@bug497`

**Contexto:**
Replicação direta do bug reportado no ticket #497. Em produção, `airral.capili` (usuário interno) criou `ACHPayment` com `achProcessType=REQUEST` e `postingDate` futuro, mas o rating letter **NÃO** foi auto-setado para `P` porque o check `username == "customer portal"` barrou a atualização. Este cenário cobre o fluxo em duas camadas: **primeiro via UI, depois via API**, com validação de persistência no DB em ambas.

**Pré-condição:**
- Conta ativa com rating letter = `null`
- Bank account cadastrada (required para criar ACH payment)
- Usuário logado = interno (ex: `automation.gow`, **NÃO** customer portal)

---

#### Parte A — Via UI (Servicing)

**Passos:**
1. **[Baseline]** Capturar rating letter em UI/API/DB → todos `null`
2. **[Action UI]** No Servicing → card de Payment Arrangement (ou botão "Make Payment" → tipo "Future ACH"):
   - `Payment Type = ACH`
   - `Process Type = REQUEST`
   - `Posting Date` = hoje + 7 dias (futuro)
   - `Amount` = valor da parcela
   - Submit
3. **[Assert UI — rating]** `ServicingCustomerPage.getRatingLetter()` **[esperado segundo regra de negócio: `P`]**
   - Se o bug ainda existir: rating permanece `null` → **documentar como bug de aplicação** no relatório, não fazer o teste falhar (assertion deve documentar estado real)
4. **[Assert API]** `accountClient.getAccountByPk(accountPk).rating` reflete o mesmo valor que a UI
5. **[Assert DB]** `db.getAccountRating(accountPk)` reflete o mesmo valor que a UI e a API
6. **[Assert Log]** `db.getActivityLogsByAccountPk(accountPk)` contém entry de criação do ACH payment; verificar presença/ausência de "Rating letter changed from null to P" **[reflex — Qualquer Mutation]**

---

#### Parte B — Via API direta

**Passos:**
7. **[Reset]** Setup nova conta idêntica (mesma pré-condição)
8. **[Action API]** `achPaymentClient.createOrUpdateAchPayment(accountPk, body)` com payload matching:
   - `achProcessType = "REQUEST"`
   - `postingDate = hoje + 7 dias`
   - `amount = parcela`
   - Header `X-User` ou auth token de usuário interno (NÃO customer portal)
9. **[Assert API]** Response 200; `getAccountByPk(accountPk).rating` — valor real observado
10. **[Assert DB]** `db.getAccountRating(accountPk)` consistente com API
11. **[Assert UI]** Abrir Servicing Customer page → `getRatingLetter()` consistente com API/DB
12. **[Assert Log]** Activity log reflete criação do ACH payment; validar presença/ausência de "Rating letter changed"

**Resultado esperado (pós-fix do ticket #497):**
- Em ambas as partes (UI e API), se todas as condições estão satisfeitas (`REQUEST` + posting futuro), rating letter **deve** ser auto-setado para `P` independente do username
- Se username check ainda for requerido por design, documentar explicitamente no relatório

**Resultado observado (hoje, pré-fix):**
- Bug: rating letter permanece `null` para usuário interno (Parte A e B) → teste documenta o gap

> **Nota para implementação:** O teste deve usar `expect.soft()` nas assertions de rating letter pós-ação, permitindo que o teste continue para capturar o estado completo (UI + API + DB + log) mesmo se a assertion falhar. Assim o relatório captura evidência completa do comportamento real.

---

## Categorias cobertas (checklist)

| Categoria | Coberto | CTs |
|-----------|:-------:|:---:|
| Happy path | ✅ | CT-01, CT-03 |
| Estado alternativo | ✅ | CT-02 (default=No) |
| Edge case | ✅ | CT-04 (não-default), CT-05 (rating P) |
| State transition | ✅ | CT-05 (precedência de rating) |
| Cross-domain | ✅ | autopay recalculation após bank CRUD |
| Auditoria | ✅ | todos (activity log validado) |

---

## Reflexos QA aplicados (consulta a `qa-domain-reflexes.md`)

| Bloco | Aplicado em |
|-------|-------------|
| 11. Qualquer Mutation (CRUD) | Todos CTs — log de auditoria + `updated_at`/`updated_by` |
| 19. Rating Letters (business rule) | CT-01, CT-02, CT-03, CT-05 |
| 58. Auto-Pay sync (business rule) | Todos CTs |

---

## Artefatos que serão criados na Fase 5

| Artefato | Path | Motivo |
|----------|------|--------|
| Page Object | `src/pages/servicing/bank-account.page.ts` | Modal Add/List/Delete bank account |
| API Client | `src/api/clients/bank-account.client.ts` | `createOrUpdateBankAccount`, `removeBankAccount`, `getBankAccounts` |
| Request bodies | `src/api/bodies/bank-account.body.ts` | Tipagem dos payloads |
| Response types | `src/api/responses/bank-account.response.ts` | Tipagem das responses |
| DB helpers | adicionar em `src/helpers/database.helpers.ts` | `getBankAccounts(accountPk)`, `waitForBankAccountDeleted` |
| Test helpers | `src/helpers/bank-account.helpers.ts` | orquestração add/delete + validações |
| Selectors | estender `src/selectors/common.selectors.ts` | modal bank account, tabela "View All", delete |
| Test file | `docs/taskTestingUown/RU04.26.1.51.0_bankAccountRatingLetterValidation_497/RU04.26.1.51.0_bankAccountRatingLetterValidation_497.spec.ts` | Implementação dos CT-01..05 |
