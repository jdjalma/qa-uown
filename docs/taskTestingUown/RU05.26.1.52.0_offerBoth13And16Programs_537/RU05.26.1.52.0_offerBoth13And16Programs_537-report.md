# Relatório de Teste: RU05.26.1.52.0_offerBoth13And16Programs_537

> ⚠️ **Este arquivo é registro de execução, NÃO fonte de padrão.**
> Pattern source = skills (`.claude/skills/`) + código (`src/`, `tests/`).
> Categorias volatile (drift-prone) → [[volatile-knowledge-registry]].
> Re-leitura permitida apenas para: (a) auditoria histórica, (b) reproduzir teste via leadPk/accountPk.
> NUNCA inferir pattern a partir deste arquivo.

## Informações da Tarefa

| Campo | Valor |
|-------|-------|
| **Título** | Offer both 13 month and 16 month programs to customer when they are eligible for 16 month |
| **URL GitLab** | https://gitlab.com/uown/backend/svc/-/work_items/537 |
| **Milestone** | Uown - RU05.26.1.52.0 |
| **Labels** | workflow::ready-for-qa |
| **Pipeline** | E2E UI-first hybrid (regra #14) |

## Descrição

Quando o BlackBox retorna `EligibleTerms="16"` (cliente elegível para 16m, sem lookahead), o produto deve oferecer AMBOS os programas 13m e 16m no fluxo do cliente, em vez de apenas 16m. O fix em `LeadProgramService.getLTOProgramsForLead` faz lookup do novo config `defaultMonthsWhen16Month` (default `"13,16"`) e expande os terms quando `EligibleTerms` contém "16" E `IsEligibleForExtraInfo != true` (guard impede expand no caso "lookahead").

Fix commits svc (master): `76eabd5cc` (2026-05-20, expand path), `2373824ad` (2026-05-20, refinement).

## Execução do Teste

| Campo | Valor |
|-------|-------|
| **Arquivo de Teste** | `tests/e2e/origination/RU05.26.1.52.0_offerBoth13And16Programs_537.spec.ts` |
| **Ambiente** | qa1 |
| **Projeto Playwright** | task-testing-origination |
| **Data de Execução** | 2026-05-24 |
| **Duração** | 2.1 minutos |
| **Resultado** | 3 passaram / 0 falharam / 0 skipped |
| **Vídeo** | N/A |
| **Trace** | Habilitado (`TRACE=on-first-retry`) |

## Evidências (Dados Utilizados/Criados)

| Tipo | PK | Papel no Teste | Criado/Existente |
|------|----|----------------|:----------------:|
| Lead | leadPk=12028 | 16m-eligible (A1+A2): KS3015/CA, income 70000, approved_terms="13,16" | Criado |
| Lead | leadPk=12029 | 13m-only (A3): KS3015/CA, income 25000, sem bankData, approved_terms="13" | Criado |
| Lead | leadPk=12030 | 16m-eligible (A5): KS3015/CA, income 70000, getMissingFields(planId=WK13)=200 | Criado |

## Capturas de Tela

> Sem capturas de tela salvas nesta execução (assertions via locators + DB read-only).

## Cenários

### CT-A1+A2

**Objetivo:** Verificar que cliente 16m-elegivel visualiza AMBAS as tabs 13m e 16m no componente MissingPaymentProgram e que o DB registra approved_terms="13,16".

**O que é verificado:** Ao navegar para a URL de redirect (sem planId), o componente MissingPaymentProgram renderiza duas tabs correspondentes a "13 Months" e "16 Months". A tab 16m exibe 3 cards de frequência (WEEKLY, BI_WEEKLY, MONTHLY conforme PK 215). A tab 13m exibe 2 cards (WEEKLY, BI_WEEKLY conforme PK 93). O DB confirma `uown_lead_approval_terms.approved_terms="13,16"` e `uw_terms="16"`. Activity log do `[LeadProgramService][getLTOProgramsForLead]` com texto `After defaulting to 13,16 terms are : 13,16` presente em `uown_los_lead_notes`.

| Dado | Valor |
|------|-------|
| leadPk | 12028 |
| Merchant | KS3015 (FifthAveFurnitureNY, Kornerstone) |
| State | CA |
| Income | 70000 |
| approved_terms | 13,16 |
| uw_terms | 16 |
| Log source | lead_notes |

#### Como verificar manualmente

1. Criar aplicação via API para KS3015/CA com `mainAnnualIncome: 70000` e `bankData` (routing Kornerstone).
2. Chamar `sendInvoice` para obter `paymentDetailsList[].redirectUrl`.
3. Remover o parâmetro `planId` da URL de redirect.
4. Navegar para a URL resultante no browser (ex: `https://apply-qa1.uownleasing.com/{shortCode}/complete`).
5. Verificar visualmente: duas tabs "13 Months" e "16 Months" presentes.
6. Clicar "16 Months": 3 cards de frequência (Weekly, Bi-Weekly, Monthly).
7. Clicar "13 Months": 2 cards de frequência (Weekly, Bi-Weekly).
8. Query DB:
   ```sql
   SELECT uw_terms, approved_terms FROM uown_lead_approval_terms
   WHERE lead_pk = 12028 ORDER BY row_created_timestamp DESC LIMIT 1;
   -- Esperado: uw_terms="16", approved_terms="13,16"
   ```
9. Query log:
   ```sql
   SELECT pk, notes FROM uown_los_lead_notes
   WHERE lead_pk = 12028 AND notes ILIKE '%After defaulting to 13,16%'
   ORDER BY pk DESC LIMIT 5;
   -- Esperado: ao menos 1 row
   ```

**PASSOU**

---

### CT-A3

**Objetivo:** Verificar que cliente com EligibleTerms="13" (regressão) visualiza APENAS tab 13m, sem opção 16m.

**O que é verificado:** Quando o BlackBox retorna EligibleTerms="13" (income baixo, sem bankData para forçar routing UOWN), o componente MissingPaymentProgram renderiza apenas a tab 13m (ou exibe cards diretamente sem tabs). A tab 16m NÃO está presente no DOM. O DB confirma `approved_terms="13"` e `uw_terms="13"`. Activity log do `[LeadProgramService][getLTOProgramsForLead]` com texto `Found program(s) eligible for term 13` presente em `uown_los_lead_notes`.

| Dado | Valor |
|------|-------|
| leadPk | 12029 |
| Merchant | KS3015 (FifthAveFurnitureNY, Kornerstone) |
| State | CA |
| Income | 25000 |
| bankData | ausente (routing UOWN) |
| approved_terms | 13 |
| uw_terms | 13 |
| Log source | lead_notes |

#### Como verificar manualmente

1. Criar aplicação via API para KS3015/CA com `mainAnnualIncome: 25000` e SEM `bankData` (routing UOWN, EligibleTerms="13").
2. Chamar `sendInvoice` para obter redirect URL.
3. Remover `planId` da URL e navegar no browser.
4. Verificar: apenas tab "13 Months" visível (ou cards diretos sem tab). Nenhum elemento "16 Months" no DOM.
5. Query DB:
   ```sql
   SELECT uw_terms, approved_terms FROM uown_lead_approval_terms
   WHERE lead_pk = 12029 ORDER BY row_created_timestamp DESC LIMIT 1;
   -- Esperado: uw_terms="13", approved_terms="13"
   ```
6. Query log:
   ```sql
   SELECT pk, notes FROM uown_los_lead_notes
   WHERE lead_pk = 12029 AND notes ILIKE '%[LeadProgramService][getLTOProgramsForLead]%'
   ORDER BY pk DESC LIMIT 5;
   -- Esperado: ao menos 1 row
   ```

**PASSOU**

---

### CT-A5

**Objetivo:** Verificar que cliente 16m-elegivel que seleciona planId 13m consegue completar getMissingFields sem erro 500 (AC5 - cenário âncora do pipeline).

**O que é verificado:** Quando um lead 16m-elegivel (approved_terms="13,16") escolhe o programa 13m via planId (ex: WK13), a chamada `getMissingFields(shortCode, { planId: WK13 })` retorna HTTP 200. Isto prova que `MissingRequiredFieldsService.resolveAndSetMerchantProgramFromPlanId` resolve corretamente o `merchant_program_pk` para 13m em uma lead cujo BlackBox retornou "16". O DB confirma `merchant_program_pk` vinculado a programa 13m (term_months=13). Activity log presente em `uown_los_lead_notes`.

| Dado | Valor |
|------|-------|
| leadPk | 12030 |
| Merchant | KS3015 (FifthAveFurnitureNY, Kornerstone) |
| State | CA |
| Income | 70000 |
| planId testado | WK13 (13m, derivado de paymentDetailsList) |
| getMissingFields status | 200 |
| approved_terms | 13,16 |
| Log source | lead_notes |

#### Como verificar manualmente

1. Criar aplicação via API para KS3015/CA com `mainAnnualIncome: 70000` e `bankData`.
2. Chamar `sendInvoice` para obter `paymentDetailsList`.
3. Identificar entry com `termInMonths=13` e extrair `planId` (ex: WK13).
4. Extrair `shortCode` do path da redirect URL.
5. Chamar: `GET /uown/applications/{shortCode}/missingFields?planId=WK13`
6. Verificar: resposta HTTP 200 (NÃO 500).
7. Navegar para URL sem planId, clicar tab "13 Months", confirmar visualmente.
8. Query DB:
   ```sql
   SELECT merchant_program_pk, lead_status FROM uown_los_lead WHERE pk = 12030;
   -- Esperado: merchant_program_pk referencia programa com term_months=13
   
   SELECT term_months, program_name FROM uown_merchant_program
   WHERE pk = (SELECT merchant_program_pk FROM uown_los_lead WHERE pk = 12030);
   -- Esperado: term_months=13
   
   SELECT approved_terms FROM uown_lead_approval_terms
   WHERE lead_pk = 12030 ORDER BY row_created_timestamp DESC LIMIT 1;
   -- Esperado: "13,16"
   ```
9. Query log:
   ```sql
   SELECT pk, notes FROM uown_los_lead_notes
   WHERE lead_pk = 12030 AND notes ILIKE '%After defaulting to 13,16 terms are : 13,16%'
   ORDER BY pk DESC LIMIT 5;
   -- Esperado: ao menos 1 row
   ```

**PASSOU**

---

## Cobertura dos Requisitos

| Requisito | Coberto | Cenário | Nota |
|-----------|:-------:|---------|------|
| AC1: UI MissingPaymentProgram mostra AMBAS tabs 13m e 16m | SIM | CT-A1+A2 | [CONFIRMADO] [test-execution:leadPk=12028] |
| AC2: DB approved_terms="13,16" para lead 16m-elegivel | SIM | CT-A1+A2 | [CONFIRMADO] [db-observation:uown_lead_approval_terms WHERE lead_pk=12028] |
| AC3: EligibleTerms="13" mostra apenas 13m (regressão) | SIM | CT-A3 | [CONFIRMADO] [test-execution:leadPk=12029] |
| AC4: Lookahead guard (IsEligibleForExtraInfo=true) | NÃO | - | REMOVIDO do escopo: dead code guard (setter removido do codebase). Zero leads com flag=true em qa1/dev1. |
| AC5: planId 13m em lead 16m-elegivel sem 500 | SIM | CT-A5 | [CONFIRMADO] [api-response:getMissingFields,200] [test-execution:leadPk=12030] |
| AC6: Cross-brand (UOWN + KS) | NÃO | - | SKIPPED: nenhum merchant UOWN (não-KS) com programas 13m+16m configurados em qa1. |
| AC7: merchant_program_pk pre-set curto-circuito | NÃO | - | Cortado do escopo (risco baixo, caminho de codigo existente). |
| AC8: Config rollback via uown_configuration_management | NÃO | - | DEFERRED: requer Exception 3 (INSERT/DELETE) sem autorização concedida. |

## Activity Log Compliance (Regra #13)

| CT | Log esperado | Encontrado | Fonte |
|----|-------------|:----------:|-------|
| A1+A2 | `[LeadProgramService][getLTOProgramsForLead]` + `After defaulting to 13,16 terms are : 13,16` | SIM | uown_los_lead_notes |
| A3 | `[LeadProgramService][getLTOProgramsForLead]` + `Found program(s) eligible for term 13` | SIM | uown_los_lead_notes |
| A5 | `[LeadProgramService][getLTOProgramsForLead]` + `After defaulting to 13,16 terms are : 13,16` | SIM | uown_los_lead_notes |

Todos os 3 CTs passaram na validação de activity log via cascata de fontes (fonte primária: `uown_los_lead_notes.notes`). Regra #13 compliance confirmada.

## Resumo da Validação

| Verificação | Resultado |
|-------------|-----------|
| Todos os cenários da tarefa cobertos | PARCIAL (5/8 ACs: AC1-AC3-AC5 CONFIRMADOS; AC4 removido; AC6 SKIPPED; AC7 cortado; AC8 DEFERRED) |
| Contratos de API conferem | SIM (getMissingFields 200 para planId 13m em lead 16m) |
| Schema do BD confere | SIM (uown_lead_approval_terms.approved_terms, uown_los_lead.merchant_program_pk) |
| Regras de negócio validadas | SIM (expand path, regression guard, planId resolution) |
| Activity log validado (regra #13) | SIM (3/3 CTs com log em uown_los_lead_notes) |
| Bugs de aplicação encontrados | NÃO |
| Total de cenários | 3 |
| Passaram | 3 |
| Falharam | 0 |
| Skipped | 0 |
| Vídeo gravado | N/A |
| Screenshots salvos | N/A |

## Cenários não executados (justificativa)

| CT Original | Status | Justificativa |
|-------------|--------|---------------|
| A4 (Lookahead) | REMOVIDO | Dead code guard: setter `setEligibleForExtraInfo` removido do `UnderwritingService`. Zero leads com flag=true em qa1/dev1. Guard é defensivo sem caminho de ativação. |
| A6 (Cross-brand UOWN) | SKIPPED | Nenhum merchant UOWN (não-KS) em qa1 possui programas 13m E 16m ativos simultaneamente. Drift de TerraceFinance confirmado. |
| A7 (Config rollback) | DEFERRED | Requer INSERT em `uown_configuration_management` (Exception 3). Autorização não concedida neste ciclo. |

## Handoff

- **Destino:** qa-doc-keeper
- **Ações pendentes:**
  - Atualizar `docs/business-rules/02-originacao-pipeline.md:472-477` (qa1 agora tem configs 16m)
  - Atualizar `docs/business-rules/appendix-c-tabelas-banco.md:165` (approved_terms não é intersecção)
  - Marcar memória `reference_qa1_16m_eligibility_blocked` como parcialmente stale
  - Criar memória "Fix #537 LIVE qa1 desde 2026-05-20"
