# SPEC — Task #74
## UOWN | AMS | New page to assign merchants to user

**Milestone:** RU04.26.1.51.0  
**Test Name:** `RU04.26.1.51.0_uownAmsNewPageToAssignMerchantsToUser_74`  
**Portal:** AMS  
**Project:** `task-testing`  
**Path:** `tests/taskTestingUown/RU04.26.1.51.0_uownAmsNewPageToAssignMerchantsToUser_74/`  
**Environment:** `qa2`

---

## Artefatos Necessários (criar antes dos testes)

| Artefato | Caminho | Notas |
|----------|---------|-------|
| Seletores AMS | `src/selectors/common.selectors.ts` | Adicionar grupo `ams*` |
| `AmsUserDetailsPage` | `src/pages/ams/ams-user-details.page.ts` | Estende `AmsBasePage` |
| `AmsUserMerchantsPage` | `src/pages/ams/ams-user-merchants.page.ts` | Estende `AmsBasePage` |
| `AmsApiClient` | `src/api/clients/ams.client.ts` | Estende `BaseClient`, host `ams` |

---

## Dados de Teste (qa2)

- **Usuário de teste padrão:** buscar via `GET /user?search=test` ou usar usuário existente com dados conhecidos (não `"*"`)
- **Usuário wildcard:** usuário com `merchantCodes="*"` (confirmar via `GET /user/{username}`)
- **Merchant refCodes para associação:** usar refCodes reais de qa2 (ex: buscar via AMS API ou DB `SELECT ref_code FROM uown_merchant LIMIT 5`)
- **URL AMS:** `testEnv.amsUrl` — resolvido via `ConfigEnvironment`
- **Credenciais:** role `'admin'`

---

## CT-E2E-01: Navegação para página de detalhes + verificação de campos

**Tipo:** E2E  
**Ambiente:** qa2  
**Tags:** `@regression @ams @smoke`

### Pré-condições
- Usuário logado no AMS (via `loginToPortalWithOptions(page, testEnv.amsUrl, testEnv, 'admin')`)
- Existe ao menos 1 usuário na listagem

### Passos
1. Navegar para `testEnv.amsUrl` (já redireciona para `/users` ou similar)
2. Aguardar tabela de usuários carregar
3. Capturar username do primeiro usuário da tabela
4. Clicar na linha do usuário
5. Aguardar navegação

### Validações
- URL atual contém `/users/[username]` (ex: `page.url().includes('/users/')`)
- Pelo menos um dos campos está visível: username, emailAddress, phoneNumber, firstName, lastName
- Sem erros de JS no console

### Dados
- Qualquer usuário existente na listagem de qa2

---

## CT-E2E-02: Editar dados do usuário na página de detalhes

**Tipo:** E2E  
**Ambiente:** qa2  
**Tags:** `@regression @ams`

### Pré-condições
- Usuário logado no AMS
- Na página `/users/[username]` de um usuário de teste (não produção)
- Usuário de teste identificado pelo `beforeAll` (ex: primeiro usuário cujo username contém "test" ou "qa")

### Passos
1. Navegar para `/users/[testUsername]` via `page.goto(testEnv.amsUrl + '/users/' + testUsername)`
2. Aguardar campos visíveis
3. Clicar em Edit / modo de edição (se houver botão)
4. Localizar campo `phoneNumber` ou `firstName`
5. Limpar e preencher novo valor (ex: `phoneNumber = '5551234567'`)
6. Clicar em Save
7. Aguardar spinner / toast de sucesso
8. Recarregar página (`page.reload()`)
9. Ler valor atual do campo

### Validações
- Toast/feedback de sucesso visível após save
- Após reload: campo exibe o valor atualizado
- `PUT /user/{username}` chamado com status 200 (verificar via network ou feedback visual)

### Dados
- `testUsername`: usuário de teste em qa2 (definido no `beforeAll`)
- Valor de teste para phoneNumber: `'5551234567'` (ou timestamp-based para unicidade)

---

## CT-E2E-03: Navegar para a página de associação de merchants

**Tipo:** E2E  
**Ambiente:** qa2  
**Tags:** `@regression @ams @smoke`

### Pré-condições
- Usuário logado no AMS
- Na página de detalhes `/users/[username]`

### Passos
1. Navegar para `/users/[testUsername]`
2. Localizar botão/link de associação de merchants (ex: "Assign Merchants", "Merchants", ícone de merchant)
3. Clicar no botão/link
4. Aguardar página de associação carregar

### Validações
- URL muda (ex: `/users/[username]/merchants` ou similar)
- Duas tabelas estão visíveis na página
- Cada tabela tem ao menos um header/coluna identificável

---

## CT-E2E-04: Associar novos merchants — happy path (duas tabelas + aditivo)

**Tipo:** E2E Hybrid  
**Ambiente:** qa2  
**Tags:** `@regression @ams @critical`

### Pré-condições
- Usuário logado no AMS
- Usuário de teste (`testUsername`) com `merchantCodes` sem `"*"` e com ≥1 merchant já associado
- Existe ao menos 1 merchant disponível na tabela de merchants não associados

### Setup (beforeAll / beforeEach)
```
Via GET /user/{testUsername}:
  - Capturar merchantCodes atuais → salvar em variável `originalMerchantCodes`
```

### Passos
1. Navegar para a página de associação de merchants de `testUsername`
2. Na tabela de merchants **disponíveis** (não associados), selecionar o checkbox/row de 1 merchant
3. Verificar que a tabela de merchants **selecionados** reflete a seleção (merchant aparece lá)
4. Clicar em Submit / Confirm / Save
5. Aguardar feedback de sucesso
6. Via `GET /user/{testUsername}`, ler `merchantCodes` atualizado

### Validações
- Feedback de sucesso visível após submit
- `merchantCodes` contém o novo merchant code
- `merchantCodes` ainda contém todos os codes de `originalMerchantCodes` (nenhum removido)
- Split por `,` do `merchantCodes` → length >= length anterior + 1

### Teardown
- Restaurar `merchantCodes` ao valor original via `PUT /user/{testUsername}` (se necessário para idempotência)

---

## CT-E2E-05: Selecionar merchant da página 2 e associar

**Tipo:** E2E Hybrid  
**Ambiente:** qa2  
**Tags:** `@regression @ams`

### Pré-condições
- Usuário logado no AMS
- Página de associação com ≥2 páginas de merchants disponíveis

### Passos
1. Navegar para a página de associação de merchants de `testUsername`
2. Verificar que a paginação está visível na tabela de disponíveis
3. Clicar no botão de próxima página / página 2
4. Verificar que a tabela atualizou (merchants diferentes da página 1)
5. Selecionar o primeiro merchant disponível na página 2
6. Confirmar a associação
7. Via `GET /user/{testUsername}`, verificar `merchantCodes`

### Validações
- Paginação funciona: merchants da página 2 são diferentes dos da página 1
- Merchant selecionado na página 2 aparece no `merchantCodes` após confirmação
- Seleção não foi perdida ao navegar entre páginas

---

## CT-E2E-06: Merchant já associado — sem duplicata

**Tipo:** E2E  
**Ambiente:** qa2  
**Tags:** `@regression @ams @edge-case`

### Pré-condições
- Usuário de teste com ≥1 merchant já associado (`merchantCodes` contém pelo menos 1 code real)

### Passos
1. Via `GET /user/{testUsername}`, capturar `merchantCodes` e isolar 1 code existente (`existingCode`)
2. Navegar para a página de associação de merchants
3. Na tabela, localizar e selecionar `existingCode` (se for exibido — pode aparecer já marcado)
4. Confirmar a associação
5. Via `GET /user/{testUsername}`, verificar `merchantCodes`

### Validações
- `merchantCodes` não contém `existingCode` duplicado (split por `,` → `Set` de tamanho não duplicado)
- Backend usa `HashSet` internamente — garantia pela lógica em `UserService.java:380`

---

## CT-API-01: POST /user/addMerchantsToUsers — happy path

**Tipo:** API  
**Ambiente:** qa2  
**Tags:** `@regression @api`

### Pré-condições
- Usuário de teste com `merchantCodes` sem `"*"`
- `merchantRefCode` de teste válido em qa2

### Passos
1. `GET /user/{testUsername}` → capturar `merchantCodes` atual (`originalCodes`)
2. `POST /user/addMerchantsToUsers` body: `{ "merchantRefCodes": "{newRefCode}", "usernames": ["{testUsername}"] }`
3. `GET /user/{testUsername}` → verificar `merchantCodes` atualizado

### Validações
- POST retorna `200`
- `merchantCodes` contém `newRefCode`
- `merchantCodes` ainda contém todos de `originalCodes`

### Teardown
- Restaurar via `PUT /user/{testUsername}` com `merchantCodes` original

---

## CT-API-02: POST /user/addMerchantsToUsers — usuário com "*"

**Tipo:** API  
**Ambiente:** qa2  
**Tags:** `@regression @api @edge-case`

### Pré-condições
- Usuário de teste `wildcardUsername` com `merchantCodes = "*"`

### Passos
1. `GET /user/{wildcardUsername}` → confirmar `merchantCodes === "*"`
2. `POST /user/addMerchantsToUsers` body: `{ "merchantRefCodes": "SOME_CODE", "usernames": ["{wildcardUsername}"] }`
3. `GET /user/{wildcardUsername}` → verificar estado final

### Validações
- POST retorna `200`
- `merchantCodes` permanece `"*"` (skip logic em `UserService.java:376`)
- Nenhuma alteração no usuário

---

## Artefatos de Implementação — Detalhes

### AmsApiClient (`src/api/clients/ams.client.ts`)
```typescript
// Host: 'ams' (baseURL = testEnv.amsUrl)
getUser(username: string): Promise<ApiResponse<AmsUser>>
updateUser(username: string, patch: Partial<AmsUser>): Promise<ApiResponse<AmsUser>>
addMerchantsToUsers(merchantRefCodes: string, usernames: string[]): Promise<ApiResponse<void>>
getUsers(params?: { search?: string; page?: number; size?: number }): Promise<ApiResponse<Page<AmsUser>>>
```

### AmsUser response interface
```typescript
interface AmsUser {
  pk: number;
  userName: string;
  emailAddress: string;
  firstName?: string;
  lastName?: string;
  phoneNumber?: string;
  merchantCodes?: string;
  status: string;
}
```

### Seletores necessários (common.selectors.ts)
```typescript
// AMS User Details Page
amsUserDetailsContainer: '[data-page="user-details"], .user-details, main',
amsUserNameDisplay: '[data-field="username"], .username-display',
amsUserEmailDisplay: '[data-field="email"], .email-display',
amsUserPhoneField: '[name="phoneNumber"], [data-field="phoneNumber"], #phoneNumber',
amsUserFirstNameField: '[name="firstName"], [data-field="firstName"], #firstName',
amsEditButton: 'button:has-text("Edit")',
amsSaveButton: 'button:has-text("Save")',
amsMerchantsLink: 'a:has-text("Merchants"), button:has-text("Assign Merchants"), a[href*="merchants"]',

// AMS Merchant Association Page
amsMerchantsAvailableTable: '[data-table="available"], .available-merchants table, table:first-of-type',
amsMerchantsSelectedTable: '[data-table="selected"], .selected-merchants table, table:last-of-type',
amsMerchantRow: 'tbody tr',
amsMerchantCheckbox: 'input[type="checkbox"]',
amsMerchantsSubmitButton: 'button:has-text("Save"), button:has-text("Confirm"), button:has-text("Associate")',
amsPaginationNext: 'button[aria-label="Next"], .pagination-next, li.next button',
```

> **NOTA:** Os seletores acima são aproximações — o subagent-impl-page-object deve inspecionar o AMS portal em qa2 via Playwright para confirmar seletores reais e ajustar conforme necessário.
