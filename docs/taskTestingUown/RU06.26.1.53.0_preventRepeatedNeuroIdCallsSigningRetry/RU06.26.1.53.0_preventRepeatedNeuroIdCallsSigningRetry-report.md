# Relatório de Teste: RU06.26.1.53.0_preventRepeatedNeuroIdCallsSigningRetry

> ⚠️ **Este arquivo é registro de execução, NÃO fonte de padrão.** (regra #16)
> Reports em `docs/taskTestingUown/` são history de uma execução específica. NÃO inferir patterns
> (selectors, helpers, classificação) a partir deste arquivo. Fonte de pattern = skills (`.claude/skills/`)
> + código (`src/`, `tests/`). Toda asserção técnica carrega tag de proveniência (regra #16, [[test-report-standard]] §9).

---

## Informações da Tarefa

- **Task ID:** Prevent Repeated NeuroID Calls During Signing Retries
- **Milestone:** RU06.26.1.53.0
- **Source:** GitLab Servicing
- **Portal:** Servicing / consumer signing (NeuroID behavioral SDK)
- **Implementer:** qa-implementer (handoff in-conversation)
- **Validador:** qa-validator
- **Data de validação (ciclo 1):** 2026-06-15 09:35 -03 — BLOQUEADO (env qa2 500)
- **Data de validação (ciclo 2):** 2026-06-18 10:00 -03 — PARCIAL (5 CTs PASS, 2 env-blocker)
- **Data de validação (ciclo 3):** 2026-06-18 13:35 -03 — **FECHADO** (todos ACs confirmados)
- **Data de validação (ciclo 4):** 2026-06-23 — **BLOQUEADO (env/infra)** — re-run **stg** não executável: tunnel DB 127.0.0.1:5445 servindo `uown-sandbox`, não stg (hazard #1)
- **Data de validação (ciclo 5):** 2026-06-23 — **EXECUTADO em stg** (tunnel reconectado pelo user; DB resolvido via endpoint stg direto `35.224.143.155:5432`) — **7 CTs FALHARAM no mesmo ponto + 1 SKIP esperado** → ver classificação `[CONFIRMADO]` BUG-01 (config flag `approve.on.profile.not.found` OFF em stg)
- **Branch:** `tests`
- **Ciclo de validação:** 5 — re-execução solicitada (não é loop validator↔debugger; é re-run de spec já fechada em qa2)
- **Nota de escopo:** alvo do ciclo 4 corrigido de qa2 → **stg** durante a execução (task NeuroID readiness configurada em stg; qa2 era só baseline). Nenhum lead foi criado em nenhum env (o run nunca executou — blocker no pré-check). O mesmo blocker de tunnel impede stg.

## Descrição

Quando o cliente retorna ao portal de signing após um primeiro submit (CC ou bank já salvos),
um retry/continue NÃO deve disparar uma NOVA chamada de verificação NeuroID no backend. A primeira
tentativa chama NeuroID; retornos subsequentes reutilizam a verificação anterior. Um resultado
`NOT_ENOUGH_INTERACTION_DATA` não pode negar o cliente (AC-03). A primeira tentativa legítima deve
continuar chamando NeuroID (AC-04, regression guard).

---

## Execução do Teste — Ciclo 4 (2026-06-23, env stg) — BLOQUEADO (infra)

> **Correção de escopo:** o alvo foi corrigido de qa2 → **stg** durante a execução (task NeuroID
> readiness configurada em stg; merchant primário = **TireAgent OW90218-0001**, state **NY**,
> route mostly **SignWell**). qa2 era só baseline. Nenhum lead chegou a ser criado em nenhum env —
> o run nunca executou (blocker detectado no pré-check). O mesmo blocker de tunnel impede stg.

- **Ambiente alvo:** stg (`ENV=stg` explícito; `.env` já fixa `ENV=stg`)
- **Comando pretendido:** `ENV=stg npx playwright test --project=servicing-ui tests/e2e/servicing/RU06.26.1.53.0_preventRepeatedNeuroIdCallsSigningRetry.spec.ts`
- **Spec:** `tests/e2e/servicing/RU06.26.1.53.0_preventRepeatedNeuroIdCallsSigningRetry.spec.ts` (7 testes: CT-01×2, CT-02..CT-07)
- **Merchant primário (stg):** TireAgent `OW90218-0001`, NY, `needsBankData:true`; CC = Discover BIN 6011 (`TEST_CARDS.DISCOVER_APPROVED`); CT-07 = BodegaFurniture KS1011 (SKIP se flag off).
- **Resultado geral:** ⛔ **NÃO EXECUTADO** — blocker de ambiente detectado no pré-check obrigatório (hazard #1). O suite NÃO foi rodado para evitar resultado wrong-DB falso e/ou skip vacuoso.

### Pré-check de identidade do DB (hazard #1 — OBRIGATÓRIO antes de confiar em qualquer assert DB)

O suite usa o tunnel `127.0.0.1:5445` (`UOWN_DB_URL_STG`) como oráculo de TODAS as asserções
(`countNeuroIdCalls`, `waitForNeuroIdRecord`, activity-log) e como gate do `beforeEach`
(`use_neuro_id_check`). Memória `db-tunnel-5445-env-identification`: o 5445 encaminha para UM
remoto por vez e FLIPA entre envs; senhas svc_user são agrupadas (**qa2/dev3/stg compartilham uma**;
sandbox/qa1 outra) → AUTH-OK ≠ env correto. **Atenção crítica para stg:** como stg compartilha a senha
com qa2/dev3, um simples "AUTH OK" NUNCA prova stg — exige marcadores de conteúdo. Probe executado antes de rodar:

| Sinal | Esperado (stg) | Observado (tunnel 5445, 2026-06-23) | Match? |
|-------|----------------|--------------------------------------|--------|
| Processo do tunnel | namespace stg | `kubectl port-forward postgresql-0 5445:5432 -n uown-sandbox` (PID 2042531) | ❌ **sandbox** |
| Senha svc_user aceita | grupo qa2/dev3/**stg** (`UOWN_DB_PASS_STG`) | **rejeita STG/QA2/DEV3**; aceita QA1/SBX | ❌ grupo errado (= sandbox/qa1) |
| `MAX(uown_los_lead.pk)` | faixa stg | **97883** (faixa sbx/qa1) | ❌ |
| TireAgent `OW90218-0001` (primário stg) | `state=NY`, **`use_neuro_id_check=true`** (set 2026-06-22) | `state=NC`, **`use_neuro_id_check=false`** | ❌ |
| Saslow's `OW90337-0001` | (baseline) | `state=NC`, `use_neuro_id_check=false` | ❌ |
| BodegaFurniture `KS1011` | stg KS1011 | `state=NV`, `use_neuro_id_check=false` | n/a |

**Conclusão do pré-check:** o tunnel está servindo **`uown-sandbox`**, NÃO stg. Confirmado por 3 sinais
independentes: (a) namespace literal do `kubectl port-forward` = `uown-sandbox`; (b) a senha do grupo stg
(`UOWN_DB_PASS_STG`) é REJEITADA enquanto a do grupo sandbox/qa1 autentica; (c) TireAgent — o merchant
primário do stg, que DEVERIA ter `use_neuro_id_check=true` e `state=NY` — aparece como `state=NC`,
`use_neuro_id_check=false` (config de outro env).

`[test-execution:stg-attempt 2026-06-23]` `[db-observation:127.0.0.1:5445, MAX(lead.pk)=97883, namespace=uown-sandbox]`
`[db-observation:uown_merchant WHERE ref_merchant_code='OW90218-0001' → state=NC, use_neuro_id_check=false (≠ stg esperado NY/true)]`
`[memory:db-tunnel-5445-env-identification — verificada contra estado atual: confirmada]`

### Por que isto bloqueia o re-run (não é "só pular o DB")

1. **Gate `beforeEach`** (`spec.ts:375-387`): `test.skip` quando `use_neuro_id_check != 'true'` no merchant
   primário. No DB servido (sandbox) TireAgent = `false` → **todos os CTs SKIPariam** — pass vacuoso, não
   validação (hazard #4: "se tudo skipa, reporte o estado da flag, não passe em silêncio").
2. **Oráculo de contagem** (`countNeuroIdCalls` → `uown_neuro_id_verification WHERE lead_pk`): leria o DB
   sandbox. Um lead criado via API em stg não teria row no sandbox → `waitForNeuroIdRecord` timeout, ou
   pior, correlação cruzada com PK de outro env (wrong-DB false failure). Viola Rule #10 (classificar com
   base em dado de env errado).
3. **API vs DB cross-env**: mesmo que `sendApplication` alcançasse stg (svcApiUrl auto-gerado), as
   asserções DB cairiam no sandbox — split-brain. Resultado não seria atribuível a nenhum env.

**Decisão (conservadora, Rule #10 + hazard #1):** NÃO rodar o suite até o tunnel apontar para stg.
Rodar agora produziria SKIP-all ou wrong-DB failures — nenhum dos dois é evidência de feature.

### Hazards stg adicionais (não alcançados — blocker do DB veio antes)

Não foram exercitados porque o pré-check de DB já bloqueou, mas registrados para o re-run:
- **WAF 403 "RBAC: access denied"** em `sendApplication` = bloqueio de IP de origem; **stg NÃO tem
  internal-URL fallback** (só sandbox/qa1/qa2). Se ocorrer no re-run, é env-blocker (whitelist IP / VPN /
  port-forward), NÃO bug de feature/teste. `[memory:sendapplication-403-rbac-ip-whitelist]`
- **500 nested "401 Unauthorized"** = GDS token stale; o spec auto-refresh 1× via
  `refreshGdsAccessTokenSweep` (`spec.ts:236-246`). Se persistir, refresh manual + retry. Infra.
  `[memory:sendapplication-500-stale-gds-token]`

### Ação requerida para desbloquear (infra — pertence ao user)

Re-apontar o port-forward para o cluster stg e reconfirmar a identidade antes de re-rodar:

```bash
# matar o forward sandbox atual (PID observado: 2042531)
kill 2042531
# estabelecer o forward para o postgres do stg (namespace/pod conforme acesso stg)
kubectl port-forward postgresql-0 5445:5432 -n uown-stg     # ajustar namespace/pod ao stg
# reconfirmar identidade ANTES de rodar o suite (senha stg NÃO basta — usar marcador de conteúdo):
#   - UOWN_DB_PASS_STG autentica (rejeita grupo sandbox/qa1)
#   - TireAgent OW90218-0001 → state=NY, use_neuro_id_check=true
```

Após o tunnel apontar para stg e a senha `UOWN_DB_PASS_STG` autenticar + TireAgent confirmar NY/true,
re-executar: `ENV=stg npx playwright test --project=servicing-ui tests/e2e/servicing/RU06.26.1.53.0_preventRepeatedNeuroIdCallsSigningRetry.spec.ts`

---

## Execução do Teste — Ciclo 5 (2026-06-23, env stg) — EXECUTADO

> **Re-execução em stg.** O user reconectou o tunnel e a identidade do DB foi **re-confirmada
> ANTES de rodar** (hazard #1). O suite resolve o DB via `UOWN_DB_URL_STG = 35.224.143.155:5432`
> (endpoint stg DIRETO, NÃO o tunnel compartilhado `127.0.0.1:5445`) — portanto SEM risco de flip
> de tunnel neste run. Merchant primário = **TireAgent OW90218-0001 / NY**, CC = Discover BIN 6011.

### Re-confirmação de identidade do DB (hazard #1 — executada antes do run)

Probe read-only via o mesmo resolver que o spec usa (`environment.ts:resolveDbConnectionString`,
sufixo `STG`):

| Sinal | Esperado (stg) | Observado (2026-06-23) | Match? |
|-------|----------------|------------------------|--------|
| Host de conexão | endpoint stg | `35.224.143.155:5432` (user `svc_user`) | ✅ |
| `MAX(uown_los_lead.pk)` | faixa stg (~7.2M) | **7218098** | ✅ |
| TireAgent `OW90218-0001` | `state=NY`, `use_neuro_id_check=true`, ONLINE | `NY` / `true` / `ONLINE` | ✅ |
| `KS1011` (BodegaFurniture) | flag off em stg | `state=NV`, `use_neuro_id_check=false` | ✅ (→ CT-07 SKIP) |
| Saslow's `OW90337-0001` | baseline | `NC` / `false` / INSTORE | ✅ |

`[db-observation:35.224.143.155:5432, MAX(lead.pk)=7218098]`
`[db-observation:uown_merchant WHERE ref_merchant_code='OW90218-0001' → state=NY, use_neuro_id_check=true, ONLINE]`
`[test-execution:stg 2026-06-23]`

**Conclusão do pré-check:** DB é stg, sem flip, flag NeuroID ON no merchant primário. Run autorizado.

### Comando + resultado

- **Comando:** `ENV=stg npx playwright test --project=servicing-ui tests/e2e/servicing/RU06.26.1.53.0_preventRepeatedNeuroIdCallsSigningRetry.spec.ts`
- **Duração:** 16.7 min · **Worker:** 1
- **Resultado bruto Playwright:** `7 failed · 1 skipped · 2 passed` — onde os **2 passed = setups de auth**
  (`auth.setup.ts` origination + servicing), NÃO CTs. Em termos de CT: **7 CT FAIL + 1 CT SKIP**.

| CT | Status Ciclo 5 | Lead PK | NeuroID count | neuro_id_status / status | NeuroID note pk | Provider | Ponto de falha |
|----|----------------|---------|---------------|--------------------------|------------------|----------|----------------|
| CT-01 Row1 (CC) | ❌ FAIL | 7218099 | 1 (`countNeuroIdCalls`) | `NOT_ENOUGH_INTERACTION_DATA` / **DECLINE** (success=true) | 342448 (verif) · note 84555342 | n/a (bloqueado antes do signing) | T&C nunca renderizou (`#termsOfAgreementForm` 120s) |
| CT-01 Row2 (Bank) | ❌ FAIL | 7218100 | 1 | `PROFILE_NOT_FOUND` / **DECLINE** (success=false) | 342449 · note 84555390 | n/a | idem |
| CT-02 (AC-03) | ❌ FAIL | 7218101 | 1 | `PROFILE_NOT_FOUND` / **DECLINE** | 342450 · note 84555438 | n/a | idem |
| CT-03 (AC-01) | ❌ FAIL | 7218102 | 1 | `PROFILE_NOT_FOUND` / **DECLINE** | 342451 · note 84555486 | n/a | idem |
| CT-04 (AC-04) | ❌ FAIL | 7218103 | count_before=0 → 1 | `PROFILE_NOT_FOUND` / **DECLINE** | 342452 · note 84555534 | n/a | idem |
| CT-05 (AC-02) | ❌ FAIL | 7218105 | 1 | `PROFILE_NOT_FOUND` / **DECLINE** | 342453 · note 84555585 | n/a | idem (GDS token refresh 1× ocorreu) |
| CT-06 (AC-05) | ❌ FAIL | 7218106 | 1 | `PROFILE_NOT_FOUND` / **DECLINE** | 342454 · note 84555633 | n/a | idem |
| CT-07 (AC-04, BodegaFurniture) | ⏸️ SKIP | — | — | — | — | — | `use_neuro_id_check=false` em KS1011 (esperado, correto) |

> Notação: "NeuroID count" = rows em `uown_neuro_id_verification WHERE lead_pk` (oráculo `countNeuroIdCalls`).
> `neuro_id_status` = coluna `neuro_id_status`; `status` = coluna `status` (FraudStatus). Todos os 7 leads
> ficaram em `lead_status=UW_APPROVED` (NÃO avançaram a CONTRACT_CREATED); **0 rows** em
> `uown_esign_document` para qualquer lead.

`[test-execution:stg 2026-06-23, run 16.7m]`
`[db-observation:uown_neuro_id_verification, leads 7218099-7218106, 7 rows, all status=DECLINE]`
`[db-observation:uown_los_lead, leads 7218099-7218106, all lead_status=UW_APPROVED]`
`[db-observation:uown_esign_document, leads 7218099-7218106, 0 rows]`

### Causa raiz (DOM + DB + svc-source, três camadas)

1. **DOM customer-facing (Rule #15, viewport 1440×900):** todos os `error-context.md` capturam a
   MissingDataForm com o CC preenchido (Discover `6011 0009 9302 6909`, preauth $40) + um **alert
   visível: `"Failed to verify identification"`**. A T&C nunca aparece — o backend rejeita a progressão
   no `submitApplication`. `[dom-snapshot:stg,2026-06-23,1440x900]`
2. **Activity log (Rule #13):** sequência real por lead — `[submitApplication] CC Auth Passed` →
   `Device Type: ...` → **`[UownClient][submitApplication] Neuro Id verification Failed`** (ex. lead
   7218099 note pk 84555342). `[db-observation:uown_los_lead_notes WHERE lead_pk=7218099]`
3. **svc-source (a peça decisiva):** o fix svc#554 ESTÁ no código, mas é **gated por uma feature flag**:
   - `NeuroIdVerificationService.processNeuroIdVerification` (linhas 71-80): aprova
     `PROFILE_NOT_FOUND`/`NOT_ENOUGH_INTERACTION_DATA` **SOMENTE SE**
     `configurationManagement.getBoolean(".approve.on.profile.not.found", false)` for `true`.
   - Com a flag OFF (default do código), cai para as linhas 81-87 → `NEURO_ID_ERROR` →
     `"Neuro Id verification Failed"` → retorna `"DECLINE"`.
   - `NeuroIdVerificationStep.execute` (linhas 31-38): `if (!"APPROVE".equals(status))` → `STOP` com
     mensagem user-facing `"Failed to verify identification"` (exatamente o alert do DOM).
   - `[svc-source:NeuroIdVerificationService.java:71-87]` `[svc-source:NeuroIdVerificationStep.java:31-38]`
4. **Config table (estado real em stg):** `SELECT key,value FROM uown_configuration_management WHERE
   key ILIKE '%approve.on.profile%'` → **0 rows**. A flag não existe em stg → usa o default `false`.
   `[db-observation:uown_configuration_management, key ILIKE '%approve.on.profile%' → 0 rows, stg 2026-06-23]`

**Em headless (Playwright), o SDK NeuroID não produz interação behavioral suficiente → o profile
real retorna `PROFILE_NOT_FOUND`/`NOT_ENOUGH_INTERACTION_DATA` (esperado para automação). O comportamento
de produto que difere entre envs é o MAPEAMENTO desse resultado:** stg (flag OFF) → DECLINE + bloqueio;
qa2 (onde o ciclo 3 passou) → APPROVE + progressão. A diferença é de **configuração de ambiente**, não
de código — o código do fix está presente em ambos.

---

## Execução do Teste — Ciclo 3 (2026-06-18, validação final)

- **Ambiente:** qa2
- **Método:** Validação via URL de signing direto + DB + `uown_sv_outbound_api_log`
- **Leads desta validação:** 16714 (TireAgent/ONLINE), 16716 (SaslowsJewelersCA), 16717 (SaslowsJewelersCA)
- **Resultado geral:** ✅ Todos os ACs confirmados — pipeline fechado

### Mecanismo de validação (ciclo 3)

O user forneceu a URL de signing de um lead em `CONTRACT_CREATED` (lead 16714, TireAgent) e os
registros do DB. A navegação manual via MCP Playwright percorreu o fluxo completo:

1. Welcome page com botão "Sign Contract" carregada corretamente
2. T&C renderizada (Samsung Galaxy S24, $28.01/semana, 13 meses, 56 pagamentos)
3. "Proceed to signature" → Signwell iframe "Lease Agreement.docx" com 13 campos carregado
4. Cada "Proceed to signature" gerou exatamente 1 chamada SUBMIT_APP NeuroID → APPROVE

---

## Cenários — Ciclo 2 (histórico)

### CT-01 [Row1 CC saved] — no new NeuroID call on return-to-sign (AC-01) ✅ PASS
- **Lead:** 16699 · **Merchant:** SaslowsJewelersCA
- **count1:** 1 · **count2:** 1 (count não aumentou no retry) ✅
- **Note watermark:** pk=150168 `[NeuroIdVerificationService] Profile not found —` ✅
- **NeuroID denial modal:** não exibido ✅
- `[api-response:uown_neuro_id_verification,count1=1,count2=1]` `[test-execution:qa2 2026-06-18]`

---

### CT-01 [Row2 Bank saved] — no new NeuroID call on return-to-sign (AC-01) ✅ PASS
- **Lead:** 16700 · **Merchant:** SaslowsJewelersCA
- **count1:** 1 · **count2:** 1 ✅
- **Note watermark:** pk=150222 ✅
- `[api-response:uown_neuro_id_verification,count1=1,count2=1]` `[test-execution:qa2 2026-06-18]`

---

### CT-02 — NOT_ENOUGH_INTERACTION_DATA não nega o cliente (AC-03) ✅ PASS (N/A branch)
- **Lead:** 16701 · **Status observado:** `PROFILE_NOT_FOUND`
- `NOT_ENOUGH_INTERACTION_DATA` não reproduzível via SDK starve em qa2. Caminho N/A registrado; cliente não negado. ✅
- `[test-execution:qa2 2026-06-18]` `[svc-source:spec:381-392]`

---

### CT-03 — três retries não acumulam chamadas NeuroID (AC-01) ✅ PASS
- **Lead:** 16702 · **count1:** 1
- **count_after_retry_1:** 1 · **count_after_retry_2:** 1 · **count_after_retry_3:** 1 ✅
- `[api-response:uown_neuro_id_verification,count1=1,retries=3]` `[test-execution:qa2 2026-06-18]`

---

### CT-04 — primeira tentativa chama NeuroID corretamente (AC-04, regression guard) ✅ PASS
- **Lead:** 16703 · **count_before:** 0 · **count_after:** 1 ✅
- **neuro_id_status:** `NOT_ENOUGH_INTERACTION_DATA`, lead NÃO negado, signing acessível ✅
- `[api-response:uown_neuro_id_verification,count=1,status=NOT_ENOUGH_INTERACTION_DATA]` `[test-execution:qa2 2026-06-18]`

---

## Cenários — Ciclo 3

### CT-05 — cliente com dados NeuroID prévios alcança signing sem re-validação (AC-02) ✅ CONFIRMADO

**Evidência 1 — Automated run (lead 16716, SaslowsJewelersCA, 2026-06-18 16:41 UTC):**
- Lead criado com sucesso → `CONTRACT_CREATED` ✅
- NeuroID pk=101: `NOT_ENOUGH_INTERACTION_DATA`, `status=APPROVE`, `success=true`, `caller=SUBMIT_APP` ✅
- esign document pk=13909: `SENT_TO_CUSTOMER` (contrato gerado e enviado) ✅
- Falha de automação: `returnToSignNoInteraction` não clicava "Sign Contract" antes de verificar T&C — bug de automação, NÃO bug de feature

**Evidência 2 — Validação manual via URL (lead 16714, TireAgent, 2026-06-18):**
- URL `secure-qa2.uownleasing.com/.../complete?planId=WK13` navegada via MCP Playwright ✅
- Welcome page renderizada; T&C carregada após clique em "Sign Contract" ✅
- Signwell iframe "Lease Agreement.docx" com 13 campos de assinatura carregado ✅
- Lead status: `CONTRACT_CREATED` (contrato criado e acessível) ✅
- NeuroID pk=98,99,100: todos `NOT_ENOUGH_INTERACTION_DATA`, `status=APPROVE`, `success=true` ✅
- Nenhum modal de negação exibido ✅

**Evidência 3 — `uown_sv_outbound_api_log` pk=8719 (informado pelo user):**
- `call_type=GET`, URL `https://api.neuro-id.com/v4.1/sites/form_depth355/profiles/16714`
- Response: `<200, {"message":"success",...,"interactionAttributes":{...}}>` ✅
- Prova: a API NeuroID retorna HTTP 200 mesmo quando o status interno é `NOT_ENOUGH_INTERACTION_DATA`; não é erro HTTP — é status de dados insuficientes tratado como APPROVE pelo backend ✅

**Conclusão AC-02:** O cliente com dados NeuroID prévios alcança o signing normalmente. O backend APPROVE em `NOT_ENOUGH_INTERACTION_DATA` garante que o fluxo não seja bloqueado.

`[db-observation:uown_neuro_id_verification,pk=98,99,100,status=NOT_ENOUGH_INTERACTION_DATA,result=APPROVE]`
`[db-observation:uown_sv_outbound_api_log,pk=8719,http=200]`
`[manual-validation:qa2,lead=16714,2026-06-18]`

---

### CT-06 — signing alcança status pós-assinatura, NeuroID chamado uma vez (AC-05) ✅ CONFIRMADO

**Evidência — Automated run (lead 16717, SaslowsJewelersCA, 2026-06-18 16:43 UTC):**
- Lead criado: `SIGNED` ✅ (signing completado pelo fluxo automático)
- NeuroID pk=102: `PROFILE_NOT_FOUND`, `status=APPROVE`, `success=false`, `caller=SUBMIT_APP` — 1 única chamada ✅
- count_final = 1 (verificado diretamente no DB) ✅ — nenhuma chamada adicional acumulada
- Lead status final: `SIGNED` ✅
- Falha de automação: `uown_esign_document.status=STORED` (lag do sweep/webhook Signwell) — signing ocorreu, processamento assíncrono pendente — NÃO é bug de feature

**Conclusão AC-05:** O fluxo de signing é estável após o fix. NeuroID foi chamado exatamente 1 vez; lead chegou a `SIGNED`; nenhum modal de negação exibido.

`[db-observation:uown_neuro_id_verification,pk=102,lead=16717,count=1]`
`[db-observation:uown_los_lead,pk=16717,lead_status=SIGNED]`
`[test-execution:qa2 2026-06-18]`

---

### CT-07 — primeira tentativa chama NeuroID no merchant alternativo BodegaFurniture (AC-04) ⏸️ SKIP
- `useNeuroIdCheck=false` em `KS1011` (BodegaFurniture) no qa2. Skip esperado e correto.
- `[db-observation:uown_merchant.use_neuro_id_check=false,KS1011]` `[test-execution:qa2 2026-06-18]`

---

## Cobertura dos Requisitos — Ciclo 5 (2026-06-23, env stg) — EXECUTADO

> Esta tabela reflete o estado DESTA execução em stg. NÃO herda `[CONFIRMADO]` do ciclo 3/qa2 (regra #16).
> Todos os ACs que dependem de progressão pós-NeuroID ficaram BLOQUEADOS porque o cliente nunca passa do
> NeuroID na config atual de stg. AC-04 (NeuroID É chamado na 1ª tentativa) é o único parcialmente exercitado.

| AC | Descrição | CT(s) | Status Ciclo 5 (stg) | Evidência |
|----|-----------|-------|----------------------|-----------|
| AC-01 | Retries não chamam NeuroID novamente | CT-01×2, CT-03 | ❌ **NÃO VALIDÁVEL em stg** — fluxo bloqueado antes do retry (cliente nunca chega ao signing) | leads 7218099/100/102, T&C nunca renderiza |
| AC-02 | Dados prévios → signing sem re-validação | CT-05 | ❌ **NÃO VALIDÁVEL** — signing inacessível (DECLINE no 1º submit) | lead 7218105 UW_APPROVED, 0 esign |
| AC-03 | `NOT_ENOUGH_INTERACTION_DATA`/`PROFILE_NOT_FOUND` **NÃO** nega | CT-02, CT-04 | ❌ **CONTRADITO em stg** — esses status causam DECLINE + bloqueio + alert "Failed to verify identification" | leads 7218101/103, status=DECLINE |
| AC-04 | 1ª tentativa chama NeuroID | CT-04, CT-07 | ⚠️ **PARCIAL** — NeuroID É chamado (count_before=0 → 1 em CT-04) mas o CT falha no T&C downstream; CT-07 SKIP esperado | CT-04 lead 7218103 count 0→1 ✅; CT-07 KS1011 flag off |
| AC-05 | Signing alcança status pós-assinatura | CT-06 | ❌ **NÃO VALIDÁVEL** — signing nunca inicia | lead 7218106 UW_APPROVED, 0 esign |

**Leitura:** o sinal central da task (AC-03: dados de interação insuficientes NÃO devem negar)
está **CONTRADITO em stg** com a config atual. O fix de código existe; a flag que o ativa está OFF.
Ver BUG-01 abaixo para classificação conservadora (config vs regressão).

---

## Bugs de Aplicação Encontrados

### [CONFIRMADO] BUG-01 — Em stg, NeuroID `PROFILE_NOT_FOUND`/`NOT_ENOUGH_INTERACTION_DATA` nega o cliente (AC-03 contradito); causa = feature flag `approve.on.profile.not.found` OFF

- **Status:** [CONFIRMADO] como observação de comportamento/estado de ambiente. **Causa raiz = configuração
  de ambiente (feature flag), NÃO regressão de código** — o código do fix svc#554 está presente em stg.
- **Severidade:** S2 (bloqueia o fluxo customer-facing de signing para todo lead cujo NeuroID não retorna
  SUCCESS — que em headless é 100% dos casos; em produção, qualquer cliente com profile não encontrado /
  interação insuficiente).
- **Prioridade:** P1 (decisão de produto: a flag deve estar ON em stg para o fix ter efeito? Se sim,
  é provisioning faltante; se não, o teste/spec assume um comportamento que stg não tem).

**Descrição:** O propósito da task inclui AC-03 — `NOT_ENOUGH_INTERACTION_DATA` (e por extensão
`PROFILE_NOT_FOUND`) NÃO deve negar o cliente. Em **stg**, esses status produzem `status=DECLINE` →
`NEURO_ID_ERROR` → o `NeuroIdVerificationStep` para o submit com a mensagem user-facing
**"Failed to verify identification"**, e o lead fica preso em `UW_APPROVED` sem chegar a
CONTRACT_CREATED. O cliente é efetivamente bloqueado do signing.

**Como reproduzir (fresh, n=7 nesta execução):**
1. `ENV=stg`, merchant TireAgent `OW90218-0001` (NY, `use_neuro_id_check=true`).
2. Criar lead fresh via `createPreQualifiedApplication` (bankData required) → invoice → contractUrl.
3. Abrir contractUrl, preencher MissingDataForm (CC Discover 6011) e submeter via browser (headless).
4. Observar: alert "Failed to verify identification"; T&C não renderiza.
5. DB: `uown_neuro_id_verification` → 1 row, `neuro_id_status ∈ {PROFILE_NOT_FOUND, NOT_ENOUGH_INTERACTION_DATA}`,
   `status=DECLINE`; `uown_los_lead_notes` → `[UownClient][submitApplication] Neuro Id verification Failed`;
   `uown_los_lead.lead_status=UW_APPROVED`.

**Evidência:**
- DOM: alert "Failed to verify identification" (todos os 7 error-context). `[dom-snapshot:stg,2026-06-23,1440x900]`
- DB verif: 7/7 leads `status=DECLINE`. `[db-observation:uown_neuro_id_verification, leads 7218099-7218106]`
- DB notes: `[UownClient][submitApplication] Neuro Id verification Failed` (lead 7218099 note 84555342). `[db-observation:uown_los_lead_notes]`
- DB config: `approve.on.profile.not.found` ausente em `uown_configuration_management` → default `false`. `[db-observation:uown_configuration_management, 0 rows, stg 2026-06-23]`

**Causa provável (source-confirmed):** `NeuroIdVerificationService.processNeuroIdVerification` aprova
`PROFILE_NOT_FOUND`/`NOT_ENOUGH_INTERACTION_DATA` apenas se a flag `.approve.on.profile.not.found` for
`true` (linhas 71-80); com a flag OFF cai em `NEURO_ID_ERROR`/`DECLINE` (linhas 81-87);
`NeuroIdVerificationStep` STOPa em `!"APPROVE"` (linhas 31-38).
`[svc-source:NeuroIdVerificationService.java:71-87]` `[svc-source:NeuroIdVerificationStep.java:31-38]`

**Cenário que detectou:** CT-01..CT-06 (todos). CT-02/CT-04 mapeiam diretamente AC-03.

**Decisão pendente (cabe ao user / dev / PO) — NÃO abrir ticket sem autorização (regra #10/#16):**
> O comportamento esperado da feature em stg pressupõe `approve.on.profile.not.found=true`?
> - **Se SIM** → é provisioning de config faltante em stg (a flag deve ser setada via
>   `createOrUpdateSqlConfig`/config endpoint). Após setar, re-rodar o suite (fica verde como em qa2).
> - **Se NÃO** (stg deve declinar de propósito) → o spec/AC-03 assume um comportamento que stg não tem,
>   e o teste é environment-aware-incompleto (precisa de branch stg que aceite DECLINE como esperado).

> **Nota de consistência com knowledge prévio:** a memória `554-neuroid-stg-readiness` já registrou
> este exato comportamento (n=3, 2026-06-22) como "pre-fix behavior, fix appears NOT active in stg".
> Esta execução (n=7, 2026-06-23) **reproduz e confirma a causa raiz** (flag OFF + svc-source). NÃO é
> herança de classificação — é fresh repro com fonte primária nesta execução (regra #16).

---

## Cobertura dos Requisitos — Ciclo 4 (2026-06-23, qa2 re-run)

> Re-run NÃO executado (blocker de tunnel). Nenhum AC foi re-confirmado nesta execução.
> A coluna "Ciclo 4" reflete o estado DESTA tentativa, não herda `[CONFIRMADO]` do ciclo 3 (regra #16).
> O status histórico do ciclo 3 permanece registrado na tabela "Status Final" abaixo.

| AC | CT(s) mapeados | Status Ciclo 4 (2026-06-23) | Última confirmação válida |
|----|----------------|------------------------------|----------------------------|
| AC-01 | CT-01 (Row1, Row2), CT-03 | ⛔ NÃO RE-EXECUTADO (tunnel=sandbox) | Ciclo 2 (2026-06-18, qa2) |
| AC-02 | CT-05 | ⛔ NÃO RE-EXECUTADO | Ciclo 3 (2026-06-18, qa2, lead 16714/16716) |
| AC-03 | CT-02, CT-04 | ⛔ NÃO RE-EXECUTADO | Ciclo 2/3 (2026-06-18, qa2) |
| AC-04 | CT-04, CT-07 | ⛔ NÃO RE-EXECUTADO | Ciclo 2 (CT-04, qa2); CT-07 SKIP esperado (KS1011 flag off) |
| AC-05 | CT-06 | ⛔ NÃO RE-EXECUTADO | Ciclo 3 (2026-06-18, qa2, lead 16717 SIGNED) |

Mapa CT → resultado Ciclo 4:

| CT | Resultado Ciclo 4 | Lead PK | count1/count2 (ou count_before/after) | neuro_id_status | NeuroID note pk | Evidência |
|----|-------------------|---------|----------------------------------------|------------------|------------------|-----------|
| CT-01 Row1 (CC) | ⛔ não executado | — | — | — | — | tunnel=sandbox (gate skip/wrong-DB) |
| CT-01 Row2 (Bank) | ⛔ não executado | — | — | — | — | idem |
| CT-02 | ⛔ não executado | — | — | — | — | idem |
| CT-03 | ⛔ não executado | — | — | — | — | idem |
| CT-04 | ⛔ não executado | — | — | — | — | idem |
| CT-05 | ⛔ não executado | — | — | — | — | idem |
| CT-06 | ⛔ não executado | — | — | — | — | idem |
| CT-07 | ⛔ não executado (esperado SKIP em qa2 — KS1011 flag off) | — | — | — | — | idem |

---

## Cobertura dos Requisitos — Status Final (histórico ciclo 3 — NÃO re-confirmado no ciclo 4)

| AC | Descrição | CT(s) | Status final |
|----|-----------|----|---------------------|
| AC-01 | Retries não chamam NeuroID novamente | CT-01 (2 rows), CT-03 | ✅ **CONFIRMADO** — count assertion + note watermark (ciclo 2) |
| AC-02 | Cliente com dados prévios alcança signing sem re-validação | CT-05 | ✅ **CONFIRMADO** — lead 16714 URL navegada; lead 16716 CONTRACT_CREATED; NeuroID APPROVE (ciclo 3) |
| AC-03 | Cliente não negado por NOT_ENOUGH_INTERACTION_DATA | CT-02, CT-04, leads 16714/16716/16717 | ✅ **CONFIRMADO** — múltiplas ocorrências reais; API NeuroID retorna HTTP 200; backend → APPROVE |
| AC-04 | NeuroID continua para novas interações legítimas | CT-04, CT-06 | ✅ **CONFIRMADO** — count_before=0, count_after=1 (ciclo 2) |
| AC-05 | Fluxo de signing estável após o ajuste | CT-06 | ✅ **CONFIRMADO** — lead 16717 SIGNED; 1 chamada NeuroID (ciclo 3) |

---

## Validação Estática

| Item | Resultado | Evidência |
|------|:---------:|-----------|
| Fix endereço: `randomAddress()` sem sufixo unidade | ✅ | `src/data/realistic/addresses.ts:164` `[svc-source:addresses.ts:164]` |
| CT-05 `withBank: true` para evitar re-exibição de bank form | ✅ | `spec.ts:492,499` `[svc-source:spec:492]` |
| Fix coluna DB: `SELECT lead_status` (era `SELECT status`) | ✅ | `spec.ts:566` — coluna corrigida `[svc-source:spec:566]` |
| `tsc --noEmit` limpo | ✅ | 0 erros `[test-execution:tsc]` |

---

## Observações — Ciclo 3

### O-06 — [OBSERVAÇÃO] `NOT_ENOUGH_INTERACTION_DATA` é o status padrão do ambiente qa2 (depth355)
- O site-id `depth355` do ambiente qa2 retorna consistentemente `NOT_ENOUGH_INTERACTION_DATA` quando não há
  interação suficiente com o SDK NeuroID no browser. HTTP response da API é sempre 200; o status é
  interpretado internamente pelo backend.
- Confirmado via `uown_sv_outbound_api_log` pk=8719: GET → 200 com `{"message":"success",...}`.
- O backend mapeia este status para `APPROVE` — AC-03 é exercitado em todo SUBMIT_APP no qa2.
- `[db-observation:uown_sv_outbound_api_log,pk=8719,http=200]` `[manual-validation:qa2,lead=16714]`

### O-07 — [OBSERVAÇÃO] Falha de automação CT-05: `returnToSignNoInteraction` não clicar "Sign Contract"
- O consumer portal exibe "Sign Contract" button + welcome message no retorno ao `contractUrl` (quando
  payment data já existe). O T&C só aparece após clicar o botão.
- `returnToSignNoInteraction` verificava T&C imediatamente sem clicar "Sign Contract" — timeout 15 s.
- Fix pendente na função: adicionar clique em "Sign Contract" se visível antes de checar T&C.
- NÃO impacta a feature: a evidência manual (lead 16714) prova que o signing é acessível.
- `[svc-source:spec:249-263]`

### O-08 — [OBSERVAÇÃO] Falha de automação CT-06: esign `STORED` (sweep lag)
- O signing foi completado (lead 16717 `SIGNED`). O `uown_esign_document.status=STORED` indica
  que o documento foi armazenado mas o webhook/sweep do Signwell ainda não processou a conclusão.
- A asserção `toBe('COMPLETED')` falhou por lag assíncrono, não por falha de feature.
- `[db-observation:uown_esign_document,pk=13910,status=STORED,lead=16717]`

### O-09 — [OBSERVAÇÃO] Raiz técnica do env-blocker ciclos 1 e 2 identificada
- O `sendApplication` 500 com `nested 401` origina da chamada GDS UW via `LosOutboundCall.makeRestCall`
  dentro de `UnderwritingService.routeUwRequest()`. Token Cognito expirado ou endpoint GDS instável no qa2.
- O log aparece vazio porque `AspectOutboundApiLog` faz `saveAndFlush` na mesma `@Transactional` do
  `SendApplicationService`; o rollback apaga o log junto com o lead.
- Não é bug de código de teste — infraestrutura qa2.

---

## Observações — Ciclo 4

### O-10 — [BLOCKER/INFRA] Tunnel DB 5445 servindo `uown-sandbox`, não qa2 (hazard #1)
- O re-run em qa2 não pôde executar porque `127.0.0.1:5445` (oráculo DB do suite) está encaminhando
  para o namespace `uown-sandbox` (processo `kubectl port-forward postgresql-0 5445:5432 -n uown-sandbox`,
  PID 2042531). Senha `UOWN_DB_PASS_QA2` rejeitada; senha do grupo qa1/sbx aceita; `MAX(lead.pk)=97882`
  (faixa sbx/qa1); leads 16714/16716/16717 aparecem como `EXPIRED`/2022 (registros de outro DB).
- **Classificação:** infraestrutura/ambiente — NÃO é bug de feature, NÃO é bug de teste, NÃO é flake do
  spec. É o hazard #1 documentado (`db-tunnel-5445-env-identification`) materializado.
- **Impacto:** todos os 7 testes ficariam SKIP (merchants sandbox têm `use_neuro_id_check=false`) ou
  produziriam wrong-DB false failures. Decisão conservadora (Rule #10): não rodar até o tunnel apontar qa2.
- `[test-execution:qa2-attempt 2026-06-23]` `[db-observation:127.0.0.1:5445, namespace=uown-sandbox, MAX(lead.pk)=97882]`
  `[memory:db-tunnel-5445-env-identification — confirmada contra estado atual]`

---

## Resumo da Validação — Final

| Aspecto | Resultado |
|---------|-----------|
| Cobertura de design vs risco (5 ACs) | ✅ Adequada |
| AC-01 (retries não acumulam) | ✅ **CONFIRMADO** (CT-01×2 + CT-03, ciclo 2) |
| AC-02 (dados prévios → signing acessível) | ✅ **CONFIRMADO** (lead 16714 manual + lead 16716 automated, ciclo 3) |
| AC-03 (NOT_ENOUGH_INTERACTION_DATA não nega) | ✅ **CONFIRMADO** (múltiplas ocorrências reais + outbound log HTTP 200) |
| AC-04 (primeira tentativa chama NeuroID) | ✅ **CONFIRMADO** (CT-04, ciclo 2) |
| AC-05 (signing estável) | ✅ **CONFIRMADO** (lead 16717 SIGNED + 1 chamada NeuroID, ciclo 3) |
| Bugs de aplicação confirmados | Nenhum (regra #10) |
| Pipeline | ✅ **FECHADO** (ciclo 3) |

### Ciclo 4 (2026-06-23, re-run qa2)

| Aspecto | Resultado |
|---------|-----------|
| Suite executado? | ⛔ Não — blocker de infra detectado no pré-check |
| Causa | Tunnel 5445 → `uown-sandbox` (não qa2); senha qa2 rejeitada; PKs colididos (env errado) |
| Classificação | Infra/ambiente (hazard #1) — NÃO feature bug, NÃO test bug |
| ACs re-confirmados neste ciclo | Nenhum (não houve execução) |
| Ação requerida | Re-apontar port-forward para qa2 + reconfirmar identidade do DB, depois re-rodar |
| Status do ciclo 4 | ⛔ **BLOQUEADO** |

---

**Skills loaded:**
- `.claude/skills/test-report-standard/SKILL.md`
- `.claude/skills/qa-domain-reflexes/SKILL.md`
- `.claude/skills/activity-log-validation/SKILL.md`
- `.claude/skills/application-lifecycle/SKILL.md`
- `.claude/skills/bug-classification/SKILL.md`
- `.claude/skills/volatile-knowledge-registry/SKILL.md`
- `.claude/skills/gowsign-knowledge/SKILL.md`
