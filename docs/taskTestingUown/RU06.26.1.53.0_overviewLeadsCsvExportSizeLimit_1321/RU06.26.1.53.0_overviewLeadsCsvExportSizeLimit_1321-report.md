# Relatório de Teste: RU06.26.1.53.0_overviewLeadsCsvExportSizeLimit_1321

> **Este arquivo é registro de execução, NÃO fonte de padrão (regra #16).** Patterns de código (selectors, helpers, page objects) vivem em `.claude/skills/` e em `src/`/`tests/`; classificações de bug vivem nas skills de domínio. NÃO inferir pattern, selector ou classificação a partir deste relatório. Re-leitura permitida apenas para: (a) qa-validator atualizando após nova execução, (b) reprodução manual via leadPk/accountPk, (c) auditoria solicitada pelo usuário. Toda asserção técnica carrega tag de proveniência (taxonomia em [[test-report-standard]] §9).

---

## Informações da Tarefa

- **Task ID:** RU06.26.1.53.0_overviewLeadsCsvExportSizeLimit_1321
- **GitLab:** #1321 — *Pods restart on large Overview CSV export (`ERR_STRING_TOO_LONG`)* (mitigação)
- **MR:** !1481
- **Milestone:** RU06.26.1.53.0 (R1.53.0)
- **Tipo:** Verificação de correção de bug (mitigação client-side), não feature nova
- **Portal:** Origination — telas Overview (`/overview`) e Leads (`/leads`)
- **Labels:** regression, qa2, origination

## Descrição

Baixar CSV da tela Overview com um resultado filtrado grande derrubava o pod do Origination (`ERR_STRING_TOO_LONG`): o proxy BFF bufferiza toda a resposta de `POST /uown/getLeadsInDateRange` e chama `Buffer.concat(...).toString()`, ultrapassando o limite de string do V8 (~512 MiB), porque o Download CSV busca todas as linhas numa única página (`maxResults = totalRows`).

A mitigação adiciona um **guard de tamanho client-side**: antes de permitir o download direto, estima o tamanho do CSV e, acima do limite, **desabilita o botão Download CSV** e direciona o usuário ao **Email CSV** (assíncrono), exibindo uma mensagem. O mesmo componente reutilizável (`FilteredCsvDownload`) passa a alimentar as duas telas (Overview e Leads).

---

## Execução do Teste

- **Spec:** `docs/taskTestingUown/RU06.26.1.53.0_overviewLeadsCsvExportSizeLimit_1321/RU06.26.1.53.0_overviewLeadsCsvExportSizeLimit_1321.spec.ts`
- **Ambiente:** QA2 (`origination-qa2.uownleasing.com`), buildId `2026-06-17T13:32:43` (pós-fix)
- **Project Playwright:** `task-testing-origination`
- **Conta:** `admin` (com permissão de download)
- **Data da validação:** 2026-06-18
- **Ciclo de validação:** 1/3
- **Duração:** 2.3 min (9 testes, 1 worker)
- **Resultado:** **9 passaram / 2 skipped** (9 = 2 auth-setup + CT-01..CT-07; 2 skipped = CT-08, CT-09 documentados source-confirmed)
- **Comando:** `npx playwright test docs/taskTestingUown/RU06.26.1.53.0_overviewLeadsCsvExportSizeLimit_1321/RU06.26.1.53.0_overviewLeadsCsvExportSizeLimit_1321.spec.ts --project=task-testing-origination`
- **Trace/vídeo:** anexos por CT em `testInfo.attach` (screenshots embutidas no resultado Playwright)

> **Nota sobre o veredito honesto (regra #10/#16):** o **fix do #1321 PASSOU** na validação — o Download CSV habilita abaixo de 48 MiB e exporta corretamente; os estados empty/permissão/escape-hatch estão corretos. As branches que exigem volume de dados (>48 MiB) ou role específico **não puderam ser exercidas no UI da QA2**. O branch >48 MiB foi posteriormente fechado na UI via sandbox (ver Execução Final). O gate de role permanece source-confirmed.

---

## Execução Final (follow-up sandbox — CT-08)

> Esta seção registra a execução de fechamento do branch >48 MiB. O histórico da execução QA2 acima (CT-01..CT-07) **não foi alterado**.

- **Spec:** mesmo arquivo (`describe` `${TEST_NAME} - sandbox`, `test.use({ envName: 'sandbox' })`)
- **Ambiente:** **sandbox** (`origination-sandbox.uownleasing.com`), pós-fix
- **Project Playwright:** `task-testing-origination`
- **Conta:** `SANDBOX_ADMIN_USERNAME=jmendes.gow` (resolução `'admin'`, admin full-visibility)
- **Data range aplicado:** `01/01/2022 → 12/31/2026` (painel TABLE do Overview)
- **Data da execução:** 2026-06-18/19
- **Ciclo de validação:** 1/3 (não houve devolução ao qa-debugger)
- **Resultado:** **CT-08 PASSOU** `[test-execution:sandbox,2026-06-18]`


| Medição                   | Valor observado                                                                                                          | Esperado                         | Resultado |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------------------------------- | --------- |
| Linhas na filtragem única | **79.605**                                                                                                               | > threshold de volume (≈66k)     | ✅ acima   |
| Tamanho estimado do CSV   | **~55.4 MB**                                                                                                             | > 48 MB (limite)                 | ✅ acima   |
| Download CSV              | **desabilitado** (classe `disabledButton`)                                                                               | desabilitado acima do limite     | ✅         |
| Tooltip ao hover          | `"This export is too large to download directly. Please use Email CSV instead. Estimated size: 55.4 MB (limit: 48 MB)."` | texto de direcionamento exato    | ✅         |
| Email CSV (escape hatch)  | **habilitado**                                                                                                           | habilitado independente do guard | ✅         |


**Consolidação da suite (todos os CTs):**


| CT        | Ambiente    | Status                                                                                            |
| --------- | ----------- | ------------------------------------------------------------------------------------------------- |
| CT-01     | qa2         | ✅ PASS                                                                                            |
| CT-02     | qa2         | ✅ PASS (1 run flaky por latência qa2; passou na re-execução)                                      |
| CT-03     | qa2         | ✅ PASS                                                                                            |
| CT-04     | qa2         | ✅ PASS                                                                                            |
| CT-05     | qa2         | ✅ PASS                                                                                            |
| CT-06     | qa2         | ✅ PASS                                                                                            |
| CT-07     | qa2         | ✅ PASS                                                                                            |
| **CT-08** | **sandbox** | ✅ **PASS** (79.605 linhas, ~55.4 MB)                                                              |
| CT-08     | qa2         | `test.skip` — qa2 tem ~47 linhas, insuficiente para disparar o guard (66k+ necessário)            |
| CT-09     | qa2/sandbox | `test.skip` — aguarda provisionamento de conta com role `agent`/`isr`/`auditor` e senha conhecida |


> Nota sobre flakiness do CT-02: uma execução isolada falhou por latência da QA2 e passou na re-execução. Classificação: `[OBSERVAÇÃO]` flake de ambiente (latência de listagem QA2 sob filtro amplo), **não** defeito do fix #1321 nem do teste — sem fresh repro determinístico, não escala para bug (regra #10).

---

## Evidências (Dados Utilizados/Criados)

Feature read-only sobre dados de listagem existentes (sem mutação, sem criação de lead). Nenhum leadPk/accountPk criado ou mutado.


| Dado                                         | Origem                                                                                   | Uso                                                                                |
| -------------------------------------------- | ---------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| Conjunto Overview não-vazio                  | listagem QA2 existente (read-only)                                                       | CT-01 — 47 linhas no momento da execução `[test-execution:qa2,2026-06-18]`         |
| Conjunto Leads não-vazio (From `01/01/2022`) | listagem QA2 existente (read-only)                                                       | CT-02/CT-07 — 6548 linhas no momento da execução `[test-execution:qa2,2026-06-18]` |
| Conjunto vazio determinístico                | filtro impossível (search `ZZZNONEXISTENT999` no Overview; Lead PK `999999999` no Leads) | CT-03/04/05/07                                                                     |


> Contagens de linhas são voláteis (listagem QA2 acumula ~44 leads/dia). Execuções anteriores observaram 46/6547; esta execução observou 47/6548 — drift esperado de dado vivo, não regressão. O teste reconcilia contra o total exibido no portal a cada run, não contra valor fixo `[db-observation:categoria volatile #13 — fixture/listagem drift]`.

---

## Capturas de Tela

Anexadas via `testInfo.attach` por CT (embutidas no relatório Playwright):


| CT    | Anexo                                            |
| ----- | ------------------------------------------------ |
| CT-01 | `CT-01-overview-download-enabled`                |
| CT-02 | `CT-02-leads-download-enabled`                   |
| CT-03 | `CT-03-overview-empty-both-disabled`             |
| CT-04 | `CT-04-leads-empty-both-disabled`                |
| CT-05 | `CT-05-overview-empty-no-tooltip`                |
| CT-06 | `CT-06-overview-email-modal`                     |
| CT-07 | `CT-07-leads-enabled` + `CT-07-leads-re-enabled` |


---

## Cenários

### CT-01 — Overview: Download CSV habilitado abaixo do limite

- **Objetivo:** confirmar que, abaixo de 48 MiB, o Download CSV permanece habilitado e exporta corretamente (regressão do caminho comum — maior blast radius).
- **O que é verificado:** o botão Download CSV renderiza e fica habilitado, sem tooltip de direcionamento; o arquivo baixado é `all-filtered-leads.csv`, tem 27 colunas, e a contagem de linhas de dados bate com o total exibido no portal.
- **AC:** AC-01, AC-05.

#### Como verificar manualmente

1. Logar no Origination QA2 (`origination-qa2.uownleasing.com`) com conta com permissão de download.
2. Ir para `/overview`; garantir conjunto não-vazio.
3. Confirmar que Download CSV está habilitado e sem tooltip ao passar o mouse.
4. Clicar Download CSV → arquivo `all-filtered-leads.csv`, 27 colunas, nº de linhas == total do portal.

**Resultado:** `[test-execution:qa2,2026-06-18]` `all-filtered-leads.csv`, 27 colunas, **47 linhas == total do portal 47**.

**PASSOU**

---

### CT-02 — Leads: Download CSV habilitado abaixo do limite

- **Objetivo:** confirmar o export do Leads (nova superfície de export pelo componente compartilhado) abaixo do limite, incluindo a coluna SSN (PII).
- **O que é verificado:** Download CSV habilitado sem tooltip; arquivo `leads-results.csv`, 17 colunas (coluna SSN presente), linhas == total do portal; artefato com SSN apagado do disco após o download.
- **AC:** AC-01, AC-05.

#### Como verificar manualmente

1. Origination QA2 → `/leads`.
2. Aplicar From date ampla (ex.: `01/01/2022`) → conjunto não-vazio.
3. Download CSV habilitado, sem tooltip.
4. Clicar Download CSV → `leads-results.csv`, 17 colunas, coluna SSN presente, nº de linhas == total do portal.

**Resultado:** `[test-execution:qa2,2026-06-18]` `leads-results.csv`, 17 colunas, **6548 linhas**, coluna SSN presente, artefato apagado.

> Higiene PII [reflex]: arquivo com SSN apagado via `deleteDownloadedFile`; conteúdo de linha nunca logado.

**PASSOU**

---

### CT-03 — Overview: conjunto vazio desabilita ambos os exports

- **Objetivo:** confirmar que um conjunto vazio desabilita Download CSV E Email CSV (Decision Table C3).
- **O que é verificado:** com search não-correspondente, a tabela mostra "There are no records to display" e ambos os botões de export ficam desabilitados.
- **AC:** AC-06.

#### Como verificar manualmente

1. `/overview` → buscar na tabela um termo impossível (ex.: `ZZZNONEXISTENT999`) → aplicar.
2. Estado vazio exibido; Download CSV e Email CSV ambos desabilitados.

**Resultado:** `[test-execution:qa2,2026-06-18]` ambos desabilitados, estado vazio presente.

**PASSOU**

---

### CT-04 — Leads: conjunto vazio desabilita ambos os exports

- **Objetivo:** espelho de CT-03 na tela Leads.
- **O que é verificado:** Lead PK impossível → estado vazio; Download CSV e Email CSV ambos desabilitados.
- **AC:** AC-06, AC-05.

#### Como verificar manualmente

1. `/leads` → From date ampla + Lead PK `999999999` → aplicar.
2. Estado vazio; ambos os exports desabilitados.

**Resultado:** `[test-execution:qa2,2026-06-18]` ambos desabilitados, estado vazio presente.

**PASSOU**

---

### CT-05 — Overview: conjunto vazio NÃO mostra tooltip de direcionamento (controle negativo de AC-03)

- **Objetivo:** distinguir o desabilitar-por-vazio do desabilitar-por-tamanho — o tooltip "too large" renderiza **só** no caso de tamanho excedido.
- **O que é verificado:** no conjunto vazio, o Download CSV está desabilitado mas, ao passar o mouse, **nenhum** tooltip de direcionamento aparece.
- **AC:** AC-03 (negativo).

#### Como verificar manualmente

1. `/overview` → estado vazio (search impossível).
2. Passar o mouse no Download CSV desabilitado → nenhum tooltip "too large to download".

**Resultado:** `[test-execution:qa2,2026-06-18]` Download CSV desabilitado, tooltip ausente (esperado).

**PASSOU**

---

### CT-06 — Overview: Email CSV disponível, modal abre, Send gated por endereço, CANCEL fecha

- **Objetivo:** confirmar o escape hatch (Email CSV) — disponível independente da permissão de download e do guard de tamanho.
- **O que é verificado:** Email CSV renderiza e fica habilitado num conjunto não-vazio; o modal abre com título "Which email should we send this CSV file to?"; Send fica desabilitado até um endereço ser digitado, depois habilita; CANCEL fecha o modal sem enviar email.
- **AC:** AC-04, AC-07 (independência do Email CSV).

#### Como verificar manualmente

1. `/overview` com conjunto não-vazio → Email CSV habilitado.
2. Clicar Email CSV → modal "Which email should we send this CSV file to?".
3. Send desabilitado vazio; digitar endereço → Send habilita.
4. CANCEL → modal fecha, nenhum email enviado.

**Resultado:** `[test-execution:qa2,2026-06-18]` título do modal correto, Send gated por endereço, CANCEL fechou. Envio real NÃO exercido (side effect fora de escopo — Q3).

> [check-points]: confirmação = o modal aceita o endereço (Send habilita), não "enviou com sucesso" sem prova. O toast de sucesso e o envio real são side effect OUT de escopo.

**PASSOU**

---

### CT-07 — Leads: disponibilidade re-avaliada na mudança de filtro

- **Objetivo:** confirmar que o controle recomputa disponibilidade quando os filtros mudam (transição de estado, segmentos reproduzíveis).
- **O que é verificado:** não-vazio ⇒ habilitado → Lead PK impossível ⇒ vazio-desabilitado → limpar Lead PK ⇒ não-vazio habilitado de novo.
- **AC:** AC-08, AC-05.

#### Como verificar manualmente

1. `/leads` → From date ampla → Download CSV habilitado.
2. Aplicar Lead PK `999999999` → vazio → Download CSV desabilitado.
3. Limpar Lead PK → não-vazio → Download CSV habilitado de novo.

**Resultado:** `[test-execution:qa2,2026-06-18]` transição enabled → empty-disabled → enabled confirmada. O segmento enabled↔disabled-por-tamanho é source-only (não reproduzível na QA2).

**PASSOU**

---

### CT-08 — >48 MiB: Download CSV desabilitado + tooltip "too large" (Overview, sandbox)

- **Objetivo:** confirmar o coração do fix — acima do limite, o botão desabilita, a requisição pesada nunca é emitida, e o usuário é direcionado ao Email CSV.
- **O que é verificado:** Download CSV desabilitado (classe `disabledButton`); tooltip exato `"This export is too large to download directly. Please use Email CSV instead. Estimated size: 55.4 MB (limit: 48 MB)."`; Email CSV permanece habilitado.
- **AC:** AC-02, AC-03 (tooltip).
- **Status:** **PASSOU (UI sandbox, 79.605 linhas) `[test-execution:sandbox,2026-06-18]`.**

> **Resolução da pendência de volume:** o estado desabilitado exige um conjunto filtrado >48 MiB. A QA2 acumula ~47 linhas totais (insuficiente — guard nunca dispara). A **sandbox** (`origination-sandbox.uownleasing.com`), com a conta `jmendes.gow` (`SANDBOX_ADMIN_USERNAME`, admin full-visibility) e date range `01/01/2022 → 12/31/2026`, retornou **79.605 linhas → ~55.4 MB estimado (limite 48 MB)** — acima do threshold. Nesse estado, na UI real: Download CSV **desabilitado** (classe `disabledButton`); tooltip exato `"This export is too large to download directly. Please use Email CSV instead. Estimated size: 55.4 MB (limit: 48 MB)."`; Email CSV **habilitado** (escape hatch preservado) `[test-execution:sandbox,2026-06-18]`. O guard do fix #1321 está agora **confirmado no UI**, não apenas na fonte. O segmento `==48 MiB ⇒ habilitado` (boundary inclusiva, Q4) permanece source-only — não pode ser atingido deterministicamente em dado real.
>
> **Nota de conta:** a conta merchant-scoped `jmendes.gow` quando usada como supervisor (`DEFAULT_SUPERVISOR`) é limitada a ~36 linhas e não dispara o guard; a execução de CT-08 usa a resolução `'admin'` (full-visibility), que enxerga as 79.605 linhas. O clique-enquanto-bloqueado (error toast no clique) não foi exercido — o estado desabilitado já comprova que a requisição pesada não é emitida; toast no clique permanece source-confirmed.

**PASSOU**

---

### CT-09 — Role sem permissão de download

- **Objetivo:** confirmar o gate de permissão — Download CSV só renderiza com `hasDownloadPermission && headers>0`; Email CSV renderiza independente.
- **O que é verificado:** com role sem permissão de download, Download CSV não renderiza; Email CSV continua renderizado/habilitado num conjunto não-vazio.
- **AC:** AC-07.
- **Status:** **SKIPPED — `[CONFIRMADO na fonte] + [BLOQUEADO por role]`.**

> **Motivo:** não existe conta sem permissão de CSV download na QA2 (Q-role). Confirmado na fonte: prop React live `hasDownloadPermission` e o gate de renderização `hasDownloadPermission && headers.length > 0` no componente `FilteredCsvDownload` (MR !1481) `[doc:docs/knowledge-base/overview-leads-csv-export-size-limit.md §"Component behaviour"]`.

**PASSOU**

---

## Cobertura dos Requisitos


| AC        | Enunciado (derivado do MR)                                                                                   | CT                                                               | Veredito                                                                                                                                                                                   |
| --------- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **AC-01** | Abaixo do limite, Download CSV habilitado e exporta como antes (filename, todas as linhas, 27/17 cols)       | CT-01, CT-02                                                     | ✅ **PASSOU** (UI QA2) `[test-execution:qa2,2026-06-18]`                                                                                                                                    |
| **AC-02** | Acima do limite, Download CSV desabilitado e não dispara a requisição                                        | CT-08                                                            | ✅ **PASSOU** (UI sandbox, 79.605 linhas, ~55.4 MB) `[test-execution:sandbox,2026-06-18]`                                                                                                   |
| **AC-03** | Download CSV desabilitado mostra mensagem de direcionamento (tooltip/toast); caso vazio NÃO mostra mensagem  | CT-08 (tooltip), CT-05 (controle negativo)                       | ✅ Tooltip **PASSOU** (UI sandbox, texto exato `[test-execution:sandbox,2026-06-18]`); controle negativo (vazio sem msg) ✅ **PASSOU** (UI QA2). Error toast no clique permanece source-only |
| **AC-04** | Email CSV permanece disponível para datasets grandes (escape hatch)                                          | CT-06                                                            | ✅ **PASSOU** (UI QA2) render+modal; "para conjunto *grande*" source-only                                                                                                                   |
| **AC-05** | Mesmo comportamento em Overview e Leads                                                                      | CT-01/02 (enabled), CT-04 (empty Leads), CT-07 (re-enable Leads) | ✅ **PASSOU** (UI QA2) eixo enabled/empty                                                                                                                                                   |
| **AC-06** | Conjunto vazio desabilita ambos (Download CSV + Email CSV)                                                   | CT-03, CT-04                                                     | ✅ **PASSOU** (UI QA2)                                                                                                                                                                      |
| **AC-07** | Download CSV gated por `hasDownloadPermission && headers>0`; Email CSV independente da permissão de download | CT-06 (Email independente), CT-09 (no-perm)                      | ✅ Email independente **PASSOU** (UI QA2); gate no-perm **[CONFIRMADO na fonte]** + **[BLOQUEADO por role]**                                                                                |
| **AC-08** | Disponibilidade re-avaliada na mudança de filtro                                                             | CT-07                                                            | ✅ **PASSOU** (UI QA2) eixo enabled↔empty; eixo enabled↔disabled-por-tamanho source-only                                                                                                    |


**Trace forward/backward:** todo AC tem ≥1 CT; todo CT mapeia a um AC. Sem órfãos.

---

## Avaliação de Cobertura vs Risco


| Área de risco (do SPEC)                                       | Nível                                  | CT cobrindo                                                            | Adequado?                                                                                                              |
| ------------------------------------------------------------- | -------------------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| Regressão de exports pequenos existentes (maior blast radius) | Alto                                   | CT-01, CT-02 (diff de arquivo)                                         | ✅ Reproduzido na UI QA2                                                                                                |
| Guard desabilitado (o fix em si)                              | Alto impacto / baixa reprodutibilidade | CT-08                                                                  | ✅ Reproduzido na UI sandbox (79.605 linhas, ~55.4 MB) — Download CSV desabilitado, tooltip exato, Email CSV habilitado |
| Algoritmo de estimativa de tamanho (`csv-response-size`)      | Alto                                   | CT-01/02 (branch below-limit) + CT-08 (branch above-limit, UI sandbox) | ✅ Ambos os branches cobertos na UI (abaixo: QA2; acima: sandbox ~55.4 MB)                                              |
| Escape hatch Email CSV                                        | Médio-Alto                             | CT-06 (render + modal + gating de Send)                                | ✅ Reproduzido na UI QA2                                                                                                |
| Gate de permissão                                             | Médio                                  | CT-09                                                                  | ⚠️ Source-confirmed; bloqueado por ausência de role                                                                    |
| Descoberta da mensagem de direcionamento                      | Médio                                  | CT-08 (tooltip), CT-05 (controle negativo)                             | ✅ Tooltip reproduzido na UI sandbox (texto exato); controle negativo coberto na UI QA2                                 |
| Tela Leads (nova superfície de export, SSN no arquivo)        | Médio                                  | CT-02 (conteúdo + higiene PII), CT-04 (empty)                          | ✅ Reproduzido na UI QA2                                                                                                |


**Conclusão de cobertura (após run final sandbox):** os caminhos reproduzíveis na QA2 (enabled / empty / escape hatch / permission-independence / filter re-eval) estão **cobertos na UI** e passaram. O coração do fix (>48 MiB ⇒ Download CSV desabilitado + tooltip de direcionamento) foi **fechado na UI** via execução em **sandbox** (79.605 linhas, ~55.4 MB > 48 MB) — não mais source-only. Permanecem source-confirmed apenas: o gate de role sem permissão (CT-09, aguarda provisionamento de conta), o error toast no clique-enquanto-bloqueado, e a boundary `==48 MiB ⇒ habilitado` (Q4, não atingível em dado real). **Não** há gap silencioso nem falso verde. Nenhuma lacuna de cobertura exige devolver ao qa-planner.

---

## Hardening de Automação (4 defeitos de teste corrigidos durante o run — NÃO são bugs de produto)

Durante a estabilização do spec, 4 problemas foram resolvidos. Todos eram **defeitos de automação**, não defeitos da mitigação #1321 `[test-execution:qa2,2026-06-18]`:

1. **Selector casava Email + Download CSV (classe compartilhada).** O selector original, com `.first()`, atingia o Email CSV em vez do Download CSV (os dois botões compartilham classe). Corrigido para alvejar o controle correto.
2. `**setDateRange` preenchia o form de KPI, não o painel da tabela.** A data ia para o formulário errado da página; o painel da tabela tem o `#fromDate` próprio. Corrigido para mirar o painel da tabela.
3. **Painel de filtro da tabela do Overview é um collapsible com re-collapse por load-race.** O painel recolhia sozinho numa corrida de carregamento; resolvido com retry de expand.
4. **Colunas Leads esperadas incluíam "Created from" erroneamente.** A expectativa do teste listava "Created from" como coluna rotulada; na verdade ela exporta com header em branco (ver Observação OBS-01). Corrigido para assertar as 16 colunas rotuladas + a contagem total (17).

> Esses itens são endurecimento de teste (selectors, navegação de filtro, sincronização de UI, expectativa de coluna). Não alteram o veredito do fix #1321.

---

## Observações de Produto

### OBS-01 — Coluna 17 do Leads CSV ("Created from") exporta com header em branco

- **Classificação:** `[OBSERVAÇÃO]` (regra #10) — Melhoria / qa-lens (coluna CSV não-rotulada). Severidade **S4** (cosmético), Prioridade **P3**.
- **Sintoma:** o arquivo `leads-results.csv` tem 17 colunas; a 17ª (chave `createdFrom`, "Created from") é exportada com **célula de header vazia** — os dados da coluna estão presentes, mas o rótulo do header sai em branco.
- **Causa provável:** a entrada de header react-csv dessa coluna tem `{name:"Created from", key:"createdFrom"}` mas **não tem `label`**, então o CSV escreve um header vazio para ela `[dom-snapshot:qa2,2026-06-18,1440x900]`.
- **Pré-existente:** config de coluna **anterior ao #1321** — NÃO introduzida pela mitigação (o #1321 é puramente o guard de tamanho client-side; não toca a definição de colunas do export). `[doc:KB §"Overview vs Leads"]`
- **Por que NÃO é bug do #1321:** sem fresh repro elevando para confirmado contra o escopo do #1321; é um quirk de config de coluna fora do escopo da mitigação. Lente qa-lens: um header de coluna sem rótulo prejudica a legibilidade do CSV para o usuário que abre o arquivo, mas não bloqueia o export nem o fix.
- **Recomendação:** abrir ticket separado para adicionar o `label` ao header `createdFrom`. **Não bloqueia o #1321.** Não abrir ticket sem autorização do usuário.

> Nenhum bug de produto introduzido pelo #1321 foi encontrado. A seção `## Bugs de Aplicação Encontrados` é omitida (regra: só `[CONFIRMADO]` entra; não há nenhum).

---

## Activity Log (regra #13)

**N/A — explicitamente não-aplicável, não é gap.** Esta feature é um **guard de export read-only client-side**: visualizar/filtrar uma listagem e construir um CSV no browser **não** são mutações de estado de negócio e **não** produzem linha em `uown_los_lead_notes` `[doc:SPEC §Activity-log note]` `[skill:activity-log-validation §"Gaps conhecidos"]`. O único side effect candidato seria o *envio* do Email CSV (assíncrono, tipicamente `uown_email_queue`/`uown_correspondence_logs`, não `uown_los_lead_notes`), mas o envio real NÃO é exercido (side effect OUT de escopo — Q3). Portanto não há log a assertar no escopo. Ausência de log aqui é comportamento **correto**, não falha — registrado explicitamente para o validador não sinalizar como falha.

---

## Resumo da Validação


| Item                                                           | Status                                                                            |
| -------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| Fix #1321 (guard de tamanho) valida abaixo do limite           | ✅ PASSOU (UI QA2)                                                                 |
| Export Overview correto (filename, 27 cols, linhas == portal)  | ✅ PASSOU                                                                          |
| Export Leads correto (filename, 17 cols, SSN, higiene PII)     | ✅ PASSOU                                                                          |
| Estados vazios desabilitam ambos os exports (Overview + Leads) | ✅ PASSOU                                                                          |
| Controle negativo: vazio sem tooltip de direcionamento         | ✅ PASSOU                                                                          |
| Escape hatch Email CSV (modal, gating de Send, CANCEL)         | ✅ PASSOU                                                                          |
| Re-avaliação na mudança de filtro (Leads)                      | ✅ PASSOU                                                                          |
| Guard >48 MiB desabilitado + tooltip de direcionamento         | ✅ PASSOU (UI sandbox, 79.605 linhas, ~55.4 MB)                                    |
| Error toast no clique-enquanto-bloqueado                       | ⚠️ [CONFIRMADO na fonte] (estado desabilitado já comprova requisição não emitida) |
| Boundary ==48 MiB ⇒ habilitado (inclusiva)                     | ⚠️ [CONFIRMADO na fonte] (Q4)                                                     |
| Gate de permissão (no-download role)                           | ⚠️ [CONFIRMADO na fonte] + [BLOQUEADO por role]                                   |
| Activity log                                                   | N/A (read-only export — correto)                                                  |
| Bug de produto introduzido pelo #1321                          | Nenhum                                                                            |
| Observação de produto (pré-existente)                          | OBS-01 (header em branco "Created from")                                          |


---

## Pendências / Questões em Aberto para o PO

- **Q1 (sign-off de AC derivados):** os 8 ACs foram **derivados do MR pelo QA**, não escritos pelo PO. Confirmar que são a barra de aceite pretendida ou fornecer ACs formais.
- **Q4 (boundary ==48 MiB):** "exatamente 48 MiB ⇒ habilitado" (inclusivo) — não pode ser atingido deterministicamente em dado real. Proposto: source-confirmed apenas. Confirmar se um caso executável é exigido.
- **Ambiente de alto volume (CT-08): RESOLVIDO.** A **sandbox** com a conta `jmendes.gow` (`SANDBOX_ADMIN_USERNAME`, admin full-visibility) e range `01/01/2022 → 12/31/2026` retorna **79.605 linhas → ~55.4 MB** (> 48 MB) → Download CSV desabilitado + tooltip exato renderizados na UI `[test-execution:sandbox,2026-06-18]`. AC-02 e AC-03(tooltip) **fechados na UI**. (Observação histórica: a investigação anterior do qa-debugger 2026-06-18 viu apenas ~36 linhas porque usou a conta merchant-scoped como supervisor — a conta `admin` full-visibility é a que enxerga as 79.605 linhas.)
- **Role sem permissão (CT-09):** existe conta/role sem permissão de CSV download na QA2? Sem ela, AC-07 (gate) permanece source-confirmed. **Sandbox follow-up (qa-debugger 2026-06-18):** permissão localizada no AMS — `overview download csv` / `leads download csv`, AMS → Roles → aba Origination. Roles sem perm: `agent`, `isr`, `auditor`. Conta candidata: `evedovatto.gow_clone` (role `agent`, sandbox; password desconhecida). Recipe de execução documentado no `.spec.ts` (CT-09 skip block) e no KB §"Download-CSV permission gate". Pendência reduzida a: provisionar conta com password conhecida + criar AMS user/role page-object.
- **Q3 (side effect Email CSV):** o QA deve assertar o *envio* do Email CSV (email real + linha em `uown_email_queue` + copy do toast de sucesso não-capturado) como DoD?
- **Staging (DoD):** os CTs reproduzíveis (CT-01..CT-07) devem rodar também em `stg` antes do fechamento, conforme DoD.

---

## Handoff

Pronto para: **qa-doc-keeper** (último agente do pipeline — sincronizar catálogos/pitfalls; registrar OBS-01 como candidato a ticket; registrar a receita sandbox de alto-volume para CT-08 — conta `admin` full-visibility + range amplo = 79.605 linhas > 48 MB).

Branch >48 MiB **fechado na UI** (sandbox). Pendências restantes: CT-09 (provisionamento de conta sem permissão), error toast no clique, boundary `==48 MiB`, e regressão em Staging (DoD). Sem devolução ao qa-debugger (nenhuma falha) nem ao qa-planner (sem gap de cobertura). Ciclo validator↔debugger: 1/3 (não acionado).

---

**Skills loaded:** `.claude/skills/test-report-standard/SKILL.md`, `.claude/skills/task-evidence-report/SKILL.md`, `.claude/skills/bug-classification/SKILL.md`, `.claude/skills/activity-log-validation/SKILL.md`, `.claude/skills/ui-first-principle/SKILL.md`, `.claude/skills/qa-lens/SKILL.md`, `.claude/skills/check-points/SKILL.md`, `.claude/skills/volatile-knowledge-registry/SKILL.md`, `.claude/skills/acceptance-criteria-review/SKILL.md`, `.claude/skills/risk-based-prioritization/SKILL.md`, `.claude/skills/defect-triage/SKILL.md`