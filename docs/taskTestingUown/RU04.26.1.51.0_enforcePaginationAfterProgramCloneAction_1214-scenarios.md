# Cenários de Teste — Enforce Pagination After Program Clone Action

**Task:** [#1214 — Enforce pagination after program clone action](https://gitlab.com/uown/frontend/origination/-/work_items/1214)
**Milestone:** RU04.26.1.51.0
**Portal:** Origination
**Data:** 2026-04-01

> **Contexto:** Tasks #1214 e #1252 devem ser testadas juntas (comentário de @davi.artur.gow em 2026-03-31).
> - #1252 (`workflow::ready-for-qa`) — rota `/programs/:id` nova
> - #1214 (`workflow::qa-in-process`) — paginação aplicada após redirect de clone de grupo

---

## Análise do Problema

### Bug (#1214)

| Localização | Comportamento (antes do fix) | Comportamento esperado |
|-------------|------------------------------|------------------------|
| `/programs` após clone de grupo | Resultado completo carregado sem paginação (todos os registros) | ≤ 10 linhas exibidas (maxResults=10 padrão) |
| Componente `CloneProgramGroup` | `router.push('/programs')` sem parâmetros de paginação | `router.push('/programs')` com paginação padrão aplicada via store |
| Controle rows-per-page | Estado inconsistente — pode refletir valor anterior | "10" selecionado como padrão |

### Causa Raiz

O `CloneProgramGroup` componente, após submeter com sucesso via `POST /uown/clonePrograms`, chama:
```typescript
router.push('/programs');
```
A navegação não resetava `programStore.paginationSettings.maxResults = 10`, causando carregamento sem paginação se o valor estava alterado em memória.

### Fix

Garantir que `getAllMerchantPrograms()` seja chamado com `maxResults=10` (padrão) quando a navegação para `/programs` ocorre após clone.

---

## User Stories

### US-1214-01: Paginação padrão após redirect de clone de grupo

**Como** admin do Origination,
**Quero** que ao ser redirecionado para `/programs` após clonar um grupo de programas,
**Para** que a lista seja exibida com paginação padrão (10 itens/página), sem carregar o resultado completo que causa lentidão no browser.

**Critérios de Aceite:**
- [ ] Após clonar um grupo de programas, o redirect para `/programs` exibe ≤ 10 linhas
- [ ] O controle "Rows per page" mostra "10" como valor padrão
- [ ] Controles de paginação (Next Page, botões de página) estão visíveis e funcionais
- [ ] Performance: sem congelamento do browser ao carregar a lista após clone

---

## Cenários de Teste

### CT-01: Após Clone Group, /programs exibe ≤ 10 linhas (paginação padrão aplicada)

**Tipo:** E2E
**Portal:** Origination
**Pré-condição:** Usuário autenticado; programa existente em qa2 com `programPk` conhecido; rota `/programs/:id` deployada (task #1252)

**Dados de Teste:**
- `programPk`: obtido via `SELECT pk FROM uown_merchant_program ORDER BY pk ASC LIMIT 1`
- `cloneGroupName`: `TestClone_1214_${Date.now()}` — gerado em runtime, ex: `TestClone_1214_1743523200000`

**Passos:**
1. Login no portal Origination (qa2)
2. Navegar diretamente para `/programs/{programPk}` (usando rota #1252)
3. Aguardar carregamento do formulário de detalhes do programa
4. Verificar que o botão "Clone Group" está visível
5. Clicar em "Clone Group" (abre modal "Clone Program Group")
6. No modal: preencher "New Program Group Name" com `TestClone_1214_${Date.now()}`
7. Clicar em "Select All" para selecionar todos os programas filtrados
8. Clicar "SAVE"
9. Aguardar toast "Programs successfully cloned!"
10. Aguardar redirect automático para `/programs`
11. **Verificar: contar linhas na tabela** (sem alterar rows-per-page)
12. **Verificar: ler valor do controle "Rows per page"**

**Resultado Esperado:**
- Toast "Programs successfully cloned!" visível
- URL final é `/programs` (sem PK — lista, não detalhes)
- `rowCount <= 10` — ≤ 10 linhas exibidas (paginação aplicada)
- Controle `select[aria-label='Rows per page:']` tem valor `"10"` selecionado
- Botão "Next Page" habilitado (confirma que há > 10 programas no total)

**Tags:** `@regression @critical`

---

### CT-02: Paginação funcional após redirect — navegar para próxima página

**Tipo:** E2E
**Portal:** Origination
**Pré-condição:** Continuação do CT-01 (usuário na `/programs` após clone, com 10 linhas exibidas)

**Passos:**
1. (Continuando CT-01) Usuário está em `/programs` com paginação aplicada
2. Verificar que o botão "Next Page" está habilitado
3. Clicar em "Next Page"
4. Aguardar carregamento da próxima página
5. Contar linhas na nova página

**Resultado Esperado:**
- Nova página de programas carrega corretamente
- `rowCount <= 10` — próxima página também tem ≤ 10 linhas
- Dados são diferentes da primeira página (programas distintos)

**Tags:** `@regression`

---

## Mapeamento de Arquivos

| Cenário | Tipo | Arquivo de Teste |
|---------|------|-----------------|
| CT-01 | E2E | `tests/taskTestingUown/RU04.26.1.51.0_enforcePaginationAfterProgramCloneAction_1214/RU04.26.1.51.0_enforcePaginationAfterProgramCloneAction_1214.spec.ts` |
| CT-02 | E2E | (mesmo arquivo, modo serial) |

## Page Object Changes Needed

Em `src/pages/origination/programs.page.ts` — adicionar à seção `// ── Task #1214`:

| Método | Descrição |
|--------|-----------|
| `openCloneGroupModal()` | Clica no botão "Clone Group" |
| `fillCloneGroupName(name)` | Preenche `input[name="programGroupName"]` no modal |
| `selectAllProgramsInModal()` | Clica no checkbox "Select All" |
| `submitCloneGroupModal()` | Clica no botão "SAVE" do modal |
| `waitForCloneGroupSuccess()` | Aguarda toast "Programs successfully cloned!" |
| `getTableRowCount()` | Conta linhas visíveis em `.rdt_TableRow` |
| `getRowsPerPageValue()` | Retorna valor do `select[aria-label='Rows per page:']` |

## Notas de Implementação

1. **Clone cria dados no DB**: o teste cria programas clonados em qa2 com nome de grupo `TestClone_1214_${Date.now()}` — único por execução via timestamp; sem cleanup automático (dados de teste em qa2)
2. **Não chamar `showMaxRows()`** após o redirect — queremos observar o estado natural da paginação
3. **Graceful skip**: se a rota `/programs/:id` (#1252) não estiver deployada, o CT-01 faz skip com mensagem clara
4. **Modo serial**: CT-01 e CT-02 dependem um do outro — usar `test.describe.configure({ mode: 'serial' })`
5. **DB requerido**: apenas para obter `programPk` no `beforeAll` — sem tunnel, testes fazem skip gracioso
