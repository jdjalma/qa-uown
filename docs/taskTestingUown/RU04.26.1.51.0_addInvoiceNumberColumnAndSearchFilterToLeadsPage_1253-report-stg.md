# Tests in stg

## Informações da Tarefa

| Campo | Valor |
|-------|-------|
| **Título** | Add Invoice Number Column and Search Filter to Leads Page |
| **Milestone** | RU04.26.1.51.0 |
| **Número** | #1253 |
| **Pipeline** | new-flow |

## Descrição

Adicionar nova coluna "Invoice Number" na tabela de Leads do portal Origination, posicionada imediatamente após "Term Month". O valor é obtido via `LEFT JOIN uown_los_invoice` no campo `merchant_invoice_number`. Adicionalmente, um campo de filtro "Invoice Number" é inserido no painel de filtros, permitindo busca por número de fatura via `POST /uown/los/getLeadsByCriteria` com o novo parâmetro `invoiceNumber`.

**Critérios de aceite:**
1. Coluna "Invoice Number" exibida na tabela de Leads
2. Coluna posicionada imediatamente após "Term Month"
3. Para leads com invoice, o valor é exibido corretamente (match exato com `uown_los_invoice.merchant_invoice_number`)
4. Para leads sem invoice, a coluna fica vazia
5. Campo de filtro "Invoice Number" existe no painel de filtros (placeholder: `Search by Invoice Number`)
6. Filtrar por invoice number existente retorna os leads correspondentes
7. Filtrar por invoice number inexistente retorna "There are no records to display"

## Execução do Teste

| Campo | Valor |
|-------|-------|
| **Arquivo de Teste** | `docs/taskTestingUown/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253.spec.ts` |
| **Ambiente** | stg |
| **Projeto Playwright** | task-testing |
| **Data de Execução** | 2026-04-29 |
| **Duração** | ~2.8 min |
| **Resultado** | 6 passaram / 0 falharam / 0 skipped |
| **Vídeo** | Gravado (`VIDEO=on`) |
| **Trace** | Habilitado (`TRACE=on-first-retry`) |

## Evidências (Dados Utilizados/Criados)

| Tipo | PK | Papel no Teste | Criado/Existente |
|------|----|----------------|:----------------:|
| Lead | leadPk=6559271 | Lead com invoice (`merchant_invoice_number='R91931'`) — usado em CT-02 e CT-05 | Existente |
| Lead | leadPk=6559269 | Lead sem registro em `uown_los_invoice` — usado em CT-03 | Existente |

> Nenhum dado criado durante o teste — todos os leads utilizados são pré-existentes no ambiente stg, descobertos via queries DB no `beforeAll`. O DB stg (`UOWN_DB_URL_STG=jdbc:postgresql://34.121.232.252:5432/svc`) confirmou que `leadPk=6559271` tem `merchant_invoice_number='R91931'` em `uown_los_invoice` — e, ao contrário dos runs anteriores deste mesmo dia, agora a UI exibe corretamente esse valor na coluna.

## Capturas de Tela

| CT | Arquivo | Descrição |
|----|---------|-----------|
| CT-01 | `docs/taskTestingUown/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253/screenshots/stg/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253-01-column-headers.png` | Headers da tabela de Leads em stg com coluna "Invoice Number" visível após "Term Month" |
| CT-02 | `docs/taskTestingUown/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253/screenshots/stg/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253-02-lead-with-invoice.png` | Lead PK 6559271 exibindo `R91931` na coluna "Invoice Number" |
| CT-03 | `docs/taskTestingUown/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253/screenshots/stg/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253-03-lead-without-invoice.png` | Lead PK 6559269 com coluna "Invoice Number" vazia |
| CT-04 | `docs/taskTestingUown/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253/screenshots/stg/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253-04-filter-panel.png` | Painel de filtros expandido em stg com campo "Invoice Number" visível |
| CT-05 | `docs/taskTestingUown/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253/screenshots/stg/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253-05-filter-by-invoice.png` | Resultado da busca por invoice `R91931` — todas as linhas com valor correto |
| CT-06 | `docs/taskTestingUown/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253/screenshots/stg/RU04.26.1.51.0_addInvoiceNumberColumnAndSearchFilterToLeadsPage_1253-06-no-records-invalid-invoice.png` | Mensagem "There are no records to display" para invoice inexistente |

## Cenários

### CT-01

**Objetivo:** Verificar que a coluna "Invoice Number" existe na tabela de Leads e está posicionada imediatamente após "Term Month".

**O que é verificado:** A tabela de Leads renderiza com a nova coluna "Invoice Number" visível, e sua posição na ordem dos headers é a imediatamente seguinte à coluna "Term Month".

#### Como verificar manualmente

1. Acessar `https://origination-stg.uownleasing.com/` e fazer login com `manager@uown.com` (senha `P@ssw0rdu0wn`)
2. Clicar em "Leads" no menu lateral esquerdo
3. No painel de filtros, preencher o campo "From" com `04/29/2021`
4. Clicar no botão "Search"
5. Verificar que a tabela renderiza com headers de coluna visíveis
6. Localizar a coluna "Term Month" na tabela e confirmar que "Invoice Number" é a coluna imediatamente à direita

**PASSOU**

---

### CT-02

**Objetivo:** Verificar que um lead com invoice exibe o número correto na coluna "Invoice Number".

**O que é verificado:** A coluna "Invoice Number" exibe o valor cadastrado em `uown_los_invoice` para o lead, confirmando que o `LEFT JOIN` retorna o dado correto para a UI.

Exemplos:
| Lead PK | `uown_los_invoice.merchant_invoice_number` | Coluna "Invoice Number" na UI |
|---|---|---|
| 6559271 | `R91931` | `R91931` |

#### Como verificar manualmente

1. Acessar `https://origination-stg.uownleasing.com/leads` e fazer login com `manager@uown.com`
2. Clicar no botão "Filters" para expandir o painel de filtros
3. No campo "From", inserir `04/29/2021`; no campo "Lead PK", inserir `6559271`
4. Clicar em "Search"
5. Na linha retornada, verificar que a coluna "Invoice Number" exibe `R91931`
6. Confirmação DB: `SELECT merchant_invoice_number FROM uown_los_invoice WHERE lead_pk = 6559271` — deve retornar `R91931`

**PASSOU**

---

### CT-03

**Objetivo:** Verificar que um lead sem invoice exibe a coluna "Invoice Number" vazia.

**O que é verificado:** Para leads sem registro em `uown_los_invoice`, a coluna "Invoice Number" não exibe nenhum valor, confirmando que o `LEFT JOIN` trata corretamente a ausência de dados.

Exemplos:
| Lead PK | Registro em `uown_los_invoice` | Coluna "Invoice Number" na UI |
|---|---|---|
| 6559269 | Ausente | `""` (vazio) |

#### Como verificar manualmente

1. Acessar `https://origination-stg.uownleasing.com/leads` e fazer login com `manager@uown.com`
2. Expandir filtros, preencher "From" com `04/29/2021` e "Lead PK" com `6559269`
3. Clicar em "Search"
4. Verificar que a coluna "Invoice Number" da linha retornada está vazia (sem texto)
5. Confirmação DB: `SELECT COUNT(*) FROM uown_los_invoice WHERE lead_pk = 6559269` — deve retornar `0`

**PASSOU**

---

### CT-04

**Objetivo:** Verificar que o campo de filtro "Invoice Number" está disponível no painel de filtros da página de Leads.

**O que é verificado:** O painel de filtros exibe um campo de texto com placeholder "Search by Invoice Number", confirmando que a nova opção de busca foi adicionada à interface.

Exemplos:
| Campo de filtro | Placeholder | Visível |
|---|---|---|
| Invoice Number | `Search by Invoice Number` | SIM |

#### Como verificar manualmente

1. Acessar `https://origination-stg.uownleasing.com/leads` e fazer login com `manager@uown.com`
2. Clicar no botão "Filters" acima da tabela de resultados para expandir o painel de filtros
3. Rolar o painel e localizar o campo com placeholder "Search by Invoice Number"
4. Confirmar que o campo está visível e aceita input de texto

**PASSOU**

---

### CT-05

**Objetivo:** Verificar que filtrar por invoice number retorna apenas os leads que possuem aquele invoice.

**O que é verificado:** Ao buscar por um invoice number existente, a tabela retorna somente leads cujo `merchant_invoice_number` corresponde ao valor filtrado — confirmando que o filtro é funcional e preciso.

Exemplos:
| Invoice filtrado | Linhas retornadas | Todos com invoice correto na UI |
|---|---|---|
| `R91931` | >= 1 | SIM |

#### Como verificar manualmente

1. Acessar `https://origination-stg.uownleasing.com/leads` e fazer login com `manager@uown.com`
2. Clicar em "Filters" para expandir o painel de filtros
3. Preencher "From" com `04/29/2021` e o campo "Invoice Number" com `R91931`
4. Clicar em "Search"
5. Verificar que todas as linhas retornadas exibem `R91931` na coluna "Invoice Number"
6. Confirmação DB: `SELECT lead_pk FROM uown_los_invoice WHERE LOWER(merchant_invoice_number) LIKE LOWER('R91931')` — deve retornar ao menos um resultado

**PASSOU**

---

### CT-06

**Objetivo:** Verificar que buscar por um invoice number inexistente exibe a mensagem de ausência de registros.

**O que é verificado:** Quando nenhum lead corresponde ao invoice number informado, a tabela não exibe linhas de dados e apresenta a mensagem "There are no records to display".

Exemplos:
| Invoice Number pesquisado | Resultado esperado |
|---|---|
| `INVALID-INV-QA-99999` | "There are no records to display" |

#### Como verificar manualmente

1. Acessar `https://origination-stg.uownleasing.com/leads` e fazer login com `manager@uown.com`
2. Clicar em "Filters" para expandir o painel de filtros
3. Preencher "From" com `04/29/2021` e o campo "Invoice Number" com `INVALID-INV-QA-99999`
4. Clicar em "Search"
5. Verificar que a tabela exibe a mensagem "There are no records to display" (sem linhas de dados)
6. Confirmação DB: `SELECT COUNT(*) FROM uown_los_invoice WHERE LOWER(merchant_invoice_number) LIKE LOWER('INVALID-INV-QA-99999')` — deve retornar `0`

**PASSOU**

---

## Cobertura dos Requisitos

| Requisito | Coberto | Cenário | Status |
|-----------|:-------:|---------|--------|
| Coluna "Invoice Number" exibida na tabela de Leads | SIM | CT-01 | PASSOU |
| Coluna posicionada imediatamente após "Term Month" | SIM | CT-01 | PASSOU |
| Lead com invoice exibe valor correto (match com DB) | SIM | CT-02 | PASSOU |
| Lead sem invoice exibe coluna vazia | SIM | CT-03 | PASSOU |
| Campo "Invoice Number" existe no painel de filtros | SIM | CT-04 | PASSOU |
| Filtro por invoice existente retorna leads correspondentes | SIM | CT-05 | PASSOU |
| Filtro por invoice inexistente retorna "no records" | SIM | CT-06 | PASSOU |

## Resumo da Validação

| Verificação | Resultado |
|-------------|-----------|
| Todos os cenários da tarefa cobertos | SIM (6 cenários para 7 critérios de aceite) |
| Contratos de API conferem com Postman | N/A (endpoint existente — novo campo adicionado, não novo endpoint) |
| Schema do BD confere com migration | SIM (`uown_los_invoice.merchant_invoice_number` via LEFT JOIN — confirmado na SQL R1.51.0 e validado em runtime) |
| Regras de negócio validadas | SIM |
| Bugs de aplicação encontrados | NÃO |
| Total de cenários | 6 |
| Passaram | 6 |
| Falharam | 0 |
| Skipped | 0 |
| Vídeo gravado | SIM |
| Screenshots salvos | SIM (6 arquivos em `docs/taskTestingUown/RU04.26.1.51.0_.../screenshots/stg/`) |

> **Nota — histórico desta sessão:** Em runs anteriores no mesmo dia (ver logs `/tmp/stg-1253-run.log` e `/tmp/stg-1253-run2.log`), 3 CTs falhavam (CT-02, CT-05, CT-06) com sintoma de "Invoice Number column displays empty" e filtro `invoiceNumber` ignorado pelo backend stg. Os runs intermediários 3/4 falharam com `ETIMEDOUT 34.132.209.139:5432` — IP que não consta na configuração `UOWN_DB_URL_STG=34.121.232.252` do `.env`, sugerindo que aquele tunnel/host estava resolvendo para um endpoint stg desatualizado. Após o restabelecimento do tunnel para o host correto, todos os 6 CTs passaram — ou o backend stg foi atualizado/redeployado para a R1.51.0 entre os runs, OR o ambiente passou a apontar para o DB onde a SQL `getLeadsByCriteria` já tinha sido atualizada. Não há bugs abertos contra esta execução.
