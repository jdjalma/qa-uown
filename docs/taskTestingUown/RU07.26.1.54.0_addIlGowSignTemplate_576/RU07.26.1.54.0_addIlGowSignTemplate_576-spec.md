# SPEC — RU07.26.1.54.0_addIlGowSignTemplate_576

> This file is a planning artifact, NOT a source of patterns. Patterns live in `.claude/skills/` and `src/`.

## Source

- Ticket capture (extracted source of truth): `docs/taskTestingUown/RU07.26.1.54.0_addIlGowSignTemplate_576/RU07.26.1.54.0_addIlGowSignTemplate_576.md`
- GitLab: https://gitlab.com/uown/backend/svc/-/work_items/576 — "UOWN | SVC | Add IL - Illinois GowSign Template"
- Milestone: `Uown | RU07.26.1.54.0` · Labels: `priority::high`, `type::development`, `workflow::ready-for-qa` · Project: `uown/backend/svc`
- Template under test: **`IL_2025_SAC_16_MONTHS`** (Illinois, 16-month SAC), pk39, `template_id=r2t4ada62hdsmojymwipdfyv`, `client_type=null`. Negative control: **`IL_2025_SAC`** (13m), pk38.

## BDD Oracle status (rule #19) — PRE-CHECKED

A registered oracle already exists for this exact work item and is cited throughout instead of re-deriving checkpoints (rule #16):

- Oracle file: `.claude/oracles/il-16m-gowsign-template.md` — scenarios **CT-01..CT-08**, registered for work item #576, `last-reviewed: 2026-06-30`, `last-reviewed-sha: cd2d2c8`.
- Index row: `_index.md` → `il-16m-gowsign-template`.

**Staleness check (rule #19(a)) — run this session:**

```bash
git log cd2d2c8..HEAD -- docs/knowledge-base/illinois-gowsign-template.md \
  src/helpers/gowsign-template-db.helpers.ts src/data/state-merchant-matrix.ts \
  src/helpers/gowsign-signing.helper.ts src/helpers/esign-db.helpers.ts
```

Output: **`3b8ce82`** touched `docs/knowledge-base/illinois-gowsign-template.md` (190 insertions — the KB file's own creation commit) after the review SHA.

> **`[BDD MAY BE STALE — docs/knowledge-base/illinois-gowsign-template.md changed since cd2d2c8]`**
> Assessment: **benign**. `3b8ce82` is the commit that *created* the discovery KB the oracle covers; both carry `last_verified/last-reviewed: 2026-06-30` and their content is mutually consistent (same template inventory pk38/pk39, same NeuroID blocker, same CT structure). No other covered file (helpers, matrix) changed. Recommendation for `qa-doc-keeper` at pipeline close: re-anchor the oracle frontmatter `last-reviewed-sha` to current HEAD to clear the false-positive flag. Until re-anchored, downstream agents must treat CT PASS results as unconfirmed per the protocol.

**Coverage vs the ticket ACs — the oracle is complete; no extension needed:**

| Ticket AC | Oracle CT | Notes |
|---|---|---|
| AC1 — Item 4 Promotional-Payoff Option | CT-02 | 1:1 |
| AC2 — Item 4a Lease-Purchase Ownership | CT-03 | 1:1 |
| AC3 — Lease purchase plan footnote | CT-05 | 1:1 |
| AC4 — EARLY PURCHASE OPTION appendix (singular) + page order | CT-06 | 1:1 |
| AC5 — Sanity §1–§14 vs standard IL SAC | CT-07 | 1:1 |
| AC6 — Runtime/PDF smoke (logo, tokens, 16m routing, non-16m regression) | CT-04 (tokens + logo) + CT-01 (16m→`IL_2025_SAC_16_MONTHS`, 13m→`IL_2025_SAC` negative control) | split across two CTs |
| — (rule #13, implicit) | CT-08 | signing completion + activity log; not an explicit ticket AC but mandatory |

No gaps → the oracle is **cited**, not rewritten.

## Scope

**IN:**
- Rendered content of `IL_2025_SAC_16_MONTHS` in the live GowSign iframe — Item 4, Item 4a, plan footer, singular EPO appendix, §1–§14 sanity (AC1–AC5 / CT-02..CT-07). UI-first (rule #14): iframe/PDF render cannot be validated by log reading.
- Token render smoke: `costPriceWithFeeNoTax`, `nextPaymentDueAmount`, `contractAmount`, `companyInfoBrandPhone` non-empty, zero `{{…}}`, logo present (AC6 / CT-04).
- Routing decision table: IL 16m → `IL_2025_SAC_16_MONTHS`, IL 13m → `IL_2025_SAC`, both `client=GOWSIGN` (AC6 / CT-01). **The non-16m regression control is IN scope** — do not drop it (per task note).
- Signing completion + activity log (rule #13 / CT-08) for whatever ceremony is reachable.

**OUT:**
- SignWell provider (IL has GowSign templates for both terms → routes GowSign; no SignWell path for IL). Standard cross-provider SignWell regression (`gowsign-knowledge`) does not apply here because the change is a state-scoped GowSign template addition, not a provider config change.
- Other state templates (#579 IN, #568 VA, #557 TN, #583 MA) — sibling tickets, separate SPECs (rule #2: don't recreate; reuse the AL/OH sibling pattern).
- 13m `IL_2025_SAC` *content* verification beyond its role as CT-01 negative control and CT-07 sanity reference — the ticket is about the 16m template.
- EPO numeric-cascade math correctness (that is `04-calculos-financeiros.md#epo-calculation` territory) — this ticket verifies *contract copy/token rendering*, not the calculator output values.
- qa2 IL template availability — UNVERIFIED (KB: tunnel down at discovery). Re-list with `getGowSignTemplatesForState` before asserting in qa2.

**AMBIGUOUS / Questions for PO:** see Open Questions. Note DoR flag: the GitLab description was empty; ACs are derived from Gustavo Martins' 2026-06-26 "Testing Steps" comment. Under the delegation gate this is AUTO (derived ACs are testable and 1:1-mapped to an existing oracle), proceeding with that documented.

## AC Coverage

Every ticket AC maps to a registered oracle checkpoint (table above). The oracle's MUST / MUST NOT content lists (Item 4 daily-accrual narrative; Item 4a `[totalNumberOfPayments]` payments of `$ [nextPaymentDueAmount]` tail; footer footnotes; singular EPO appendix + 5-bullet formula + Item-14-follows page order; §1–§14 sanity) are the authoritative assertions — see `.claude/oracles/il-16m-gowsign-template.md` §Oracles. `qa-implementer` inherits those checkpoints; it does not invent pass criteria.

## Risk Analysis

| Area | Risk | Why | Coverage |
|---|---|---|---|
| **NeuroID automation blocker** | **S1 — blocks CI** | Only route to an IL 16m lease is a Kornerstone ONLINE merchant on `secure-stg.kornerstoneliving.com`, gated by NeuroID. Both `submitApplication` (API) and browser auto-advance are blocked; the reload→"Sign Contract" bypass is proven **only MCP-manual**, no page object exists. CT-02..CT-06 + CT-08 cannot run headless today. | Strategy split (below) + Open Question escalated |
| Token rendering regression (16m migration) | High | AL 16m sibling had `nextPaymentDueAmount` render **blank** in Item 3/4a — a confirmed migration regression. IL is the same template family. | CT-03 + CT-04 (non-empty assert + dispatch-note check for "variables map missing") |
| State-scoped contract copy (novelty) | High | Brand-new template (`IL_2025_SAC_16_MONTHS`); repo had zero `IL_2025_SAC` references before this task → genuine greenfield. Customer-facing, regulated (legal lease document). | CT-02, CT-03, CT-05, CT-06 |
| Wrong-template routing | High | Program term (WK16 vs WK13), not SSN suffix, drives selection; stale `state-merchant-matrix.ts` still says IL=SIGNWELL (drift). Wrong routing = customer signs the wrong contract. | CT-01 (positive + negative control) |
| §1–§14 state-law copy drift | Medium | 16m template must not leak standard-IL §4 day-count promo, §4a %-discount EPO, or VA/TN reinstatement wording. | CT-07 |
| Page-order / duplication | Medium | EPO appendix must be singular and Item 14 must follow immediately, no duplicate page. | CT-06 |
| Signing + activity log (rule #13) | Medium | No log = nothing happened; migration could drop notes. | CT-08 |

Risk-dimension note (`risk-based-prioritization`): CT-01/CT-03/CT-04 score highest (novelty 3 + vendor integration 2 + customer-facing regulated 3 + recent-regression history on the AL sibling). CT-02/CT-05/CT-06 are P0 by the floor rule "touches a GoSign template of state X → P0 includes visual verification". All P0/P1 — nothing deferred.

## Test Strategy

**Approach:** Hybrid, UI-first (rule #14), split by the NeuroID constraint into two execution lanes.

**Justification:** The deliverable is *visual PDF/iframe content* — logo, tokens, exact contract copy, page order. This is the exact class the Daniel's Jewelers CA bug (BUG-01, 2026-05-06) proved cannot be validated by backend log reading. Iframe render is mandatory. Setup is via API only as a precondition accelerator (`sendApplication` / `sendInvoice`), never as a substitute for the render.

**Lane A — Automatable in CI (no NeuroID):**
- Path: **TerraceFinance `OL90202-0001`** (ONLINE, UOWN gateway `secure-stg.uownleasing.com`, no NeuroID), customer state IL.
- Covers: CT-01 **negative control** (13m → `IL_2025_SAC`, `client=GOWSIGN`) and CT-07 **sanity reference** (the standard IL SAC body the 16m template is diffed against). Full render + sign with existing helpers (`createPreQualifiedApplication` → `sendInvoice` → `MissingDataFormPage`/`ContractCompletePage` → `TermsOfAgreementPage` → `AlternativeContractModalPage` → `signGowSignInFrame`).
- Constraint: TerraceFinance ONLINE + IL reaches **only the 13m** template — so Lane A does NOT reach the 16m-specific content (Item 4/4a/EPO appendix). It anchors routing + sanity only.

**Lane B — MCP-manual only (16m content, the ticket's core):**
- Path: **Kornerstone ONLINE (KS3015 "5th Ave Furniture (NY)"**, `valid_states` includes IL, programs `['13 month','16 month']`), customer state IL, `bankData=TEST_BANK` (routing `123456780`, account `160781900000` — Kornerstone requires it or 400), pick **planId=WK16**.
- Covers: CT-02..CT-06 (16m content) + CT-01 positive (16m → `IL_2025_SAC_16_MONTHS`) + CT-08 (signing + log).
- **Blocked from CI:** `submitApplication` returns `error:"Failed to verify identification."` (NeuroID), no `uown_esign_document` created, lead stuck `UW_APPROVED` (proven stg lead 7218278). Browser auto-advance also NeuroID-blocked. The reload→"Sign Contract" bypass works **only via Playwright MCP, manually** — there is no page object for the Kornerstone reload surface.
- Execution: `qa-validator` / analyst drives KS3015 IL 16m in MCP Playwright, fills the kornerstoneliving `/complete` CC form, reload → "Sign Contract" → Terms → Proceed → reads the GowSign iframe body, validates CT-02..CT-06/CT-08 checkpoints, and **freezes the exact canonical wording** so the oracle's CT-02..CT-06 promote from `[EXPECTED]` to `[CONFIRMED]`.

**Environments:** `stg` throughout (discovery env; DoD requires stg regardless). qa2 IL availability UNVERIFIED — re-list templates before any qa2 run. Sandbox/local 5445 tunnel were DOWN at discovery.

**Suites to activate:** state-scoped GowSign content — `gowsign-contract-content*` family pattern for Lane A. No SignWell visual diff (IL is GowSign-only). No dual-brand (this is a single state-template addition, not a `submitApplication` change).

**Merchant preflight (rule #12):** Lane A/B both create fresh leads, but on **special ONLINE merchants** (TerraceFinance, KS3015) whose config is not the standard `merchant-config-contract.ts` INSTORE contract. Do **not** run the standard preflight auto-heal against these merchants (it would mutate out-of-scope ONLINE config = side effect). Pass `skipMerchantPreflight: true`; instead assert via `getGowSignTemplatesForState(db,'IL')` that pk38/pk39 exist before driving. `bankData` is the real precondition for Kornerstone, not a checkbox contract.

**Data (rule #9):** fresh IL lead per run; no reuse of existing leads. Money assertions use `toBeCloseTo` (float repr, not `toEqual`).

## Scenarios (prioritized)

> All checkpoints are defined authoritatively in `.claude/oracles/il-16m-gowsign-template.md`. Below is the prioritized execution plan + persona framing; do not duplicate the MUST/MUST NOT lists here — read the oracle.

### CT-01 — Routing 16m vs 13m [P0, Lane A negative control automatable / Lane B positive MCP-manual]
- **Technique:** Decision table — state IL × term {13, 16} → {`IL_2025_SAC`, `IL_2025_SAC_16_MONTHS`}, both `client=GOWSIGN`.
- **Persona:** Agent (Origination) selecting the plan; customer receives the correct contract.
- **Setup:** template inventory assert (`getGowSignTemplatesForState`); IL lead via KS3015 (WK16) and via TerraceFinance/KS3015 (WK13).
- **Exercise/Validate:** oracle CT-01 table — 16m selection requires reaching CONTRACT_CREATED (`assertSelectedTemplateForLead`); 13m control fully automatable via TerraceFinance.
- **Pitfalls:** esign doc row only exists at CONTRACT_CREATED (not UW_APPROVED); stale `state-merchant-matrix.ts` IL=SIGNWELL is drift — trust live DB.

### CT-02 — Item 4 Promotional-Payoff Option [P0, Lane B MCP-manual]
- **Technique:** Content assertion (MUST heading "Promotional-Payoff Option:" + daily-accrual narrative / MUST NOT day-count, expiry, promo wording, `{{epoDays}}`/`{{epoExpiryDate}}`).
- **Persona:** Customer reading the contract on the signing screen.
- **Validate:** oracle CT-02 checkpoints against normalized iframe `body.innerText()`.

### CT-03 — Item 4a Lease-Purchase Ownership [P0, Lane B MCP-manual]
- **Technique:** Regex on the ownership tail (`[totalNumberOfPayments]` payments of `$ [nextPaymentDueAmount]` (plus tax); Total `$ [contractAmount]` (including tax)(including the application fee); "Total Cost," + "and you will own the Property").
- **Regression watch:** `nextPaymentDueAmount` MUST be non-empty (AL 16m rendered it blank). MUST NOT show %-discount, TN or VA wording.
- **Validate:** oracle CT-03.

### CT-04 — Token render smoke + logo [P0, Lane B MCP-manual; token-presence partially cross-checkable via `uown_esign_document.request.document.variables`]
- **Technique:** Error guessing (migration token drop) + cross-source value match.
- **Validate:** oracle CT-04 — every monetary token non-empty and matching the variables map (float tolerance), zero `{{…}}`, logo `<img>` at top, absence of `[DocumentDispatchService][GowSign] … variables map missing … token(s)` note for the lead. (Covers AC6 render half.)

### CT-05 — Lease-purchase-plan footer [P0, Lane B MCP-manual]
- **Validate:** oracle CT-05 — "Traditional [N] Month Lease Purchase Plan*", "*with a Promotional Payoff option", "Early Purchase Option Available Off Unpaid Balance", populated grid, both footnotes; MUST NOT show EPO payoff chart or numbered Options 1 & 2.

### CT-06 — EARLY PURCHASE OPTION appendix (singular) + page order [P0, Lane B MCP-manual]
- **Validate:** oracle CT-06 — singular heading, "16-month term for ownership", "purchase … at any time", the 5-bullet formula, "call us at [phone]"; MUST NOT show plural OPTIONS / Option 1/2 / promo deadline / sub-points a–e / customer initials on this page; Item 14 follows immediately, no duplicate page.
- **Pitfall:** watch the line break `16-\nmonth` (AL/OH sibling) — an exact `"16-month"` match fails.

### CT-07 — Sanity §1–§14 vs standard IL SAC [P1, Lane A reference automatable / diff MCP-manual]
- **Technique:** State-transition-free copy diff; divergence reported as `[finding]`, not auto-pass.
- **Validate:** oracle CT-07 full MUST/MUST NOT list (§1 no "Damage to Property"; §2 "Total" label; §3 delivery-date term start; §6 late-fee/collection amounts; §7 reinstatement 16/60 not VA 5/21/45; §8 wording; §14 "prior to the last payment due date").

### CT-08 — Signing completion + activity log (rule #13) [P0, Lane B MCP-manual]
- **Persona:** Customer completes the GowSign ceremony; agent sees SIGNED.
- **Validate:** oracle CT-08 — lead `SIGNED`; `uown_esign_document` progression `SENT_TO_CUSTOMER → SIGNED → STORED` with `doc_signed_time_stamp`; notes `[ContractService][isLeaseOrLeaseModSigned]…SIGNED`, `[EsignRedirectService][updateSignStatus]…to SIGNED`, `[ESIGNSERVICE][parseCCPeekConsent]`.
- **Post-signature gotcha:** `uown_esign_document.request` is overwritten to the string `getDocumentStatus` — query the post-sign row by `lead_pk + client='GOWSIGN'`, not `request LIKE '{%'`.

## User Journey Perspective

- **Customer (Website / customer-facing signing surface, real mobile):** clicks the merchant/Kornerstone link → `/complete` CC pre-auth → reviews the lease → Sign Contract → reads the GowSign iframe (small screen, scroll) → signs → sees completion. The CT-02..CT-06 copy is *what this customer reads and legally agrees to* — the iframe render is the only place these bugs surface. Kornerstone host adds the NeuroID gate the real customer passes behaviorally but automation cannot.
- **Agent (Origination):** selects the plan (WK16/WK13) that drives template routing (CT-01), watches the lead advance to SIGNED (CT-08).
- **Cross-portal assertion:** customer signs → agent sees SIGNED badge + activity-log line (rule #13). No Servicing-side scope for this ticket.
- **Break points:** customer on mobile abandons mid-signing; NeuroID "Failed to verify identification" (the real-world manifestation of the automation blocker) — a real customer retries and passes; automation cannot.

## Out-of-scope decisions

- SignWell regression / visual diff — IL routes GowSign for both terms; no SignWell path. (Deviates from the general `gowsign-knowledge` "SignWell regression mandatory" rule *only* because there is no SignWell IL route; documented, not skipped by omission.)
- Dual-brand — not a `submitApplication` change; single state-template addition.
- EPO calculator numeric correctness — this ticket verifies contract copy/token rendering, not calculator math.
- Sibling state templates (#579/#568/#557/#583) — separate SPECs.

## Open questions

1. **[BLOCKER for full CI coverage — already escalated per KB]** NeuroID gate on the Kornerstone host blocks automated IL 16m signing. Is a **test-merchant NeuroID bypass in stg** feasible? Without it, CT-02..CT-06 + CT-08 remain MCP-manual and cannot become CI regression. (Owner: PO/dev + ops.)
2. Should a **page object for the Kornerstone reload → "Sign Contract" surface** be built (would let Lane B be automated *if* NeuroID is bypassed)? Currently none exists.
3. **qa2 IL template availability** is UNVERIFIED (tunnel down at discovery). Confirm pk38/pk39 exist in qa2 before running there; DoD needs stg regardless.
4. The **exact canonical wording** of Item 4 / Item 4a / footer / EPO appendix must be frozen from a real `IL_2025_SAC_16_MONTHS` render (MCP-manual) to promote oracle CT-02..CT-06 from `[EXPECTED]` to `[CONFIRMED]`. Until then, those checkpoints assert against sibling-derived expectations, not IL-confirmed copy.
5. `appendix-h-epo-template-registry.md` has **no IL entry** — add one once the 16m EPO wording is frozen (qa-doc-keeper follow-up).

## Handoff

**Ready for: qa-implementer** — for **Lane A** (CT-01 negative control + CT-07 sanity reference via TerraceFinance IL 13m, fully automatable) and the routing/token cross-checks that do not require the 16m render.

**Escalated / MCP-manual: qa-validator** — for **Lane B** (CT-02..CT-06 16m content + CT-01 positive + CT-08 signing) via Playwright MCP on KS3015 IL 16m using the reload→"Sign Contract" bypass, to validate the oracle checkpoints and freeze canonical wording. This lane is blocked from CI by NeuroID (Open Question 1) — surface the blocker verdict in the evidence report.

**Then: qa-doc-keeper** — re-anchor the oracle `last-reviewed-sha` (clear the stale flag), add the IL entry to `appendix-h`, and promote CT-02..CT-06 to `[CONFIRMED]` once wording is frozen.

**Skills loaded:**
- `.claude/skills/scope-analysis/SKILL.md`
- `.claude/skills/acceptance-criteria-review/SKILL.md`
- `.claude/skills/risk-based-prioritization/SKILL.md`
- `.claude/skills/test-strategy-decision/SKILL.md`
- `.claude/skills/test-design-techniques/SKILL.md`
- `.claude/skills/user-journey-perspective/SKILL.md`
- `.claude/skills/test-plan-template/SKILL.md`
- `.claude/skills/check-points/SKILL.md`
- `.claude/skills/qa-lens/SKILL.md`
- `.claude/skills/gowsign-knowledge/SKILL.md`
- `.claude/skills/ui-first-principle/SKILL.md`
- `.claude/skills/activity-log-validation/SKILL.md`
- `.claude/oracles/_index.md`
- `.claude/oracles/il-16m-gowsign-template.md`
- `docs/knowledge-base/illinois-gowsign-template.md`
- `docs/business-rules/appendix-g-cenarios-risco.md` (§1 state determination — ONLINE=customer state / INSTORE=merchant state)
