## Issue: UOWN | SVC | Add IL - Illinois GowSign Template

- URL: https://gitlab.com/uown/backend/svc/-/work_items/576
- Labels: `priority::high`, `type::development`, `workflow::ready-for-qa`
- Milestone: `Uown | RU07.26.1.54.0`
- Issue Number: 576
- Project: `uown/backend/svc`
- Assignee/Author: Gustavo Martins
- State: opened
- Suggested pipeline: custom (no `e2e`/`api`/`bug`/`refactor`/`docs` label match — GowSign PDF template content verification, UI-first per rule #14)

## Description

Empty in GitLab (no formal description). QA testing steps were provided as an issue comment (2026-06-26) — used below as the de-facto AC source. Template under test: IL 16-Month SAC.

## Detected ACs

> **DoR FLAG:** issue description is empty — no formal AC. ACs below are derived from Gustavo Martins' "IL 16-Month SAC — Testing Steps" comment.

- **AC1 (Step 1 — Item 4, Promotional-Payoff Option):** heading "Promotional-Payoff Option:" (not "[X] Day Promotional Payoff Option"); buy at any time (no "during the first X days" window); `$ [costPriceWithFeeNoTax]` plus tax, minus on-time payments, **plus daily lease fees through exercise date**; late payments void the option. Must NOT show "This option expires on [date]", "Cash Price, [epoFeeText]"/"Cash Price of the item(s)" promo wording, "You have other purchase options described below", or unpopulated/populated `{{epoDays}}` / `{{epoExpiryDate}}`.
- **AC2 (Step 2 — Item 4a, Lease-Purchase Ownership):** must be current to elect EPO; EPO = daily lease charges through exercise date + fees/taxes − on-time payments; IL ownership tail exact wording: `[totalNumberOfPayments]` payments of `$ [nextPaymentDueAmount]` **(plus tax)**, total `$ [contractAmount]` **(including tax) (including the application fee)**, quoted "Total Cost," then "and you will own the Property". Must NOT show X% discount on remaining lease payments, TN-style "plus $ [salesTax] in sales tax" + separate "processing fee", or VA-style reinstatement/fee language.
- **AC3 (Step 3 — Lease purchase plan footnote):** "Traditional [N] Month Lease Purchase Plan"; "*with a Promotional Payoff option"; "Early Purchase Option Available Off Unpaid Balance"; populated `[leasePurchasePlan]` grid; footnotes `* These amounts include sales tax…` and `** Total Cost includes sales tax, application fee and delivery fee.`. Must NOT show an EPO payoff chart/table or numbered promotional/EPO option list (Options 1 & 2).
- **AC4 (Step 4 — EARLY PURCHASE OPTION appendix, singular):** "This Agreement has a 16-month term for ownership."; "you may purchase the Property at any time before the end of the lease term."; single formula intro + the same 5-bullet formula (cost → taxes → fees → daily accrual through exercise date → minus payments); "call us at `[phone]`". Must NOT show plural "EARLY PURCHASE OPTIONS", Option 1/2 structure, promo deadline, sub-points a.–e., or customer initials on this page. Page-order check: next section is Item 14 (ACH Payments), and the EPO page must not be duplicated elsewhere.
- **AC5 (Step 5 — Sanity check vs standard IL SAC, regression on non-EPO clauses):**
  - §1: no "Damage to Property: ___" line; delivery footnote "*(*) Total delivery fee"
  - §2: Processing Fee + LDW (N/A) lines present; label is "Total" (not "Total Initial Payment")
  - §3: term starts on delivery date (not paycheck); "Your Regular lease rate $ …" (no "is" before `$`)
  - §6: intro "These charges are reasonably related…"; (b) Late Fee $5 within 3 days; (c) In-Home Collection $10.00
  - §7: agreement expires on missed payment; reinstatement 16 days / 60 days after return (not VA's 5/21/45)
  - §8: "You are lease the Property" (matches source doc wording)
  - ACH debits: remaining payments use `nextPaymentDueAmount` (not `WithTax`)
  - §14 CANCELLATION: "prior to the last payment due date"
  - Must NOT show: standard IL §4 day-count promo/`epoExpiryDate`, standard IL §4a %-discount EPO, or appendix Options 1/2 with initials.
- **AC6 (Step 6 — Runtime/PDF smoke test):** generate one IL 16-month lease in GowSign; logo renders; `costPriceWithFeeNoTax`, `nextPaymentDueAmount`, `contractAmount`, `companyInfoBrandPhone` populate (no blank `{{…}}`); 16-month plan routes to this template (not standard `IL_2025_SAC`); non-16-month IL lease still uses the standard IL SAC.

## Relevant comments

- **Gustavo Martins (2026-06-26):** full "IL 16-Month SAC — Testing Steps" walkthrough (Steps 1–6 + pass/fail summary table) — captured verbatim in Detected ACs above. Primary QA validation script for this ticket.

## Test naming

- describe: `RU07.26.1.54.0_addIlGowSignTemplate_576`
- file: `RU07.26.1.54.0_addIlGowSignTemplate_576.spec.ts`

## Next step

Forward to `qa-planner` with this context. Per rule #14 (UI-first) and rule #19 (BDD Oracle), visual/PDF rendering of the GowSign iframe cannot be validated via backend log reading alone — requires MCP Playwright inspection of the rendered PDF for an IL 16-month lease, plus a regression check that non-16-month IL leases still route to the standard `IL_2025_SAC` template (AC6). Cross-reference [[gowsign-knowledge]] and sibling tickets #579 (IN), #568 (VA), #557 (TN), #583 (MA) in this same milestone (rule #2: don't recreate existing artifacts).
