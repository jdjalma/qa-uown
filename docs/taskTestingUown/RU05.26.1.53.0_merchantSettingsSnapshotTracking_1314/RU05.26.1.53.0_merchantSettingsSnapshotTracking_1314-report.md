# Relatório de Teste: RU05.26.1.53.0_merchantSettingsSnapshotTracking_1314

> ⚠️ **Este arquivo é registro de execução, NÃO fonte de padrão (regra #16).**
> Patterns (selectors, helpers, classificação) vivem em skills (`.claude/skills/`) e código (`src/`, `tests/`).
> Não inferir selectors/helpers/classificações a partir deste relatório. Re-leitura permitida apenas para: (a) atualização pós-execução pelo `qa-validator`, (b) reprodução manual via leadPk/accountPk, (c) auditoria solicitada pelo usuário. PKs e estado de DB aqui são snapshot datado — categoria volatile, verificar fonte primária antes de reusar.

---

## Informações da Tarefa

- **Task:** Merchant Settings Snapshot Tracking (#1314)
- **Work item:** https://gitlab.com/uown/frontend/origination/-/work_items/1314
- **Milestone:** UOWN | Origination | Merchant Settings Snapshot Tracking
- **Labels:** `@origination` `@regression` `@critical` `@qa2`
- **Pipeline:** qa-planner → qa-implementer → qa-debugger (ciclos 1–2) → **qa-validator (este relatório — re-execução 2026-06-17, autoritativa)** → qa-doc-keeper

## Descrição

Na aprovação de underwriting o sistema grava um **snapshot imutável de lead** da config UW do merchant (EPO 5%, EPO 10%, UW Pipeline, Fraud Threshold) em `uown_los_lead_merchant_settings_snapshot`. No funding/criação da conta SVC esses quatro valores são copiados para um **snapshot imutável de conta** em `uown_sv_account_merchant_settings_snapshot`, vinculado a account+lead+merchant. Edições posteriores da config do merchant nunca podem mutar nenhum dos dois snapshots. O snapshot de conta deriva do snapshot de lead (não relê o merchant ao vivo no funding).

**Cobertura de brand (INVIOLÁVEL — ssn-test-modalities §5):** a suite cobre as DUAS brands — `terraceFinance` (UOWN, merchant pk 26) e `KS1011`/"Bodega Furniture" (KORNERSTONE, merchant pk 315, `fraud_threshold=5`).

---

## Execução do Teste

- **Arquivo:** `docs/taskTestingUown/RU05.26.1.53.0_merchantSettingsSnapshotTracking_1314/RU05.26.1.53.0_merchantSettingsSnapshotTracking_1314.spec.ts`
- **Ambiente:** **qa2** (`origination-qa2.uownleasing.com`; DB `UOWN_DB_URL_QA2` → `127.0.0.1:5445/svc` via túnel kubectl)
- **Project Playwright:** `task-testing-origination` (grep `/@origination/`, depends-on `auth-origination`)
- **Comando:** `ENV=qa2 npx playwright test --project=task-testing-origination RU05.26.1.53.0_merchantSettingsSnapshotTracking_1314.spec.ts --workers=1` (serial-safe; DB fixture lê `process.env.ENV` — `ENV=qa2` obrigatório)
- **Data da execução (autoritativa):** **2026-06-17 ~11:00–11:08Z UTC** (run #2 — full suite, ambos os blocos, 1 worker).
- **Data da re-execução do ramo denial (autoritativa para TC-02/TC-KS-denial):** **2026-06-17 ~10:37Z UTC** (`-g "TC-02|TC-KS-denial"`, `--workers=1`). Disparada por qa-debugger após o usuário habilitar `auto_deny_application=TRUE` em pk 26 e pk 315; validada aqui pelo qa-validator via oráculo DB read-only (leads 16597/16598). Resultado: **`4 passed`** (2 auth setups + TC-02 + TC-KS-denial).
- **Duração:** 7.9m (full suite, 1 worker) / ~5s (run dedicado do ramo denial).
- **Branch:** `tests`
- **DB TimeZone:** GMT/UTC `[db-observation:qa2, SELECT current_setting('TimeZone') → 'GMT', 2026-06-17T10:59Z]` — sem risco de TZ-drift (db-polling-pattern pitfall #6 não aplicável).
- **Ciclo de validação:** re-execução pós-fechamento de ciclo (não consumiu novo ciclo validator↔debugger — a única falha remanescente é a invariante de brand intencionalmente firing, não defeito test-side).
- **Resultado agregado (runner + oráculo DB convergentes):**
  **TERRACE: 100% verde.** **KS: TC-KS-01 verde, TC-KS-02 FALHOU no brand cross-check (achado conhecido, reproduzido).** **Ramo denial (TC-02 + TC-KS-denial): VERDE — auto-deny habilitado pelo usuário em qa2.**
  TC-06-baseline ✅ · TC-01 ✅ · TC-03a ✅ · TC-03b ✅ · TC-03c ✅ · TC-03d ✅ · TC-05 ✅ · TC-07 ✅ · **TC-02 ✅ (auto-deny, lead 16597, `lead_status='DENIED'`, snapshot COUNT=0)** · **TC-KS-01 ✅** · **TC-KS-02 ❌ (brand) — snapshot OK, `company='UOWN'`** · **TC-KS-denial ✅ (auto-deny, lead 16598, `lead_status='DENIED'`, snapshot COUNT=0)**.
  Contagem do runner: full suite **11 passed / 1 failed (TC-KS-02) / 2 skipped** (run #2, ramo denial ainda SKIP) + run dedicado do ramo denial **4 passed** (2 auth + TC-02 + TC-KS-denial). Consolidado: **TC-02 e TC-KS-denial agora PASS** (não mais SKIP).

> **Nota de infra (run #1 abortado, 2026-06-17 ~10:55Z):** a primeira tentativa fail-fastou no PRIMEIRO read de DB de cada TC (`assertMerchantContract`/`assertKsMerchantContract`) — `password authentication failed for user "svc_user"` nos 2 primeiros TCs e depois `connect ECONNREFUSED 127.0.0.1:5445` (queda do túnel kubectl no meio do run). Nenhum lead foi criado, NENHUMA asserção de feature executou. **Não é drift de contrato nem achado de feature — é blocker transitório de conectividade DB.** O túnel se reestabeleceu; probe read-only confirmou DB acessível + autenticando + merchants nos valores de contrato (sem drift), e o run #2 (autoritativo, acima) rodou limpo. Detalhe completo na seção "Blocker de infra (run #1)".

> **tsc:** suite e `merchant-setting.page.ts` compilam limpo. Erros pré-existentes de `tsc --noEmit` em `src/scripts/` são fora do escopo desta task.

---

## Resultado por TC (run 2026-06-17 — autoritativo)

> **Mapa lead→TC desta execução** (logs do runner + oráculo DB): 16586=TC-06-baseline · 16587=TC-01 · 16588=TC-03a · 16589=TC-03b · 16590=TC-03c · 16591=TC-03d · 16592=TC-05 · 16593=TC-07 · **16594=TC-KS-01** · **16595=TC-KS-02** · **16597=TC-02 (auto-deny, run dedicado 10:37Z)** · **16598=TC-KS-denial (auto-deny, run dedicado 10:37Z)**.

### Estado de merchant ANTES e DEPOIS `[db-observation:qa2, 2026-06-17T10:59Z e 11:08Z]`
- **pk 26 (terraceFinance/UOWN):** `epo5=true, epo10=true, uw_pipeline='Test', fraud_threshold=1`, `client_type=TERRACE_FINANCE` — **sem drift** (auto-restore via afterAll executou: log `[teardown] terraceFinance restored to contract values via UI`).
- **pk 315 (KS1011/KORNERSTONE):** `epo5=true, epo10=true, uw_pipeline='Test', fraud_threshold=5`, `client_type=KORNERSTONE` — **sem drift** (read-only; nenhuma edição/heal — Rule #16/#9).
- **`auto_deny_application` (ambos):** `TRUE` em **pk 26 E pk 315** `[db-observation:qa2, uown_merchant WHERE pk IN (26,315), 2026-06-17T10:37Z]` — habilitado deliberadamente pelo usuário em qa2 para fornecer um gatilho de denial determinístico (pre-UW, Step 2 `merchantAutoDenyCheck`). **Caveat operacional:** com a flag ON, todas as aplicações desses merchants são auto-negadas — os TCs baseados em aprovação (TC-01/TC-06-baseline/TC-03a–d/TC-05/TC-07/TC-KS-01/TC-KS-02) ficam **não-executáveis** até a flag ser desligada. O run #2 (aprovação) e o run dedicado do ramo denial são, portanto, mutuamente exclusivos quanto ao estado da flag. NÃO mutado por este qa-validator (read-only).

### Estado das tabelas de snapshot (final) `[db-observation:qa2, 2026-06-17T11:08Z]`
- `uown_los_lead_merchant_settings_snapshot`: total **46 rows** (max pk 48). Esta run gravou +9 lead snaps (pk 39–48, exceto o de TC-07 que foi removido pelo DELETE escopado).
- `uown_sv_account_merchant_settings_snapshot`: total **12 rows** (max pk 12). Esta run gravou +3 account snaps (pk 10 TERRACE/TC-06, pk 11 TERRACE/TC-05, **pk 12 KS/TC-KS-02**).

### Bloco TERRACE (qa2/terraceFinance)

| TC | Lead | Account / snapshot | Status runner | Veredicto (oráculo DB) |
|---|---|---|---|---|
| TC-06-baseline | 16586 | account 11555 (ACTIVE), acct-snap pk=10 | ✅ PASS | account snapshot 4 valores=contrato (fraud=1, merchant=26), cópia íntegra do lead snap pk=39 `[db-observation:qa2, account_pk=11555]` |
| TC-01 | 16587 | lead-snap pk=40 | ✅ PASS | COUNT=1, 4 valores=contrato (fraud=1, merchant=26) `[db-observation:qa2, lead_pk=16587]` |
| TC-03a (Fraud 1→5) | 16588 | lead-snap pk=41 | ✅ PASS | edit real (toast "Merchants updated successfully.", ao vivo=5); lead snapshot `fraud_threshold=1` imutável `[db-observation:qa2, lead_pk=16588]` |
| TC-03b (UW Pipeline Test→Prod) | 16589 | lead-snap pk=42 | ✅ PASS | edit real (ao vivo='Prod'); lead snapshot `uw_pipeline='Test'` imutável `[db-observation:qa2, lead_pk=16589]` |
| TC-03c (EPO 5% true→false) | 16590 | lead-snap pk=43 | ✅ PASS | edit real (`epo5 bulk-set to false`); lead snapshot `epo5=true` imutável `[db-observation:qa2, lead_pk=16590]` |
| TC-03d (EPO 10% true→false) | 16591 | lead-snap pk=44 | ✅ PASS | edit real (`epo10 bulk-set to false`); lead snapshot `epo10=true` imutável `[db-observation:qa2, lead_pk=16591]` |
| TC-05 (account deriva de lead) | 16592 | account 11556 (ACTIVE), acct-snap pk=11 | ✅ PASS | merchant ao vivo=5 no funding; lead snap=1; **account snap `fraud_threshold=1` (NÃO 5)** — invariante de compliance HOLD `[db-observation:qa2, account_pk=11556]` |
| TC-07 (sem account snap sem lead snap) | 16593 | account 11557 (ACTIVE), **acct-snap COUNT=0** | ✅ PASS | DELETE escopado removeu o lead snap (`leadSnaps=[]` confirmado no oráculo); funding criou a conta 11557 mas **nenhum** account snapshot `[db-observation:qa2, lead_pk=16593, account_pk=11557]` |
| TC-02 (denial via auto-deny) | 16597 | — (lead negado, sem account) | ✅ PASS | `lead_status='DENIED'` (auto-deny pre-UW `MERCHANT_AUTO_DENIED`, NÃO `UW_DENIED`); lead snapshot COUNT=0; activity log `merchantAutoDenyCheck` + `[ApplicationProcessor] Application denied at step: ...merchant is set to be auto denied.` `[db-observation:qa2, lead_pk=16597]` `[test-execution:qa2,2026-06-17]` |

### Bloco KORNERSTONE (qa2/Kornerstone/KS1011)

| TC | Lead | Account / snapshot | Status runner | Veredicto (oráculo DB) |
|---|---|---|---|---|
| TC-KS-01 (KS lead snap, fraud=5) | 16594 | lead-snap pk=47 | ✅ PASS | COUNT=1, `fraud_threshold=5` (NÃO 1), `merchant_pk=315`, epo5/epo10=true, uw_pipeline='Test' `[db-observation:qa2, lead_pk=16594]` |
| TC-KS-02 (KS account snap + brand) | 16595 | account 11558 (ACTIVE), acct-snap pk=12 | ❌ **FAIL (no brand cross-check)** | **Snapshot OK**: account snap pk=12 `fraud_threshold=5`, `merchant_pk=315`, cópia íntegra do lead snap pk=48. **FALHA**: `uown_sv_account.company='UOWN'` (esperado `KORNERSTONE`) `[db-observation:qa2, account_pk=11558, lead_pk=16595]` |
| TC-KS-denial (denial via auto-deny) | 16598 | — (lead negado, sem account) | ✅ PASS | `lead_status='DENIED'` (auto-deny pre-UW `MERCHANT_AUTO_DENIED`, NÃO `UW_DENIED`); lead snapshot COUNT=0; activity log `merchantAutoDenyCheck` + `[ApplicationProcessor] Application denied at step: ...merchant is set to be auto denied.`; paridade de brand com TC-02 `[db-observation:qa2, lead_pk=16598]` `[test-execution:qa2,2026-06-17]` |

---

## Cenários

> As tabelas acima registram o **resultado**; esta seção descreve cada cenário e a **reprodução manual** (precondição de merchant → criar/aprovar lead → ação → query de DB). Tabelas reais: `uown_los_lead_merchant_settings_snapshot`, `uown_sv_account_merchant_settings_snapshot`, `uown_merchant`, `uown_sv_account`. Merchants: TERRACE pk 26 (`OL90202-0001`, fraud=1, client_type TERRACE_FINANCE) e KS pk 315 (`KS1011`/Bodega Furniture, fraud=5, client_type KORNERSTONE).

### Bloco TERRACE (qa2/terraceFinance — merchant pk 26)

### TC-06-baseline

**Objetivo:** Provar que, no funding/criação da conta SVC, o sistema grava um snapshot de conta em `uown_sv_account_merchant_settings_snapshot` com os 4 valores de config UW do merchant. É a baseline do caminho feliz fim-a-fim (aprovação → funding → snapshot de conta).

**O que é verificado:** Existe exatamente 1 row de account snapshot para a conta criada, com `epo5=true`, `epo10=true`, `uw_pipeline='Test'`, `fraud_threshold=1` (valores do contrato do TERRACE pk 26) e `merchant_pk=26`. O account snapshot é cópia íntegra do lead snapshot correspondente.

**Como verificar manualmente:**
1. Confirmar precondição do merchant: `SELECT epo5, epo10, uw_pipeline, fraud_threshold, client_type FROM uown_merchant WHERE pk = 26;` → deve retornar `true, true, 'Test', 1, 'TERRACE_FINANCE'`.
2. Criar e aprovar um lead TERRACE (merchant pk 26) via fluxo de pré-qualificação até `UW_APPROVED`.
3. Levar o lead a funding (criação da conta SVC) até a conta ficar `ACTIVE`.
4. Confirmar a conta: `SELECT pk, account_status, merchant_pk FROM uown_sv_account WHERE lead_pk = <leadPk>;`.
5. Confirmar o snapshot de conta: `SELECT COUNT(*), epo5, epo10, uw_pipeline, fraud_threshold, merchant_pk FROM uown_sv_account_merchant_settings_snapshot WHERE account_pk = <accountPk> GROUP BY epo5, epo10, uw_pipeline, fraud_threshold, merchant_pk;` → COUNT=1, valores = contrato (fraud=1, merchant=26).

**Status:** ✅ PASSOU — lead 16586 → account 11555 (ACTIVE), account snapshot pk=10 com os 4 valores = contrato (fraud=1, merchant=26), cópia íntegra do lead snap pk=39 `[db-observation:qa2, account_pk=11555]` `[test-execution:qa2,2026-06-17]`.

### TC-01

**Objetivo:** Provar que o snapshot de **lead** é gravado na aprovação de underwriting, em `uown_los_lead_merchant_settings_snapshot`, com os 4 valores da config UW do merchant.

**O que é verificado:** Após `UW_APPROVED`, existe exatamente 1 (COUNT=1) row de lead snapshot para o lead, com `epo5=true`, `epo10=true`, `uw_pipeline='Test'`, `fraud_threshold=1` e `merchant_pk=26` (valores do contrato do TERRACE pk 26).

**Como verificar manualmente:**
1. Confirmar precondição: `SELECT epo5, epo10, uw_pipeline, fraud_threshold FROM uown_merchant WHERE pk = 26;` → `true, true, 'Test', 1`.
2. Criar e aprovar um lead TERRACE (merchant pk 26) até `UW_APPROVED`.
3. Confirmar o lead snapshot: `SELECT COUNT(*), epo5, epo10, uw_pipeline, fraud_threshold, merchant_pk FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk = <leadPk> GROUP BY epo5, epo10, uw_pipeline, fraud_threshold, merchant_pk;` → COUNT=1, valores = contrato (fraud=1, merchant=26).

**Status:** ✅ PASSOU — lead 16587, lead snapshot pk=40, COUNT=1, 4 valores = contrato (fraud=1, merchant=26) `[db-observation:qa2, lead_pk=16587]` `[test-execution:qa2,2026-06-17]`.

### TC-03a

**Objetivo:** Provar a **imutabilidade** do lead snapshot quanto ao **Fraud Threshold**: editar o merchant ao vivo (1→5) após a aprovação NÃO altera o valor já capturado no snapshot do lead.

**O que é verificado:** Edição real do Fraud Threshold do merchant pk 26 (1→5) via UI (toast de sucesso, valor ao vivo passa a 5), mas o lead snapshot do lead aprovado antes da edição mantém `fraud_threshold=1` (imutável).

**Como verificar manualmente:**
1. Precondição: merchant pk 26 com `fraud_threshold=1`.
2. Criar e aprovar um lead TERRACE até `UW_APPROVED` (gera o lead snapshot com fraud=1).
3. No portal Merchant Setting, editar o Fraud Threshold do merchant pk 26 para **5** e salvar (esperar toast "Merchants updated successfully.").
4. Confirmar mudança ao vivo: `SELECT fraud_threshold FROM uown_merchant WHERE pk = 26;` → 5.
5. Confirmar imutabilidade do snapshot: `SELECT fraud_threshold FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk = <leadPk>;` → ainda **1** (não 5).
6. Restaurar o merchant para o valor de contrato (fraud=1) após o teste.

**Status:** ✅ PASSOU — lead 16588, lead snapshot pk=41; edição real (toast "Merchants updated successfully.", ao vivo=5); lead snapshot `fraud_threshold=1` imutável `[db-observation:qa2, lead_pk=16588]` `[test-execution:qa2,2026-06-17]`.

### TC-03b

**Objetivo:** Provar a imutabilidade do lead snapshot quanto ao **UW Pipeline**: editar o merchant ao vivo (Test→Prod) após a aprovação NÃO altera o snapshot.

**O que é verificado:** Edição real do UW Pipeline do merchant pk 26 (Test→Prod) via UI (valor ao vivo passa a 'Prod'), mas o lead snapshot mantém `uw_pipeline='Test'`.

**Como verificar manualmente:**
1. Precondição: merchant pk 26 com `uw_pipeline='Test'`.
2. Criar e aprovar um lead TERRACE até `UW_APPROVED`.
3. No portal Merchant Setting, mudar o UW Pipeline do merchant pk 26 para **Prod** e salvar.
4. Confirmar ao vivo: `SELECT uw_pipeline FROM uown_merchant WHERE pk = 26;` → 'Prod'.
5. Confirmar imutabilidade: `SELECT uw_pipeline FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk = <leadPk>;` → ainda **'Test'**.
6. Restaurar o merchant para 'Test' após o teste.

**Status:** ✅ PASSOU — lead 16589, lead snapshot pk=42; edição real (ao vivo='Prod'); lead snapshot `uw_pipeline='Test'` imutável `[db-observation:qa2, lead_pk=16589]` `[test-execution:qa2,2026-06-17]`.

### TC-03c

**Objetivo:** Provar a imutabilidade do lead snapshot quanto ao **EPO 5%**: editar o merchant ao vivo (true→false) após a aprovação NÃO altera o snapshot.

**O que é verificado:** Edição real do EPO 5% do merchant pk 26 (true→false) via UI (bulk-set), mas o lead snapshot mantém `epo5=true`.

**Como verificar manualmente:**
1. Precondição: merchant pk 26 com `epo5=true`.
2. Criar e aprovar um lead TERRACE até `UW_APPROVED`.
3. No portal Merchant Setting, desmarcar o EPO 5% do merchant pk 26 (true→false) e salvar.
4. Confirmar ao vivo: `SELECT epo5 FROM uown_merchant WHERE pk = 26;` → false.
5. Confirmar imutabilidade: `SELECT epo5 FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk = <leadPk>;` → ainda **true**.
6. Restaurar o merchant para `epo5=true` após o teste.

**Status:** ✅ PASSOU — lead 16590, lead snapshot pk=43; edição real (`epo5 bulk-set to false`); lead snapshot `epo5=true` imutável `[db-observation:qa2, lead_pk=16590]` `[test-execution:qa2,2026-06-17]`.

### TC-03d

**Objetivo:** Provar a imutabilidade do lead snapshot quanto ao **EPO 10%**: editar o merchant ao vivo (true→false) após a aprovação NÃO altera o snapshot.

**O que é verificado:** Edição real do EPO 10% do merchant pk 26 (true→false) via UI (bulk-set), mas o lead snapshot mantém `epo10=true`.

**Como verificar manualmente:**
1. Precondição: merchant pk 26 com `epo10=true`.
2. Criar e aprovar um lead TERRACE até `UW_APPROVED`.
3. No portal Merchant Setting, desmarcar o EPO 10% do merchant pk 26 (true→false) e salvar.
4. Confirmar ao vivo: `SELECT epo10 FROM uown_merchant WHERE pk = 26;` → false.
5. Confirmar imutabilidade: `SELECT epo10 FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk = <leadPk>;` → ainda **true**.
6. Restaurar o merchant para `epo10=true` após o teste.

**Status:** ✅ PASSOU — lead 16591, lead snapshot pk=44; edição real (`epo10 bulk-set to false`); lead snapshot `epo10=true` imutável `[db-observation:qa2, lead_pk=16591]` `[test-execution:qa2,2026-06-17]`.

### TC-05

**Objetivo:** Provar que o snapshot de **conta deriva do snapshot de LEAD**, não do merchant ao vivo no funding. Se o merchant é alterado ENTRE aprovação e funding, o account snapshot copia o valor do lead snapshot (antigo), não o valor ao vivo (novo).

**O que é verificado:** Lead aprovado com fraud=1; merchant pk 26 alterado para `fraud_threshold=5` antes do funding; no funding o account snapshot grava `fraud_threshold=1` (== lead snapshot), **NÃO 5** (valor ao vivo). Invariante de compliance "HOLD" — funding nunca relê o merchant ao vivo.

**Como verificar manualmente:**
1. Precondição: merchant pk 26 com `fraud_threshold=1`.
2. Criar e aprovar um lead TERRACE até `UW_APPROVED` (lead snapshot grava fraud=1).
3. ANTES do funding, editar o merchant pk 26 via UI para `fraud_threshold=5` (esperar log/toast "Fraud Threshold set to: 5"); confirmar `SELECT fraud_threshold FROM uown_merchant WHERE pk = 26;` → 5.
4. Levar o lead a funding até a conta ficar `ACTIVE`.
5. Confirmar: `SELECT fraud_threshold FROM uown_sv_account_merchant_settings_snapshot WHERE account_pk = <accountPk>;` → **1** (== lead snapshot), NÃO 5.
6. Restaurar o merchant para fraud=1 após o teste.

**Status:** ✅ PASSOU — lead 16592 → account 11556 (ACTIVE), account snapshot pk=11; merchant ao vivo=5 no funding; lead snap=1; account snapshot `fraud_threshold=1` (NÃO 5) — invariante de compliance HOLD `[db-observation:qa2, account_pk=11556]` `[test-execution:qa2,2026-06-17]`.

### TC-07

**Objetivo:** Provar que **não há account snapshot sem lead snapshot** (gate): se o lead snapshot estiver ausente no funding, a conta SVC ainda é criada (`ACTIVE`), mas nenhum account snapshot é gravado (skip silencioso, sem bloquear a criação da conta).

**O que é verificado:** Após remover (DELETE escopado, autorizado, somente neste lead) o lead snapshot, o funding cria a conta `ACTIVE` porém **0** account snapshots. Os leads anteriores permanecem intactos e o merchant sem drift.

**Como verificar manualmente:**
1. Precondição: merchant pk 26 nos valores de contrato.
2. Criar e aprovar um lead TERRACE até `UW_APPROVED` (gera lead snapshot).
3. Com autorização explícita, remover SOMENTE o lead snapshot deste lead: `DELETE FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk = <leadPk>;` (escopado ao lead do TC; confirmar `leadSnaps=[]` para esse lead).
4. Levar o lead a funding até a conta ficar `ACTIVE`.
5. Confirmar a conta: `SELECT account_status FROM uown_sv_account WHERE lead_pk = <leadPk>;` → 'ACTIVE'.
6. Confirmar ausência de account snapshot: `SELECT COUNT(*) FROM uown_sv_account_merchant_settings_snapshot WHERE account_pk = <accountPk>;` → **0**.

> Nota: o DELETE é mutação de DB — exige autorização explícita do usuário (Rule #9/security). Escopado ao lead do TC; nenhum outro snapshot afetado.

**Status:** ✅ PASSOU — lead 16593 (DELETE escopado autorizado no lead snapshot) → account 11557 (ACTIVE), account snapshot COUNT=0; `leadSnaps=[]` confirmado no oráculo; leads anteriores intactos, merchant sem drift `[db-observation:qa2, lead_pk=16593, account_pk=11557]` `[test-execution:qa2,2026-06-17]`.

### TC-02

**Objetivo:** Provar que um lead **negado** NÃO gera lead snapshot — a feature de snapshot é gated em aprovação UW, então qualquer lead que não atinja `UW_APPROVED` (incluindo o negado) não deve gravar nada. O gatilho de denial usado é o **merchant auto-deny** (`auto_deny_application=TRUE`), habilitado pelo usuário em qa2.

**O que é verificado:** Para um lead TERRACE auto-negado (`auto_deny_application=TRUE` no merchant pk 26), o pipeline executa o step pre-UW `merchantAutoDenyCheck` (Step 2, antes do underwriting), resultando em `uown_los_lead.lead_status='DENIED'` (interno `MERCHANT_AUTO_DENIED` — **distinto de `UW_DENIED`**, que é a recusa do motor UW). O lead nunca chega a `UW_APPROVED`, então `COUNT(*) FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk = <leadPk>` = 0. Activity log (Rule #13) presente: nota `merchantAutoDenyCheck` + `[ApplicationProcessor] Application denied at step: Application denied as merchant is set to be auto denied.`.

**Como verificar manualmente:**
1. Precondição: merchant pk 26 nos valores de contrato **E** `auto_deny_application=TRUE`: `SELECT auto_deny_application FROM uown_merchant WHERE pk = 26;` → `true`.
2. Criar um lead TERRACE (merchant pk 26) com **SSN normal** (`generateTestSSN(true)`, não ending-in-9 — para isolar o auto-deny como única causa de negação) em estado leasing-elegível (TX, pois `stateCheck` precede o auto-deny no pipeline).
3. Pollar o status até `DECLINED/DENIED`.
4. Confirmar status pre-UW: `SELECT lead_status FROM uown_los_lead WHERE pk = <leadPk>;` → `DENIED` (NÃO `UW_DENIED`, NÃO `BLACKLIST_DENIED`).
5. Confirmar ausência de snapshot: `SELECT COUNT(*) FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk = <leadPk>;` → **0**.
6. Confirmar activity log: `SELECT notes FROM uown_los_lead_notes WHERE lead_pk = <leadPk> AND notes ILIKE '%auto denied%';` → nota do `merchantAutoDenyCheck` / `[ApplicationProcessor] Application denied at step: ...merchant is set to be auto denied.`.

**Status:** ✅ **PASSOU** — lead **16597** (merchant pk 26), `lead_status='DENIED'` (auto-deny pre-UW `MERCHANT_AUTO_DENIED`), lead snapshot COUNT=0, activity log do `merchantAutoDenyCheck` presente `[db-observation:qa2, lead_pk=16597]` `[test-execution:qa2,2026-06-17]`.

> **Nuance honesta (Rule #10/#16):** auto-deny é uma **negação PRE-UW** (`MERCHANT_AUTO_DENIED`/`lead_status='DENIED'`), **NÃO** uma recusa do motor UW (`UW_DENIED` — ambos existem como valores distintos em qa2). A invariante do snapshot (negativo) está corretamente exercitada: o gate de gravação é `UW_APPROVED`, então **qualquer** não-aprovação (auto-deny incluído) valida o ramo "denial → sem snapshot". O caminho literal "denied by underwriting" (engine decline) permanece **NÃO exercitado** — detalhe residual de cobertura, **NÃO bloqueador** (ver Avaliação de cobertura). O gatilho ending-in-9 → `UW_DENIED` continua sendo no-op em qa2 (pitfall #109 / [skill:ssn-test-modalities §6]); por isso o teste usa SSN normal e o auto-deny do merchant como única causa de negação.

---

### Bloco KORNERSTONE (qa2/Kornerstone — merchant pk 315 / KS1011)

### TC-KS-01

**Objetivo:** Provar que o snapshot de lead dispara na aprovação também para a brand KORNERSTONE, capturando os valores específicos do KS1011 (`fraud_threshold=5`, não o 1 do TERRACE), com `merchant_pk=315`.

**O que é verificado:** Após `UW_APPROVED` de um lead KS (merchant pk 315), existe exatamente 1 (COUNT=1) row de lead snapshot com `fraud_threshold=5` (NÃO 1), `merchant_pk=315`, `epo5=true`, `epo10=true`, `uw_pipeline='Test'`. Confirma que o snapshot lê o merchant correto e não herda valores do TERRACE.

**Como verificar manualmente:**
1. Confirmar precondição do merchant: `SELECT epo5, epo10, uw_pipeline, fraud_threshold, client_type FROM uown_merchant WHERE pk = 315;` → `true, true, 'Test', 5, 'KORNERSTONE'` (KS1011, Bodega Furniture).
2. Criar e aprovar um lead KS (merchant pk 315) até `UW_APPROVED` (KS exige bankData — Modalidade B).
3. Confirmar o lead snapshot: `SELECT COUNT(*), fraud_threshold, merchant_pk, epo5, epo10, uw_pipeline FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk = <leadPk> GROUP BY fraud_threshold, merchant_pk, epo5, epo10, uw_pipeline;` → COUNT=1, `fraud_threshold=5`, `merchant_pk=315`.

**Status:** ✅ PASSOU — lead 16594, lead snapshot pk=47, COUNT=1, `fraud_threshold=5` (NÃO 1), `merchant_pk=315`, epo5/epo10=true, uw_pipeline='Test' `[db-observation:qa2, lead_pk=16594]` `[test-execution:qa2,2026-06-17]`.

### TC-KS-02

**Objetivo:** Provar que o snapshot de conta dispara no funding para a brand KORNERSTONE, copiando os valores KS-specific (fraud=5, merchant=315) do lead snapshot — e, como cross-check de paridade de brand (FORA do escopo da feature de snapshot e do ticket #1314), conferir que `uown_sv_account.company='KORNERSTONE'`.

**O que é verificado:** (a) **Feature de snapshot** — account snapshot com `fraud_threshold=5`, `merchant_pk=315`, cópia íntegra do lead snapshot; (b) **brand cross-check (paridade, fora do escopo da feature)** — `uown_sv_account.company` = `KORNERSTONE`.

**Como verificar manualmente:**
1. Precondição: merchant pk 315 com `fraud_threshold=5`, `client_type=KORNERSTONE`.
2. Criar e aprovar um lead KS (merchant pk 315) até `UW_APPROVED`, depois levar a funding até a conta ficar `ACTIVE`.
3. Confirmar o snapshot de conta: `SELECT fraud_threshold, merchant_pk FROM uown_sv_account_merchant_settings_snapshot WHERE account_pk = <accountPk>;` → `5, 315` (cópia íntegra do lead snapshot).
4. Cross-check de brand: `SELECT company FROM uown_sv_account WHERE pk = <accountPk>;` → esperado `KORNERSTONE`.

**Status:** ❌ **FALHOU no cross-check de brand (snapshot OK).** Lead 16595 → account 11558 (ACTIVE), account snapshot pk=12. A **feature de snapshot operou corretamente**: account snapshot pk=12 com `fraud_threshold=5`, `merchant_pk=315`, cópia íntegra do lead snap pk=48. A falha é **exclusivamente** o brand cross-check: `uown_sv_account.company='UOWN'` (esperado `KORNERSTONE`) → **OQ-KS-1**. OQ-KS-1 é achado de brand-stamping **FORA do escopo da feature de snapshot e do ticket #1314** (zero requisito de brand no ticket; cross-check adicionado por ssn-test-modalities §5), origem confirmada em `uown_los_lead.company='UOWN'` na criação do lead — NÃO no snapshot/funding. NÃO suavizado. `[db-observation:qa2, account_pk=11558, lead_pk=16595]` `[test-execution:qa2,2026-06-17]`.

### TC-KS-denial

**Objetivo:** Provar que um lead KORNERSTONE negado NÃO gera lead snapshot — paridade de brand com o TC-02 no ramo negativo. Mesmo gatilho: merchant auto-deny (`auto_deny_application=TRUE` no KS1011, pk 315).

**O que é verificado:** Para um lead KS auto-negado (merchant pk 315), o pipeline executa `merchantAutoDenyCheck` (pre-UW), resultando em `lead_status='DENIED'` (interno `MERCHANT_AUTO_DENIED`, NÃO `UW_DENIED`). O lead nunca chega a `UW_APPROVED`, então `COUNT(*) FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk = <leadPk>` = 0. Activity log (Rule #13) presente. KS exige bankData no path de criação mesmo no ramo de denial (pitfall #5).

**Como verificar manualmente:**
1. Precondição: merchant pk 315 nos valores de contrato **E** `auto_deny_application=TRUE`: `SELECT auto_deny_application FROM uown_merchant WHERE pk = 315;` → `true`.
2. Criar um lead KS (merchant pk 315) com **SSN normal** (`generateTestSSN(true)`) + bankData (Modalidade B / pitfall #5), em estado leasing-elegível.
3. Pollar o status até `DECLINED/DENIED`.
4. Confirmar status: `SELECT lead_status FROM uown_los_lead WHERE pk = <leadPk>;` → `DENIED` (NÃO `UW_DENIED`, NÃO `BLACKLIST_DENIED`).
5. Confirmar ausência de snapshot: `SELECT COUNT(*) FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk = <leadPk>;` → **0**.
6. Confirmar activity log: nota do `merchantAutoDenyCheck` / `[ApplicationProcessor] Application denied at step: ...merchant is set to be auto denied.`.

**Status:** ✅ **PASSOU** — lead **16598** (merchant pk 315), `lead_status='DENIED'` (auto-deny pre-UW `MERCHANT_AUTO_DENIED`), lead snapshot COUNT=0, activity log do `merchantAutoDenyCheck` presente; paridade de brand com TC-02 confirmada `[db-observation:qa2, lead_pk=16598]` `[test-execution:qa2,2026-06-17]`.

> **Nuance honesta (Rule #10/#16):** idêntica ao TC-02 — auto-deny é negação PRE-UW (`MERCHANT_AUTO_DENIED`), não UW-engine decline (`UW_DENIED`). O ramo "denial → sem snapshot" está validado para a brand KS; o caminho literal "denied by underwriting" permanece não exercitado (detalhe residual, não bloqueador). `[db-observation:qa2, lead_pk=16598]`.

---

## Veredicto empírico — invariantes da feature (oráculo DB primário)

> **1) Lead snapshot dispara na aprovação UW (ambas as brands). CONFIRMADO.**
> TERRACE: 7 lead snaps gravados (pk 39–45), cada COUNT=1, 4 valores=contrato (fraud=1, merchant=26). KS: lead snap pk=47/48 com `fraud_threshold=5` + `merchant_pk=315` — valores específicos do KS1011, NÃO herdaram o 1 do terraceFinance. `[db-observation:qa2]`

> **2) Account snapshot dispara no funding (ambas as brands). CONFIRMADO (3× em dado fresh).**
> TERRACE: account 11555 (snap pk=10), 11556 (snap pk=11). KS: account 11558 (snap pk=12) com `fraud_threshold=5` + `merchant_pk=315`. Cada um exatamente 1 row, cópia íntegra do lead snap correspondente. `[db-observation:qa2]`

> **3) Snapshot de conta deriva do snapshot de LEAD, não do merchant ao vivo. CONFIRMADO.**
> TC-05: merchant editado para `fraud_threshold=5` ENTRE aprovação e funding (log `Fraud Threshold set to: 5`); account snap (pk=11) gravou `fraud_threshold=1` (== lead snap pk=45), NÃO 5. `[db-observation:qa2, account_pk=11556]`

> **4) Imutabilidade dos snapshots de lead sob edição do merchant. CONFIRMADO nos 4 campos.**
> TC-03a–d editaram cada um dos 4 settings via UI (toasts de sucesso + valor ao vivo mudado) e os lead snaps (pk 41–44) permaneceram nos valores de contrato. `[db-observation:qa2]`

> **5) Account snapshot é GATED no lead snapshot. CONFIRMADO (dado fresh, lead 16593).**
> TC-07: o DELETE escopado removeu o lead snap (`leadSnaps=[]` no oráculo); o funding criou a conta SVC **11557 (ACTIVE)** porém **nenhum** account snapshot (`acctSnaps=[]`). Behavior projetada: copy gated no lead snapshot; ausente, é pulado sem bloquear a criação da conta. `[db-observation:qa2, account_pk=11557, lead_pk=16593]`

> **6) Cópia íntegra dos 4 valores no snapshot de conta KS. CONFIRMADO.**
> TC-KS-02: o account snap pk=12 carregou `fraud_threshold=5` + `merchant_pk=315`, idênticos ao lead snap pk=48 — **a feature de snapshot operou corretamente para a brand KS**, inclusive os valores específicos do KS1011. `[db-observation:qa2, account_pk=11558]`

Classificação geral da **feature de snapshot**: **[CONFIRMADO]** — reprodução em dados fresh + oráculo DB primário, alinhado a Rule #10 e [skill:bug-classification]. A feature de snapshot (lead+conta, criação+imutabilidade+derive-from-lead+gate+brand-specific values) está correta em 100% dos pontos exercitados, nas DUAS brands. A única falha (TC-KS-02 brand cross-check) é um achado de **brand-stamping fora do escopo da feature de snapshot** — ver seção de achados.

---

## Escopo do #1314 e correspondência com o dev test plan (fonte primária: ticket)

> `[user-provided:gitlab-1314]` — Yuri Araujo synopsis + Priyanka Namburu (PO) + Davi Artur (dev).

**Escopo (Business Objective):** a feature é **audit / reporting / analytics ONLY** — preservar a config exata de merchant/program usada na criação do lead/account para reporting, auditoria, análise de underwriting e tracking de performance de merchant. Citação literal: *"No additional reporting metadata is required beyond the snapshot values and entity relationship identifiers."* **NÃO há consumidor funcional runtime no escopo** — a ideia "early-payoff/EPO usa o snapshot em vez da config ao vivo" é explicitamente **OUT of scope** (feature futura separada). Esta validação NÃO testa (e não deveria testar) nenhuma mudança de comportamento runtime baseada nos valores do snapshot.

**Business rules confirmadas pelo dev (Davi Artur execution plan):** snapshots criados **apenas para applications APROVADAS**; preservam EPO 5%, EPO 10%, UW Pipeline, Fraud Threshold; **o account snapshot é populado a partir do LEAD snapshot, nunca relendo o Merchant Settings ao vivo na criação da conta** (*"Never read current Merchant Settings during account creation. Always use the values stored in the Lead Snapshot"*). Integration points: lead snapshot via listener de `ApplicationApprovedEvent` (fim do `ApplicationProcessor`); account snapshot em `LosToSvcImportService.importFromLosToSvc`.

**Schema deployado (autoritativo):** tabelas `uown_los_lead_merchant_settings_snapshot` / `uown_sv_account_merchant_settings_snapshot`, colunas `epo5`/`epo10`/`uw_pipeline`/`fraud_threshold`. O draft inicial do dev (`uown_lead_merchant_settings_snapshot` / `uown_account_merchant_settings_snapshot` com `epo_5_percent`/`epo_10_percent`) foi **superado** — os nomes deployados (que testamos) são os corretos `[db-observation:qa2]`.

### Correspondência dev-test-plan ↔ nossa suite

| Dev test plan | Nossa suite | Nota |
|---|---|---|
| dev TC-01 (lead snap na aprovação) | TC-01 | 1:1 |
| dev TC-02 (denial → sem snapshot) | TC-02 + TC-KS-denial | ✅ PASS (denial via merchant auto-deny em qa2; leads 16597/16598). Nuance: auto-deny é pre-UW (`MERCHANT_AUTO_DENIED`), não UW-engine decline (`UW_DENIED`) — invariante negativa coberta nas 2 brands |
| dev TC-03 (imutável — dev cobre só Fraud via DB UPDATE) | TC-03a + **TC-03b/c/d** | **EXCEDE o dev plan** — cobrimos os 4 campos (Fraud, UW Pipeline, EPO 5%, EPO 10%) via UI real |
| dev TC-04 (account snap no funding) | TC-06-baseline | 1:1 |
| dev TC-05 (account deriva do lead, não merchant ao vivo) | TC-05 | 1:1 |
| dev TC-06 (sem account snap sem lead snap + WARN log) | TC-07 | 1:1 (capturamos a ausência do account snap; o WARN log é o coverage gap — ver TC-08) |
| PO QA Validations Scenario 1/2/3 (Priyanka) | subset das acima | todos cobertos |

> Onde excedemos: TC-03b/c/d (imutabilidade por-campo dos 4 settings via UI, vs dev que cobriu só Fraud via DB UPDATE) + cobertura de brand KS (TC-KS-01/02), que o dev plan não exige.

## Achados (Findings)

| ID | Tipo | Sev | Prio | Descrição |
|----|------|-----|------|-----------|
| OQ-KS-1 | **[HIPÓTESE]** (OUT of #1314 scope) | S2 | P1 | `uown_los_lead.company='UOWN'` para leads KS1011 (merchant_pk=315) — brand carimbada na criação do lead, não no funding/snapshot. **NÃO bloqueia as ACs do #1314** — o ticket tem zero requisito de brand/Kornerstone; é nosso cross-check de paridade adicionado por [skill:ssn-test-modalities §5]. `[user-provided:gitlab-1314]` |
| OQ-KS-2 | **[OBSERVAÇÃO]** | S3 | P2 | `submitApplication` retorna `"Failed to verify identification"` para leads KS1011 (apesar de `is_seon_id_check_required=FALSE`) |
| OBS-LOG | **EXPECTED / by-design** | — | — | Criação de snapshot não emite nota em `uown_los_lead_notes` — a trilha de auditoria é APP INFO/WARN LOGS (NÃO `lead_notes`), confirmado pela fonte primária do ticket. Não é gap de observabilidade. `[user-provided:gitlab-1314, dev-test-plan]` |

### OQ-KS-1 — `company='UOWN'` em conta KS1011 (brand cross-check FALHOU) — [HIPÓTESE]

**Sintoma (runner):** TC-KS-02 step "BRAND CROSS-CHECK (MANDATORY)" falhou — `expect(account.company).toBe('KORNERSTONE')` recebeu `"UOWN"` (account 11558, lead 16595).

**Reprodução:** fresh data nesta execução (leads 16594, 16595) — **3º ciclo independente** observando o mesmo comportamento (ciclos anteriores: leads 16584, 16585 em 2026-06-16).

**Evidência primária (origem da divergência identificada):** `[db-observation:qa2, uown_los_lead WHERE pk IN (16594,16595)]`
- `uown_los_lead.company='UOWN'` **já no nível do LEAD**, com `merchant_pk='315'` (KS1011). Ou seja, a brand é carimbada `UOWN` na **criação do lead**, e propaga para `uown_sv_account.company` no funding. **A origem NÃO é a cópia do snapshot nem o funding** — é a derivação de brand no lead.
- A feature de snapshot opera corretamente: lead snap pk=47/48 e account snap pk=12 carregam `merchant_pk=315` + `fraud_threshold=5` (valores do KS1011), confirmando que o snapshot lê o merchant certo. O divergente é apenas a coluna `company`.
- `KORNERSTONE` é um valor válido e populado: 228 contas em qa2 `[db-observation:qa2, uown_sv_account WHERE company='KORNERSTONE']` — a semântica de coluna/query do cross-check está correta.

**Co-sinal:** ver OQ-KS-2 (todos os leads KS retornaram `"Failed to verify identification"` no `submitApplication`).

**Candidatos de causa (NÃO confirmados — exigem dev/PO):**
1. Brand derivada de `client_type`/auth do contexto de criação, e não de `merchant_pk` — o path de criação via API (`createPreQualifiedApplication` com merchant KS1011) pode não estar roteando como KORNERSTONE em qa2.
2. Os fatos de brand do KS1011 no addendum (client_type KORNERSTONE) podem ter sofrido drift no provisioning de qa2 — embora `uown_merchant.client_type=KORNERSTONE` esteja confirmado no DB.

**Classificação (Rule #10):** **[HIPÓTESE]** — reproduzido em dado fresh (3×), mas **fora do escopo da feature de snapshot** (a feature carrega merchant_pk/fraud_threshold corretamente); a divergência de brand é um candidato a defeito da aplicação no path de derivação de brand do lead KS. **NÃO classificado como `[CONFIRMADO]` bug da aplicação** sem confirmação de dev/PO sobre o comportamento esperado de `company` para leads KS criados via este path em qa2. A asserção foi deixada firing intencionalmente (NÃO suavizada/flipada) para manter o achado visível (Rule #9/#10). **Nenhuma mutação de DB foi feita para forçar verde.**

**Ação:** surfaçar OQ-KS-1 a dev/PO antes de marcar a AC de paridade de brand KS como MET.

### OQ-KS-2 — `submitApplication` "Failed to verify identification" para KS — [OBSERVAÇÃO]

**Sintoma:** ambos os leads KS (16594, 16595) tiveram `submitApplication` retornando `"error":"Failed to verify identification"`, enquanto os leads TERRACE retornaram `"A credit card is required before submitting the application."` (mensagem normal/diferente). Apesar disso, os leads KS atingiram `UW_APPROVED`/`FUNDING` e geraram snapshots corretos.

**Evidência:** `[test-execution:qa2,run-2026-06-17T11:00Z]` (logs `[Setup] submitApplication succeeded: {...,"error":"Failed to verify identification"...}` para leads 16594/16595).

**Classificação:** **[OBSERVAÇÃO]** — co-sinal de OQ-KS-1 (path de identidade/brand KS pode não ter engajado, embora `is_seon_id_check_required=FALSE` para o KS1011). Não-bloqueante para a feature de snapshot (que passou). Surfaçar junto de OQ-KS-1.

---

## TC-02 / TC-KS-denial — ramo de denial (RESOLVIDO via merchant auto-deny — ✅ PASS em ambas as brands)

**Status: ✅ PASSOU (run dedicado 2026-06-17 ~10:37Z, `4 passed`; verificado read-only pelo qa-validator nos leads 16597/16598).**
> O usuário habilitou `auto_deny_application=TRUE` em pk 26 (terraceFinance) e pk 315 (KS1011) em qa2, fornecendo o **gatilho de denial determinístico** que faltava. Os testes foram **des-skipados**, renomeados para referenciar auto-deny, com SSN trocado para normal (`generateTestSSN(true)`) — assim o auto-deny é a **única** causa de negação — e ganharam uma precondição read-only assertando `auto_deny_application=TRUE` (fail-fast loud se rodado com a flag OFF).
>
> **Mecanismo (fonte primária):** auto-deny é o step pre-UW `merchantAutoDenyCheck` (Step 2 do pipeline de originação, **antes** do underwriting) `[doc:docs/business-rules/02-originacao-pipeline.md §Step 2]`. Produz `uown_los_lead.lead_status='DENIED'` (interno `MERCHANT_AUTO_DENIED`) — **distinto de `UW_DENIED`** (a recusa do motor UW; ambos coexistem como valores separados em qa2). O lead nunca chega a `UW_APPROVED`, logo o gate do snapshot nunca dispara.
>
> **Verificação read-only do qa-validator (independente):**
> - `uown_merchant`: `auto_deny_application=true` em pk 26 e pk 315; contrato sem drift `[db-observation:qa2, uown_merchant WHERE pk IN (26,315), 2026-06-17T10:37Z]`.
> - lead **16597** (pk 26) e lead **16598** (pk 315): ambos `lead_status='DENIED'` `[db-observation:qa2, uown_los_lead WHERE pk IN (16597,16598)]`.
> - snapshot: `SELECT ... FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk IN (16597,16598)` → **0 rows** (COUNT=0 em ambos) `[db-observation:qa2]`.
> - activity log (Rule #13): em ambos os leads, `... Executed : stateCheckExecuted :  merchantAutoDenyCheck` + `[ApplicationProcessor] Application denied at step: Application denied as merchant is set to be auto denied.` `[db-observation:qa2, uown_los_lead_notes WHERE lead_pk IN (16597,16598)]`.
>
> **Classificação: `[CONFIRMADO]` — invariante "denial → sem lead snapshot" confirmada em dado fresh nas DUAS brands.** Justificativa (Rule #10): a regra confirmada pelo dev é "snapshots criados APENAS para applications APROVADas" `[user-provided:gitlab-1314, dev-test-plan]`; um lead auto-negado nunca atinge `UW_APPROVED`, portanto não há snapshot — exercitado em dado fresh (leads 16597/16598) para UOWN e KS.
>
> **Nuance residual (honesta, NÃO bloqueador):** auto-deny é negação **PRE-UW** (`MERCHANT_AUTO_DENIED`), não UW-engine decline (`UW_DENIED`). O caminho literal "denied by underwriting" (engine decline) **permanece NÃO exercitado**. Como o gate de gravação do snapshot é `UW_APPROVED`, **qualquer** não-aprovação valida a invariante negativa — o ramo está coberto. Registrado como **detalhe residual de cobertura**, não como gap bloqueador. O gatilho ending-in-9 → `UW_DENIED` continua no-op em qa2 (pitfall #109 / [skill:ssn-test-modalities §6]).
>
> **Caveat operacional:** com `auto_deny_application=TRUE` em pk 26 + pk 315, **todos** os TCs baseados em aprovação (TC-01, TC-06-baseline, TC-03a–d, TC-05, TC-07, TC-KS-01, TC-KS-02) ficam **não-executáveis** até a flag ser desligada. Este run dedicado validou **apenas** o ramo de denial; o run #2 (aprovação) foi feito com a flag OFF. Os dois estados são mutuamente exclusivos.

---

## TC-08 — Probe de activity log na criação do snapshot

**Status: EXPECTED / by-design — ausência de nota em `lead_notes` é o comportamento correto (confirmado por fonte primária)**
> Para os leads desta execução (16586/16587 TERRACE, 16594 KS), nenhuma entrada de `uown_los_lead_notes` referenciando o snapshot foi encontrada `[db-observation:qa2, uown_los_lead_notes ILIKE '%snapshot%']` (logs `[TC-08] No uown_los_lead_notes entry...`).
>
> **Reclassificação (era `[OBSERVAÇÃO]` possível gap de observabilidade → agora EXPECTED / by-design):** a fonte primária do ticket prova que a **trilha de auditoria do #1314 são APPLICATION LOGS (INFO/WARN), NÃO `uown_los_lead_notes`** `[user-provided:gitlab-1314, dev-test-plan]`. O dev (Davi Artur) documentou os 6 padrões exatos:
> - INFO `Snapshot created for leadPk=` / INFO `Snapshot created for accountPk=`
> - WARN `Snapshot already exists for leadPk=` (skip idempotente lead) / WARN `Snapshot already exists for accountPk=` (skip idempotente conta)
> - WARN `No merchant found for leadPk=` (skip lead sem merchant)
> - WARN `No lead snapshot found for leadPk=<leadPk>, skipping account snapshot` (path do TC-07 / dev-TC-06)
>
> Portanto a **ausência de nota em `lead_notes` é o comportamento ESPERADO** — a própria tabela de snapshot (`row_created_timestamp`/`agent`) + os app-logs são a trilha. Rule #13 NÃO se aplica (a regra exige log de ação de negócio; aqui o log existe, é app-log, não `lead_notes`).
>
> **Coverage gap (relativo ao dev test plan, NÃO às 4 ACs):** os 6 app-logs acima **NÃO foram capturados nesta run** — são logs de app-server, fora do alcance do DB/runner. Honestidade: NÃO afirmamos que os logs dispararam, apenas que a ausência em `lead_notes` é esperada. **Recomendação:** capturar em run futuro ao menos o WARN do TC-07 (`No lead snapshot found...skipping account snapshot`) e um INFO `Snapshot created` (via tail do log do pod svc / Loki) para fechar a paridade com o dev test plan.

---

## Blocker de infra (run #1 — abortado, NÃO conta como veredicto de feature)

A primeira tentativa de 2026-06-17 (~10:55Z) falhou em 6 TCs, **todos no primeiro read de DB** (`assertMerchantContract`/`assertKsMerchantContract`, linha 140/672 da spec), antes de qualquer criação de lead ou asserção de feature:
- TCs 1–2 (TC-06-baseline, TC-01): `password authentication failed for user "svc_user"` `[test-execution:qa2,run-2026-06-17T10:55Z]`
- TC-05/TC-07/TC-03a/TC-KS-01 em diante: `connect ECONNREFUSED 127.0.0.1:5445` (queda do túnel kubectl no meio do run).

**Classificação: blocker transitório de conectividade DB — NÃO drift de contrato, NÃO achado de feature.** Verificado read-only logo após (`[db-observation:qa2, 2026-06-17T10:59Z]`): DB acessível, autenticando, e ambos os merchants nos valores de contrato (pk26 fraud=1, pk315 fraud=5 — **sem drift**). Conforme instrução do orquestrador (item #5) NÃO houve auto-heal de pk 26 nem pk 315. O run #2 (autoritativo) rodou limpo após a recuperação do túnel.

---

## Evidências (Dados Utilizados/Criados)

> Todos os leads criados frescos nesta execução. TERRACE via `createPreQualifiedApplication` (merchant pk 26, SSN `generateTestSSN(true)`); KS com bankData (Modalidade B, pitfall #5). Categoria volatile (fixture PK drift) — reconfirmar via SELECT antes de reusar `[skill:volatile-knowledge-registry §13]`.

| PK | Papel | Origem | TC | Evidência |
|---|---|---|---|---|
| lead 16586 | aprovado → funded (UOWN) | criado | TC-06-baseline | lead snap pk=39; account 11555 (ACTIVE); account snap pk=10 (fraud=1) `[db-observation:qa2]` |
| lead 16587 | aprovado (UOWN) | criado | TC-01 | lead snap pk=40, COUNT=1, fraud=1 `[db-observation:qa2]` |
| lead 16588 | aprovado; Fraud→5 | criado | TC-03a | lead snap pk=41, `fraud_threshold=1` imutável `[db-observation:qa2]` |
| lead 16589 | aprovado; Pipeline→Prod | criado | TC-03b | lead snap pk=42, `uw_pipeline='Test'` imutável `[db-observation:qa2]` |
| lead 16590 | aprovado; EPO5→false | criado | TC-03c | lead snap pk=43, `epo5=true` imutável `[db-observation:qa2]` |
| lead 16591 | aprovado; EPO10→false | criado | TC-03d | lead snap pk=44, `epo10=true` imutável `[db-observation:qa2]` |
| lead 16592 | aprovado → funded; Fraud→5 | criado | TC-05 | lead snap pk=45; account 11556 (ACTIVE); account snap pk=11 `fraud_threshold=1` (NÃO 5) `[db-observation:qa2]` |
| lead 16593 | aprovado → lead snap DELETADO (escopado, autorizado) → funded | criado | TC-07 | lead snap removido (`leadSnaps=[]`); account 11557 (ACTIVE); **account snap COUNT=0** `[db-observation:qa2, lead_pk=16593, account_pk=11557]` |
| lead 16594 | aprovado (KS, merchant_pk=315) | criado | TC-KS-01 | lead snap pk=47, COUNT=1, `fraud_threshold=5`, `merchant_pk=315`; **`uown_los_lead.company='UOWN'`** `[db-observation:qa2, lead_pk=16594]` |
| lead 16595 | aprovado → funded (KS, merchant_pk=315) | criado | TC-KS-02 | lead snap pk=48 (fraud=5); account 11558 (ACTIVE); account snap pk=12 (fraud=5, merchant=315); **`uown_sv_account.company='UOWN'` (≠ KORNERSTONE) — OQ-KS-1** `[db-observation:qa2, lead_pk=16595, account_pk=11558]` |
| lead 16597 | auto-negado (UOWN, merchant_pk=26) | criado | TC-02 | `lead_status='DENIED'` (`MERCHANT_AUTO_DENIED`); lead snapshot COUNT=0; activity log `merchantAutoDenyCheck` + `[ApplicationProcessor] Application denied at step: ...auto denied.` `[db-observation:qa2, lead_pk=16597]` |
| lead 16598 | auto-negado (KS, merchant_pk=315) | criado | TC-KS-denial | `lead_status='DENIED'` (`MERCHANT_AUTO_DENIED`); lead snapshot COUNT=0; activity log `merchantAutoDenyCheck` + `[ApplicationProcessor] Application denied at step: ...auto denied.` `[db-observation:qa2, lead_pk=16598]` |
| account 11555 | conta SVC UOWN (funding) | criado | TC-06-baseline | `account_status='ACTIVE'`, company='UOWN', merchant_pk=26 `[db-observation:qa2]` |
| account 11556 | conta SVC UOWN (funding) | criado | TC-05 | `account_status='ACTIVE'`, company='UOWN', merchant_pk=26 `[db-observation:qa2]` |
| account 11557 | conta SVC UOWN (funding) | criado | TC-07 | `account_status='ACTIVE'`; SEM account snapshot (gate no lead snapshot ausente) `[db-observation:qa2]` |
| account 11558 | conta SVC KS (funding) | criado | TC-KS-02 | `account_status='ACTIVE'`, **company='UOWN'** (esperado KORNERSTONE — OQ-KS-1), merchant_pk=315 `[db-observation:qa2]` |

- Merchant pk 26 (`OL90202-0001` terraceFinance) reconfirmado nos 4 valores de contrato APÓS a execução: `epo5=true, epo10=true, uw_pipeline='Test', fraud_threshold=1` `[db-observation:qa2, uown_merchant WHERE pk=26, 2026-06-17T11:08Z]`. Sem drift (auto-restore via UI executou).
- Merchant pk 315 (`KS1011` Bodega Furniture) reconfirmado: `epo5=true, epo10=true, uw_pipeline='Test', fraud_threshold=5`, `client_type=KORNERSTONE` `[db-observation:qa2, uown_merchant WHERE pk=315, 2026-06-17T11:08Z]`. Sem drift (read-only, nenhum heal — Rule #16/#9).
- `auto_deny_application=TRUE` em **pk 26 E pk 315** `[db-observation:qa2, uown_merchant WHERE pk IN (26,315), 2026-06-17T10:37Z]` — habilitado pelo usuário em qa2 (gatilho de denial determinístico do run dedicado TC-02/TC-KS-denial). NÃO mutado por este qa-validator. Caveat: bloqueia TCs de aprovação enquanto ON.
- `uown_los_lead_merchant_settings_snapshot`: total final **46 rows** (max pk 48). `uown_sv_account_merchant_settings_snapshot`: total final **12 rows** (max pk 12).

## Capturas de Tela

| TC | Arquivo |
|---|---|
| TC-KS-02 | `reports/test-results/.../test-failed-1.png` (gerado pelo runner na falha do brand cross-check; oráculo primário é DB) |

> Demais TCs são API+DB (snapshot é artefato de backend sem affordance de UI) → sem screenshot (justificado SPEC §1, Rule #14 exceção a+c). TCs de imutabilidade exercitam a UI Merchant Setting mas o oráculo é DB; passaram sem error-context.

---

## Cobertura dos Requisitos (AC Matrix — run 2026-06-17)

| AC | Descrição | Status | Evidência |
|----|-----------|--------|-----------|
| **AC-01** (snapshot na aprovação) | Snapshot de lead criado na aprovação UW | ✅ **MET** | TC-01 + TC-KS-01 PASS (COUNT=1 em ambas as brands) `[db-observation:qa2]` |
| **AC-01** (snapshot no funding) | Snapshot de conta criado no funding | ✅ **MET** | TC-06-baseline (account 11555) + TC-KS-02 step de snapshot (account 11558, snap pk=12) `[db-observation:qa2]` |
| **AC-01** (sem snapshot no denial) | Denial não grava snapshot | ✅ **MET** | TC-02 (lead 16597, UOWN) + TC-KS-denial (lead 16598, KS) PASS — denial via merchant auto-deny (`auto_deny_application=TRUE`), `lead_status='DENIED'`, snapshot COUNT=0, activity log do `merchantAutoDenyCheck` presente em ambas as brands `[db-observation:qa2]` `[test-execution:qa2,2026-06-17]`. **Nuance (registrada honestamente):** o gatilho é negação PRE-UW (`MERCHANT_AUTO_DENIED`), NÃO UW-engine decline (`UW_DENIED`) — o caminho literal "denied by underwriting" permanece não exercitado. Como o gate do snapshot é `UW_APPROVED`, qualquer não-aprovação valida o ramo negativo → AC **MET**; engine-decline é detalhe residual de cobertura, não bloqueador. |
| **AC-02 / AC-03** (imutabilidade lead) | Edição do merchant não muta snapshot de lead | ✅ **MET** | TC-03a–d PASS — 4 settings editados via UI, 4 lead snaps (pk 41–44) imutáveis `[db-observation:qa2]` |
| **AC-02 / AC-04** (conta deriva de lead) | Snapshot de conta == snapshot de lead, não merchant ao vivo | ✅ **MET** | TC-05 PASS — merchant=5 no funding, account snap=1 (account 11556, snap pk=11) `[db-observation:qa2]` |
| **AC-04** (4 campos preservados) | EPO5, EPO10, UW Pipeline, Fraud Threshold | ✅ **MET** (lead+conta, ambas brands) | TC-01/TC-06/TC-KS-01/TC-KS-02 (4 colunas, fraud=1 UOWN / fraud=5 KS) + TC-03a–d (imutabilidade por-campo) |
| **(invariante)** (sem account snap sem lead snap) | Skip silencioso; conta ainda criada | ✅ **MET** | TC-07 PASS — lead 16593 sem lead snap → account 11557 (ACTIVE) mas 0 account snapshots `[db-observation:qa2]` |
| **Brand parity KS** (company=KORNERSTONE) — *OUT of #1314 scope* | Conta KS carimbada KORNERSTONE | ❌ NOT MET (mas **NÃO é AC do #1314**) | TC-KS-02 FAIL — `uown_sv_account.company='UOWN'` (origem: `uown_los_lead.company='UOWN'` na criação). **OQ-KS-1 [HIPÓTESE]** — surfaçar a dev/PO. Achado FORA do escopo da feature de snapshot E fora do escopo do ticket #1314 (zero requisito de brand no ticket); é nosso cross-check de paridade [skill:ssn-test-modalities §5]. **NÃO deve bloquear o fechamento das 4 ACs do #1314** — snapshot carrega merchant_pk=315/fraud=5 corretamente em KS. `[user-provided:gitlab-1314]` |

**Resumo AC:** as **4 ACs do #1314 (AC-01 aprovação + funding + denial, AC-02, AC-03, AC-04)** e a invariante "sem account snap sem lead snap" estão **MET — confirmados empiricamente nas DUAS brands** (UOWN e KS). O ramo denial do AC-01, antes UNVERIFIED, agora está **MET** via merchant auto-deny (leads 16597/16598; snapshot COUNT=0; `lead_status='DENIED'`), com a nuance honesta registrada de que o gatilho é pre-UW (`MERCHANT_AUTO_DENIED`) e não UW-engine decline (`UW_DENIED`) — detalhe residual de cobertura, não bloqueador. A **paridade de brand KS (`company=KORNERSTONE`) NÃO é AC do #1314** (zero requisito de brand no ticket) — OQ-KS-1 é nosso cross-check adicionado e **não bloqueia o fechamento das 4 ACs**, embora deva ser surfaçado a dev/PO como candidato a defeito de app independente.

---

## Avaliação de cobertura vs Risco (SPEC §Risk)

| Área de risco (SPEC) | Nível | TC | Coberto? |
|---|---|---|---|
| Account snapshot no funding | **HIGH** | TC-06-baseline, TC-KS-02 | ✅ **Adequado** — disparou e validado (3× em dado fresh, ambas brands) |
| Account-snapshot-deriva-de-lead (trap de re-leitura ao vivo) | **HIGH** | TC-05 | ✅ **Adequado** — merchant=5 ignorado; account snap=1 |
| Sem account snap sem lead snap | **HIGH** | TC-07 | ✅ **Adequado** — DELETE escopado → conta criada, 0 account snapshots |
| Brand parity KS (company + valores específicos KS1011) | **HIGH** | TC-KS-01, TC-KS-02 | ⚠️ **Parcial** — valores KS (fraud=5/merchant=315) ✅ confirmados; **brand `company` ❌ (OQ-KS-1)** |
| Lead snapshot na aprovação | MED | TC-01, TC-KS-01 | ✅ Adequado |
| Imutabilidade lead snapshot (4 campos) | MED | TC-03a–d | ✅ **Adequado** — 4 settings cobertos individualmente |
| Sem snapshot no denial | LOW-MED | TC-02, TC-KS-denial | ✅ **Adequado** — denial via merchant auto-deny (leads 16597/16598, ambas brands); snapshot COUNT=0. Residual: gatilho é pre-UW (`MERCHANT_AUTO_DENIED`), não UW-engine decline (`UW_DENIED`) — não bloqueador |
| Activity log (Rule #13) | MED (observ.) | TC-08 | ✅ Probado — `[OBSERVAÇÃO]` ausência de nota |

---

## Bugs de Aplicação Encontrados

**Nenhum [CONFIRMADO] na feature de snapshot.** Todas as asserções da feature de snapshot que executaram passaram (criação na aprovação, criação no funding, imutabilidade nos 4 campos, derive-from-lead, gate, valores brand-specific do KS1011), nas DUAS brands.

O único achado é **OQ-KS-1 (`[HIPÓTESE]`, fora do escopo da feature de snapshot)**: `uown_sv_account.company='UOWN'` para conta KS1011 (origem confirmada em `uown_los_lead.company='UOWN'` na criação do lead). Por Rule #10 NÃO entra na seção de bugs confirmados sem confirmação de dev/PO sobre o comportamento esperado de `company` no path de criação de lead KS em qa2. Reproduzido 3× em dado fresh — surfaçado como questão aberta a dev/PO, com a asserção mantida firing.

---

## Resumo da Validação

| Item | Status |
|---|---|
| Lead snapshot na aprovação (UOWN, 4 valores, COUNT=1) | ✅ CONFIRMADO (7 leads, cada COUNT=1) |
| Lead snapshot na aprovação (KS, fraud=5, merchant=315) | ✅ CONFIRMADO (lead 16594, pk=47) |
| Account snapshot dispara no funding (UOWN) | ✅ CONFIRMADO (2×: account 11555 + 11556) |
| Account snapshot dispara no funding (KS, fraud=5) | ✅ CONFIRMADO (account 11558, snap pk=12) |
| Imutabilidade do lead snapshot (TC-03a–d, 4 campos) | ✅ CONFIRMADO |
| Conta deriva de lead com merchant alterado (TC-05) | ✅ CONFIRMADO (account snap=1, não 5) |
| Sem account snap sem lead snap (TC-07) | ✅ CONFIRMADO (account 11557 ACTIVE, 0 account snapshots) |
| **Brand cross-check KS (company=KORNERSTONE)** | ❌ **FALHOU — `company='UOWN'` (OQ-KS-1 [HIPÓTESE])** |
| Denial não grava snapshot (TC-02 / TC-KS-denial) | ✅ CONFIRMADO via auto-deny (leads 16597/16598, snapshot COUNT=0, ambas brands; nuance: pre-UW `MERCHANT_AUTO_DENIED`, não `UW_DENIED`) |
| Activity log do auto-deny (Rule #13, TC-02 / TC-KS-denial) | ✅ CONFIRMADO (`merchantAutoDenyCheck` + `[ApplicationProcessor] Application denied at step: ...` em 16597 e 16598) |
| Activity log na criação do snapshot (TC-08) | ⚠️ `[OBSERVAÇÃO]` ausente (não-bloqueante) |
| TC-07 DELETE escopado (só lead 16593) | ✅ Confirmado (leads anteriores intactos; merchant sem drift) |
| Merchant pk26 / pk315 sem drift pós-execução | ✅ Confirmado (pk26 auto-restore UI; pk315 read-only) |
| Blocker de infra run #1 (DB auth/tunnel) | ✅ Caracterizado — transitório, recuperado; run #2 limpo |
| tsc da suite + page object limpo | ✅ |
| Bugs de aplicação na feature de snapshot | ✅ NENHUM |

---

## Handoff

**Runner: 11 passed (inclui 2 auth setups) / 1 failed (TC-KS-02 brand) / 2 skipped (TC-02, TC-KS-denial).** Feature de snapshot **validada nas duas brands** (criação + imutabilidade + derive-from-lead + gate + valores KS-specific). A única falha é a invariante de brand intencionalmente firing — achado fora do escopo da feature.

**Pronto para:** `qa-doc-keeper` — para (a) registrar OQ-KS-1/OQ-KS-2 (brand `company='UOWN'` em lead KS1011 / `"Failed to verify identification"` no submitApplication KS) como pitfall/questão aberta em [[application-lifecycle]] ou skill de brand; (b) **registrar o gatilho de denial via merchant `auto_deny_application` (pre-UW `merchantAutoDenyCheck`, `lead_status='DENIED'`/`MERCHANT_AUTO_DENIED`, distinto de `UW_DENIED`) como alternativa determinística ao ending-in-9 (no-op em qa2) — pitfall/conhecimento durável em [[ssn-test-modalities]] / [[application-lifecycle]], Rule #11**; (c) anotar o blocker de infra transitório (svc_user auth + queda de túnel kubectl no run #1).

**Pendências para fechamento total da DoD (NÃO bloqueiam o core da feature de snapshot, mas impedem fechar a task):**
1. **OQ-KS-1** — decisão de dev/PO sobre o comportamento esperado de `uown_los_lead.company` / `uown_sv_account.company` para leads KS1011 criados via API em qa2. A paridade de brand KS (`company=KORNERSTONE`) permanece **NOT MET** até esclarecimento. NÃO suavizar/flipar a asserção; NÃO mutar DB para forçar verde (Rule #9/#10). **(Achado FORA do escopo do #1314 — não bloqueia as 4 ACs.)**
2. **Re-run em stg** exigido pelo DoD da SPEC antes de fechar a task.

> **RESOLVIDO (não mais pendente):** TC-02 / TC-KS-denial — o ramo denial do AC-01 agora está **MET** via merchant auto-deny (leads 16597/16598, snapshot COUNT=0 em ambas as brands). Removido das pendências. Detalhe residual (não bloqueador): o gatilho é negação pre-UW (`MERCHANT_AUTO_DENIED`), não UW-engine decline (`UW_DENIED`).

**`-evidence.md` (stakeholder-facing): NÃO gerado** — pipeline ainda aberto (OQ-KS-1 + re-run em stg em aberto; e há 1 falha de invariante de brand deliberada). Gerar apenas quando OQ-KS-1 for resolvido com dev/PO, o re-run em stg for feito, e o usuário sinalizar fechamento (Rule #17).

---

## Knowledge-sync (qa-doc-keeper — 2026-06-17)

Folding do confronto com o ticket #1314 nos docs/catalogos (Rule #4, Rule #11):
- **Reclassificado** TC-08/OBS-LOG: `[OBSERVAÇÃO]` → **EXPECTED/by-design** (trilha é app INFO/WARN logs, não `lead_notes`; `[user-provided:gitlab-1314, dev-test-plan]`).
- **Adicionado** coverage gap dos 6 app-logs (não capturados; recomendado capturar TC-07 WARN + INFO `Snapshot created` em run futuro).
- **Adicionado** seção de escopo (audit-only, sem consumidor runtime; payoff/EPO OUT) + correspondência dev-test-plan ↔ nossa suite (excedemos em TC-03b/c/d + brand KS).
- **Marcado** OQ-KS-1 como OUT of #1314 scope (não bloqueia as 4 ACs — zero requisito de brand no ticket).
- `docs/scenarios/1314-merchant-settings-snapshot.md` atualizado (escopo, audit-trail, correspondência, caveat sandbox/qa1 do gate).

> **Schema-doc discrepancy (flag, não corrigido):** `docs/taskTestingUown/database-schema.md` e `docs/database-schema.md` **NÃO listam** as tabelas de snapshot do #1314 (só têm `jv_snapshot`, não relacionado). Não há draft com os nomes errados do dev nesses docs — então não há discrepância a corrigir, apenas ausência. Quando os docs de schema forem regenerados, devem incluir `uown_los_lead_merchant_settings_snapshot` / `uown_sv_account_merchant_settings_snapshot` (cols `epo5`/`epo10`/`uw_pipeline`/`fraud_threshold`).

> **BLOQUEIO de write em skills (pendente de ação do user):** as edições nos skills (`[[application-lifecycle]]` pitfalls #112/#113, `[[ssn-test-modalities]]` §8 OQ-KS-1/OQ-KS-2, `[[volatile-knowledge-registry]]` cross-ref do gate ending-in-9) foram **NEGADAS por permissão** nesta sessão (`.claude/skills/**` não-graváveis). O conteúdo durável de Rule #11 está pronto mas NÃO foi persistido nos skills — ver bloco "Flagged for user review" no output do doc-keeper. Até persistir, este conhecimento vive apenas neste report + scenarios (que são history, não fonte de pattern — Rule #16), ou seja, **Rule #11 NÃO está satisfeita** até os skills serem atualizados.

---

**Skills loaded:**
- `.claude/skills/test-report-standard/SKILL.md`
- `.claude/skills/bug-classification/SKILL.md`
- `.claude/skills/volatile-knowledge-registry/SKILL.md`
- `.claude/skills/activity-log-validation/SKILL.md`
- `.claude/skills/db-polling-pattern/SKILL.md`
- `.claude/skills/ssn-test-modalities/SKILL.md`
- `.claude/skills/qa-domain-reflexes/SKILL.md`
