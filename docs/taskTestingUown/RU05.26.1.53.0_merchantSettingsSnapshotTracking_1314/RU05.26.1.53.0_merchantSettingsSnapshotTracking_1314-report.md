# Relatório de Teste: RU05.26.1.53.0_merchantSettingsSnapshotTracking_1314

> ⚠️ **Este arquivo é registro de execução, NÃO fonte de padrão (regra #16).**
> Patterns (selectors, helpers, classificação) vivem em skills (`.claude/skills/`) e código (`src/`, `tests/`).
> Não inferir selectors/helpers/classificações a partir deste relatório. Re-leitura permitida apenas para: (a) atualização pós-execução pelo `qa-validator`, (b) reprodução manual via leadPk/accountPk, (c) auditoria solicitada pelo usuário. PKs e estado de DB aqui são snapshot datado — categoria volatile, verificar fonte primária antes de reusar.

---

## Informações da Tarefa

- **Task:** Merchant Settings Snapshot Tracking (#1314)
- **Work item:** https://gitlab.com/uown/frontend/origination/-/work_items/1314
- **Milestone:** UOWN | Origination | Merchant Settings Snapshot Tracking
- **Labels:** `@origination` `@regression` `@critical` `@qa2`
- **Pipeline:** qa-planner → qa-implementer → qa-debugger (ciclos 1–2) → **qa-validator (este relatório — ciclo 3, autoritativo)** → qa-doc-keeper

## Descrição

Na aprovação de underwriting o sistema grava um **snapshot imutável de lead** da config UW do merchant (EPO 5%, EPO 10%, UW Pipeline, Fraud Threshold) em `uown_los_lead_merchant_settings_snapshot`. No funding/criação da conta SVC esses quatro valores são copiados para um **snapshot imutável de conta** em `uown_sv_account_merchant_settings_snapshot`, vinculado a account+lead+merchant. Edições posteriores da config do merchant nunca podem mutar nenhum dos dois snapshots. O snapshot de conta deriva do snapshot de lead (não relê o merchant ao vivo no funding).

---

## Execução do Teste

- **Arquivo:** `docs/taskTestingUown/RU05.26.1.53.0_merchantSettingsSnapshotTracking_1314/RU05.26.1.53.0_merchantSettingsSnapshotTracking_1314.spec.ts`
- **Ambiente:** **qa2** (`origination-qa2.uownleasing.com`; DB `UOWN_DB_URL_QA2` → `127.0.0.1:5445/svc` via túnel)
- **Project Playwright:** `task-testing-origination`
- **Comando:** `ENV=qa2 npx playwright test <suite> --workers=1` (serial-safe; DB fixture lê `process.env.ENV` — `ENV=qa2` obrigatório)
- **Data da execução (ciclo 3, autoritativa):** 2026-06-16 ~19:38–19:43Z UTC (TC-06-baseline/TC-01/TC-03a–d/TC-05) + **TC-07 em 2026-06-16T21:02–21:03Z** (run targeted `-g "TC-07"`, após autorização do DELETE).
- **Duração:** 5.0m (full suite ciclo 3, 1 worker) + 39.8s (TC-07 targeted, 1 worker).
- **Branch:** `tests`
- **DB TimeZone:** GMT/UTC `[db-observation:qa2, SELECT current_setting('TimeZone') → 'GMT', 2026-06-16T21:02Z]` — sem risco de TZ-drift (db-polling-pattern pitfall #6 não aplicável).
- **Ciclo de validação:** **3/3** (validator↔debugger — último ciclo; TC-07 não consumiu novo ciclo — foi desbloqueio por autorização, sem devolução ao debugger).
- **Resultado agregado (ciclo 3 + TC-07 — runner + oráculo DB convergentes):**
  **AC-core VALIDADO E VERDE NO RUNNER.** 8 TCs PASS / 0 failed / 1 skip (TC-02 env).
  TC-06-baseline ✅ · TC-01 ✅ · TC-03a ✅ · TC-03b ✅ · TC-03c ✅ · TC-03d ✅ · TC-05 ✅ · **TC-07 ✅ (DESBLOQUEADO)** · TC-02 ⏭️ SKIP (env).

> **Mudança vs ciclo 2:** os dois defeitos test-side do ciclo 2 (F-CYCLE2-A `afterAll` com fixtures test-scoped; F-CYCLE2-B EPO triple-checkbox `#epoN-false` not visible) foram **resolvidos pelo qa-debugger**. No ciclo 3 NÃO há mais falha do runner — TC-03c, TC-03d e TC-05, que falhavam/short-circuitavam, agora **passam no runner E no oráculo DB**. O auto-restore do merchant (afterAll) voltou a executar: log `[teardown] terraceFinance restored to contract values via UI`.

> **Desbloqueio TC-07 (2026-06-16T21:02Z):** o usuário autorizou o DB DELETE escopado (CLAUDE.md Exception 2 / Rule #9) e o qa-implementer removeu o `.skip`, fiando `DELETE FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk = ctx.leadPk` (lead fresco do próprio teste) + sanity `COUNT=0` pré-funding. **TC-07 PASSOU** — ver bloco TC-07 abaixo.

> **tsc:** suite e `merchant-setting.page.ts` compilam limpo. Erros pré-existentes de `tsc --noEmit` em `src/scripts/` são fora do escopo desta task.

---

## Resultado por TC (ciclo 3 — autoritativo)

> **Mapa lead→TC desta execução** (dos logs do runner): 16573=TC-06-baseline · 16574=TC-01 · 16575=TC-03a · 16576=TC-03b · 16577=TC-03c · 16578=TC-03d · 16579=TC-05 · **16580=TC-07**.

### Baseline / estado pós-execução `[db-observation:qa2, 2026-06-16T21:02Z]`
- Merchant pk26 ANTES e DEPOIS (inclui run TC-07): `epo5=true, epo10=true, uw_pipeline='Test', fraud_threshold=1` — **sem drift** (auto-restore executou; DELETE escopado de TC-07 não tocou o merchant).
- `uown_los_lead_merchant_settings_snapshot`: total **33 rows** (max pk 33). Ciclo 3 gravou +7 (pk 27–33). **TC-07 (lead 16580): aprovação gravou 1 row e o DELETE escopado a removeu** → net zero (33→33, max pk inalterado). Os 7 leads anteriores (16573–16579) permanecem com snapshot intacto `[db-observation:qa2, prior_leads_intact=7]`.
- `uown_sv_account_merchant_settings_snapshot`: total **7 rows** (max pk 7); inalterado pelo TC-07 — **nenhum account snapshot criado para account 11552** (a invariante sob teste).

| TC | Lead criado | Account / snapshot | Status runner | Veredicto (oráculo DB) |
|---|---|---|---|---|
| TC-06-baseline | lead 16573 | account 11550 (ACTIVE), acct-snap pk=6 | ✅ PASS | account snapshot 4 valores = contrato, cópia íntegra do lead snapshot pk=27 `[db-observation:qa2, account_pk=11550]` |
| TC-01 | lead 16574 | lead-snap pk=28 | ✅ PASS | COUNT=1, 4 valores = contrato `[db-observation:qa2, lead_pk=16574]` |
| TC-03a (Fraud 1→5) | lead 16575 | lead-snap pk=29 | ✅ PASS | edit real (toast "Merchants updated successfully.", merchant ao vivo=5); lead snapshot `fraud_threshold=1` imutável `[db-observation:qa2, lead_pk=16575]` |
| TC-03b (UW Pipeline Test→Prod) | lead 16576 | lead-snap pk=30 | ✅ PASS | edit real (ao vivo='Prod'); lead snapshot `uw_pipeline='Test'` imutável `[db-observation:qa2, lead_pk=16576]` |
| TC-03c (EPO 5% true→false) | lead 16577 | lead-snap pk=31 | ✅ PASS | edit real (`epo5 bulk-set to false`); lead snapshot `epo5=true` imutável `[db-observation:qa2, lead_pk=16577]` |
| TC-03d (EPO 10% true→false) | lead 16578 | lead-snap pk=32 | ✅ PASS | edit real (`epo10 bulk-set to false`); lead snapshot `epo10=true` imutável `[db-observation:qa2, lead_pk=16578]` |
| TC-05 (account deriva de lead, merchant mudado) | lead 16579 | account 11551 (ACTIVE), acct-snap pk=7 | ✅ PASS | merchant ao vivo=5 no funding; lead snapshot=1; **account snapshot `fraud_threshold=1` (NÃO 5)** — invariante de compliance HOLD `[db-observation:qa2, account_pk=11551]` |
| TC-02 (denial) | — | — | ⏭️ SKIP | env — sem trigger UW_DENIED determinístico em qa2 (ver TC-02 abaixo) |
| TC-07 (sem account snap sem lead snap) | lead 16580 | account 11552 (ACTIVE), **acct-snap COUNT=0** | ✅ PASS | DELETE escopado removeu o lead snapshot (sanity COUNT=0 pré-funding); funding criou a conta 11552 mas **nenhum** account snapshot (`account_pk=11552 COUNT=0`) `[db-observation:qa2, account_pk=11552]` |

### Restauração do merchant pós-execução
Auto-restore via afterAll (caminho de edit da própria feature, NÃO DB raw) executou em ambos os blocos serial. Estado final confirmado `[db-observation:qa2, 2026-06-16T20:44Z]`: `epo5=true, epo10=true, uw_pipeline='Test', fraud_threshold=1` — **sem drift**.

---

## Veredicto empírico — invariantes da feature (oráculo DB primário)

> **1) Account snapshot dispara no funding em qa2. CONFIRMADO (reproduzido 2× em dado fresh).**
> TC-06-baseline (account 11550, snap pk=6) e TC-05 (account 11551, snap pk=7) gravaram cada um exatamente 1 row de account snapshot, com os 4 valores idênticos ao lead snapshot correspondente. Contas criadas `account_status='ACTIVE'`.

> **2) Snapshot de conta deriva do snapshot de LEAD, não do merchant ao vivo. CONFIRMADO.**
> TC-05: merchant editado para `fraud_threshold=5` ENTRE aprovação e funding (logs: `Fraud Threshold set to: 5`); o account snapshot (pk=7) gravou `fraud_threshold=1` (== lead snapshot pk=33), NÃO 5. A invariante de compliance segura.

> **3) Imutabilidade dos snapshots de lead sob edição do merchant. CONFIRMADO nos 4 campos.**
> TC-03a–d editaram cada um dos 4 settings via UI (toasts de sucesso + valor ao vivo mudado, confirmado por DB) e os respectivos lead snapshots (pk 29–32) permaneceram nos valores de contrato.

> **4) Account snapshot é GATED no lead snapshot — sem lead snapshot, sem account snapshot (mas a conta É criada). CONFIRMADO (dado fresh, lead 16580).**
> TC-07: aprovação gravou o lead snapshot, o DELETE escopado o removeu (sanity COUNT=0 pré-funding), e o funding levou o lead a FUNDING criando a conta SVC **11552 (ACTIVE)** — porém **nenhum** account snapshot foi gravado (`uown_sv_account_merchant_settings_snapshot` COUNT=0 para account_pk=11552; total da tabela inalterado em 7). Esta é a **behavior projetada**: a cópia do account snapshot deriva do lead snapshot existente; ausente o lead snapshot, o copy é pulado silenciosamente sem bloquear a criação da conta. `[db-observation:qa2, account_pk=11552, lead_pk=16580]` `[test-execution:qa2,TC-07-2026-06-16T21:02Z]`

Classificação geral: **[CONFIRMADO]** — reprodução em dados fresh criados pela própria execução + leitura DB primária, alinhado a Rule #10 e [skill:bug-classification]. **TC-07 NÃO revelou achado de feature** (não houve account snapshot indevido nem erro de criação de conta) — é PASS de behavior projetada.

---

## Evidências (Dados Utilizados/Criados)

> Todos os leads criados frescos nesta execução via `createPreQualifiedApplication` (merchant `terraceFinance` pk 26, SSN `generateTestSSN(true)`). Categoria volatile (fixture PK drift) — reconfirmar via SELECT antes de reusar `[skill:volatile-knowledge-registry §13]`.

| PK | Papel | Origem | TC | Evidência |
|---|---|---|---|---|
| lead 16573 | aprovado → funded | criado | TC-06-baseline | lead snap pk=27; account 11550 (ACTIVE); account snap pk=6 `[db-observation:qa2]` |
| lead 16574 | aprovado | criado | TC-01 | lead snap pk=28, COUNT=1, 4 valores `[db-observation:qa2]` |
| lead 16575 | aprovado; merchant editado (Fraud→5) | criado | TC-03a | lead snap pk=29, `fraud_threshold=1` imutável `[db-observation:qa2]` |
| lead 16576 | aprovado; merchant editado (Pipeline→Prod) | criado | TC-03b | lead snap pk=30, `uw_pipeline='Test'` imutável `[db-observation:qa2]` |
| lead 16577 | aprovado; merchant editado (EPO5→false) | criado | TC-03c | lead snap pk=31, `epo5=true` imutável `[db-observation:qa2]` |
| lead 16578 | aprovado; merchant editado (EPO10→false) | criado | TC-03d | lead snap pk=32, `epo10=true` imutável `[db-observation:qa2]` |
| lead 16579 | aprovado → funded; merchant editado (Fraud→5) | criado | TC-05 | lead snap pk=33; account 11551 (ACTIVE); account snap pk=7 `fraud_threshold=1` `[db-observation:qa2]` |
| lead 16580 | aprovado → lead snap DELETADO (escopado, autorizado) → funded | criado | TC-07 | aprovação gravou 1 lead snap; DELETE escopado removeu (COUNT=0 pré-funding); account 11552 (ACTIVE); **account snap COUNT=0** `[db-observation:qa2, lead_pk=16580, account_pk=11552]` |
| account 11550 | conta SVC (funding) | criado | TC-06-baseline | `account_status='ACTIVE'` `[db-observation:qa2, uown_sv_account WHERE lead_pk=16573]` |
| account 11551 | conta SVC (funding) | criado | TC-05 | `account_status='ACTIVE'` `[db-observation:qa2, uown_sv_account WHERE lead_pk=16579]` |
| account 11552 | conta SVC (funding) | criado | TC-07 | `account_status='ACTIVE'`; SEM account snapshot (gate no lead snapshot ausente) `[db-observation:qa2, uown_sv_account WHERE lead_pk=16580]` |

- Merchant pk 26 (`OL90202-0001` terraceFinance) reconfirmado nos 4 valores de contrato APÓS a execução (incl. TC-07): `epo5=true, epo10=true, uw_pipeline='Test', fraud_threshold=1` `[db-observation:qa2, uown_merchant WHERE pk=26, 2026-06-16T21:02Z]`. Sem drift — o DELETE de TC-07 ficou escopado a `lead_pk=16580` e não tocou o merchant.
- `uown_los_lead_merchant_settings_snapshot`: ciclo 3 gravou +7 (pk 27–33). TC-07 gravou +1 (lead 16580) e o **DELETE escopado removeu +1** → total final 33 (max pk 33), inalterado. Os 7 leads anteriores intactos `[db-observation:qa2, prior_leads_intact=7]`.
- `uown_sv_account_merchant_settings_snapshot`: total final **7 rows** (pk 6, 7 do ciclo 3; sem orphans, 1 por funding). **TC-07 NÃO adicionou row** (gate no lead snapshot ausente — account 11552 sem snapshot).

## Capturas de Tela

| TC | Arquivo |
|---|---|
| — | N/A — todos os TCs verdes no ciclo 3 (sem `test-failed-*.png`). TCs de imutabilidade exercitam UI mas o oráculo é DB; nenhuma falha gerou error-context. |

> Demais TCs são API+DB (sem affordance de UI no snapshot) → sem screenshot (justificado SPEC §1, Rule #14 exceção a+c — snapshot é artefato de backend).

---

## Cenários

### TC-06-baseline — Snapshot de conta criado no funding com os 4 valores

**Objetivo:** provar que o funding (criação da conta SVC) gera o snapshot de conta imutável com os quatro valores UW.
**O que é verificado:** ao levar um lead aprovado até FUNDING, o sistema cria a conta SVC e grava exatamente uma row de snapshot de conta, com EPO5/EPO10/UW Pipeline/Fraud Threshold idênticos ao snapshot de lead, vinculada a account+lead+merchant, com timestamp.

#### Como verificar manualmente
1. Confirmar `uown_merchant` pk 26 = `epo5=true, epo10=true, uw_pipeline='Test', fraud_threshold=1`.
2. Criar lead pré-qualificado aprovado p/ terraceFinance; aguardar `UW_APPROVED`.
3. Levar a FUNDING (SIGNED → settle → FUNDING); resolver `account_pk` via `SELECT pk FROM uown_sv_account WHERE lead_pk=$1`.
4. `SELECT * FROM uown_sv_account_merchant_settings_snapshot WHERE account_pk=$1` → esperar 1 row, 4 valores = contrato, links corretos, `row_created_timestamp` presente, valores == snapshot de lead.

**Status: PASSOU** — lead 16573 → account 11550, account snap pk=6, 4 valores conferem, cópia íntegra do lead snap pk=27 `[db-observation:qa2]`.

---

### TC-01 — Snapshot de lead na aprovação UW, 4 valores + COUNT=1

**Objetivo:** provar que a aprovação UW grava exatamente um snapshot de lead com os quatro valores.
**O que é verificado:** ao aprovar um lead, o sistema grava 1 (e só 1 — idempotência) row em `uown_los_lead_merchant_settings_snapshot` com os quatro valores UW vigentes no merchant e o link de merchant.

#### Como verificar manualmente
1. Reconfirmar merchant pk 26 (4 valores).
2. Criar lead pré-qualificado aprovado; aguardar `UW_APPROVED`.
3. `SELECT count(*) FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk=$1` → 1.
4. Conferir os 4 valores + `merchant_pk=26` + `row_created_timestamp`.

**Status: PASSOU** — lead 16574, snap pk=28, COUNT=1, 4 valores conferem `[db-observation:qa2]`.

---

### TC-02 — Sem snapshot de lead no denial UW

**Objetivo:** provar que o denial UW NÃO grava snapshot de lead (write é gated em `UW_APPROVED`).
**O que é verificado:** lead negado pela engine não deve ter row de snapshot de lead.

**Status: SKIPPED (ciclo 3 — mantido `test.skip` por design; root cause CONFIRMADO em ciclos anteriores)**
> Em qa2 não existe trigger de `UW_DENIED` determinístico via SSN ending-in-9. **Root cause confirmado (ciclo 2, registrado aqui, NÃO re-investigado neste ciclo):** o activity log do lead 16556 (ciclo 1) mostrou `NEW → PENDING_UW → [UnderwritingService][runUnderwriting] UW is run. Lead Status UW_APPROVED`, aprovação BlackBox $4190, **0 chamadas de API outbound**. Ou seja, qa2 decide via a engine interna **BlackBox (ABB) SEM vendor externo**, e o **short-circuit mock de ending-in-9 NÃO se aplica em qa2** (é comportamento exclusivo de qa1/sandbox). Logo TC-02 não pode usar ending-in-9 em qa2.
>
> **Classificação: `[CONFIRMADO] env/test-data` (NÃO é bug da aplicação).** A feature de snapshot está corretamente gated em `UW_APPROVED`; o que falta é um gatilho de denial confiável em qa2 — questão aberta de PO/dev. `[db-observation:qa2, lead 16556 activity log]` `[user-provided:orchestrator,2026-06-16]`
> **Ação:** mantido `test.skip` com corpo completo (roda inalterado quando um trigger `UW_DENIED` confirmado for fornecido). O ramo negativo de denial (AC-01) permanece **UNVERIFIED em qa2** até: (a) um trigger UW_DENIED confirmado em qa2, OU (b) execução em sandbox/qa1.

---

### TC-03a — Imutabilidade do snapshot de lead: Fraud Threshold (1→5)

**Objetivo:** provar que editar Fraud Threshold no merchant via UI não muta o snapshot de lead existente.
**O que é verificado:** após aprovar um lead (snapshot gravado), editar o setting via página Origination Merchant Setting e confirmar que o snapshot de lead permanece com o valor original.

#### Como verificar manualmente
1. Aprovar lead; capturar snapshot original.
2. Origination → Merchant Setting → **filtrar a tabela pelo código `OL90202-0001`** → selecionar a row → editar Fraud Threshold para 5 → salvar.
3. Confirmar que o merchant ao vivo mudou e que o snapshot de lead continua `fraud_threshold=1`.

**Status (ciclo 3): PASSOU** `[db-observation:qa2, lead_pk=16575 pk=29]`
> Filtro "Search table" + seleção da row OK; toast "Merchants updated successfully." (merchant ao vivo → 5). Snapshot de lead 16575 permaneceu `fraud_threshold=1` (imutável), demais 3 campos inalterados.

---

### TC-03b — Imutabilidade (UW Pipeline 'Test'→'Prod')

**Status (ciclo 3): PASSOU** `[db-observation:qa2, lead_pk=16576 pk=30]`
> Editou UW Pipeline Test→Prod via UI (toast de sucesso, ao vivo → 'Prod'). Snapshot de lead 16576 permaneceu `uw_pipeline='Test'` (imutável). O defeito de afterAll do ciclo 2 (F-CYCLE2-A) foi resolvido — runner verde, auto-restore executou.

---

### TC-03c — Imutabilidade (EPO 5% true→false)

**Status (ciclo 3): PASSOU** `[db-observation:qa2, lead_pk=16577 pk=31]`
> Editou EPO 5% true→false via UI (log `epo5 bulk-set to false`, toast de sucesso). Snapshot de lead 16577 permaneceu `epo5=true` (imutável). O defeito de page-object do ciclo 2 (F-CYCLE2-B, `#epo5-false` not visible) foi resolvido pelo qa-debugger — o triple-checkbox agora opera corretamente.

---

### TC-03d — Imutabilidade (EPO 10% true→false)

**Status (ciclo 3): PASSOU** `[db-observation:qa2, lead_pk=16578 pk=32]`
> Editou EPO 10% true→false via UI (log `epo10 bulk-set to false`, toast de sucesso). Snapshot de lead 16578 permaneceu `epo10=true` (imutável). No ciclo 2 não havia executado (serial short-circuit após TC-03c); no ciclo 3 executou e passou.

---

### TC-05 — Snapshot de conta deriva do snapshot de lead (não do merchant ao vivo)

**Objetivo:** provar a invariante de compliance: trocar Fraud Threshold entre aprovação e funding NÃO altera o snapshot de conta (ele reflete o valor de aprovação, não o ao vivo).
**O que é verificado:** lead aprovado com `fraud_threshold=1` → edita merchant para 5 via UI → funding → snapshot de conta deve ser 1 (== snapshot de lead), nunca 5.

#### Como verificar manualmente
1. Aprovar lead (snapshot `fraud_threshold=1`).
2. Origination Merchant Setting → **filtrar por `OL90202-0001`** → editar Fraud Threshold 1→5 → salvar.
3. Funding → resolver `account_pk`.
4. `account snapshot fraud_threshold` deve ser 1 (== lead snapshot), não 5.

**Status (ciclo 3): PASSOU** `[db-observation:qa2, account_pk=11551 pk=7]`
> Merchant editado para `fraud_threshold=5` entre aprovação e funding (log `Fraud Threshold set to: 5`). Lead snapshot (pk=33) = 1; **account snapshot (pk=7) = 1, NÃO 5**. A invariante de compliance (account deriva do lead snapshot, não relê o merchant ao vivo) está confirmada. Auto-restore restaurou o merchant a `fraud_threshold=1` após o teste.

---

### TC-07 — Sem snapshot de conta sem snapshot de lead (DESBLOQUEADO — DELETE autorizado)

**Objetivo:** provar que, sem um snapshot de lead, o funding NÃO grava snapshot de conta (a cópia de conta é gated na existência do snapshot de lead) — porém a conta SVC é criada normalmente.
**O que é verificado:** removendo o snapshot de lead de um lead aprovado e levando-o a FUNDING, a conta SVC é criada (ACTIVE) mas nenhuma row de account snapshot é gravada.

#### Como verificar manualmente
1. Aprovar lead fresco; confirmar que o lead snapshot foi gravado.
2. **[autorizado]** `DELETE FROM uown_los_lead_merchant_settings_snapshot WHERE lead_pk=$1` escopado ao lead do próprio teste; sanity `SELECT COUNT(*) … = 0` antes do funding.
3. Levar a FUNDING; resolver `account_pk` via `SELECT pk FROM uown_sv_account WHERE lead_pk=$1` → conta presente, `account_status='ACTIVE'`.
4. `SELECT COUNT(*) FROM uown_sv_account_merchant_settings_snapshot WHERE account_pk=$1` → esperar **0**.

**Status (ciclo 3 — desbloqueado): PASSOU** `[db-observation:qa2, lead_pk=16580, account_pk=11552]` `[test-execution:qa2,TC-07-2026-06-16T21:02Z]`
> Aprovação gravou o lead snapshot do lead 16580; o DELETE escopado (autorizado pelo usuário em 2026-06-16 — CLAUDE.md Exception 2 / Rule #9) o removeu (sanity COUNT=0 confirmado pré-funding). O funding levou o lead a `lead_status=FUNDING` e criou a conta SVC **11552 (`account_status='ACTIVE'`)**, porém **nenhum account snapshot** foi gravado (`account_pk=11552 COUNT=0`; total da tabela inalterado em 7). O DELETE ficou escopado: os 7 leads anteriores (16573–16579) mantiveram seus snapshots intactos `[db-observation:qa2, prior_leads_intact=7]`. Merchant pk26 nos valores de contrato após a run.
>
> **Classificação:** `[CONFIRMADO]` behavior projetada (reprodução em dado fresh, oráculo DB primário). A AC "sem account snapshot sem lead snapshot" está **MET**. NÃO é achado de feature — não houve account snapshot indevido nem erro de criação de conta.

---

### TC-08 — Probe de activity log na criação do snapshot

**Objetivo (probe informativo, Rule #13):** descobrir se a criação do snapshot emite nota em `uown_los_lead_notes`.
**O que é verificado:** query `uown_los_lead_notes WHERE lead_pk=$1 AND notes ILIKE ANY('%snapshot%','%MerchantSetting%','%merchant_settings%')`.

**Status: [OBSERVAÇÃO] — nenhuma nota encontrada (não-bloqueante)**
> Para os leads 16573 (TC-06-baseline) e 16574 (TC-01), **nenhuma** entrada de `uown_los_lead_notes` referenciando o snapshot foi encontrada `[db-observation:qa2, uown_los_lead_notes ILIKE '%snapshot%']` (logs `[TC-08] [OBSERVAÇÃO] No uown_los_lead_notes entry...`). Conforme SPEC §2: o snapshot é uma tabela de auditoria — suas próprias colunas `row_created_timestamp`/`agent` servem de trilha; a ausência de nota em `lead_notes` é uma **possível lacuna de observabilidade a esclarecer com PO/dev**, NÃO um bloqueador e NÃO um bug (Rule #10/#13 + [skill:qa-domain-reflexes §4/§11], [skill:volatile-knowledge-registry §9 — activity log schema é categoria volatile]). Classificação conservadora: `[OBSERVAÇÃO]` single-env.

---

## Cobertura dos Requisitos (AC Matrix — ciclo 3)

| AC | Descrição | Status | Evidência |
|----|-----------|--------|-----------|
| **AC-01** (snapshot na aprovação) | Snapshot de lead criado na aprovação UW | ✅ **MET** | TC-01 PASS (lead 16574, COUNT=1) + leads 16573/16575–16579 (7 lead snaps) |
| **AC-01** (snapshot no funding) | Snapshot de conta criado no funding | ✅ **MET** | TC-06-baseline PASS (account 11550, snap pk=6) `[db-observation:qa2]` |
| **AC-01** (sem snapshot no denial) | Denial não grava snapshot | ⚠️ **UNVERIFIED em qa2** | TC-02 SKIP — qa2 não tem trigger UW_DENIED determinístico (ending-in-9 aprova via BlackBox, sem short-circuit). Requer trigger confirmado OU run em sandbox/qa1. |
| **AC-02 / AC-03** (imutabilidade lead) | Edição do merchant não muta snapshot de lead | ✅ **MET** | TC-03a–d PASS — 4 settings editados via UI, 4 lead snaps (pk 29–32) imutáveis `[db-observation:qa2]` |
| **AC-02 / AC-04** (conta deriva de lead) | Snapshot de conta == snapshot de lead, não do merchant ao vivo | ✅ **MET** | TC-05 PASS — merchant=5 no funding, account snap=1 (== lead snap), NÃO 5 (account 11551, snap pk=7) `[db-observation:qa2]` |
| **AC-04** (4 campos preservados) | EPO5, EPO10, UW Pipeline, Fraud Threshold | ✅ **MET** (lead+conta) | TC-01 + TC-06-baseline (4 colunas asseridas) + TC-03a–d (imutabilidade por-campo) |
| **(invariante)** (sem account snap sem lead snap) | Skip silencioso quando não há lead snap; conta ainda criada | ✅ **MET** | TC-07 PASS — lead 16580 sem lead snap (DELETE escopado) → funding criou account 11552 (ACTIVE) mas **0 account snapshots** `[db-observation:qa2, account_pk=11552]` |

**Resumo AC:** AC-01 (aprovação + funding), AC-02, AC-03, AC-04 **e a invariante "sem account snap sem lead snap" (TC-07)** confirmados empiricamente (criação + imutabilidade + derive-from-lead + gate). AC-01 (ramo de denial) **UNVERIFIED em qa2** (env, não bug) — única AC ainda não verificada.

---

## Avaliação de cobertura vs Risco (SPEC §Risk)

| Área de risco (SPEC) | Nível | TC | Coberto? |
|---|---|---|---|
| Account snapshot no funding (0 rows em qa2 antes do deploy) | **HIGH** | TC-06-baseline | ✅ **Adequado** — disparou e validado (2×) |
| Account-snapshot-deriva-de-lead (trap de re-leitura ao vivo) | **HIGH** | TC-05 | ✅ **Adequado** — merchant=5 ignorado; account snap=1 |
| Sem account snap sem lead snap | **HIGH** | TC-07 | ✅ **Adequado** — DELETE escopado → conta criada, 0 account snapshots |
| Lead snapshot na aprovação | MED | TC-01 | ✅ Adequado |
| Imutabilidade lead snapshot (4 campos) | MED | TC-03a–d | ✅ **Adequado** — 4 settings cobertos individualmente |
| Sem snapshot no denial | LOW-MED | TC-02 | ⚠️ Gap de env — trigger de denial não dispara em qa2 |
| Activity log (Rule #13) | MED (observ.) | TC-08 | ✅ Probado — `[OBSERVAÇÃO]` ausência de nota |

---

## Bugs de Aplicação Encontrados

**Nenhum.** Todas as asserções da feature de snapshot que efetivamente executaram passaram (criação na aprovação, criação no funding, imutabilidade nos 4 campos, derive-from-lead). Os ciclos 1–2 só revelaram defeitos **test-side** (já resolvidos pelo qa-debugger):
- **F-CYCLE2-A** (resolvido) — `test.afterAll` usava fixtures test-scoped (`page`/`testEnv`); ciclo 3 verde + auto-restore funcionando.
- **F-CYCLE2-B** (resolvido) — page object EPO triple-checkbox `#epoN-false` not visible; ciclo 3 TC-03c/d verdes.
- **TC-02** — test-data/env (sem trigger UW_DENIED determinístico em qa2) → `[OBSERVAÇÃO]`/`[CONFIRMADO] env`, não bug da aplicação.

Conforme Rule #10/#16 + [skill:bug-classification]: nenhuma reprodução em dado fresh apontou inconsistência determinística da aplicação; o comportamento de snapshot foi correto em 100% dos pontos exercitados.

---

## Resumo da Validação

| Item | Status |
|---|---|
| Lead snapshot na aprovação (4 valores, COUNT=1) | ✅ CONFIRMADO (7 leads, cada COUNT=1) |
| Account snapshot dispara no funding | ✅ CONFIRMADO (2×: account 11550 + 11551) |
| Imutabilidade do lead snapshot (TC-03a–d, 4 campos) | ✅ CONFIRMADO |
| Conta deriva de lead com merchant alterado (TC-05) | ✅ CONFIRMADO (account snap=1, não 5) |
| 4 campos preservados (EPO5/EPO10/Pipeline/Fraud) | ✅ CONFIRMADO (lead + conta) |
| Sem account snap sem lead snap (TC-07) | ✅ CONFIRMADO (account 11552 ACTIVE, 0 account snapshots) |
| Denial não grava snapshot (TC-02) | ⚠️ UNVERIFIED em qa2 (env, não bug) |
| Activity log na criação do snapshot (TC-08) | ⚠️ `[OBSERVAÇÃO]` ausente (não-bloqueante) |
| TC-07 DELETE escopado (só lead 16580) | ✅ Confirmado (7 leads anteriores intactos; merchant sem drift) |
| Merchant pk26 sem drift pós-execução | ✅ Confirmado (auto-restore OK) |
| tsc da suite + page object limpo | ✅ |
| Bugs de aplicação | ✅ NENHUM |

---

## Handoff

**Ciclo de validação: 3/3 (último).** Runner verde — **8 TCs PASS (incluindo TC-07 desbloqueado) / 0 failed / 1 skip (TC-02 env)**. Core da feature **validado**, invariante "sem account snap sem lead snap" agora **MET**.

**Pronto para:** `qa-doc-keeper` — para alimentar catálogos/pitfalls com os fixes test-side dos ciclos 1–2 (filtro "Search table" no MerchantSettingPage; afterAll worker-scoped; EPO triple-checkbox) e registrar o gap de env do TC-02 (qa2 sem short-circuit ending-in-9 / BlackBox decide sem vendor) em [[application-lifecycle]] (Rule #11).

**Pendências para fechamento total da DoD (NÃO bloqueiam o core, mas impedem fechar a task):**
1. **TC-02** — decisão de PO/dev sobre um trigger `UW_DENIED` determinístico em qa2, OU execução em sandbox/qa1 onde o short-circuit ending-in-9 vale. AC-01 (ramo denial) permanece UNVERIFIED em qa2 até então.
2. **Re-run em stg** exigido pelo DoD da SPEC antes de fechar a task.

> **TC-07 RESOLVIDO** (2026-06-16) — DELETE autorizado, rodado, PASS, behavior projetada confirmada. Saiu da lista de pendências.

**`-evidence.md` (stakeholder-facing): NÃO gerado** — pipeline não fechado (TC-02 + stg em aberto). Gerar apenas quando os 2 itens restantes da DoD forem resolvidos e o usuário sinalizar fechamento (Rule #17).

---

**Skills loaded:**
- `.claude/skills/test-report-standard/SKILL.md`
- `.claude/skills/bug-classification/SKILL.md`
- `.claude/skills/qa-domain-reflexes/SKILL.md`
- `.claude/skills/db-polling-pattern/SKILL.md`
- `.claude/skills/volatile-knowledge-registry/SKILL.md`
