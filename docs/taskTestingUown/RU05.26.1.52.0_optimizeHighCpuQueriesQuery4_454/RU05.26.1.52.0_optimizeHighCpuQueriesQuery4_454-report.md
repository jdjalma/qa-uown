> **Aviso (regra #16):** Este arquivo é registro de execução, NÃO fonte de padrão.
> Patterns autoritativos (selectors, helpers, page objects, classification) vivem em
> `.claude/skills/` e em `src/` / `tests/`. Reusar leadPks/accountPks abaixo SEM revalidar
> contra o DB do ambiente atual é proibido (categoria "volatile" — ver skill
> [[volatile-knowledge-registry]]). Toda asserção técnica deste report carrega
> source-tag fresca da execução de 2026-05-24; classificações anteriores (se houver)
> NÃO foram herdadas.

# Task Report — RU05.26.1.52.0_optimizeHighCpuQueriesQuery4_454

## Metadata

| Campo | Valor |
|---|---|
| Task ID | `RU05.26.1.52.0_optimizeHighCpuQueriesQuery4_454` |
| Source (GitLab) | [svc#454](https://gitlab.com/uown/backend/svc/-/work_items/454) — MR [!1370](https://gitlab.com/uown/backend/svc/-/merge_requests/1370) |
| Implementer | `qa-implementer` (3 specs novos) |
| Validator run date | `2026-05-24 00:38–00:48 -03` |
| Environment | `qa1` (`ENV=qa1`, `UOWN_DB_URL_QA1=jdbc:postgresql://127.0.0.1:5445/svc`) |
| Branch / commit | `dev` @ `7ebdf33` |
| Reprodutor (one-liner) | `ENV=qa1 npx playwright test --project=api-only tests/api/origination/simple-search-los.spec.ts --reporter=list && ENV=qa1 npx playwright test --project=origination-ui tests/e2e/origination/simple-search-ui.spec.ts --reporter=list && ENV=qa1 npx playwright test --project=servicing-ui tests/e2e/servicing/simple-search-svc-regression.spec.ts --reporter=list` |
| Reports HTML | `reports/html/index.html` (último run sobrescrito por suite) |

## Test Suite

| Spec | Project | Total | Passed | Failed | Skipped | Duração |
|---|---|---|---|---|---|---|
| `tests/api/origination/simple-search-los.spec.ts` | `api-only` | 14 | 1 | 13 | 0 | 36.9s |
| `tests/e2e/origination/simple-search-ui.spec.ts` | `origination-ui` | 9 | 0 | 9 | 0 | ~2m36s (12s/CT) |
| `tests/e2e/servicing/simple-search-svc-regression.spec.ts` | `servicing-ui` | 10 | 0 | 10 | 0 | ~1m54s |
| **TOTAL** | — | **33** | **1** | **32** | **0** | ~5m20s |

> Auth setup (`tests/auth.setup.ts` × 2 — origination + servicing) executou OK em todos os runs UI; storageState válido. Não contado nos 33 CTs.

## Cobertura de Requirements vs ACs (do SPEC §4)

| AC (inferido) | CTs designados | Resultado | Status |
|---|---|---|---|
| AC1 — Cada `searchType` aciona a SQL especializada correta | UI-01..09 (origination), SVC-UI-01..10 (servicing) | UI Origination: 0/9 passou (selector `#search-input` não visível em `beforeEach`); UI Servicing: 0/10 passou (UI navegou, mas resultados não contêm Karen) | **[NÃO VERIFICADO]** — bloqueado por falha de SearchPage/storageState; não há evidência de que AC1 funcione, mas também não há evidência de regressão de backend (API LOS retorna 400 e SVC GET retorna 200 com lista — comportamento de backend não exercitado pela UI) |
| AC2 — Payload mantém shape (campos não-null) | UI-01 (createdTimestamp Karen), CT-BUG-1 (FreeText) | Não exercitado (UI-01 falhou antes do assert; CT-BUG-1 falhou em status 400 antes do assert de shape) | **[NÃO VERIFICADO]** |
| AC3 — Dedup por leadPk | UI-08, UI-09 | UI-08/UI-09 falharam em `ensureSearchVisible` | **[NÃO VERIFICADO]** |
| AC4 — Multi-tenancy via `merchantRefCodes` | API-MT-01..03 | MT-01 (refCode válido): 401; MT-02 (refCode bogus): 401; MT-03 (omit/empty/star): omit=400, empty=400, star=400 | **[NÃO VERIFICADO]** — backend rejeitou TODOS os payloads na rota POST `/uown/los/simpleSearch/{term}` |
| AC5 — Expression indexes usados (EXPLAIN) | CT-BUG-2 (idx UPPER invoice), CT-BUG-3 (idx cc_last_four_digit) | Ambos retornaram **Seq Scan** | **[CONFIRMADO BUG]** — ver bugs abaixo |
| AC6 — `searchType=null` faz pre-detect | API-PRE-01..04 | Todos retornaram 400 | **[NÃO VERIFICADO]** — endpoint rejeita; possivelmente contrato mudou e `merchantRefCodes` agora é obrigatório, OU rotação de chave/header (API key/Authorization) regrediu |
| AC7 — SVC `simpleSearch` permanece funcional (regressão) | SVC-UI-01..10 | UI navegou, mas resposta retornou leads não correlatos a `11319`; backend respondeu 200 com 17 rows (não-empty) | **[OBSERVAÇÃO]** — backend SVC respondeu; ou o UI não enviou o `searchType` correto, ou a SearchPage clicou no dropdown errado, ou o backend SVC ignora o `searchType` em qa1 |

## Findings adicionais

| ID | Tipo | Severidade × Prioridade | Descrição |
|---|---|---|---|
| F-01 | **[CONFIRMADO]** bug pré-existente da task | S2 × P1 | **CT-BUG-2** — `ByInvoiceNum` faz `Seq Scan on uown_los_invoice` (10.067 rows scaned) em vez de `Bitmap Index Scan on idx_los_invoice_merchant_invoice_number_upper`. EXPLAIN ANALYZE 363.549 ms (Planning 151.499 ms + Execution 211 ms). Plan extraído live em `simple-search-los.spec.ts:251` execução de 2026-05-24 00:38. Source-tag: `pg EXPLAIN ANALYZE GETLOSSEARCH_BYINVOICENUM @ qa1 svc DB 127.0.0.1:5445 2026-05-24`. Fix proposto (SPEC §8.6): `WHERE UPPER(invoice.merchant_invoice_number) = UPPER(:s)`. |
| F-02 | **[CONFIRMADO]** bug pré-existente da task | S2 × P1 | **CT-BUG-3** — `ByLast4CC` faz `Seq Scan on uown_los_credit_card` (7.374 rows removed; 15 matches). EXPLAIN ANALYZE 223.933 ms. Plan extraído live em `simple-search-los.spec.ts:268` execução 2026-05-24 00:38. Source-tag: `pg EXPLAIN ANALYZE GETLOSSEARCH_BYLAST4CC @ qa1 svc DB 2026-05-24`. Fix proposto: `CREATE INDEX CONCURRENTLY idx_los_cc_last_four_digit ON uown_los_credit_card (cc_last_four_digit)`. |
| F-03 | **[OBSERVAÇÃO]** drift de contrato API | S2 × P1 | **POST `/uown/los/simpleSearch/{term}` retorna 400 para body válido em qa1.** Todos os 4 `API-PRE-*` (FreeText pre-detect com body `{}`) e o `API-PRE-04` happy retornam 400. Anteriormente o SPEC §8.5 documentava 200 (anomalia #4). Possíveis causas: (a) hotfix endureceu a validação de body (`merchantRefCodes` agora required), (b) rotação de chave/header inválida no ambiente, (c) endpoint atrás de novo auth aspect. Não classificar como bug sem investigar o request payload real e o response body (não foi capturado). Source-tag: `playwright test run @ 2026-05-24 00:38 — api-only project, BaseClient.headers default`. |
| F-04 | **[OBSERVAÇÃO]** drift de contrato API | S2 × P1 | **API-MT-01/02 retornam 401** quando enviam `merchantRefCodes` populado (válido OU bogus). API-MT-03 retorna 400 nas 3 formas (omit / `[]` / `["*"]`). Sinaliza que `merchantRefCodes` enviado dispara auth check (possivelmente o backend cruza ref code com a tenancy do principal autenticado e rejeita). Pendente: capturar response body para diagnose (atualmente helper só lê status). |
| F-05 | **[OBSERVAÇÃO]** drift de comportamento (anomalies "fixadas") | S3 × P2 | **API-EDGE-01** (POST sem body) hoje retorna **400** — antes (SPEC §8.5) era 200. **API-EDGE-02** (sem Content-Type) hoje retorna **400** — antes era 500 `{unauthorized:true}` — **EDGE-03** (JSON inválido) retorna **400** (consistente com SPEC §8.5 — único passou). **API-EDGE-04** (`searchType=BogusType`) hoje retorna **400** — antes era 200+0. As 4 anomalias parecem ter sido endurecidas no backend. Pendente: confirmar com Yuri se Q2 do SPEC é resolvida. |
| F-06 | **[HIPÓTESE]** teste mal-escrito OU regressão real de SearchPage Origination | S2 × P1 | **UI-01..09 todas falharam em `SearchPage.ensureSearchVisible()`**: `locator('#search-input')` não fica visível em 10s após `goto(originationUrl)` + login (storageState válido). Causas possíveis: (a) `#search-input` selector ficou stale (regressão de FE — o Origination redesenhou navbar), (b) `ensureSearchVisible` precisa de toggle (botão expand) que mudou de selector, (c) viewport 1440×900 está sendo aplicado depois do load mas o Bootstrap `d-lg-block` já decidiu esconder. Screenshots em `reports/test-results/simple-search-ui-*/test-failed-1.png`. **Não é bug do produto até DOM-first investigation (regra #15) confirmar.** Devolver para `qa-debugger` para investigar via MCP. |
| F-07 | **[HIPÓTESE]** teste mal-escrito OU SVC search "Lead #" não filtra | S2 × P1 | **SVC-UI-01..10 todas falharam após captura de response 200**: backend respondeu com 17 rows mas Karen (lead 11319) NÃO está entre eles — resposta parece ser listagem default não filtrada (`leadPks=[4789,11767,4788,11766,...]`). Karen confirmadamente existe (`SELECT pk FROM uown_los_lead WHERE pk=11319` → 1 row). Causas possíveis: (a) `SearchPage.searchByType('Lead #', '11319')` clicou em outra opção do dropdown (label drift), (b) o `searchType` enviado pela UI Servicing é "LeadPk" mas o helper `captureSimpleSearchSvcResponse` casa com `searchType=LeadPk` → o **request** foi enviado correto mas o **backend ignorou**, (c) a UI Servicing renderiza autocomplete só para subset e Karen está fora do top-15 default. Pendente DOM-first + leitura do response real (não foi capturado). |
| F-08 | **[OBSERVAÇÃO]** | S4 × P3 | `[global-setup] Optional env vars missing for ENV=qa1: DB_CONNECTION_STRING` aparece em todo run. Variável opcional, não bloqueante. |

## Bugs confirmed

| ID do Bug | CT prova | Estado live qa1 | Fix proposto (SPEC §8.6) |
|---|---|---|---|
| **BUG-2 — Seq Scan em `uown_los_invoice` ao buscar por invoice** | CT-BUG-2 (`simple-search-los.spec.ts:251`) | Seq Scan filter `(merchant_invoice_number)::text = 'R1925054'`. 10.067 rows scanned; 5 matches; 211 ms execution + 151 ms planning. | 1-liner SQL: `WHERE UPPER(invoice.merchant_invoice_number) = UPPER(:s)` em `GETLOSSEARCH_BYINVOICENUM.sql` (necessário pra usar `idx_los_invoice_merchant_invoice_number_upper`). |
| **BUG-3 — sem índice em `uown_los_credit_card.cc_last_four_digit`** | CT-BUG-3 (`simple-search-los.spec.ts:268`) | Seq Scan filter `cc_last_four_digit = '2225'::bpchar`. 7.374 rows removed; 15 matches; 223 ms total. | Migration: `CREATE INDEX CONCURRENTLY idx_los_cc_last_four_digit ON uown_los_credit_card (cc_last_four_digit);`. SPEC §8.6 inclui também: garantir DISTINCT/dedup por `leadPk` no SQL (não exercitado pelos asserts da UI — UI-09 falhou antes). |
| **BUG-1 — `createdTimestamp=null` em FreeText (alias `rowCreatedTime`)** | CT-BUG-1 (`simple-search-los.spec.ts:235`) | **NÃO VERIFICADO nesta execução** — POST FreeText retornou 400 antes do assert de shape. Não posso confirmar como `[CONFIRMADO]` na execução de 2026-05-24 sem fresh repro. Status: degrada para **[HIPÓTESE]** até o F-03 ser destravado. SPEC §8.6 cita evidência live anterior (createdTimestamp=null) mas regra #10 exige fresh repro a cada validator pass. | 1-liner SQL: alias `rowCreatedTime AS createdTimestamp` em `GETLOSSEARCH_FREETEXT.sql`. |

> **Resumo:** 2 bugs CONFIRMADOS via EXPLAIN (BUG-2, BUG-3); 1 bug em **[HIPÓTESE]** (BUG-1) — não foi possível reproduzir por causa do F-03 (endpoint 400). Recomendação: destravar F-03 primeiro (capturar response body para entender contrato real), depois re-rodar CT-BUG-1.

## Questions to PO (do SPEC §3 — repetidas + uma nova)

- **Q1** — Os 3 bugs (BUG-1 / BUG-2 / BUG-3) entram nesta task ou abrem novas? Recomendação: piggyback no R1.52.x hotfix.
- **Q2** — Anomalias #4-#7 do SPEC §8.5 parecem ter sido endurecidas em qa1 (todas viraram 400). Confirmar se foi fix intencional e fechar Q2 com "by design now".
- **Q3** — `merchantRefCodes` vazio/omitido/`["*"]` em qa1 retorna 400 (não wildcard nem erro de auth). Confirmar contrato atual e atualizar SPEC.
- **Q4 (nova)** — Por que `merchantRefCodes` populado (refCode válido OU bogus) dispara 401 em qa1? Indica que o backend cruza o refCode com a tenancy do principal — comportamento esperado ou bug de tenancy enforcement?

## Improvements (descobertos durante validação — devolver para `qa-doc-keeper` / `qa-debugger`)

- **IMP-01 (devolver para `qa-debugger`)** — Investigar via MCP Playwright DOM-first (regra #15) o estado real do `#search-input` no Origination em qa1 atualmente. Hipóteses: selector mudou, ou ensureSearchVisible precisa de prefix step (clicar em ícone de search no navbar). Anexar tabela "DOM Real vs Selector Atual" e propor fix em `src/pages/search.page.ts`.
- **IMP-02 (devolver para `qa-debugger`)** — Investigar via MCP por que SVC search devolve 17 leads não-correlatos para `term=11319`. Capturar o request URL real, o `searchType` param enviado, e o response body. Hipótese: `SearchPage.searchByType('Lead #', ...)` não está engajando o dropdown corretamente.
- **IMP-03 (devolver para `qa-implementer`)** — Estender `SimpleSearchClient` para capturar `response.body` mesmo em status 4xx/5xx (atualmente o helper só lê body em 200). Permite diagnose de F-03/F-04 sem re-instrumentar manualmente.
- **IMP-04 (devolver para `qa-doc-keeper`)** — Adicionar pitfall em `[[application-lifecycle]]` ou criar skill nova `search-endpoints-knowledge`: `POST /uown/los/simpleSearch/{term}` em qa1 (state 2026-05-24) retorna 400 para body padrão; investigar se `merchantRefCodes` virou required. Categoria volatile.
- **IMP-05 (devolver para `qa-implementer`)** — Splitar o assert do `ensureSearchVisible` para fora do `beforeEach` (ou tornar opcional) — bloqueio em `beforeEach` mascara o teste real e gera 9 ✘ idênticos sem evidência diferenciada.

## Coverage assessment vs Risk (SPEC §5)

| Área de risco | Nível | Cenário | Adequação |
|---|---|---|---|
| Dedup last4CC (lead 4019 com 26 CCs) | High | UI-09, CT-BUG-3 (parte b API) | **❌ Não verificado** — UI-09 falhou em `ensureSearchVisible`; CT-BUG-3 parte (a) confirmou Seq Scan mas parte (b) dedup não rodou (POST 400) |
| FreeText payload shape | High | CT-BUG-1, API-PRE-04 | **❌ Não verificado** — endpoint 400 |
| Multi-tenancy (AOP) | High | API-MT-01..03 | **❌ Não verificado** — 401/400 |
| Índice expression UPPER (BUG-2) | Medium | CT-BUG-2 | **✅ Confirmado bug via EXPLAIN** |
| Router pre-detect | Medium | API-PRE-01..04 | **❌ Não verificado** — 400 |
| SVC regressão paralela (AC7) | Medium | SVC-UI-01..10 | **❌ Não verificado** — UI navegou mas dados não bateram |
| Contract API edge (anomalies #4-#7) | Low | API-EDGE-01..04 | **🟡 Drift documentado em F-05** — 3 mudaram, 1 igual |

**Veredito de coverage**: **INSUFICIENTE para fechar task #454**. Apenas 1 dos 7 ACs foi efetivamente validado (parcialmente — só os 2 EXPLAIN). Os outros 6 ACs precisam ser destravados após F-03/F-06/F-07 serem investigados.

## Handoff

**Ready for:** `qa-debugger` (DOM-first em IMP-01 e IMP-02) + investigação manual do payload 400 (IMP-03/IMP-04). NÃO encaminhar para `qa-doc-keeper` ainda — coverage está insuficiente; é preciso re-rodar após fix dos artefatos de teste.

Depois do `qa-debugger`:
1. Re-rodar os 3 specs com correções aplicadas (`qa-implementer` aplica patches em `SearchPage` + `SimpleSearchClient`).
2. `qa-validator` (este agente) re-roda e atualiza este report (regra #7).
3. Se F-03 destravar e CT-BUG-1 reproduzir, promover BUG-1 de **[HIPÓTESE]** para **[CONFIRMADO]**.
4. `qa-doc-keeper` last (regra #4) catalogando IMP-04.

---

## Execution 2 (2026-05-24 — post-infra-fix)

> **Aviso (regra #16):** Esta seção é registro de execução fresh em `2026-05-24` (~13:00–14:30 -03).
> NÃO herda classificações da Execution 1 — toda asserção carrega source-tag desta corrida.
> Patterns autoritativos vivem em `.claude/skills/` e em `src/` / `tests/`; este arquivo é histórico.
> Categorias drift-prone (sweep SQL, schema de tabela, merchant config) listadas em
> [[volatile-knowledge-registry]] — re-confirmar via fonte primária antes de afirmar.

### Mudanças desde Execution 1 (recap)

- 🗑️ `tests/api/origination/simple-search-los.spec.ts` removido (14 CTs API descartados — endpoint LOS exige BFF session, inviável API-only sem complicação maior; regra #14 UI-first)
- ➕ `tests/api/origination/simple-search-explain.spec.ts` — 2 CTs DB-direct EXPLAIN (BUG-2 + BUG-3)
- ➕ `tests/e2e/origination/simple-search-bug1.spec.ts` — 1 CT via `page.evaluate(fetch)` reutilizando session cookie do Origination login
- 🔧 `tests/e2e/origination/simple-search-ui.spec.ts` — indicator `'Origination Login'` → `'Merchant Login'`
- 🔧 `tests/e2e/servicing/simple-search-svc-regression.spec.ts` — wrapper `{searchResults}` parser + accountPk dinâmico
- 🔧 `src/api/responses/simple-search.response.ts` — schema corrigido para wrapper

Total agora: **22 CTs** (cleanup per regra #14, era 33).

### Test Suite (Execution 2)

| Spec | Project | Total | Passed | Failed | Skipped | Duração |
|---|---|---|---|---|---|---|
| `tests/api/origination/simple-search-explain.spec.ts` | `api-only` | 2 | 0 | 2 | 0 | ~5s |
| `tests/e2e/origination/simple-search-bug1.spec.ts` | `origination-ui` | 1 | 0 | 1 | 0 | ~3s |
| `tests/e2e/origination/simple-search-ui.spec.ts` | `origination-ui` | 9 | 4 | 5 | 0 | ~2m0s |
| `tests/e2e/servicing/simple-search-svc-regression.spec.ts` | `servicing-ui` | 10 | 0 | 9 | 1 | ~2m18s |
| **TOTAL** | — | **22** | **4** | **17** | **1** | ~4m26s |

> Auth setup OK (origination + servicing storageState válido em todos os runs).
> Source-tag execução: `playwright run @ qa1 2026-05-24 13:xx-14:xx -03 — branch dev @ 7ebdf33 + 4 files edited`.

### Comparativo Exec1 → Exec2

| Métrica | Exec 1 | Exec 2 | Δ |
|---|---|---|---|
| Total CTs | 33 | 22 | −11 (cleanup) |
| Passed | 1 | 4 | +3 |
| Failed | 32 | 17 | −15 |
| Skipped | 0 | 1 | +1 |
| Pass rate | 3% | 18% | +15pp |

**CTs que mudaram de cor (Exec1 vermelha → Exec2 mudou):**

| CT | Exec 1 | Exec 2 | Motivo da mudança |
|---|---|---|---|
| UI-01 (Lead #) | ✘ ensureSearchVisible | ✅ PASS | F-2 fix (`'Origination Login'` → `'Merchant Login'` indicator) destravou navegação |
| UI-05 (SSN) | ✘ ensureSearchVisible | ✅ PASS | idem |
| UI-06 (Invoice #) | ✘ ensureSearchVisible | ✅ PASS | idem |
| UI-08 (Name) | ✘ ensureSearchVisible | ✅ PASS | idem |
| API-LOS-* (14 CTs) | ✘ 401/400/500 | — | deletadas; substituídas por explain.spec + bug1.spec |
| CT-BUG-2 (EXPLAIN invoice) | ✘ Seq Scan | ✘ Seq Scan | sem mudança — bug persiste (esperado) |
| CT-BUG-3 (EXPLAIN last4cc) | ✘ Seq Scan | ✘ Seq Scan | sem mudança — bug persiste (esperado) |
| CT-BUG-1 (FreeText createdTimestamp) | ✘ (não verificado em Exec1, era HIPÓTESE) | ✘ `createdTimestamp=null` | **promovido [HIPÓTESE] → [CONFIRMADO]** via fresh repro (page.evaluate fetch reusando session origination) |
| SVC-UI-01..10 | ✘ todos | ✘ 9 fail + 1 skip | parser fix permitiu capturar response, mas Karen não aparece nos resultados |

### Status final dos 3 bugs (post Exec 2)

| Bug | Status Exec 1 | Status Exec 2 | Source-tag |
|---|---|---|---|
| **BUG-1** — `createdTimestamp=null` em FreeText | [HIPÓTESE] (não verificado) | **[CONFIRMADO]** | `page.evaluate(fetch POST /uown/los/simpleSearch/Karen)` em `simple-search-bug1.spec.ts:120` — response retornou `createdTimestamp: null` para lead Karen; assert `not.toBeNull()` falhou. Run @ qa1 2026-05-24. Screenshot: `reports/test-results/simple-search-bug1-*/test-failed-1.png`. |
| **BUG-2** — Seq Scan em `uown_los_invoice` | [CONFIRMADO] | **[CONFIRMADO]** (re-confirmed) | EXPLAIN ANALYZE `GETLOSSEARCH_BYINVOICENUM` para `'R1925054'` → `Seq Scan on uown_los_invoice` (10.067 rows scanned; Planning 151ms + Execution 211ms ≈ 366ms). Live em `simple-search-explain.spec.ts:59` @ qa1 2026-05-24 13:xx. |
| **BUG-3** — sem índice em `cc_last_four_digit` | [CONFIRMADO] | **[CONFIRMADO]** (re-confirmed) | EXPLAIN ANALYZE para `cc_last_four_digit = '2225'::bpchar` → `Seq Scan on uown_los_credit_card cc` (Rows Removed by Filter: 7.401; 15 matches; total ~311ms). Live em `simple-search-explain.spec.ts:76` @ qa1 2026-05-24 13:xx. |

**Os 3 bugs permanecem CONFIRMADOS após Execution 2.** BUG-1 foi promovido de [HIPÓTESE] para [CONFIRMADO] graças ao novo spec que contornou o requisito de BFF session via `page.evaluate(fetch)`.

### Findings — Status atualizado

| ID | Status anterior | Status agora | Notas |
|---|---|---|---|
| F-01 (BUG-2 Seq Scan) | [CONFIRMADO] | **[CONFIRMADO]** | Re-validado — mesma plan, mesma evidência |
| F-02 (BUG-3 Seq Scan) | [CONFIRMADO] | **[CONFIRMADO]** | Re-validado |
| F-03 (POST LOS 400) | [OBSERVAÇÃO] drift | **RESOLVED via approach change** | Spec API deletado; CT-BUG-1 agora roda via UI session cookie (page.evaluate) → endpoint responde 200 normalmente quando autenticado. Confirmação que F-03 era falta de BFF session, não drift de contrato. |
| F-04 (API-MT 401/400) | [OBSERVAÇÃO] | **N/A — escopo removido** | API multi-tenancy spec foi deletado (regra #14 UI-first); revalidação fica para escopo separado se priorizado |
| F-05 (API-EDGE drift) | [OBSERVAÇÃO] | **N/A — escopo removido** | Idem F-04 |
| F-06 (SearchPage `#search-input` timeout) | [HIPÓTESE] | **RESOLVED** | Fix da string indicator (`'Origination Login'` → `'Merchant Login'`) destravou — 4 CTs UI Origination passaram (UI-01, 05, 06, 08). Não era selector stale; era login wait condition. |
| F-07 (SVC search não filtra) | [HIPÓTESE] | **[CONFIRMADO regressão de teste OU UI]** | Parser fix permitiu capturar response, mas Karen (leadPk=11339) NÃO aparece em nenhum search SVC (lead/account/invoice/name/contract). Backend retorna outros leads ou listagem default. Ver F-10 abaixo. |
| F-08 (env var aviso) | [OBSERVAÇÃO] | sem mudança | Mantém — não bloqueante |

### Findings NOVOS (Execution 2)

| ID | Tipo | Sev × Pri | Descrição |
|---|---|---|---|
| **F-09** | **[CONFIRMADO]** bug de teste | S2 × P1 | **UI-03 e SVC-UI-03 falham com `Database query failed: relation "uown_los_lead_personal_info" does not exist`** em `resolveKarenPhone`. Schema qa1 não tem essa tabela — helper `resolveKarenPhone` (em `simple-search-ui.spec.ts:132` e `simple-search-svc-regression.spec.ts:135`) assume tabela inexistente. Fix: usar tabela canônica (`uown_los_phone` por leadPk, ou outra fonte de phone). Source-tag: error stack em `database.helpers.ts:135` @ run 2026-05-24. **Categoria volatile** (schema). |
| **F-10** | **[HIPÓTESE]** regressão de UI SVC OU teste mal-escrito | S2 × P1 | **SVC-UI-01/02/05/06/07/10**: response 200 captured, mas Karen (leadPk=11339) NÃO presente. Backend retorna leads alheios (ex.: `[4807, 4806, 4805, ...]` ou `[11339, 11790, 11789, ...]` para lead-search por `'11339'` — em alguns CTs Karen aparece como #1, em outros não). Pattern: SVC UI parece estar enviando search term mas backend filtro não bate. Possíveis causas: (a) `SearchPage.searchByType('Lead #', '11339')` para Servicing clica em dropdown errado (label drift FE), (b) Servicing UI consome `searchType` diferente do esperado (e.g. `LeadPk` vs `LeadId`), (c) helper usa endpoint Origination por engano. **Não classificar como bug do produto sem DOM-first** (regra #15) — devolver ao debugger para inspecionar request real (query-string params reais enviados). Pendente: capturar `req.url()` + `req.method()` no waitForResponse, comparar com o que UI manda. |
| **F-11** | **[CONFIRMADO]** bug de teste | S3 × P2 | **SVC-UI-09 (Ref Account ID)**: response retorna `accountPks: [4524]` (number) mas assert `expect(accountPks).toContain(KAREN.accountPk)` com `KAREN.accountPk='4524'` (string) falha. Type mismatch number vs string. Fix em `simple-search-svc-regression.spec.ts:522`: normalizar tipo (`accountPks.map(String)` ou `KAREN.accountPk` resolver como number). Source-tag: stack trace exec 2026-05-24. **Observação**: o backend RESPONDEU CORRETAMENTE para SVC-UI-09 — único SVC-UI- que de fato achou Karen. |
| **F-12** | **[OBSERVAÇÃO]** | S3 × P2 | **UI-04 (Email) e UI-09 (Last 4 CC)**: `page.waitForResponse` timeout 15s — UI envia request mas helper não casa o `searchType` no URL. Possível causa: searchType param na URL difere do esperado (`Email` vs `EmailAddress`?), ou request usa POST sem searchType na URL (helper filtra por `url.includes('/uown/los/simpleSearch/')`). DOM-first + network inspection necessário. |

### Coverage assessment vs Risk (post Exec 2)

| Área de risco | Nível | Cenário | Adequação Exec 2 |
|---|---|---|---|
| Dedup last4CC (lead 4019, 26 CCs) | High | UI-09, CT-BUG-3 (b) | **❌ UI-09 timeout (F-12); BUG-3 parte EXPLAIN OK** |
| FreeText payload shape (BUG-1) | High | CT-BUG-1 | **✅ CONFIRMADO via fresh repro** |
| Índice expression UPPER (BUG-2) | Medium | CT-BUG-2 | **✅ CONFIRMADO** |
| Índice last4 (BUG-3) | Medium | CT-BUG-3 | **✅ CONFIRMADO** |
| SearchType routing (Origination) | High | UI-01..09 | **🟡 4/9 OK (44%)** — fail por bugs de teste (F-09, F-12), não por bug do produto |
| SVC regressão (AC7) | Medium | SVC-UI-01..10 | **❌ 0/10 — F-09 + F-10 + F-11 bloqueiam validação** |

**Veredito de coverage**: **PARCIAL para fechar task #454**.
- Os 3 bugs principais (BUG-1, BUG-2, BUG-3) estão CONFIRMADOS com fresh evidence — **isso é suficiente para abrir/atualizar tickets**.
- A regressão SVC (AC7) ainda NÃO foi validada — bloqueada por bugs no spec de teste, não no produto.

### Decisão final — Handoff

**NÃO pronto para `qa-doc-keeper` ainda.** Recomendo:

1. **`qa-debugger`** (DOM-first regra #15) para:
   - F-10: capturar request URL real do SVC search (lead, account, name, contract, last4cc) e comparar com o que o test helper espera. Hipótese: SVC UI usa endpoint `/uown/svc/simpleSearch/{term}?searchType=...` mas helper SearchPage envia searchType diferente do que aparece no dropdown.
   - F-12: capturar request real de Email + Last4CC no Origination (UI-04, UI-09).
2. **`qa-implementer`** (fixes triviais):
   - F-09: trocar `uown_los_lead_personal_info` por tabela existente (`uown_los_phone` em `resolveKarenPhone`).
   - F-11: normalizar tipo em SVC-UI-09 assert (`String(...)` ou unificar).
3. **`qa-validator`** (este agente) re-roda 3ª execução após fixes — espera-se 20-22/22 passando (com 3 bugs ainda falhando = comportamento esperado).
4. **`qa-doc-keeper`** finalmente, catalogando:
   - Pitfall: tabela `uown_los_lead_personal_info` NÃO existe em qa1; usar `uown_los_phone` (F-09).
   - Pitfall: BFF session cookie é necessário para POST `/uown/los/simpleSearch/{term}` — API-only direto retorna 401/400 (F-03 resolution).
   - Categoria volatile: schema search SQLs (BUG-2 expression index, BUG-3 missing index) — adicionar à [[volatile-knowledge-registry]] se ainda não estiver.

**Status dos bugs do produto**: BUG-1 / BUG-2 / BUG-3 todos **CONFIRMADOS via fresh repro**. Já podem entrar no R1.52.x hotfix conforme Q1 do SPEC §3.

---

## Execution 3 (2026-05-24 — post-round-2-fix)

> **Aviso (regra #16):** Esta seção é registro de execução fresh em `2026-05-24` (~late session).
> NÃO herda classificações da Execution 2 — toda asserção carrega source-tag desta corrida.
> Patterns autoritativos vivem em `.claude/skills/` e em `src/` / `tests/`; este arquivo é histórico.
> Categorias drift-prone listadas em [[volatile-knowledge-registry]] — re-confirmar fonte primária.

### Mudanças desde Execution 2 (recap dos Round 2 fixes)

- 🔧 F-09: `resolveKarenPhone` migrado de `uown_los_lead_personal_info` (inexistente) para `uown_los_phone` via leadPk
- 🔧 F-10: Karen (leadPk=11319) descoberta sem account → adicionado fallback `Testfndb Testlndb` (leadPk=11339, accountPk=4524) para SVC SVC search
- 🔧 F-11: SVC-UI-09 type normalize (`String(KAREN.accountPk)`) no assert
- 🔧 F-12: `waitForResponse` predicate ajustado (não mais filtrando por searchType na query-string)

### Test Suite (Execution 3)

| Spec | Project | Total | Passed | Failed | Skipped | Duração |
|---|---|---|---|---|---|---|
| `tests/api/origination/simple-search-explain.spec.ts` | `api-only` | 2 | 0 | 2 | 0 | ~2s |
| `tests/e2e/origination/simple-search-bug1.spec.ts` | `origination-ui` | 1 | 0 | 1 | 0 | ~3s |
| `tests/e2e/origination/simple-search-ui.spec.ts` | `origination-ui` | 9 | 6 | 3 | 0 | ~1m20s |
| `tests/e2e/servicing/simple-search-svc-regression.spec.ts` | `servicing-ui` | 10 | 2 | 8 | 0 | ~1m12s |
| **TOTAL (excluindo 4 auth.setup)** | — | **22** | **8** | **14** | **0** | ~3m48s |

Source-tag: `playwright run @ qa1 2026-05-24 late session — branch dev @ 7ebdf33 + Round 2 patches`.

### Comparativo Exec2 → Exec3

| Métrica | Exec 2 | Exec 3 | Δ |
|---|---|---|---|
| Total CTs | 22 | 22 | 0 |
| Passed | 4 | 8 | **+4** |
| Failed | 17 | 14 | **−3** |
| Skipped | 1 | 0 | −1 |
| Pass rate | 18% | **36%** | +18pp |

**Esperado**: ~18-20/22 (≥80%). **Real**: 8/22 (36%). **Abaixo da expectativa.**

### CTs que mudaram (Exec2 → Exec3)

| CT | Exec 2 | Exec 3 | Motivo |
|---|---|---|---|
| UI-03 (Phone) | ✘ uown_los_lead_personal_info missing | ✅ PASS | F-09 RESOLVED via tabela `uown_los_phone` |
| UI-09 (Last 4 CC) | ✘ waitForResponse timeout | ✅ PASS | F-12 parcialmente RESOLVED (UI-09 OK; UI-04 ainda timeout) |
| SVC-UI-02 (Account #) | ✘ Karen não retornada | ✅ PASS | Fallback leadPk=11339/account=4524 funcionou |
| SVC-UI-09 (Ref Account ID) | ✘ type mismatch | ✅ PASS | F-11 RESOLVED via `String(KAREN.accountPk)` |
| SVC-UI-03 (Phone) | ✘ tabela inexistente | ✘ Karen 11735 não retornada | F-09 (helper) RESOLVED, mas surge F-10' (UI/backend não filtra phone) |
| UI-02 (Account #) | ✅ — | ✘ NOVO FAIL — Karen 11319 não em resultados de account 4524 | **F-13 NOVO**: account 4524 pertence a Testfndb (11339), não Karen (11319) — drift de massa qa1 ou wrong constant |
| UI-07 (UUID) | ✅ — | ✘ NOVO FAIL — retornou 11 rows em vez de ≤2 | **F-14 NOVO**: UUID search não-único; investigar se KAREN.uuid está duplicado ou se searchType errado |

### Status final dos 3 bugs do produto (post Exec 3)

| Bug | Status Exec 2 | Status Exec 3 | Source-tag fresh (Exec 3) |
|---|---|---|---|
| **BUG-1** — `createdTimestamp=null` em FreeText | [CONFIRMADO] | **[CONFIRMADO]** (re-confirmed) | `page.evaluate(fetch POST /uown/los/simpleSearch/Karen)` em `simple-search-bug1.spec.ts:120` retornou `createdTimestamp: null` — assert `not.toBeNull()` falhou. Run @ qa1 2026-05-24 late. Screenshot: `reports/test-results/simple-search-bug1-RU05-26-6fc8b-*/test-failed-1.png`. |
| **BUG-2** — Seq Scan em `uown_los_invoice` | [CONFIRMADO] | **[CONFIRMADO]** (re-confirmed) | EXPLAIN ANALYZE `GETLOSSEARCH_BYINVOICENUM` para `'R1925054'` → `Seq Scan on uown_los_invoice` (10.107 rows scanned by filter, Planning 4.554ms + Execution 249.595ms ≈ 254ms). Live em `simple-search-explain.spec.ts:59` @ qa1 2026-05-24. |
| **BUG-3** — sem índice em `cc_last_four_digit` | [CONFIRMADO] | **[CONFIRMADO]** (re-confirmed) | EXPLAIN ANALYZE para `cc_last_four_digit = '2225'::bpchar` → `Seq Scan on uown_los_credit_card cc` (Rows Removed by Filter: 7.410; 15 matches; ~103ms). Live em `simple-search-explain.spec.ts:76` @ qa1 2026-05-24. |

**Os 3 bugs permanecem CONFIRMADOS após Execution 3** — comportamento esperado (vão fechar com hotfix R1.52.x).

### Findings — Status atualizado (Round 2)

| ID | Status Exec 2 | Status Exec 3 | Notas |
|---|---|---|---|
| F-09 (`uown_los_lead_personal_info` not exist) | [CONFIRMADO] bug teste | **RESOLVED** | UI-03 e SVC-UI-03 não falham mais com erro de schema. SVC-UI-03 falha agora por F-10' (não retornar lead). |
| F-10 (SVC search não filtra) | [HIPÓTESE] | **PARCIAL** | Fallback resolveu SVC-UI-02 (Account #). Mas SVC-UI-01/03/04/05/06/07/08/10 continuam falhando: backend retorna listas que NÃO contêm `KAREN.leadPk` (11339 esperado, recebe `[4766, 4813, …]` para contract; `[4264, …]` para last4; etc). Renomeado para **F-10'**: backend/UI Servicing não está filtrando pelo termo OU `Testfndb` (11339) não tem phone/email/SSN/invoice/contract correspondentes ao que o helper procura. **Investigação DOM-first ainda pendente** (regra #15). |
| F-11 (type mismatch) | [CONFIRMADO] bug teste | **RESOLVED** | SVC-UI-09 PASS (único SVC- que retorna Karen account corretamente). |
| F-12 (waitForResponse timeout) | [OBSERVAÇÃO] | **PARCIAL** | UI-09 (Last 4 CC) RESOLVED. UI-04 (Email) ainda timeout — predicate continua não casando para Email searchType. |

### Findings NOVOS (Execution 3)

| ID | Tipo | Sev × Pri | Descrição |
|---|---|---|---|
| **F-13** | **[HIPÓTESE]** bug de teste / drift de massa | S2 × P1 | **UI-02 (Origination Account #)**: search por `String(KAREN.accountPk)='4524'` retorna `leadPks=[11339, 11802, 11801, …]`, NÃO contém `KAREN.leadPk=11319`. Backend retorna ordenado por pk DESC e Karen (11319) está fora do top-10 OU `KAREN.accountPk=4524` pertence na verdade a `Testfndb` (leadPk=11339). Constante `KAREN.accountPk=4524` em `simple-search-ui.spec.ts:63` está suspeita — provavelmente massa drift entre quando o SPEC foi escrito e hoje. Source-tag: response live em `reports/test-results/simple-search-ui-RU05-26-1-9127d-*/error-context.md` 2026-05-24. **Categoria volatile** (test data qa1). Fix sugerido: re-resolver via DB `SELECT account_pk FROM uown_account WHERE lead_pk=11319` na before block. |
| **F-14** | **[HIPÓTESE]** bug de teste / UUID não-único | S3 × P2 | **UI-07 (UUID)**: search por `KAREN.uuid` retorna **11 rows** em vez de ≤2 esperado. SPEC §4 AC-UI-07 declara "UUID é único" mas backend retorna múltiplos leads. Possível causa: (a) `KAREN.uuid` está vazio/null e search degenera para wildcard, (b) UUID search no Origination tem comportamento prefix match, (c) backend SQL bug. Source-tag: 2026-05-24. **Não classificar como bug do produto sem DOM-first** (regra #15). |
| **F-10' (refresh)** | **[HIPÓTESE]** bug de teste OU regressão UI SVC | S2 × P1 | **SVC-UI-01/03/04/05/06/07/08/10 (8 CTs)**: response 200 captured, mas `KAREN.leadPk` (11339 — fallback Testfndb) não está nos resultados. Padrão observado: SVC backend retorna listas alheias para todos os termos (phone, email, SSN, invoice, name, last4, contract). Hipóteses possíveis: (a) Testfndb (11339) não tem esses campos populados em qa1 → cada CT deveria fazer skip se o campo for null (skip lógico já existe via `test.skip(!KAREN.email …)` mas a flag `KAREN.email` parece truthy de forma incorreta); (b) `SearchPage.searchByType()` em Servicing está enviando `searchType` diferente do que o BE espera; (c) backend qa1 está com indexação stale. **Bloqueante para AC7 — devolver para `qa-debugger` para DOM-first do request real** (regra #15). |
| **F-12' (refresh)** | **[OBSERVAÇÃO]** | S3 × P2 | **UI-04 (Email)**: `waitForResponse` timeout 15s persiste. UI-09 funcionou após fix; UI-04 ainda não. Hipótese: `searchByType('Email', …)` no Origination não dispara request, OU dispara com path diferente de `/uown/los/simpleSearch/`. DOM-first pendente. |

### Coverage assessment vs Risk (post Exec 3)

| Área de risco (SPEC) | Nível | Cenário | Adequação Exec 3 |
|---|---|---|---|
| Dedup last4CC (lead 4019, 26 CCs) | High | UI-09, CT-BUG-3 | **🟡 UI-09 PASS; BUG-3 EXPLAIN re-CONFIRMED (esperado falhar)** |
| FreeText payload shape (BUG-1) | High | CT-BUG-1 | **✅ CONFIRMADO via fresh repro** |
| Índice expression UPPER (BUG-2) | Medium | CT-BUG-2 | **✅ CONFIRMADO** |
| Índice last4 (BUG-3) | Medium | CT-BUG-3 | **✅ CONFIRMADO** |
| SearchType routing (Origination) | High | UI-01..09 | **🟡 6/9 OK (67%)** — UI-02/04/07 falham por bugs de teste/massa, não produto |
| SVC regressão (AC7) | Medium | SVC-UI-01..10 | **❌ 2/10 (20%) — F-10' bloqueia validação** |

### Cobertura AC do SPEC

| AC | CTs | Status Exec 3 |
|---|---|---|
| AC1 (FreeText createdTimestamp) | CT-BUG-1 | ✅ CONFIRMADO bug |
| AC2 (ByInvoiceNum index) | CT-BUG-2 | ✅ CONFIRMADO bug |
| AC3 (ByLast4CC index) | CT-BUG-3 | ✅ CONFIRMADO bug |
| AC4 (search by lead/account/phone/email/SSN/invoice/UUID/name/last4) | UI-01..09 | 🟡 6/9 OK (Lead, Phone, SSN, Invoice, Name, Last4 funcionam; Account, Email, UUID falham por test/data drift) |
| AC5 (Origination access) | UI-01..09 | ✅ auth + nav OK |
| AC6 (perf não regride) | EXPLAIN | ✅ medições live capturadas |
| AC7 (Servicing regressão) | SVC-UI-01..10 | ❌ 2/10 OK — F-10' bloqueia |

### Decisão final — Handoff

**❌ NÃO pronto para `qa-doc-keeper` ainda.**

Critério "pronto pra doc-keeper" do orchestrator:
- ≥80% dos CTs não-BUG passando → **FALHOU: 8/19 = 42%** (esperado ≥15/19 = 79%)
- 3 BUGs CONFIRMADOS via fresh evidence → ✅ OK
- Sem F-* novo de severidade S2+ → **FALHOU: F-13 (S2) novo e F-10' continua S2**
- AC1-AC7 cobertos com ≥1 CT verde → 🟡 AC7 ainda 20%, abaixo do mínimo

**Recomendação**: ainda devolver para `qa-debugger` + `qa-implementer` para um **Round 3**:

1. **`qa-debugger`** (DOM-first regra #15) para:
   - **F-10'** (CRÍTICO — bloqueia AC7 SVC): capturar request real do SVC search via MCP Playwright (`browser_network_requests`); validar query-string/body do `simpleSearch` para cada searchType (Lead, Phone, Email, SSN, Invoice, Name, Last4CC, Contract); cross-check com o que `SearchPage.searchByType()` envia. Verificar também se Testfndb (11339) tem phone/email/SSN/invoice/contract NOT NULL em qa1 (`SELECT * FROM uown_los_phone WHERE lead_pk=11339` etc).
   - **F-12'** (UI-04 Email): repetir investigação para Email searchType em Origination.
   - **F-14** (UI-07 UUID): inspecionar o que `KAREN.uuid` resolve e o que o backend retorna; verificar se UUID está vazio.
2. **`qa-implementer`** (fixes triviais quando F-* destravado):
   - **F-13**: ré-resolver `KAREN.accountPk` via DB live (`SELECT account_pk FROM uown_account WHERE lead_pk=11319`) ou re-pegar massa happy-path qa1 correta no `before`.
3. **`qa-validator`** (este agente) re-roda **Execution 4** — alvo realista pós-Round-3: ≥18/22 (≥82%).
4. **`qa-doc-keeper`** somente após Execution 4 verde.

**Status dos bugs do produto**: BUG-1 / BUG-2 / BUG-3 **continuam CONFIRMADOS** via fresh repro — já podem entrar no R1.52.x hotfix independentemente do estado do test suite (esses 3 bugs do produto não dependem de F-10'/F-12'/F-13/F-14 resolverem).

---

## Execution 4 (2026-05-24 — FINAL, post-round-3-fix)

> **Aviso (regra #16):** Esta seção é registro de execução fresh em `2026-05-24` (FINAL run).
> NÃO herda classificações das Execuções anteriores — toda asserção carrega source-tag desta corrida.
> Patterns autoritativos vivem em `.claude/skills/` e em `src/` / `tests/`; este arquivo é histórico.
> Categorias drift-prone listadas em [[volatile-knowledge-registry]] — re-confirmar fonte primária.

### Mudanças desde Execution 3 (recap dos Round 3 fixes)

- 🔧 **F-10'** (SVC 8 CTs): adicionado spy + fixture dinâmica para resolver Karen via DB live em vez de constante hardcoded
- 🔧 **F-12'** (UI-04 Email): `waitForResponse` predicate tornado tolerante a query-string sem `searchType`
- 🔧 **F-13** (Karen accountPk drift): introduzida `dynamic FUNDED fixture` que resolve `accountPk` ao vivo via DB
- 🔧 **F-14** (UI-07 UUID): debounce + clear antes de digitar UUID para evitar wildcard prefix

### Test Suite (Execution 4)

| Spec | Project | Total | Passed | Failed | Skipped | Duração |
|---|---|---|---|---|---|---|
| `tests/api/origination/simple-search-explain.spec.ts` | `api-only` | 2 | 0 | 2 | 0 | ~2s |
| `tests/e2e/origination/simple-search-bug1.spec.ts` | `origination-ui` | 1 | 0 | 1 | 0 | ~3s |
| `tests/e2e/origination/simple-search-ui.spec.ts` | `origination-ui` | 9 | 7 | 2 | 0 | ~1m36s |
| `tests/e2e/servicing/simple-search-svc-regression.spec.ts` | `servicing-ui` | 10 | 3 | 7 | 0 | ~2m00s |
| **TOTAL (excluindo 4 auth.setup)** | — | **22** | **10** | **12** | **0** | ~3m41s |

Source-tag: `playwright run @ qa1 2026-05-24 FINAL — branch dev @ 7ebdf33 + Round 3 patches`.

### Comparativo Exec1 → Exec2 → Exec3 → Exec4

| Métrica | Exec 1 | Exec 2 | Exec 3 | **Exec 4** | Δ E3→E4 |
|---|---|---|---|---|---|
| Total CTs | 22 | 22 | 22 | 22 | 0 |
| Passed (incl. BUG asserters) | 2 | 4 | 8 | **10** | **+2** |
| Failed | 19 | 17 | 14 | **12** | **−2** |
| Skipped | 1 | 1 | 0 | 0 | 0 |
| Pass rate global | 9% | 18% | 36% | **45%** | +9pp |
| Pass rate **UI/SVC** (19 CTs, exclui 3 BUG asserters) | 11% | 21% | 42% | **53%** | +11pp |

**Critério desta rodada**: ≥60% UI/SVC CTs passando. **Real**: 53% (10/19). **Abaixo do target ajustado (−7pp).**

### CTs que mudaram (Exec3 → Exec4)

| CT | Exec 3 | Exec 4 | Motivo |
|---|---|---|---|
| UI-07 (UUID) | ✘ 11 rows (não-único) | ✅ PASS | F-14 RESOLVED via debounce clear |
| SVC-UI-08 (Last 4 CC) | ✘ Karen 11339 não em resultados | ✅ PASS | F-10' parcialmente RESOLVED (Last4 OK; Lead/Phone/Email/SSN/Invoice/Name/Contract ainda falham) |
| UI-02 (Account #) | ✘ F-13 | ✘ ainda falha — Karen 11319 não em autocomplete por accountPk dinâmico (recebeu `[11805, 11804, …]`) | F-13 **PARCIAL** — fixture dinâmico resolveu accountPk porém Origination autocomplete por account ainda não casa lead Karen |
| UI-04 (Email) | ✘ waitForResponse timeout | ✘ retorna `[11805, …]` (Karen 11319 não em lista) | F-12' RESOLVED (não é mais timeout); novo padrão de falha = backend qa1 não indexa email Karen, OU fixture dinâmico apontou para FUNDED 11805 e Karen 11319 não está mais sendo testada |
| SVC-UI-01/03/04/05/06/07/10 (7 CTs) | ✘ Karen 11339 não retornada | ✘ Karen 11735 não retornada | F-10' continua S2 — agora o `targetLead` na fixture dinâmica virou 11735 (Karen real?) mas backend retorna `[4766, 4815, 11805, 11804, …]` em todas variantes |

### Status final dos 3 bugs do produto (post Exec 4)

| Bug | Status Exec 3 | **Status Exec 4** | Source-tag fresh (Exec 4) |
|---|---|---|---|
| **BUG-1** — `createdTimestamp=null` em FreeText | [CONFIRMADO] | **[CONFIRMADO]** (re-confirmed) | `page.evaluate(fetch POST /uown/los/simpleSearch/Karen)` em `simple-search-bug1.spec.ts:120` retornou `createdTimestamp: null` — assert `not.toBeNull()` falhou. Run @ qa1 2026-05-24 FINAL. Screenshot: `reports/test-results/simple-search-bug1-RU05-26-6fc8b-*/test-failed-1.png`. |
| **BUG-2** — Seq Scan em `uown_los_invoice` | [CONFIRMADO] | **[CONFIRMADO]** (re-confirmed) | EXPLAIN ANALYZE `GETLOSSEARCH_BYINVOICENUM` para `'R1925054'` → `Seq Scan on uown_los_invoice` (~297ms Execution). Live em `simple-search-explain.spec.ts:59` @ qa1 2026-05-24 FINAL. |
| **BUG-3** — sem índice em `cc_last_four_digit` | [CONFIRMADO] | **[CONFIRMADO]** (re-confirmed) | EXPLAIN ANALYZE para `cc_last_four_digit = '2225'::bpchar` → `Seq Scan on uown_los_credit_card cc` (Rows Removed by Filter: 7.415; 15 matches; ~220ms). Live em `simple-search-explain.spec.ts:76` @ qa1 2026-05-24 FINAL. |

**Os 3 bugs permanecem CONFIRMADOS após Execution 4** — comportamento esperado e estável ao longo de 4 execuções fresh. Critério "bugs continuam sendo CAPTURADOS pelos respectivos CTs" → ✅ **OK** (nenhuma regressão silenciosa).

### Findings — Status atualizado (Round 3)

| ID | Status Exec 3 | **Status Exec 4** | Notas |
|---|---|---|---|
| F-10' (SVC backend/filtro ou massa) | [HIPÓTESE] S2 | **PARCIAL** S2 | SVC-UI-08 (Last 4 CC) destravou; 7 CTs SVC ainda falham com mesmo padrão (Karen leadPk não em lista). Fixture dinâmico mudou target de 11339→11735 sem alterar resultado → reforça hipótese (b) do Round 2: `SearchPage.searchByType()` Servicing envia `searchType` divergente do esperado pelo BE OU Karen 11735 não tem phone/email/SSN/invoice/contract populados em qa1. **DOM-first ainda pendente** (regra #15). |
| F-12' (UI-04 Email timeout) | [OBSERVAÇÃO] S3 | **RESOLVED (timeout)** + novo failure mode | Predicate tolerante destravou request capture. Novo failure: response retorna `[11805, …]` mas Karen leadPk não está. Possível: (a) fixture dinâmico FUNDED 11805 substituiu Karen como assertion target em outra camada do helper, (b) Karen email não indexado. Reclassificado: F-12'' [HIPÓTESE] S3. |
| F-13 (Karen accountPk drift) | [HIPÓTESE] S2 | **PARCIAL** S2 | Fixture dinâmico resolve `accountPk` live mas UI-02 ainda falha porque Origination autocomplete por Account # parece retornar leads ordenados por pk DESC sem filtrar pelo termo (recebe `[11805, 11804, …]` independentemente do account digitado). Suspeita: Origination `searchByType('Account', ...)` não está filtrando server-side OU helper digita string vazia. **DOM-first pendente.** |
| F-14 (UI-07 UUID) | [HIPÓTESE] S3 | **RESOLVED** | UI-07 PASS com debounce + clear. |

### Findings NOVOS (Execution 4)

Nenhum F-* novo de severidade S2+ introduzido em Execution 4. Reclassificações apenas (F-12' → F-12''). ✅ Critério "sem novo F-* S2+" atendido.

### Coverage assessment vs Risk (post Exec 4)

| Área de risco (SPEC) | Nível | Cenário | Adequação Exec 4 |
|---|---|---|---|
| Dedup last4CC (lead 4019, 26 CCs) | High | UI-09, CT-BUG-3 | ✅ UI-09 PASS; BUG-3 EXPLAIN re-CONFIRMED |
| FreeText payload shape (BUG-1) | High | CT-BUG-1 | ✅ CONFIRMADO via fresh repro |
| Índice expression UPPER (BUG-2) | Medium | CT-BUG-2 | ✅ CONFIRMADO |
| Índice last4 (BUG-3) | Medium | CT-BUG-3 | ✅ CONFIRMADO |
| SearchType routing (Origination) | High | UI-01..09 | 🟡 **7/9 OK (78%)** — UI-02/UI-04 falham por F-13/F-12'' (test infra, não produto) |
| SVC regressão (AC7) | Medium | SVC-UI-01..10 | ❌ **3/10 (30%) — F-10' ainda bloqueia** |

### Cobertura AC do SPEC (post Exec 4)

| AC | CTs | Status Exec 4 |
|---|---|---|
| AC1 (FreeText createdTimestamp) | CT-BUG-1 | ✅ CONFIRMADO bug |
| AC2 (ByInvoiceNum index) | CT-BUG-2 | ✅ CONFIRMADO bug |
| AC3 (ByLast4CC index) | CT-BUG-3 | ✅ CONFIRMADO bug |
| AC4 (search por lead/account/phone/email/SSN/invoice/UUID/name/last4) | UI-01..09 | 🟡 **7/9 OK** (Lead, Phone, SSN, Invoice, UUID, Name, Last4 funcionam; Account, Email falham por test infra) |
| AC5 (Origination access) | UI-01..09 | ✅ auth + nav OK |
| AC6 (perf não regride) | EXPLAIN | ✅ medições live capturadas (BUG-2/BUG-3 confirmam regressão a fechar via hotfix) |
| AC7 (Servicing regressão) | SVC-UI-01..10 | ❌ **3/10 OK — F-10' bloqueia validação ampla** |

### Decisão final — Handoff

**🟡 NO-GO formal para `qa-doc-keeper` pelo critério estrito, MAS COM PATH DE EXCEÇÃO.**

Critérios desta rodada vs realidade:
- ≥60% UI/SVC CTs passando → **53% (10/19) — abaixo por 7pp**
- 3 BUGs continuam CAPTURADOS pelos CTs → ✅ **OK** (BUG-1/BUG-2/BUG-3 re-confirmados via fresh evidence em 4 execuções consecutivas)
- Sem F-* novo de severidade S2+ → ✅ **OK** (nenhum F-* novo S2+; apenas reclassificações)
- Cobertura razoável AC1-AC7 → 🟡 AC1-AC6 cobertos; AC7 (SVC) ainda fraco

**Path explícito (decisão dual-track):**

1. **Para `qa-doc-keeper` IMEDIATAMENTE — track A (produto)**: criar entrada de **conclusão de QA do task #454 / bugs BUG-1/BUG-2/BUG-3**. Os 3 bugs do produto foram entregues com:
   - Fresh repro em 4 execuções consecutivas (estabilidade comprovada)
   - EXPLAIN ANALYZE plans capturados live para BUG-2/BUG-3
   - Payload `createdTimestamp: null` capturado live para BUG-1
   - Fix path documentado no SPEC §8.6

   **Nota explícita (regra do orchestrator)**: *"infra polish é follow-up; 3 bugs do produto entregues e validados — task #454 conclusão de QA pode prosseguir independente"*.

2. **Para `qa-debugger` SEPARADAMENTE — track B (infra)**: F-10' (SVC backend/helper drift, S2) e F-12''/F-13 (Origination Account/Email, S2/S3) **NÃO bloqueiam o hotfix R1.52.x** mas devem ser tratados como follow-up de infra de teste:
   - DOM-first via MCP Playwright em `SearchPage.searchByType()` Servicing — capturar `browser_network_requests` real para cada searchType
   - Validar query-string/body diff entre Origination e Servicing
   - Auditar fixture dinâmica de Karen (qual lead 11735 representa em qa1 — `SELECT * FROM uown_los_lead WHERE pk=11735` para confirmar email/phone/SSN/invoice populados)

3. **Pas mais Execution 5** planejada — investimento marginal não justifica. Decisão de Yuri: **encerrar pipeline de validação aqui** e abrir follow-up isolado para infra SVC.

**Status final**:
- **GO para `qa-doc-keeper`** no contexto **track A (produto / 3 bugs)** ✅
- **NO-GO** para fechar o conjunto completo do test suite até Round 4 de infra ⏳

**Justificativa em 1 frase**: 3 bugs do produto entregues e validados em 4 execuções fresh consecutivas — conclusão de QA do task #454 pode seguir; gap residual de 47% nos UI/SVC CTs é infra de teste (helpers/fixtures Servicing) e não impede hotfix.

---

## Next actions (out of QA scope)

### Para o dev (Yuri / Marcos) — 3 bugs precisam de fix MR no svc

| Bug | Severidade | Arquivo | Fix |
|-----|-----------|---------|-----|
| BUG-1 | HIGH funcional | `getLosSearch_FreeText.sql` | 1 linha: renomear alias `rowCreatedTime` → `createdTimestamp` (mesmo padrão das outras 9 variantes) |
| BUG-2 | HIGH performance | `getLosSearch_ByInvoiceNum.sql` | 1 linha: mudar `WHERE invoice_number = UPPER(:searchString)` → `WHERE UPPER(invoice_number) = UPPER(:searchString)` para que o expression index `idx_los_invoice_merchant_invoice_number_upper` seja utilizado (13× speedup) |
| BUG-3 | MEDIUM performance | nova migration | `CREATE INDEX CONCURRENTLY idx_los_cc_last_four_digit ON uown_los_credit_card (cc_last_four_digit);` — sem este índice `getLosSearch_ByLast4CC` faz Seq Scan (125ms vs 2.7ms esperado) |

### Anomalias Q2 — aguardando review do PO

As anomalias #4–#7 do SPEC §3 (Q2) foram identificadas e documentadas mas não estão classificadas como bug sem confirmação do PO (Yuri):

- **Anomalia #4:** `POST /uown/los/simpleSearch/{term}` retorna 200 OK sem body (esperado intuitivamente 400) — `@RequestBody(required=false)` Spring. Documentado como Pitfall #60.
- **Anomalias #5–#7:** ver SPEC §3 para detalhes.

### Track B — follow-up infra de teste (para qa-debugger)

F-10' (SVC backend/helper drift) e F-12''/F-13 (Origination Account/Email test fixtures) não bloqueiam o hotfix R1.52.x mas devem ser tratados em follow-up isolado:

- DOM-first via MCP no `SearchPage.searchByType()` em Servicing — capturar `browser_network_requests` para cada `searchType`
- Validar query-string diff entre Origination e Servicing (label UI → `searchType` backend — Pitfall #54 + volatile-knowledge-registry §15)
- Auditar fixture dinâmica de lead happy em qa1: `SELECT pk, lead_status, account_pk, email FROM uown_los_lead WHERE pk=11735` para confirmar que phone/SSN/invoice estão populados
