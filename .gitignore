node_modules/
dist/
test-results/
playwright-report/
blob-report/
allure-results/
allure-report/
reports/

# Task test files are local only — not pushed to remote
.auth/
target/
*.log

# Environment files with secrets
.env

# Agent lock files (coordination between terminals)
.claude/locks/

# Local Claude Code settings (personal overrides — not shared)
.claude/settings.local.json

# Ad-hoc screenshots (root-level debug screenshots)
*.png
*.jpg
*.jpeg

# Playwright MCP snapshots (temporary debug artifacts from MCP tooling)
.playwright-mcp/

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Ad-hoc investigation/probe scripts (local throwaway — not shared)
src/scripts/_probe_*
src/scripts/_update_lead*
tests/**/__scratch_*.spec.ts
tests/api/_sticky_multi_fresh.spec.ts

# Local working spreadsheets
qa-sla.xlsx
